---
phase: 06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom
plan: 07
type: execute
wave: 4
depends_on: ["06.2-01", "06.2-02", "06.2-04", "06.2-05", "06.2-06"]
files_modified:
  - apps/web/app/[locale]/page.tsx
  - apps/web/app/prices/page.tsx
  - apps/web/app/prices/error.tsx (deleted)
  - apps/web/app/demo/page.tsx
  - apps/web/app/contacts/page.tsx
  - apps/web/modules/home (deleted: cta-banner.tsx, features.tsx, hero.tsx, problem.tsx, solution.tsx, stagger-grid.tsx, testimonials.tsx, unified-source.tsx)
  - apps/web/modules/prices (deleted: pricing-cards.tsx, comparison-table.tsx, faq-accordion.tsx, types.ts)
  - apps/web/modules/contacts (deleted: contact-form.tsx, contact-info.tsx, faq-accordion.tsx)
  - apps/web/modules/demo/demo-cta.tsx (deleted)
  - apps/web/modules/demo/demo-lead-form.tsx (deleted)
autonomous: true
requirements: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visiting / (and /ru, /en) renders the full single-page landing site in design order: Hero, ChannelMarquee, ProblemSolution, HowItWorks, AdminShowcase, Features, Pricing, Reviews, LeadSection, Faq — every section wired, every anchor nav link resolves to a real element"
    - "/prices, /demo, /contacts no longer exist as content destinations — they 301-redirect to landing anchors (D-01, D-03)"
    - "/blog and /blog/[slug] are completely unaffected — still real, unlocalized, Ukrainian-only routes (D-09)"
    - "No dead module files remain (modules/home/*, modules/prices/*, modules/contacts/*, demo-cta.tsx, demo-lead-form.tsx all removed) and the build has zero dangling imports"
  artifacts:
    - "apps/web/app/[locale]/page.tsx (final, fully composed)"
    - "apps/web/app/{prices,demo,contacts}/page.tsx (thin redirect stubs)"
  key_links:
    - "PricingPlan[] is fetched server-side in app/[locale]/page.tsx (same GET /public/pricing-plans + revalidate:60 pattern as the old app/prices/page.tsx) and passed as a prop into PricingSection — not fetched client-side"
    - "Redirect targets in the 3 retired route stubs are hardcoded literals (no user input), eliminating open-redirect risk by construction"
---

<objective>
Compose every section built across Plans 01/04/05/06 into the final single-page landing experience (D-01), retire `/prices`/`/demo`/`/contacts` as content destinations with redirects to their new landing-page homes (D-03), and remove every file that becomes dead code as a result — closing out the phase.

Reversibility note: this task is where D-01's route consolidation becomes effectively irreversible in practice — deleting the 4 old page-specific module directories (`modules/home`, `modules/prices`, `modules/contacts`, plus 2 demo files) means reverting to separate routed pages later would require rebuilding them from git history, not a config flip. D-01 is rated "costly" (not "one-way") in CONTEXT.md, so no blocking checkpoint is required, but this is the concrete point where that cost is incurred — flagged here for traceability.

Purpose: ship the finished, verified single-page landing site.
Output: fully composed `app/[locale]/page.tsx`; 3 redirect stubs; zero dead files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom/06.2-CONTEXT.md
@apps/web/app/[locale]/page.tsx
@apps/web/app/prices/page.tsx
@apps/web/app/demo/page.tsx
@apps/web/app/contacts/page.tsx
@apps/web/shared/lib/routes.ts
@apps/web/shared/lib/api-url.ts
@apps/web/modules/prices/types.ts
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: compose the full landing page (fetch PricingPlan data + assemble all 10 sections in design order)</name>
  <files>apps/web/app/[locale]/page.tsx</files>
  <read_first>
    - apps/web/app/[locale]/page.tsx (Plan 01's current state — renders only `<Hero />`)
    - apps/web/app/prices/page.tsx (existing `GET /public/pricing-plans` fetch pattern — `getServerApiUrl()`, `next: { revalidate: 60 }`, error/empty-state handling — port this fetch into the new page, keep the same revalidate window)
    - apps/web/modules/landing/{types,pricing-section}.tsx (Plan 06 output — `PricingPlan` type + `<PricingSection plans={...} />` prop contract)
  </read_first>
  <action>
    Rewrite `apps/web/app/[locale]/page.tsx` as an async Server Component: fetch `GET \`${getServerApiUrl()}/public/pricing-plans\`` with `next: { revalidate: 60 }` (same pattern as the old `app/prices/page.tsx`, including its `if (!res.ok) throw new Error(...)` guard), parse to `PricingPlan[]`. Then render, in this exact order (matching the design file's section sequence):

    ```
    <Hero />
    <ChannelMarquee />
    <ProblemSolution />
    <HowItWorks />
    <AdminShowcase />
    <Features />
    <PricingSection plans={plans} />
    <Reviews />
    <LeadSection />
    <Faq />
    ```

    Import each from its Plan 04/05/06 location (`@/modules/landing/{hero,channel-marquee,problem-solution,how-it-works,admin-showcase,features,pricing-section,reviews,lead-section,faq}`). If `plans.length === 0`, still render the full page (unlike the old `app/prices/page.tsx`'s dedicated empty-state branch — an empty pricing section is a Plan-02-reseed-failure signal, not a reason to hide the rest of the landing page) but pass an empty array through to `PricingSection`, which should already handle rendering nothing/a minimal fallback for zero plans (if it does not, that is acceptable for this phase — Plan 02 already guarantees 3 published rows exist under normal operation).
  </action>
  <verify>
    <automated>pnpm --filter web check-types && pnpm --filter web build 2>&1 | tail -60</automated>
    <human-check>Run `pnpm --filter web dev`, visit `/`. Click every Header/Footer anchor nav item (`#product`, `#pricing`, `#demo`, `#lead`, `#features`, `#faq`) and confirm each scrolls to its real, correctly-positioned section (no clipping under the sticky header — Pitfall 1). Confirm the pricing section shows the reseeded Старт/Клініка/Мережа plans (Plan 02). Repeat on `/ru` and `/en` — confirm chrome/copy is translated, plan content stays Ukrainian.</human-check>
  </verify>
  <done>`/`, `/ru`, `/en` each render all 10 sections in order with zero console errors; every anchor id (`product`, `demo`, `features`, `pricing`, `lead`, `faq`) exists exactly once in the rendered DOM; pricing section shows real reseeded data.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: retire /prices, /demo, /contacts as redirects (D-01, D-03)</name>
  <files>apps/web/app/prices/page.tsx, apps/web/app/prices/error.tsx (deleted), apps/web/app/demo/page.tsx, apps/web/app/contacts/page.tsx</files>
  <read_first>
    - apps/web/app/prices/page.tsx, apps/web/app/demo/page.tsx, apps/web/app/contacts/page.tsx (current full pages — being replaced with thin redirect stubs)
    - .planning/phases/06.2-.../06.2-CONTEXT.md — D-01, D-02 (the superseded `260818-wjz` quick task — do not resume or reference it, this task supersedes its intent entirely), D-03 (redirect chosen over 404, resolved by this task)
  </read_first>
  <action>
    Replace the contents of `apps/web/app/prices/page.tsx` with a minimal Server Component that calls `redirect('/#pricing')` from `next/navigation` (plain, not the locale-aware `redirect` from `@/i18n/navigation` — these 3 old routes are themselves outside the `[locale]` segment, matching `/blog`'s position in the tree, so a plain unprefixed redirect target is correct and always lands on the uk-default landing page; D-03 accepts this as sufficient since there is no production traffic yet). Delete `apps/web/app/prices/error.tsx` (its only purpose was to catch data-fetch errors on the old page; a redirect-only stub has no data fetch and needs no error boundary).

    Replace `apps/web/app/demo/page.tsx` with the same pattern: `redirect('/#demo')`.

    Replace `apps/web/app/contacts/page.tsx` with: `redirect('/#lead')`.

    Every redirect target is a hardcoded literal string — no query parameter, header, or other user-controlled input is ever read to construct these targets (eliminates open-redirect risk by construction, see this plan's threat model).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code} %{redirect_url}\n" http://localhost:3000/prices 2>&1 || echo "server not running — verify manually"</automated>
    <human-check>With `pnpm --filter web dev` running, visit `/prices`, `/demo`, `/contacts` directly and confirm each redirects to `/#pricing`, `/#demo`, `/#lead` respectively and lands scrolled to the right section.</human-check>
  </verify>
  <done>All 3 old routes redirect (307/308) to their landing-page anchor equivalents; `apps/web/app/prices/error.tsx` no longer exists; no dynamic/user-controlled value appears in any redirect target.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: delete dead module files + final full-phase verification</name>
  <files>apps/web/modules/home/*, apps/web/modules/prices/{pricing-cards.tsx,comparison-table.tsx,faq-accordion.tsx,types.ts}, apps/web/modules/contacts/{contact-form.tsx,contact-info.tsx,faq-accordion.tsx}, apps/web/modules/demo/demo-cta.tsx, apps/web/modules/demo/demo-lead-form.tsx</files>
  <read_first>
    - Output of `grep -rl "modules/home\|modules/prices/pricing-cards\|modules/prices/comparison-table\|modules/prices/faq-accordion\|modules/prices/types\|modules/contacts/contact-form\|modules/contacts/contact-info\|modules/contacts/faq-accordion\|modules/demo/demo-cta\|modules/demo/demo-lead-form" apps/web/app apps/web/modules/landing apps/web/shared` before deleting anything — confirm zero remaining importers outside the files being deleted themselves (Task 1/2 already stopped importing them; this is a final confirmation, not a discovery step)
  </read_first>
  <action>
    Delete the entire `apps/web/modules/home/` directory (`cta-banner.tsx, features.tsx, hero.tsx, problem.tsx, solution.tsx, stagger-grid.tsx, testimonials.tsx, unified-source.tsx` — all 8 files; their content was already adapted into `modules/landing/*` across Plans 01/04/05, they now exist purely as unimported dead code).

    Delete `apps/web/modules/prices/pricing-cards.tsx`, `comparison-table.tsx` (no design-file equivalent this phase — the design has no comparison-matrix section, and the client's "not overloaded" instruction argues against reintroducing one; this is a deliberate scope reduction of a PREVIOUSLY-SHIPPED feature, not of anything this phase's CONTEXT.md decided to keep — note this explicitly in the summary as an intentional design-fidelity choice, not silent scope creep), `faq-accordion.tsx`, and `types.ts` (superseded by `modules/landing/types.ts`, Plan 06). The `modules/prices/` directory should now be empty — remove it too if your tooling leaves empty directories behind.

    Delete `apps/web/modules/contacts/contact-form.tsx`, `contact-info.tsx`, `faq-accordion.tsx` (all superseded by `modules/landing/lead-section.tsx` and `modules/landing/faq.tsx`). Remove the now-empty `modules/contacts/` directory.

    Delete `apps/web/modules/demo/demo-cta.tsx` and `apps/web/modules/demo/demo-lead-form.tsx` (D-07's single-funnel resolution — these are the dead separate-modal code path). Do NOT delete `demo-tabs.tsx`, `bot-tab.tsx`, `admin-tab.tsx`, or `_data.ts` — those stay, embedded live inside `modules/landing/admin-showcase.tsx` (Plan 04).

    Run the full verification suite: `pnpm --filter web check-types`, `pnpm --filter web build`, `pnpm --filter web lint`.
  </action>
  <verify>
    <automated>pnpm --filter web check-types && pnpm --filter web build 2>&1 | tail -60 && pnpm --filter web lint 2>&1 | tail -40</automated>
    <human-check>Full click-through of the final `/`, `/ru`, `/en`, `/blog` — confirm no broken links, no console errors, scroll-anchor clipping fixed on both mobile and desktop breakpoints (Pitfall 1), and that the site overall reads as lean/uncluttered per the client's explicit "not overloaded" instruction rather than an assembly of every possible section.</human-check>
  </verify>
  <done>`pnpm --filter web check-types`, `build`, and `lint` all pass with zero errors; `apps/web/modules/home/`, the 4 named `modules/prices/` files, the 3 named `modules/contacts/` files, and the 2 named `modules/demo/` files no longer exist; `grep -rl` for any of their old import paths across `apps/web/app`/`apps/web/modules/landing`/`apps/web/shared` returns nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → `/prices`, `/demo`, `/contacts` redirect stubs | Legacy entry points (bookmarks, external links, search-engine index) now server-redirect to landing anchors — no data fetch, no input processing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-11 | Tampering (open redirect) | `app/{prices,demo,contacts}/page.tsx` | low | mitigate | Every redirect target is a hardcoded literal (`/#pricing`, `/#demo`, `/#lead`) — no query string, header, or referrer value is ever read to construct the target, so these stubs cannot be used to redirect a visitor to an attacker-controlled external URL. |
| T-06.2-12 | Repudiation (dead-code confusion) | Deleted module directories | low | mitigate | The Task 3 pre-delete grep confirms zero remaining importers before any file is removed, and the build/lint/check-types gate at the end of Task 3 would fail loudly on any missed dangling import — no silent breakage possible. |
</threat_model>

<verification>
Full site click-through across `/`, `/ru`, `/en`, `/blog` and the 3 retired routes; `check-types`/`build`/`lint` all green; zero dead files remain; every `<threat_model>` item across all 7 plans in this phase resolves to `mitigate` or `accept` with a stated rationale.
</verification>

<success_criteria>
Phase 06.2's goal is fully realized: one scrolling, fully-translated (uk/ru/en) landing page at `/` composed from the design file's sections and copy, `/blog` untouched, `/prices`/`/demo`/`/contacts` retired as destinations with working redirects, and zero leftover dead code.
</success_criteria>

<output>
Create `.planning/phases/06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom/06.2-07-SUMMARY.md` when done
</output>
