---
phase: 06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom
plan: 04
type: execute
wave: 3
depends_on: ["06.2-03"]
files_modified:
  - apps/web/modules/landing/channel-marquee.tsx
  - apps/web/modules/landing/problem-solution.tsx
  - apps/web/modules/landing/how-it-works.tsx
  - apps/web/modules/landing/admin-showcase.tsx
  - apps/web/modules/demo/admin-tab.tsx
  - apps/web/modules/demo/bot-tab.tsx
autonomous: true
requirements: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The problem/solution split, how-it-works steps, and the interactive admin+bot demo all render as translated landing-page sections, ready for Plan 07 to compose into the page"
    - "Zero emoji remain in modules/demo/admin-tab.tsx or modules/demo/bot-tab.tsx (client's established no-emoji constraint, Phase 06.1 precedent)"
  artifacts:
    - "apps/web/modules/landing/channel-marquee.tsx"
    - "apps/web/modules/landing/problem-solution.tsx"
    - "apps/web/modules/landing/how-it-works.tsx"
    - "apps/web/modules/landing/admin-showcase.tsx"
  key_links:
    - "admin-showcase.tsx embeds the EXISTING modules/demo/demo-tabs.tsx (BotTab+AdminTab) inside a new browser-chrome wrapper — reuses already-built, on-brand interactive simulation rather than a static screenshot placeholder (CONTEXT.md Claude's Discretion, leaning kept)"
    - "id=\"demo\" lives on admin-showcase.tsx (the interactive section), NOT on how-it-works.tsx — Header/Footer's #demo anchor targets the interactive simulation, matching user expectation of what \"Демо\" should scroll to"
---

<objective>
Build the "upper-middle" landing sections: the channel-logo marquee, the problem/solution dark-navy comparison split, the static 3-step how-it-works explainer, and the admin/bot interactive demo showcase — the last of which is where this phase's CONTEXT.md "Claude's Discretion" question (keep the interactive BotTab/AdminTab simulation vs. a static screenshot) gets resolved by reusing the already-built, on-brand simulation (`modules/demo/demo-tabs.tsx`), wrapped in a new browser-chrome frame matching the design file's admin-panel showcase section. Cleans up the two emoji issues RESEARCH flagged in the reused demo components (Pitfall 2) before they ship on the primary landing page.

Purpose: prove out the "reuse the existing interactive demo, don't rebuild a static screenshot" decision, plus the two lightweight static-copy sections around it.
Output: 4 new `modules/landing/*` section components + emoji-free demo components.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom/06.2-CONTEXT.md
@apps/web/messages/uk.json
@apps/web/modules/home/problem.tsx
@apps/web/modules/demo/demo-tabs.tsx
@apps/web/modules/demo/admin-tab.tsx
@apps/web/modules/demo/bot-tab.tsx
@apps/web/shared/components/section.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: channel-marquee.tsx + problem-solution.tsx</name>
  <files>apps/web/modules/landing/channel-marquee.tsx, apps/web/modules/landing/problem-solution.tsx</files>
  <read_first>
    - Design file lines 94-99 (marquee — infinite-scroll logo strip) and lines 101-133 (problem/product section — dark navy bg, two-column "Без DentaBot" vs "З DentaBot" comparison cards)
    - apps/web/modules/home/problem.tsx (existing light 4-card grid pattern — this phase's problem-solution.tsx deliberately does NOT reuse this visual pattern; it switches to the design's dark two-column comparison layout, source-of-truth wins)
    - apps/web/shared/components/section.tsx (`tone="navy"` variant, already exists, reused directly)
  </read_first>
  <action>
    Create `apps/web/modules/landing/channel-marquee.tsx`: a thin bordered strip (`border-t border-b border-[var(--dt-border)] bg-dt-warm-white overflow-hidden py-4`) containing a CSS `animate-marquee`-style infinitely-scrolling row of `t.raw('marquee.channels')` (8 brand names), duplicated once in the DOM (render the array twice back-to-back) for a seamless loop, using a plain CSS `@keyframes` translateX(-50%) animation (add a small `animate-marquee` utility via an inline `<style>` tag or a Tailwind arbitrary-value `animate-[marquee_32s_linear_infinite]` — do not add a new global CSS file for one component). Text styled `font-dt-mono text-xs uppercase tracking-wide text-dt-graphite/50` (matches design's JetBrains Mono marquee text treatment). No `id` on this section (not a nav target).

    Create `apps/web/modules/landing/problem-solution.tsx`, `'use client'`: `<Section id="product" tone="navy" className="scroll-mt-16 lg:scroll-mt-20">` wrapping a `Container`, `Reveal`-wrapped `SectionHeading` (`tone="navy"`, `eyebrow={t('problemSolution.eyebrow')}`, `title={t('problemSolution.title')}`, `description={t('problemSolution.description')}`). Below it, a 2-column grid (`grid gap-6 md:grid-cols-2`): left column is a bordered/translucent card (`border border-white/10 bg-white/5 rounded-dt-card p-8`) with `t('problemSolution.beforeLabel')` as a mono-uppercase label followed by `t.raw('problemSolution.before')` (4 strings) each prefixed with a short dash marker (matches design's `—` bullet style, not a checkmark); right column is a light `PremiumCard`-style card (`bg-dt-warm-white text-dt-graphite rounded-dt-card p-8 shadow-[var(--shadow-dt-hover)]`) with `t('problemSolution.afterLabel')` label followed by `t.raw('problemSolution.after')` (4 strings) each prefixed with a small teal checkmark circle (reuse the existing checkmark-circle visual pattern from `apps/web/modules/home/problem.tsx`'s icon treatment, adapted to a plain `Check` from `@phosphor-icons/react/ssr` in a small teal circle, not the 4 distinct Phosphor icons `problem.tsx` currently uses per pain-point — this section's 4 items per column don't map 1:1 to distinct icons, use one consistent checkmark for all 4 "after" items and one consistent dash for all 4 "before" items, matching the design's own simpler visual language).
  </action>
  <verify>
    <automated>grep -c "useTranslations" apps/web/modules/landing/problem-solution.tsx</automated>
  </verify>
  <done>Both components render without TypeScript errors (`pnpm --filter web check-types`), `problem-solution.tsx` uses `useTranslations('problemSolution')`/`t.raw`, `channel-marquee.tsx` renders all 8 channel names duplicated for a seamless loop.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: how-it-works.tsx — static 3-step explainer</name>
  <files>apps/web/modules/landing/how-it-works.tsx</files>
  <read_first>
    - Design file lines 135-152 (the "Як це працює" section — 3 numbered cards, no interactivity, `id="demo"` in the ORIGINAL design markup — this phase intentionally moves that anchor id to admin-showcase.tsx instead, see Task 3; how-it-works.tsx itself gets NO id)
    - apps/web/modules/home/solution.tsx (existing step-list visual pattern — number badge + title + description — reused for consistency, though this component's actual step CONTENT comes from the design's 3-step `steps` array, not solution.tsx's 6-step patient/clinic split)
  </read_first>
  <action>
    Create `apps/web/modules/landing/how-it-works.tsx`, no `'use client'` needed (static, no interactivity): `<Section>` (default `warm-white` tone, no id) wrapping `Container`, `Reveal`-wrapped `SectionHeading` (`eyebrow={t('howItWorks.eyebrow')}`, `title={t('howItWorks.title')}`). Below it, a 3-column grid (`grid gap-6 md:grid-cols-3` via the existing `StaggerGrid`/`StaggerItem` pattern from `apps/web/modules/home/stagger-grid.tsx`) of `PremiumCard`s, each rendering one entry from `t.raw('howItWorks.steps')` (`{n, title, text}`): the `n` field ("01"/"02"/"03") rendered large and faint (`font-dt-mono text-3xl font-medium text-dt-navy/15`, matches design's oversized faint step-number treatment), `title` as an `h3` (`text-dt-h3 font-dt-heading font-semibold text-dt-navy`), `text` as body copy (`text-dt-graphite`).
  </action>
  <verify>
    <automated>grep -c "howItWorks" apps/web/modules/landing/how-it-works.tsx</automated>
  </verify>
  <done>Component renders 3 step cards from `t.raw('howItWorks.steps')`, no `id` attribute present on the section, `pnpm --filter web check-types` passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: admin-showcase.tsx (embeds interactive DemoTabs) + emoji cleanup in reused demo components</name>
  <files>apps/web/modules/landing/admin-showcase.tsx, apps/web/modules/demo/admin-tab.tsx, apps/web/modules/demo/bot-tab.tsx</files>
  <read_first>
    - Design file lines 154-174 (admin showcase section — browser-chrome frame: 3 colored dots + url-bar pill wrapping a screenshot placeholder)
    - apps/web/modules/demo/demo-tabs.tsx (the existing tab-switcher — `<DemoTabs />` renders `BotTab`/`AdminTab` behind a 2-button tablist, reused wholesale here, unmodified)
    - apps/web/modules/demo/admin-tab.tsx lines 117-163 (the 5 sidebar `Button`s with emoji labels: `📊 Dashboard`, `📅 Записи`, `👨‍⚕️ Лікарі`, `🧑‍🤝‍🧑 Пацієнти`, `⚙️ Налаштування` — already imports `lucide-react` icons `Calendar, CheckCircle, Clock, Phone, User, X` for OTHER UI in this same file, confirming lucide-react — not Phosphor — is this file's own established icon convention (it deliberately stays on `@repo/ui`/lucide-react per CLAUDE.md's carve-out for the admin-panel simulation, NOT the dt-*/Phosphor system used elsewhere in this phase))
    - apps/web/modules/demo/bot-tab.tsx line 27 (`seedGreetings()` — `'Вітаю! Я DentaBot 🦷'`)
    - .planning/phases/06.2-.../06.2-RESEARCH.md — "Common Pitfalls / Pitfall 2" (full rationale for both cleanups + the accepted `@repo/ui`-vs-`dt-*` visual-seam tradeoff)
  </read_first>
  <action>
    In `apps/web/modules/demo/admin-tab.tsx`: add `LayoutDashboard, Stethoscope, Users, Settings` to the existing `lucide-react` import (already imports `Calendar` — reuse it for the "Записи" button instead of importing a duplicate). Replace each of the 5 sidebar button labels: `📊 Dashboard` → `<LayoutDashboard className="mr-2 h-4 w-4" />Dashboard`, `📅 Записи` → `<Calendar className="mr-2 h-4 w-4" />Записи`, `👨‍⚕️ Лікарі` → `<Stethoscope className="mr-2 h-4 w-4" />Лікарі`, `🧑‍🤝‍🧑 Пацієнти` → `<Users className="mr-2 h-4 w-4" />Пацієнти`, `⚙️ Налаштування` → `<Settings className="mr-2 h-4 w-4" />Налаштування`. This keeps the file on its established lucide-react convention (not Phosphor — that convention belongs to the dt-* system this file is explicitly exempt from per CLAUDE.md).

    In `apps/web/modules/demo/bot-tab.tsx` line 27: change `'Вітаю! Я DentaBot 🦷'` to `'Вітаю! Я DentaBot'` (drop the trailing emoji + preceding space, no replacement icon needed — this is chat-bubble text, not a labeled button).

    Create `apps/web/modules/landing/admin-showcase.tsx`, no `'use client'` needed at the top level (the interactivity lives inside the already-`'use client'` `DemoTabs`): `<Section id="demo" className="scroll-mt-16 lg:scroll-mt-20" tone="warm-white">` wrapping `Container`, `Reveal`-wrapped `SectionHeading` (`eyebrow={t('adminShowcase.eyebrow')}`, `title={t('adminShowcase.title')}`, `description={t('adminShowcase.description')}`). Below it, a browser-chrome frame wrapper matching the design's structure (rounded card, `overflow-hidden`, hairline border + `shadow-[var(--shadow-dt-hover)]`): a top bar (`bg-dt-navy/[0.03] border-b border-[var(--dt-border)] px-4 py-3 flex items-center gap-2`) with 3 small colored dots (coral/amber/sage — reuse `bg-dt-coral`, an inline `bg-[#d4a24c]` or the existing amber token if present, `bg-dt-teal` or a sage-ish tone) and a centered/left `t('adminShowcase.urlLabel')` pill (`font-dt-mono text-xs text-dt-graphite/60 bg-dt-warm-white rounded px-3 py-1`), then the actual content area rendering `<DemoTabs />` (imported from `@/modules/demo/demo-tabs`) directly — no screenshot placeholder, this IS the live interactive demo replacing the design's static screenshot slot. Accept the resulting `@repo/ui`-vs-`dt-*` visual seam as a known, deliberate tradeoff (RESEARCH Pitfall 2 / Open Question 3) — do not attempt to re-theme `DemoTabs`'/`AdminTab`'s internals to match `dt-*` tokens, that would violate CLAUDE.md's explicit carve-out.
  </action>
  <verify>
    <automated>grep -rE "📊|📅|👨‍⚕️|🧑‍🤝‍🧑|⚙️|🦷" apps/web/modules/demo/admin-tab.tsx apps/web/modules/demo/bot-tab.tsx | wc -l</automated>
    <human-check>Visually confirm admin-showcase.tsx's browser-chrome frame reads as an intentional "live demo, different surface" moment rather than a jarring inconsistency at landing-page scroll scale (RESEARCH Open Question 3) — flag if it reads as broken rather than deliberate.</human-check>
  </verify>
  <done>The grep above returns 0 (zero emoji remain in either file); `admin-showcase.tsx` exists with `id="demo"`, renders `<DemoTabs />` inside a browser-chrome frame, and compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — this plan only adds presentational landing sections and edits demo-simulation UI labels; the embedded `DemoTabs` component was already public-facing at `/demo` prior to this phase with no user-input surface beyond its own already-shipped lead-form CTA (unchanged here).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-07 | Information Disclosure | `admin-tab.tsx`/`bot-tab.tsx` demo data | low | accept | All rendered data (`_data.ts`) is already-shipped, hardcoded, fictional demo content ("Клініка «Посмішка»") — no real patient/clinic data is ever displayed, unchanged by this plan. |
</threat_model>

<verification>
`pnpm --filter web check-types` passes for all 4 new/modified files; zero emoji remain in the demo module; `admin-showcase.tsx` carries `id="demo"`.
</verification>

<success_criteria>
The "how it works" and "admin/bot demo" story is fully built and translated, with the client's no-emoji constraint honored throughout the reused demo simulation.
</success_criteria>

<output>
Create `.planning/phases/06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom/06.2-04-SUMMARY.md` when done
</output>
