---
phase: 06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/seed-pricing.ts
  - packages/db/package.json
autonomous: true
requirements: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GET /public/pricing-plans returns 3 published plans whose name/description/features match the design file's copy (Старт/Клініка/Мережа), not the old placeholder pricing content"
    - "Re-running the reseed script never deletes or duplicates rows — it is idempotent (Pitfall 3: seed.ts forbids deleteMany/truncate)"
  artifacts:
    - "packages/db/prisma/seed-pricing.ts"
    - "packages/db/package.json db:seed:pricing script"
  key_links:
    - "seed-pricing.ts upserts by PricingPlan.name (no unique constraint exists — matched via findFirst, per D-04's no-schema-change constraint) rather than deleteMany+createMany"
---

<objective>
Reseed the real `PricingPlan` table (D-04: stays wired to the live API, no hardcoding, no schema change) with the design file's Старт/Клініка/Мережа copy (D-05: reseed mechanism is a standalone idempotent script), so the phase's `#pricing` landing section (Plan 06) renders real, on-brand content the moment it's built — independent of and parallel to Plan 01's i18n work (different domain, zero file overlap).

Advisory note: this reseed intentionally keeps `PricingPlan` content Ukrainian-only (D-11/D-09's boundary — pricing chrome around the plan cards gets translated in Plan 06, plan `name`/`description`/`features` do not). Not reopened here.

Purpose: make the live pricing data match the new design's sales copy without touching the schema or the existing admin-bootstrap seed.ts.
Output: a rerunnable, non-destructive seed script + 3 published PricingPlan rows in the DB.
</objective>

<artifacts>
## Artifacts this phase produces (this plan)
- `packages/db/prisma/seed-pricing.ts` — new, idempotent reseed script
- `packages/db/package.json` — modified, new `db:seed:pricing` script entry
- 3 `PricingPlan` rows in the DB (Старт/Клініка/Мережа, `published: true`) — data artifact, not a file
</artifacts>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom/06.2-CONTEXT.md
@.planning/phases/06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom/06.2-RESEARCH.md
@packages/db/prisma/schema.prisma
@packages/db/prisma/seed.ts
@packages/db/package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: write idempotent PricingPlan reseed script</name>
  <files>packages/db/prisma/seed-pricing.ts, packages/db/package.json</files>
  <read_first>
    - packages/db/prisma/seed.ts (existing pattern — PrismaPg adapter construction, its own header comment: "Never deleteMany/truncate any table")
    - packages/db/prisma/schema.prisma lines 118-133 (`PricingPlan` model — `id/name/monthlyPrice/yearlyPrice/description/features/isPopular/sortOrder/published`, `name` has NO `@unique` constraint)
    - apps/server/src/pricing-plans/dto/create-pricing-plan.dto.ts (confirms exact writable field set/types — `monthlyPrice`/`yearlyPrice` are strings)
    - .planning/phases/06.2-.../06.2-RESEARCH.md — "Common Pitfalls / Pitfall 3" (idempotent findFirst+create/update pattern, matched by `name`) and "Pitfall 4" (design's `for`/`tag`/`cta`/`items` fields don't exist on the real DTO — field mapping below resolves this)
    - Design file `/Users/artemdanko/Downloads/Design system tokens Phase 01.1/DentaBot Landing.dc.html` lines 479-483 (`planData` — the literal Старт/Клініка/Мережа copy this script ports)
  </read_first>
  <action>
    Create `packages/db/prisma/seed-pricing.ts`, structured identically to `seed.ts` (same `PrismaPg` adapter + `PrismaClient` construction pattern, same `main().catch(...).finally(...)` shape, own header comment repeating the "never deleteMany/truncate" convention). Do NOT modify `seed.ts` itself — this is a sibling file per RESEARCH's Pitfall 3 recommendation, keeping the "never truncate" convention legible per-file.

    Define a `PLANS` array of exactly 3 entries mapping the design file's `planData` (line 479-483) onto the real `CreatePricingPlanDto` shape (Pitfall 4 — the design's `for`/`tag`/`cta`/`items` fields do not exist on the DTO):
    - **Старт**: `monthlyPrice: '890'`, `yearlyPrice: '712'` (design's own `Math.round(p.month * 0.8 / 10) * 10` yearly-discount formula applied ahead of time, since the real API stores static price strings with no runtime billing-period-toggle math), `description` = design's `for` field folded in verbatim: `"Кабінет або один лікар"`, `features` = design's `items[]` verbatim (4 entries: "Telegram-бот із записом", "Розклад одного лікаря", "Автонагадування", "Базова аналітика"), `isPopular: false`, `sortOrder: 0`, `published: true`.
    - **Клініка**: `monthlyPrice: '1890'`, `yearlyPrice: '1510'`, `description`: `"2–8 лікарів, кілька кабінетів"`, `features` (6 entries per design: "Усе з тарифу «Старт»", "Спільний розклад усіх лікарів", "Доступ за ролями", "Аналітика по джерелах запису", "Відгуки після візиту", "Viber і WhatsApp"), `isPopular: true` (design's `tag: "Найпопулярніший"` maps to this boolean — consumed by the existing `PremiumBadge` in Plan 06's pricing-section.tsx, no separate `tag` string field needed), `sortOrder: 1`, `published: true`.
    - **Мережа**: `monthlyPrice: '3900'`, `yearlyPrice: '3120'`, `description`: `"Кілька філій під одним брендом"`, `features` (5 entries per design: "Усе з тарифу «Клініка»", "Кілька філій в одній панелі", "Зведені звіти по мережі", "API та інтеграції", "Персональний менеджер"), `isPopular: false`, `sortOrder: 2`, `published: true`.

    Do NOT store the design's `cta` field anywhere (Pitfall 4 — cosmetic, non-blocking, resolved in Plan 06 by keeping the existing single "Обрати план"-style CTA rather than per-plan distinct labels).

    Implement `main()` as: for each `PLANS` entry, `const existing = await prisma.pricingPlan.findFirst({ where: { name: plan.name } })`; if found, `await prisma.pricingPlan.update({ where: { id: existing.id }, data: plan })`; else `await prisma.pricingPlan.create({ data: plan })`. This is the idempotent upsert-by-name pattern (no `@unique` on `name` exists, so `prisma.pricingPlan.upsert()` cannot be used directly — Pitfall 3). Never call `deleteMany`/`truncate`.

    Add `"db:seed:pricing": "dotenv -e ../../.env -- tsx prisma/seed-pricing.ts"` to `packages/db/package.json`'s `scripts`, matching the existing `db:seed` script's exact invocation pattern.
  </action>
  <verify>
    <automated>grep -c "deleteMany\|truncate" packages/db/prisma/seed-pricing.ts</automated>
  </verify>
  <done>`packages/db/prisma/seed-pricing.ts` exists, contains zero `deleteMany`/`truncate` calls (grep above returns 0), defines exactly 3 plan entries (Старт/Клініка/Мережа) with `published: true`, and `packages/db/package.json` has a `db:seed:pricing` script.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: run the reseed against the dev DB and confirm idempotency</name>
  <files>packages/db/prisma/seed-pricing.ts</files>
  <precondition>`DATABASE_URL` is configured in the repo-root `.env` and a reachable Postgres instance exists — the same precondition `packages/db`'s existing `db:seed`/`db:migrate` scripts already depend on (established Phase 4).</precondition>
  <read_first>
    - packages/db/prisma/seed-pricing.ts (the script written in Task 1)
    - apps/server/src/pricing-plans/pricing-plans.service.ts (`findAllPublished` — confirms `published: true` + `orderBy: sortOrder asc` is what the public API surfaces)
  </read_first>
  <action>
    Run `pnpm --filter @repo/db db:seed:pricing` once. Then run it a second time immediately (proves idempotency — Pitfall 3's core requirement). Between the two runs, query the row count for `PricingPlan` (e.g. via a one-off `psql "$DATABASE_URL" -c "select count(*) from \"PricingPlan\";"` or an equivalent Prisma Studio / short Node script using `@repo/db`'s generated client) and confirm it is 3 after BOTH runs, never 6. If `apps/server` is running locally (`pnpm --filter server dev`), additionally curl `GET /public/pricing-plans` and confirm the response contains exactly the 3 names Старт/Клініка/Мережа with `published: true` implied (public endpoint already filters to published-only) and non-empty `features` arrays.
  </action>
  <verify>
    <automated>pnpm --filter @repo/db db:seed:pricing && pnpm --filter @repo/db db:seed:pricing 2>&1 | tail -20</automated>
  </verify>
  <done>Reseed script runs successfully twice in a row with zero errors; the `PricingPlan` table (or the public API, if the server is reachable) shows exactly 3 rows named Старт/Клініка/Мережа after both runs — no duplication, no data loss.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator shell → `packages/db/prisma/seed-pricing.ts` | A locally-run, credentialed (DATABASE_URL) script with full write access to the `PricingPlan` table — not exposed to any HTTP surface or untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-04 | Tampering (data loss) | `seed-pricing.ts` | medium | mitigate | All 3 plan entries are hardcoded literals in the script (no user/HTTP input reaches this code path); the idempotent `findFirst`→`update`/`create` pattern with zero `deleteMany`/`truncate` calls (verified via the Task 1 grep gate) means the script can only ever write the 3 named rows, never wipe unrelated `platform-admin`-created rows in a shared dev/staging DB. |
| T-06.2-05 | Tampering (injection) | Prisma query construction | low | accept | All writes go through Prisma's parameterized query builder (`findFirst`/`update`/`create` with typed `data` objects) — no raw SQL string concatenation exists in this script, so SQL injection is architecturally not possible here. |
</threat_model>

<verification>
`packages/db/prisma/seed-pricing.ts` exists, contains zero destructive-delete calls, and running it twice leaves exactly 3 `PricingPlan` rows (Старт/Клініка/Мережа, all `published: true`) with no duplication.
</verification>

<success_criteria>
The real `PricingPlan` API returns the design file's sales copy, ready for Plan 06's pricing section to consume with zero hardcoding.
</success_criteria>

<output>
Create `.planning/phases/06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom/06.2-02-SUMMARY.md` when done
</output>
