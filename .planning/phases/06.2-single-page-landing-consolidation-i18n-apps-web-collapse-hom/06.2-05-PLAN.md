---
phase: 06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom
plan: 05
type: execute
wave: 3
depends_on: ["06.2-03"]
files_modified:
  - apps/web/modules/landing/features.tsx
  - apps/web/modules/landing/reviews.tsx
  - apps/web/modules/landing/faq.tsx
autonomous: true
requirements: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Features (8-item grid), Reviews (3 testimonials), and FAQ (5 questions) render as translated landing-page sections, ready for Plan 07 to compose"
    - "Only ONE consolidated FAQ component exists for the landing page — the old modules/prices and modules/contacts duplicate accordions are not touched by this plan (they get removed in Plan 07 once their pages retire) and their content is NOT merged in (Pitfall 5)"
  artifacts:
    - "apps/web/modules/landing/features.tsx"
    - "apps/web/modules/landing/reviews.tsx"
    - "apps/web/modules/landing/faq.tsx"
  key_links:
    - "faq.tsx sources its 5 questions exclusively from the design file's canonical faq array (via messages.faq), not from either pre-existing 7/8-question duplicate"
---

<objective>
Build the three remaining "lower-page, pre-conversion" landing sections: Features (8-item icon grid, closely matching the existing `modules/home/features.tsx` pattern), Reviews (3-item testimonials, simplified to design's initials-avatar style — no external Unsplash images), and FAQ (the single consolidated 5-question accordion that replaces two pre-existing near-duplicate FAQ components, per RESEARCH Pitfall 5).

Purpose: finish the content-grid sections using already-proven `dt-*` primitives, fully parallel to Plan 04/06 (disjoint files).
Output: 3 new `modules/landing/*` section components.
</objective>

<artifacts>
## Artifacts this phase produces (this plan)
- `apps/web/modules/landing/features.tsx` — new, `id="features"`
- `apps/web/modules/landing/reviews.tsx` — new
- `apps/web/modules/landing/faq.tsx` — new, `id="faq"`
</artifacts>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom/06.2-CONTEXT.md
@apps/web/messages/uk.json
@apps/web/modules/home/features.tsx
@apps/web/modules/home/testimonials.tsx
@apps/web/shared/components/premium-accordion.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: features.tsx</name>
  <files>apps/web/modules/landing/features.tsx</files>
  <read_first>
    - apps/web/modules/home/features.tsx (existing 8-item `StaggerGrid`/`PremiumCard` pattern with one distinct Phosphor icon per card — already matches the design's 8-item grid count, reused directly)
    - Design file lines 176-187 (features section — 8 items, each with a `mark`/`title`/`text`; the design uses a monospace numeric "mark" per card rather than an icon, but this phase keeps the existing icon-per-card visual treatment already proven in Phase 06.1 rather than switching to numeric marks — deliberate continuity choice, not a regression)
  </read_first>
  <action>
    Create `apps/web/modules/landing/features.tsx`, `'use client'`: `<Section id="features" className="scroll-mt-16 lg:scroll-mt-20">` wrapping `Container`, `Reveal`-wrapped `SectionHeading` (`title={t('features.title')}`, no eyebrow/description — matches design's single-heading structure). Below it, the existing `StaggerGrid`/`StaggerItem`/`PremiumCard` 8-item grid pattern from `modules/home/features.tsx`, but content sourced from `t.raw('features.items')` (`{title, text}` x8) instead of hardcoded JSX — map over the array with `.map((item, i) => ...)`, keeping the SAME 8 existing Phosphor icons in the SAME order as `modules/home/features.tsx` (`ChatCircleText, Calendar, Bell, ChartBar, Star, Gear, Users, Check`, all from `@phosphor-icons/react/ssr`) since the design's 8 feature items map 1:1 conceptually onto the existing 8 (Telegram bot / shared schedule / auto-reminders / analytics / role access / reviews / reschedule-cancel / multi-branch — same concepts as the existing grid, just re-copied per the design's exact wording via `t.raw`).
  </action>
  <verify>
    <automated>grep -c "t.raw('features.items')" apps/web/modules/landing/features.tsx</automated>
  </verify>
  <done>Renders exactly 8 feature cards from `t.raw('features.items')`, `id="features"` present with scroll-mt, `pnpm --filter web check-types` passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: reviews.tsx</name>
  <files>apps/web/modules/landing/reviews.tsx</files>
  <read_first>
    - apps/web/modules/home/testimonials.tsx (existing 3-card pattern — currently uses real Unsplash photos for 1 of 3 reviewers, initials-circle placeholder for the other 2)
    - Design file lines 221-234, 590-593 (reviews section + the canonical 3-review array — design uses initials-circle avatars for ALL 3 reviews, no photos, plus 5-star rating row per card)
  </read_first>
  <action>
    Create `apps/web/modules/landing/reviews.tsx`, `'use client'`: `<Section>` (default tone, no id — not a nav target) wrapping `Container`, `Reveal`-wrapped `SectionHeading` (`title={t('reviews.title')}`). Below it, a 3-column `StaggerGrid` of `PremiumCard`s from `t.raw('reviews.items')` (`{text, initials, name, clinic}` x3), each card rendering: a 5-star row (5x `StarIcon weight="fill" className="h-4 w-4 text-yellow-400"` from `@phosphor-icons/react/ssr`, matching the existing `Stars()` helper in `modules/home/testimonials.tsx` — reuse that exact pattern), the review `text` in italic body copy, then an initials-circle avatar (`flex h-12 w-12 items-center justify-center rounded-full bg-dt-navy/10 font-dt-heading font-semibold text-dt-navy`, rendering `item.initials`) + `name`/`clinic` — for ALL 3 cards this way (simplified vs. the existing component, which used a real photo for one card; this phase drops the external Unsplash dependency for this section entirely, matching the design's uniform initials-avatar treatment).
  </action>
  <verify>
    <automated>grep -c "t.raw('reviews.items')" apps/web/modules/landing/reviews.tsx</automated>
  </verify>
  <done>Renders exactly 3 review cards from `t.raw('reviews.items')`, all using initials-circle avatars (no external image dependency), `pnpm --filter web check-types` passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: faq.tsx — single consolidated FAQ</name>
  <files>apps/web/modules/landing/faq.tsx</files>
  <read_first>
    - apps/web/shared/components/premium-accordion.tsx (existing `PremiumAccordion`/`PremiumAccordionItem`/`PremiumAccordionTrigger`/`PremiumAccordionContent` primitives, reused directly)
    - apps/web/modules/prices/faq-accordion.tsx and apps/web/modules/contacts/faq-accordion.tsx (the two pre-existing near-duplicate accordions — read ONLY to confirm they are NOT the source for this component's content; both stay untouched by this plan and get deleted in Plan 07 once their pages retire, per RESEARCH Pitfall 5)
    - Design file lines 290-305, 605-610 (FAQ section + the canonical 5-question array — this IS the source of truth)
  </read_first>
  <action>
    Create `apps/web/modules/landing/faq.tsx`: `<Section id="faq" className="scroll-mt-16 lg:scroll-mt-20" tone="muted">` wrapping `Container`, `Reveal`-wrapped `SectionHeading` (`title={t('faq.title')}`). Below it, a `<div className="mx-auto max-w-3xl">` containing `<PremiumAccordion type="single" collapsible className="space-y-4">` mapping over `t.raw('faq.items')` (`{question, answer}` x5, EXACTLY 5 — not 7 or 8) into `PremiumAccordionItem`/`PremiumAccordionTrigger`/`PremiumAccordionContent`, matching the exact JSX shape already used in `modules/prices/faq-accordion.tsx` (structurally identical, content different and sourced via `t.raw` instead of a hardcoded array).
  </action>
  <verify>
    <automated>node -e "const m=require('./apps/web/messages/uk.json'); if(m.faq.items.length!==5){console.error('expected 5 faq items, got',m.faq.items.length);process.exit(1)} console.log('ok')"</automated>
  </verify>
  <done>Renders exactly 5 FAQ items from `t.raw('faq.items')` (verified against `messages/uk.json` above), `id="faq"` present with scroll-mt, does not import from `modules/prices/faq-accordion.tsx` or `modules/contacts/faq-accordion.tsx`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — presentational content sections only, no new input surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-08 | Tampering | `apps/web/modules/landing/{features,reviews,faq}.tsx` | low | accept | Static marketing content rendered from build-time JSON, no runtime user input reaches these components. |
</threat_model>

<verification>
`pnpm --filter web check-types` passes for all 3 components; FAQ renders exactly 5 items (not 7/8 from either old duplicate); Reviews uses zero external image URLs.
</verification>

<success_criteria>
The remaining content-grid sections are built, translated, and ready for Plan 07's final composition — with the FAQ duplication problem (Pitfall 5) resolved by construction, not by post-hoc cleanup.
</success_criteria>

<output>
Create `.planning/phases/06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom/06.2-05-SUMMARY.md` when done
</output>
