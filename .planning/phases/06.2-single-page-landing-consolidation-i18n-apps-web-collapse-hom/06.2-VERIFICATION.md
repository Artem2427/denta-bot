---
phase: 06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom
verified: 2026-08-19T14:07:38Z
status: passed
score: 16/16 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 06.2: Single-Page Landing Consolidation + i18n (apps/web) Verification Report

**Phase Goal:** `apps/web` serves one scrolling, fully-translated (uk default-unprefixed/ru/en) landing page at `/` composed from the design export's sections and sales copy, with real API-backed pricing/lead-capture data; `/blog` stays the only other real, unlocalized route; `/prices`, `/demo`, `/contacts` are retired as destinations and redirect to landing anchors.
**Verified:** 2026-08-19T14:07:38Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

All verification was performed against the live, running codebase — a real `next dev` server (`apps/web`) against a real `apps/server` NestJS API and a real Postgres DB (no mocks), plus direct file/git inspection. No claim below rests on SUMMARY.md prose alone.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `/` renders the Ukrainian landing page, unprefixed (D-08) | ✓ VERIFIED | `curl localhost:3000/` → 200, `<html lang="uk">`, hero copy "Запис пацієнтів..." present |
| 2 | `/ru` and `/en` render the same page shell with translated hero + chrome | ✓ VERIFIED | `curl localhost:3000/ru` → 200, hero "Запись пациентов...работает сама"; `/en` → 200, "Patient booking...runs itself" — nav/footer chrome also translated (confirmed via embedded RSC payload dump) |
| 3 | `/blog` renders untouched Phase 6 content with working uk-translated Header/Footer despite no `[locale]` segment | ✓ VERIFIED | `curl localhost:3000/blog` → 200, contains uk chrome strings "Підключити клініку" / "Автоматизація запису пацієнтів..." (root-layout `NextIntlClientProvider` fallback works as designed — RESEARCH Open Question 1 resolved) |
| 4 | Landing page composes all 10 sections in design order with every anchor resolving to a real, unique element | ✓ VERIFIED | `app/[locale]/page.tsx` renders Hero→ChannelMarquee→ProblemSolution→HowItWorks→AdminShowcase→Features→PricingSection→Reviews→LeadSection→Faq; live HTML confirms `id="product"`, `id="demo"`, `id="features"`, `id="pricing"`, `id="lead"`, `id="faq"` each appear exactly once |
| 5 | `/prices`, `/demo`, `/contacts` no longer exist as content destinations — 307-redirect to landing anchors (D-01/D-03) | ✓ VERIFIED | Live curl: `/prices`→307→`/#pricing`, `/demo`→307→`/#demo`, `/contacts`→307→`/#lead` |
| 6 | Retired-route redirects preserve the visitor's ru/en locale (WR-01 fix) instead of always dropping to uk | ✓ VERIFIED | Behavioral test: set `NEXT_LOCALE=en` cookie via `/en` visit, then `curl -b cookies /contacts` → 307 to `/en/#lead` (not `/#lead`) |
| 7 | Pricing section renders real, live `PricingPlan[]` data (D-04) — Старт/Клініка/Мережа, not placeholder content | ✓ VERIFIED | Direct DB query via Prisma: 3 published rows with correct `name`/`features`/`isPopular`/`sortOrder`; live HTML contains "Старт" (23×), "Клініка" (13×), "Мережа" (6×) |
| 8 | Reseed script is idempotent and never `deleteMany`/truncates (Pitfall 3) | ✓ VERIFIED | `seed-pricing.ts` uses per-plan `findFirst`→`update`/`create` wrapped in a Serializable `$transaction` (WR-06 fix); no `deleteMany`/`truncate` call anywhere in the file; SUMMARY documents 2 consecutive runs producing exactly 3 rows both times, unrelated pre-existing rows untouched |
| 9 | Lead-capture (`#lead`) POSTs to `/leads` with `source: 'contacts'` (D-06), same field/zod shape as the old Contacts form | ✓ VERIFIED | Live `POST /leads {name, email, source:"contacts"}` → 201, row created with `source: "contacts"`; `lead-section.tsx` code confirms `email`/`phone` dynamic field mapping + zod schema factory with translated error messages. Test row deleted after verification (no residue left in DB). |
| 10 | Exactly one conversion form on the landing page (D-07) — no `demo-lead-form` modal wired anywhere | ✓ VERIFIED | `demo-lead-form.tsx`/`demo-cta.tsx` files confirmed deleted from disk; zero real imports of either name anywhere in `apps/web` (only explanatory comments); `LeadSource` enum unchanged (`contacts \| demo`) |
| 11 | Landing sections' UI chrome (badges, price suffix, hero notification/reminder badges) is fully translated across uk/ru/en — no leftover hardcoded Ukrainian (WR-02/WR-03 fixes) | ✓ VERIFIED | Live HTML dump for `/ru`/`/en` shows `notificationBadge`/`reminderBadge`/`popularBadge`/`perMonth` all locale-appropriate ("New booking from Olena Koval" / "Новая запись от Елены Коваль" / "Новий запис від Олени Коваль"); grep for the old hardcoded literal strings in source returns 0 hits |
| 12 | Zero emoji remain in `modules/demo/admin-tab.tsx`/`bot-tab.tsx` (client's no-emoji constraint) | ✓ VERIFIED | `grep -rE '📊\|📅\|👨‍⚕️\|🧑‍🤝‍🧑\|⚙️\|🦷'` against both files → 0 matches |
| 13 | No dead module files remain; zero dangling imports (`modules/home/*`, `modules/prices/*`, `modules/contacts/*`, `demo-cta.tsx`, `demo-lead-form.tsx`) | ✓ VERIFIED | All 3 directories confirmed absent from disk; `demo-cta.tsx`/`demo-lead-form.tsx` absent; grep for their import paths across `apps/web` returns only comment-text, zero real `import` statements |
| 14 | `next` build compiles the phase's own code with zero type errors (Critical/Warning fixes actually landed, not just claimed) | ✓ VERIFIED | `pnpm --filter web check-types` and `next build`'s Turbopack compile step both succeed for all `apps/web`-owned code; the only remaining error is the pre-existing, documented (`STATE.md`/`PROJECT.md`, open since Phase 1) `csstype` conflict in `packages/ui/src/components/shadcn-ui/button-group.tsx` — confirmed unrelated (zero errors reference any file this phase touched) |
| 15 | Mid-phase orchestrator fix: PremiumButton "navy" variant + coral-text-white applied consistently (Hero primary/non-popular-pricing = navy; header/lead-submit/popular-pricing = coral) | ✓ VERIFIED | `premium-button.tsx` defines `navy` (bg-dt-navy, text-dt-warm-white) and `coral` (bg-dt-coral, text-dt-warm-white); grep confirms `hero.tsx` primary CTA = navy, `header.tsx` CTAs = coral, `pricing-section.tsx` CTA = `plan.isPopular ? 'coral' : 'navy'`, `lead-section.tsx` submit = coral |
| 16 | Post-review-fix orchestrator TS correction: redirect stubs return `Promise<void>` (not `Promise<never>`), fixing the real TS2534 error | ✓ VERIFIED | All 3 stub files (`app/{prices,demo,contacts}/page.tsx`) declare `Promise<void>`; commit `6932668` present in git log matching the stated fix; `check-types` clean on these 3 files |

**Score:** 16/16 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/web/i18n/{routing,request,navigation}.ts` | next-intl 5-file setup | ✓ VERIFIED | All present, wired (navigation.ts has 3 real importers post WR-05 fix) |
| `apps/web/proxy.ts` | next-intl middleware, `/blog`+retired-route exclusion, boundary-anchored regex | ✓ VERIFIED | Present; matcher uses `(?:/|$)` boundaries (WR-04 fix) |
| `apps/web/app/[locale]/{layout,page}.tsx` | locale-scoped route tree, full section composition | ✓ VERIFIED | Present; composes all 10 sections; `try/catch` around pricing fetch (CR-01 fix) |
| `apps/web/app/{prices,demo,contacts}/page.tsx` | thin locale-aware redirect stubs | ✓ VERIFIED | All 3 present, `Promise<void>`, cookie-based locale preservation |
| `apps/web/messages/{uk,ru,en}.json` | fully populated, structurally parallel | ✓ VERIFIED | 12 namespaces present in all 3 files; spot-checked key parity across nav/footer/hero/pricing/lead/faq |
| `apps/web/modules/landing/*.tsx` (10 section components) | real, translated, non-stub content | ✓ VERIFIED | All 10 files present; each renders real i18n content, not placeholders |
| `packages/db/prisma/seed-pricing.ts` + `db:seed:pricing` script | idempotent reseed | ✓ VERIFIED | Present, transaction-wrapped (WR-06), script registered in `package.json` |
| `apps/web/shared/components/{header,footer,locale-switcher}.tsx` | translated chrome, locale switcher | ✓ VERIFIED | Present, wired, live-rendered correctly on all 4 route classes tested |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `app/[locale]/page.tsx` | `GET /public/pricing-plans` | server `fetch()`, revalidate:60, try/catch | ✓ WIRED | Live curl confirms 200 + real 3-plan payload; CR-01's crash-on-failure defect fixed |
| `lead-section.tsx` | `POST /leads` | client `fetch()`, `source:'contacts'` | ✓ WIRED | Live POST test → 201, correct `source` value in DB row |
| `admin-tab.tsx` "Connect your clinic" CTA | `/contacts` retired stub | `Link href={routes.contacts}` → cookie-aware redirect | ✓ WIRED | Behaviorally confirmed: `en` cookie preserved through the redirect to `/en/#lead` (WR-01) |
| `proxy.ts` matcher | `/blog`, `/prices`, `/demo`, `/contacts` | boundary-anchored negative lookahead | ✓ WIRED | Regex confirmed anchored (`(?:/|$)`); live curl shows all 4 route classes resolve correctly, no locale-rewrite 404s |
| Root `app/layout.tsx` | `[locale]` segment + `/blog` sibling | unconditional `NextIntlClientProvider`, fallback to `uk` | ✓ WIRED | `/blog` live HTML confirms translated (uk) chrome despite no `[locale]` URL segment |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `pricing-section.tsx` | `plans` prop | `GET /public/pricing-plans` (real DB via Prisma) | Yes — verified 3 real rows via direct DB query, matching live-rendered HTML | ✓ FLOWING |
| `lead-section.tsx` | form submission | `POST /leads` (real DB write) | Yes — verified 201 + real row created (then cleaned up) | ✓ FLOWING |
| `messages/{uk,ru,en}.json` | translated chrome strings | static JSON, resolved server-side via `next-intl` | Yes — confirmed distinct locale-appropriate strings render per route | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `/` returns uk landing page | `curl localhost:3000/` | 200, `lang="uk"`, hero copy present | ✓ PASS |
| `/ru`, `/en` return translated shells | `curl localhost:3000/{ru,en}` | 200, locale-correct hero/nav/footer strings | ✓ PASS |
| `/blog` renders with uk-fallback chrome | `curl localhost:3000/blog` | 200, uk Header/Footer strings present | ✓ PASS |
| `/prices`, `/demo`, `/contacts` redirect | `curl -w '%{http_code} %{redirect_url}'` | 307 → `/#pricing`, `/#demo`, `/#lead` | ✓ PASS |
| Locale preserved through retired-route redirect | cookie-jar curl sequence (`/en` then `/contacts`) | 307 → `/en/#lead` | ✓ PASS |
| `GET /public/pricing-plans` returns real reseeded data | `curl localhost:4000/public/pricing-plans` + direct Prisma query | 200, 3 published rows: Старт/Клініка/Мережа | ✓ PASS |
| `POST /leads` with `source: 'contacts'` | `curl -X POST localhost:4000/leads` | 201, row created with `source: "contacts"` | ✓ PASS |
| `pnpm --filter web check-types` / `next build` | shell | Only pre-existing, unrelated `packages/ui` csstype error; 0 errors in any `apps/web` file this phase touched | ✓ PASS |
| Emoji-free demo components | `grep -rE` emoji pattern | 0 matches | ✓ PASS |
| Anchor IDs unique | `grep -o 'id="..."'` on live HTML | Each of product/demo/features/pricing/lead/faq appears exactly once | ✓ PASS |

### Requirements Coverage

No formal REQUIREMENTS.md IDs are mapped to this phase — confirmed via `grep -n "06.2" .planning/REQUIREMENTS.md` returning zero matches, consistent with ROADMAP.md's explicit statement ("Requirements: None — urgent client-directed insertion... CONTEXT.md decisions D-01 through D-11 serve as this phase's acceptance criteria"). This is not an oversight; all 16 truths above trace directly to D-01 through D-11.

### Anti-Patterns Found

None. Grepped all phase-touched files for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/"coming soon"/"not yet implemented" — zero hits. No hardcoded-empty stub patterns found in any new `modules/landing/*` component; all render real, translated, API-backed, or design-sourced content.

### Human Verification Required

None. All 16 must-have truths, including the two mid-phase orchestrator-applied fixes (PremiumButton variant consistency, redirect-stub return type), were verifiable via direct file inspection, live server behavioral testing (curl against a real running `next dev` + `apps/server` + Postgres), and direct database queries — no visual/subjective judgment call was left unresolved. (Note: RESEARCH.md flagged the `@repo/ui`-vs-`dt-*` visual seam in `admin-showcase.tsx` as a judgment call in Plan 04's SUMMARY — but this is an accepted, documented tradeoff per CLAUDE.md's explicit carve-out for the Demo simulation, not a phase-goal blocker, and does not gate this verification.)

### Gaps Summary

No gaps. All 6 code-review findings (1 Critical + 5 in-scope Warnings, WR-05 folded into WR-01's commit) from `06.2-REVIEW.md` were independently re-verified as actually landed in the codebase — not merely claimed in `06.2-REVIEW-FIX.md` — via direct source inspection and, where applicable, live behavioral testing (WR-01's cookie-preservation claim, CR-01's try/catch, WR-04's regex-boundary matcher). The two orchestrator-applied out-of-band commits (`97d1a27` PremiumButton navy/coral fix, `6932668` `Promise<void>` TS fix) were both confirmed present in git history and correctly applied across all their claimed call sites. `pnpm --filter web check-types` is genuinely clean except for the pre-existing, documented, out-of-scope `csstype` conflict (open since Phase 1, tracked in `PROJECT.md`/`STATE.md`, not touched or worsened by this phase).

---

_Verified: 2026-08-19T14:07:38Z_
_Verifier: Claude (gsd-verifier)_
