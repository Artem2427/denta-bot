---
phase: 06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom
plan: 03
subsystem: i18n
tags: [next-intl, i18n, content, translation, messages, json]

# Dependency graph
requires:
  - phase: 06.2-01
    provides: apps/web/messages/{uk,ru,en}.json with nav/footer/hero namespaces and next-intl routing infrastructure
provides:
  - "9 new namespaces (marquee, problemSolution, howItWorks, adminShowcase, features, pricing, reviews, lead, faq) fully populated in all 3 message files"
  - "Stable message-key contract Wave 3 plans (04/05/06) consume via useTranslations()/getTranslations()"
affects: [06.2-04, 06.2-05, 06.2-06]

# Actuals (#2632)
actuals:
  tokens: 7400
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "next-intl message namespace per landing section, keys named to match design intent, not literal design-file field names (e.g. features.items[].title/text, not f.mark/f.title/f.text)"

key-files:
  created: []
  modified:
    - apps/web/messages/uk.json
    - apps/web/messages/ru.json
    - apps/web/messages/en.json

key-decisions:
  - "marquee.channels kept byte-identical across uk/ru/en (proper nouns / channel names, not translated), per plan spec"
  - "features namespace drops the design's numeric mark field (01-08) — Plan 05's 8-item grid uses Phosphor icons per index instead"
  - "lead namespace field names/values reuse contact-form.tsx's existing Ukrainian labels/errors verbatim in uk.json so Wave 3 can wire useTranslations() with zero copy drift from the already-shipped form"
  - "faq uses the design file's canonical 5-question set only — did not merge in the old modules/prices or modules/contacts FAQ content (Pitfall 5 from RESEARCH.md)"
  - "ru/en pricing plan tier names referenced inside FAQ answers (Клініка/Мережа) were adapted to Клиника/Сеть (ru) and Clinic/Network (en) as marketing chrome text — the actual PricingPlan DB rows stay Ukrainian-only per D-04/D-11, unaffected by this plan"

patterns-established:
  - "Message namespace key names describe UI role (e.g. beforeLabel/afterLabel, ctaLabel) not raw design-file variable names — keeps the message contract legible for Wave 3 without needing to cross-reference the .dc.html export"

requirements-completed: []

coverage:
  - id: D1
    description: "All 9 remaining landing-section namespaces (marquee/problemSolution/howItWorks/adminShowcase/features/pricing/reviews/lead/faq) exist in uk.json/ru.json/en.json with identical key structure"
    verification:
      - kind: unit
        ref: "node -e namespace-presence check (Task 1 <verify>) — all namespaces present"
        status: pass
      - kind: unit
        ref: "node -e key-and-array-length parity check (Task 2 <verify>) — key parity confirmed across uk/ru/en"
        status: pass
    human_judgment: false
  - id: D2
    description: "uk.json carries persuasive Ukrainian sales copy from the design file; ru.json/en.json are adapted-not-literal translations preserving the same persuasive intent"
    verification: []
    human_judgment: true
    rationale: "Translation quality/persuasive-intent fidelity is a judgment call — no automated test can verify adaptation quality, only structural parity (covered by D1)"

duration: 12min
completed: 2026-08-19
status: complete
---

# Phase 06.2 Plan 03: Landing Copy Content Authoring Summary

**Authored all 9 remaining landing-page message namespaces (marquee/problemSolution/howItWorks/adminShowcase/features/pricing/reviews/lead/faq) in uk.json (canonical, ported from the DentaBot Landing design export) plus adapted ru.json/en.json translations, with 100% key-and-array-length parity verified across all 3 locale files.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-08-19T09:14:53Z
- **Completed:** 2026-08-19T09:18:41Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- `apps/web/messages/uk.json` extended with 9 new namespaces (158 lines added), sourced from `DentaBot Landing.dc.html`'s `painBefore`/`painAfter`/`steps`/`features`/`reviews`/`guarantees`/`faq` arrays and section copy, keeping Plan 01's `nav`/`footer`/`hero` untouched
- `apps/web/messages/ru.json` and `apps/web/messages/en.json` extended with the same 9 namespaces (158 lines each), containing persuasive-intent adaptations (not literal translations) per D-10
- `lead` namespace's field labels/placeholders/errors in uk.json match `apps/web/modules/contacts/contact-form.tsx`'s existing hardcoded Ukrainian strings verbatim, so Wave 3 can rewire that component to `useTranslations()` with zero copy drift
- Full 3-locale key-and-array-length parity confirmed via automated verification (both tasks' `<verify>` scripts passed)

## Task Commits

Each task was committed atomically:

1. **Task 1: author uk.json — canonical namespaces for all remaining sections** - `56ff2b4` (feat)
2. **Task 2: author ru.json and en.json — adapted (not literal) translations, key-parity verified** - `d0f4b0c` (feat)

_Note: This is a content-only plan; no TDD tasks._

## Files Created/Modified
- `apps/web/messages/uk.json` - added marquee/problemSolution/howItWorks/adminShowcase/features/pricing/reviews/lead/faq namespaces (canonical Ukrainian copy)
- `apps/web/messages/ru.json` - same 9 namespaces, adapted Russian copy
- `apps/web/messages/en.json` - same 9 namespaces, adapted English copy

## Decisions Made
- `marquee.channels` kept identical across all 3 locale files (channel/brand names, not meaningfully translatable) — per plan's explicit instruction, including the Ukrainian-spelled "SMS-шлюз"/"Веб-віджет" entries carried through verbatim to ru/en as the plan specified
- `features.items` drops the design's numeric `mark` field ("01"-"08") since Plan 05 will use Phosphor icons per grid index instead
- FAQ content sourced exclusively from the design file's 5-question canonical set, not merged with either pre-existing `modules/prices`/`modules/contacts` FAQ component content (per RESEARCH.md Pitfall 5)
- Pricing tier names referenced inside FAQ answer text (a chrome/marketing-copy context, not the DB record itself) were adapted to `Клиника`/`Сеть` (ru) and `Clinic`/`Network` (en) — the actual `PricingPlan` DB rows remain Ukrainian-only per D-04/D-11, this is unrelated marketing-copy phrasing inside a translated sentence

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The full message-key contract for the landing page is now stable and complete across all 3 locales — Wave 3's parallel component-building plans (04/05/06) can build every remaining section purely via `useTranslations()`/`getTranslations('namespace')` calls with zero missing-key risk on uk/ru/en
- No blockers. `pricing` namespace intentionally contains chrome-only strings (eyebrow/title/billing toggle/footnote/CTA label) — actual plan name/description/features stay DB-sourced Ukrainian-only per D-04/D-11, unchanged by this plan
- `lead` namespace's exact key names/values are ready for Plan 04/05/06 to wire `contact-form.tsx` to `useTranslations('lead')` without needing to re-derive field copy

---
*Phase: 06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom*
*Completed: 2026-08-19*

## Self-Check: PASSED

All created/modified files verified present on disk; both task commits (`56ff2b4`, `d0f4b0c`) verified present in git log.
