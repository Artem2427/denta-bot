---
phase: 06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/fonts.ts
  - apps/web/app/layout.tsx
  - apps/web/app/premium-theme.css
  - apps/web/shared/components/section.tsx
  - apps/web/shared/components/eyebrow.tsx
  - apps/web/shared/components/section-heading.tsx
  - apps/web/shared/components/stat.tsx
  - apps/web/modules/home/problem.tsx
  - apps/web/modules/home/hero.tsx
  - apps/web/app/demo/page.tsx
autonomous: true
requirements: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every apps/web page renders headings/body in Manrope and has JetBrains Mono available as a third font register, both covering the cyrillic subset (Ukrainian copy renders with no fallback-font flash)"
    - "apps/web/app/premium-theme.css defines --dt-border, --dt-ring, --dt-duration-fast/base, --text-dt-eyebrow, --radius-dt-frame/frame-inner, and the D-03 spacing scale additively — no existing dt- token name is removed or renamed"
    - "Home's Problem section renders byte-identical Ukrainian copy to before, now composed from the new Section + SectionHeading primitives instead of page-local duplicated markup"
    - "Home's Hero stat numbers (500+ / 9 500+ / 98%) still count up on scroll into view, now rendered through the Stat primitive, with useCountUp's animation logic untouched"
    - "The Demo page's DEMO MODE badge renders the same coral pill it does today, now via the Eyebrow primitive and --text-dt-eyebrow token instead of the .dt-caption utility class, and .dt-caption no longer exists in premium-theme.css"
  artifacts:
    - path: "apps/web/shared/components/section.tsx"
      provides: "Section primitive: semantic <section>, CVA tone variant (warm-white default / navy / muted), vertical rhythm wired to --dt-section-y"
    - path: "apps/web/shared/components/eyebrow.tsx"
      provides: "Eyebrow primitive: uppercase mono label, CVA tone variant (navy default / on-navy)"
    - path: "apps/web/shared/components/section-heading.tsx"
      provides: "SectionHeading primitive composing optional Eyebrow + h2 + optional description paragraph"
    - path: "apps/web/shared/components/stat.tsx"
      provides: "Stat primitive for KPI/number displays (tabular-nums value + Eyebrow-styled label)"
  key_links:
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/app/fonts.ts"
      via: "interHeading.variable / interBody.variable / jetbrainsMono.variable classNames on <body>"
      pattern: "jetbrainsMono.variable"
    - from: "apps/web/modules/home/problem.tsx"
      to: "apps/web/shared/components/section-heading.tsx"
      via: "SectionHeading import replacing the inline mb-12 heading div"
      pattern: "SectionHeading"
    - from: "apps/web/app/premium-theme.css"
      to: "apps/web/shared/components/eyebrow.tsx"
      via: "text-dt-eyebrow Tailwind utility generated from the --text-dt-eyebrow @theme token"
      pattern: "text-dt-eyebrow"
---

<objective>
Establish the Phase 06.1 design-system foundation: swap the heading/body font from Inter to Manrope and add a JetBrains Mono register (D-01, D-02), add the phase's new `dt-` tokens to `premium-theme.css` additively (D-03 through D-09, D-19, D-22, D-23), and build the four new shared primitives named in the client brief — `Section`, `Eyebrow`, `SectionHeading`, `Stat` (D-11, D-12) — proving the whole chain end-to-end by applying them to Home's Problem section and Hero stats, and by migrating the one real `.dt-caption` call site (D-07).

Purpose: this is the tracer wave every other 06.1 plan builds on — Waves 2's five plans (shared-component retrofits + the Home/Prices/Contacts/Demo/Blog sweeps) all import `Section`/`Eyebrow`/`SectionHeading`/`Stat` from this plan's new files and consume the tokens this plan adds to `premium-theme.css`. Nothing here changes rendered Ukrainian copy — every task is a markup/CSS restructuring exercise per the phase's "no copy changes" hard constraint.
Output: `apps/web/app/fonts.ts` (Manrope + JetBrains Mono), `apps/web/app/premium-theme.css` (new tokens), 4 new primitive components, `apps/web/modules/home/problem.tsx` and `apps/web/modules/home/hero.tsx` (first real consumers), `apps/web/app/demo/page.tsx` (dt-caption migrated).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-CONTEXT.md
@.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-RESEARCH.md
@apps/web/app/fonts.ts
@apps/web/app/layout.tsx
@apps/web/app/premium-theme.css
@apps/web/shared/components/premium-button.tsx
@apps/web/shared/lib/cn.ts
@apps/web/modules/home/problem.tsx
@apps/web/modules/home/hero.tsx
@apps/web/app/demo/page.tsx
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Fonts + dt- tokens + Section/Eyebrow/SectionHeading — proven on Home's Problem section</name>
  <files>apps/web/app/fonts.ts, apps/web/app/layout.tsx, apps/web/app/premium-theme.css, apps/web/shared/components/section.tsx, apps/web/shared/components/eyebrow.tsx, apps/web/shared/components/section-heading.tsx, apps/web/modules/home/problem.tsx</files>
  <reversibility rating="costly">D-01's Manrope swap touches every rendered page's typography and is already user-confirmed live during context-gathering (2026-08-18) — do not re-litigate or gate behind a checkpoint; flagged here only so the executor knows this specific choice is locked, not open for reconsideration.</reversibility>
  <action>
    In apps/web/app/fonts.ts (D-01): replace the `Inter` import from next/font/google with `Manrope`. Keep the two exported const identifiers exactly as they are today — `interHeading` and `interBody` — per the locked decision's literal wording ("swap the interHeading/interBody exports to Manrope"); do not rename them, layout.tsx's import statement must not need to change. `interHeading`: weights ['700','800'], `interBody`: weights ['400','500'], both subsets ['latin','cyrillic'], display 'swap', same variable names (--font-dt-heading-raw / --font-dt-body-raw). Update the file's top comment since it currently explains a "plain Inter" rationale that is now stale.

    Add a third export (D-02): `jetbrainsMono` from `JetBrains_Mono` (next/font/google), weight ['500'], subsets ['latin','cyrillic'], variable '--font-dt-mono-raw', display 'swap'.

    In apps/web/app/layout.tsx: add `${jetbrainsMono.variable}` to the body element's className template string, alongside the existing `${interHeading.variable} ${interBody.variable}`. Import `jetbrainsMono` from './fonts' alongside the existing two imports.

    In apps/web/app/premium-theme.css, add new tokens additively — never remove or rename an existing token:
    - In the `@theme` block: the D-03 spacing scale, ten tokens named --spacing-dt-4 through --spacing-dt-112 at these rem values (16px base): 4px=0.25rem, 8px=0.5rem, 12px=0.75rem, 16px=1rem, 24px=1.5rem, 32px=2rem, 48px=3rem, 64px=4rem, 96px=6rem, 112px=7rem. Confirmed via sitewide grep this session that zero arbitrary-bracket spacing values (p-[...], gap-[...], m-[...]) exist anywhere in apps/web today, so there is no mechanical migration to perform — these tokens' first real consumers are the Section and SectionHeading primitives built later in this same task (D-03's own text explicitly allows "don't force-fit" existing values that already match Tailwind's default scale).
    - Also in `@theme`: the D-07 eyebrow type-scale token pair, --text-dt-eyebrow: 0.8125rem and its companion --text-dt-eyebrow--letter-spacing: 0.02em, following the existing --text-dt-h1/--text-dt-h2 companion-value pattern already in the file. Note for the Eyebrow component below: Tailwind v4's @theme text-scale companions cover font-size/line-height/letter-spacing/font-weight but NOT text-transform, so "uppercase" must be its own utility class on the component, not baked into this token.
    - Also in `@theme`: the D-22 phone-frame radius tokens --radius-dt-frame: 3rem and --radius-dt-frame-inner: 2.5rem (consumed later in Wave 2's Demo sweep, apps/web/modules/demo/bot-tab.tsx — no action needed on that file in this task).
    - In the `@theme inline` block: add --font-dt-mono: var(--font-dt-mono-raw); as a third line following the existing --font-dt-heading / --font-dt-body pattern.
    - In the `:root` block: add --dt-border: rgba(26, 43, 61, 0.08); (D-04 — raw custom property, matching the existing --dt-container-max / --dt-ease-expo-out pattern already in that block; this resolves D-04's "Claude's Discretion" mechanism choice as a raw custom property, not a new @theme color token, consumed via border-[var(--dt-border)] arbitrary-value utilities). Add --dt-ring: var(--color-dt-teal); (D-05 — references the existing @theme color rather than duplicating its hex literal). Add --dt-duration-fast: 180ms; and --dt-duration-base: 240ms; (D-06).
    - D-08 needs no new CSS: the existing --radius-dt-card/--radius-dt-input and --shadow-dt-sm/card/hover tokens already match the brief exactly — do not touch them.
    - D-09 regression guard: after all edits above, confirm no hex literal (#rrggbb-shaped string) was introduced in any file this task touches.

    Create apps/web/shared/components/section.tsx (D-11, D-12): a CVA-based component wrapping a semantic <section> element, data-slot="section". Define a `tone` variant with three values: warm-white (default) applying bg-dt-warm-white; navy applying bg-dt-navy; muted applying bg-dt-navy/5 (D-19 — resolves RESEARCH.md's open question, targeting the exact-duplicate bg-dt-navy/5 py-8 lg:py-12 string RESEARCH.md found identically in comparison-table.tsx, the Prices FAQ section, and the Contacts FAQ section — those three get migrated in Wave 2 plans, not here). Base classes apply vertical padding via the arbitrary-value utility py-[clamp(var(--spacing-dt-64),8vw,var(--dt-section-y))] — this wires up the --dt-section-y: 7rem custom property that has sat defined-but-unused in premium-theme.css's :root block since Phase 01.1 (first real consumption). Forward all standard <section> props plus className merged via cn (from @/shared/lib/cn), so a caller can override padding. Section does NOT render Container internally — callers keep composing <Section><Container>...</Container></Section> exactly like today's <section className="..."><Container>, so migrating a call site is a pure wrapper swap with zero JSX-nesting change. Do not force any page-level hero/intro section (Home's Hero, or the top sections of Contacts/Prices/Blog/Demo, which use a distinct taller pt-24/pb-8-shaped offset to clear the fixed header) into this default rhythm in this task — that decision belongs to the later sweep plans that touch those specific files.

    Create apps/web/shared/components/eyebrow.tsx (D-11, D-12): a CVA-based <span data-slot="eyebrow">, `tone` variant with two values — navy (default, text-dt-navy) and on-navy (text-dt-warm-white/80, for placement on Section tone="navy" backgrounds). Base classes: uppercase text-dt-eyebrow font-dt-mono (text-dt-eyebrow is the Tailwind utility generated by the --text-dt-eyebrow/--text-dt-eyebrow--letter-spacing tokens added above).

    Create apps/web/shared/components/section-heading.tsx (D-11, D-12): composes an optional Eyebrow (via an `eyebrow?: string` prop), a required `title: React.ReactNode` rendered as <h2 className="text-dt-h2 font-dt-heading font-bold text-balance"> (text-balance is Tailwind's built-in text-wrap utility — no new CSS needed), and an optional `description?: React.ReactNode` rendered as <p className="mx-auto mt-4 max-w-2xl text-dt-body text-pretty">. Accept a `tone?: 'warm-white' | 'navy'` prop mirroring Section's tone, switching heading/description color between text-dt-navy/text-dt-graphite (default) and text-dt-warm-white/text-dt-warm-white/80 (navy). Outer wrapper: <div data-slot="section-heading" className="mb-dt-48 text-center"> — this swaps the newly-added --spacing-dt-48 token in for today's mb-12 (identical 48px value: the first real consumer of the D-03 spacing scale, zero visual change). CRITICAL scope note: leave the `eyebrow` prop unset at every call site this phase touches, in this task and every later Wave 2 sweep plan — none of the 9 duplicated heading blocks RESEARCH.md catalogued currently carry eyebrow-shaped text, and typing in new label copy would violate this phase's "no copy changes" hard constraint. The prop exists for future content work, not this phase.

    Apply the tracer slice end-to-end: rewrite apps/web/modules/home/problem.tsx's `<section className="py-8 lg:py-12"><Container><Reveal><div className="mb-12 text-center"><h2>Знайомо?</h2><p>Як зараз виглядає запис у більшості клінік</p></div></Reveal>` opening to use `<Section><Container><Reveal><SectionHeading title="Знайомо?" description="Як зараз виглядає запис у більшості клінік" /></Reveal>` — same rendered Ukrainian text, same Container/Reveal/StaggerGrid nesting below it, only the wrapper markup changes. While this file is open, also apply D-21's color migration to its 4 icon-background chips: bg-red-100 to bg-dt-coral/10, bg-orange-100 to bg-dt-amber/10, bg-yellow-100 to bg-dt-amber/10 (orange and yellow both map to the nearest palette color, amber — the fixed 7-color palette has no separate orange/yellow), bg-purple-100 to bg-dt-teal/10 (nearest cool accent). Leave the emoji glyphs and card copy untouched.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec eslint --max-warnings 0 app/fonts.ts app/layout.tsx shared/components/section.tsx shared/components/eyebrow.tsx shared/components/section-heading.tsx modules/home/problem.tsx && ! pnpm exec tsc --noEmit 2>&1 | grep "error TS" | grep -v "button-group.tsx"</automated>
  </verify>
  <done>fonts.ts exports interHeading/interBody (now Manrope) + jetbrainsMono; layout.tsx registers all three variables; premium-theme.css compiles with the new tokens present and no existing token removed; Section/Eyebrow/SectionHeading exist and type-check; Home's Problem section renders the same two lines of Ukrainian copy via SectionHeading with zero eyebrow text; the eslint+tsc gate above passes with no new error beyond the known pre-existing packages/ui/button-group.tsx one.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Stat primitive — applied to Home's Hero stats</name>
  <files>apps/web/shared/components/stat.tsx, apps/web/modules/home/hero.tsx</files>
  <action>
    Create apps/web/shared/components/stat.tsx (D-11, D-12): <div data-slot="stat"> accepting `value: React.ReactNode` and `label: string` (plus className merged via cn, default wrapper class "space-y-1"). Render value inside <div className="text-dt-h2 font-dt-heading font-bold text-dt-teal tabular-nums"> (tabular-nums keeps digit width stable during count-up). Render label by reusing the Eyebrow component built in Task 1 (import from './eyebrow'), tone="navy", with a "mt-1 block" className override — do not hand-roll a second uppercase/mono treatment, compose Eyebrow directly (D-10's "reuse in place" spirit applied to the new primitives too).

    In apps/web/modules/home/hero.tsx's HeroStat function: replace the inline `<div className="space-y-1"><div className="text-dt-h2 font-dt-heading font-bold text-dt-teal">{formatStatNumber(count, stat.thousands)}{stat.suffix}</div><div className="text-sm text-dt-graphite">{stat.label}</div></div>` markup with `<Stat value={\`${formatStatNumber(count, stat.thousands)}${stat.suffix}\`} label={stat.label} />` (import Stat from '@/shared/components/stat'). Keep the useCountUp hook call and formatStatNumber function exactly as they are — D-10's "reuse existing hooks in place" applies here: only the display markup changes, the count-up animation logic is untouched.

    While this file is open, also migrate its one D-04 hairline occurrence: the "Нова платформа для стоматологій" badge pill (around line 52) currently uses `border border-dt-navy/10` — change to `border border-[var(--dt-border)]`.

    Explicit no-change notes for this file (do not act on these, they are documented decisions, not omissions): D-25 — the Unsplash stock <Image> stays exactly as-is, replacing it with a real product screenshot is a content/copy decision explicitly out of this purely-visual phase's scope. D-26 — the idleBounceAnimate/idleBounceTransition motion.div (the floating "Новий запис від Олени Коваль" notification card) stays exactly as-is: it is already useReducedMotion()-gated and subtle, and CONTEXT.md's considered decision is to keep it rather than treat it as the "aggressive infinite loop" the brief's motion rules warn against.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec eslint --max-warnings 0 shared/components/stat.tsx modules/home/hero.tsx && ! pnpm exec tsc --noEmit 2>&1 | grep "error TS" | grep -v "button-group.tsx"</automated>
  </verify>
  <done>Stat exists, composes Eyebrow for its label, and type-checks; Hero renders its 3 stats (500+/9 500+/98%) via Stat with count-up behavior unchanged; the hero badge pill's border now references --dt-border; the Unsplash image and idle-bounce card are unmodified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate the one .dt-caption call site to Eyebrow and deprecate the utility class</name>
  <files>apps/web/app/demo/page.tsx, apps/web/app/premium-theme.css</files>
  <action>
    In apps/web/app/demo/page.tsx (line 13), replace `<span className="dt-caption mb-4 inline-block rounded-full bg-dt-coral/10 px-3 py-1 text-dt-coral">DEMO MODE</span>` with `<Eyebrow className="mb-4 inline-block rounded-full bg-dt-coral/10 px-3 py-1 text-dt-coral">DEMO MODE</Eyebrow>` (import Eyebrow from '@/shared/components/eyebrow'). The passed className's text-dt-coral overrides Eyebrow's default text-dt-navy via cn's tailwind-merge conflict resolution, preserving today's exact coral-pill look while the underlying uppercase/letter-spacing/font-size mechanism moves from the .dt-caption utility class to the new --text-dt-eyebrow token + font-dt-mono (D-07).

    Confirmed via grep this session: this is the only real `.dt-caption` utility-class call site sitewide. Do NOT touch apps/web/modules/demo/bot-tab.tsx's two `text-dt-caption` occurrences (chat timestamp row, lines 169-170) — those consume the separate, unrelated `--text-dt-caption` type-scale token (font-size only, 0.8125rem) that stays exactly as-is; conflating the two would silently change chat-timestamp typography, which is out of scope (RESEARCH.md Pitfall 2).

    After the one call site is migrated, delete the now-dead `.dt-caption { letter-spacing: 0.02em; text-transform: uppercase; }` block from premium-theme.css's `@layer utilities` section (D-07's deprecation step, done last so there is never a moment where the class is deleted before its only consumer stops referencing it).
  </action>
  <verify>
    <automated>cd apps/web && grep -c "dt-caption\"" app/demo/page.tsx | grep -qx 0 && ! grep -q "\.dt-caption {" app/premium-theme.css && pnpm exec eslint --max-warnings 0 app/demo/page.tsx app/premium-theme.css && ! pnpm exec tsc --noEmit 2>&1 | grep "error TS" | grep -v "button-group.tsx"</automated>
  </verify>
  <done>The Demo page's "DEMO MODE" badge renders via Eyebrow with the identical coral-pill visual; the .dt-caption utility class no longer exists in premium-theme.css; bot-tab.tsx's two text-dt-caption chat-timestamp occurrences are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Build-time font fetch | `next/font/google` fetches Manrope/JetBrains Mono from Google's font CDN at build time and self-hosts the resulting files — no runtime third-party request from the browser |
| CSS token authoring | New `dt-` custom properties/tokens are static, developer-authored values compiled by Tailwind — no user input reaches this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-06.1-01 | Tampering | apps/web/app/fonts.ts (next/font/google build-time fetch) | low | accept | Fonts are self-hosted at build time (no runtime CDN dependency in the shipped page); Next.js validates subset names against Google's font metadata and fails the build loudly on a bad subset — this is the same already-adopted pattern Phase 01.1 used for Inter, extended to two more font families, not a new trust boundary |
| T-06.1-02 | Information Disclosure | Section/Eyebrow/SectionHeading/Stat (new presentational components) | low | accept | All four are pure prop-driven wrappers around existing static/mock page content — no new data flow, no user input, same trust boundary as the components they replace |
| T-06.1-03 | Denial of Service | premium-theme.css new @theme tokens | low | accept | Static CSS custom properties compiled at build time; no runtime computation, no user-controllable input can grow this surface |

</threat_model>

<verification>
Run `cd apps/web && pnpm exec tsc --noEmit 2>&1 | grep "error TS"` and confirm the only line is the pre-existing `packages/ui/src/components/shadcn-ui/button-group.tsx` error (confirmed present in the baseline before this plan ran — not introduced by this plan, not this plan's responsibility to fix). Run `cd apps/web && pnpm exec eslint --max-warnings 0 app/fonts.ts app/layout.tsx app/premium-theme.css shared/components/section.tsx shared/components/eyebrow.tsx shared/components/section-heading.tsx shared/components/stat.tsx modules/home/problem.tsx modules/home/hero.tsx app/demo/page.tsx` and confirm zero output. Manually diff Home's Problem/Hero sections and the Demo page's badge against the pre-phase screenshots to confirm zero Ukrainian text changed and only spacing/typography/color shifted.
</verification>

<success_criteria>
Manrope + JetBrains Mono load sitewide with the cyrillic subset; premium-theme.css's new tokens (spacing scale, --dt-border, --dt-ring, --dt-duration-fast/base, --text-dt-eyebrow, --radius-dt-frame/frame-inner) exist without disturbing any pre-existing token; Section, Eyebrow, SectionHeading, Stat exist, type-check, and are proven end-to-end on Home's Problem section and Hero stats with zero copy changes; the .dt-caption utility class is fully retired in favor of the new Eyebrow/token pair.
</success_criteria>

<output>
Create `.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-01-SUMMARY.md` when done
</output>