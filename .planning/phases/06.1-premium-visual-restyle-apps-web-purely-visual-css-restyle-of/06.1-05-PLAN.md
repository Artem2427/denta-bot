---
phase: 06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of
plan: 05
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - apps/web/app/demo/page.tsx
  - apps/web/modules/demo/bot-tab.tsx
  - apps/web/app/blog/page.tsx
  - apps/web/app/blog/[slug]/page.tsx
  - apps/web/modules/blog/related-posts.tsx
autonomous: true
requirements: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Demo page's bot phone-mockup frame renders with dt- radius tokens instead of arbitrary rounded-[3rem]/rounded-[2.5rem] values, and its shadow references --shadow-dt-hover instead of the plain Tailwind shadow-2xl"
    - "Blog's list page and post-detail page render byte-identical Ukrainian copy and unchanged post data (still fetched from the real API per Phase 6 — this plan does not touch any fetch call)"
    - "Related Posts' heading now has the same vertical rhythm every other section on the site has, closing the one section sitewide that previously had no padding className at all"
  artifacts: []
  key_links:
    - from: "apps/web/modules/demo/bot-tab.tsx"
      to: "apps/web/app/premium-theme.css"
      via: "rounded-dt-frame / rounded-dt-frame-inner utilities generated from --radius-dt-frame/--radius-dt-frame-inner"
      pattern: "rounded-dt-frame"
---

<objective>
Apply D-17's primitive sweep to the Demo and Blog routes, plus D-22/D-23's phone-frame token migration in `bot-tab.tsx` and D-04's remaining hairline-border call sites there. Closes RESEARCH.md's flagged `related-posts.tsx` anti-pattern (a bare `<section>` with no padding className at all, the one page on the site with an inconsistent vertical rhythm).

Purpose: completes the D-17 sweep on the last two of the five routes. Runs in parallel with Plans 02/03/04 — all depend only on Plan 01, and this plan's file list has zero overlap with any other Wave 2 plan (app/demo/page.tsx was touched by Plan 01 Task 3 for the dt-caption migration only — that plan runs in Wave 1, strictly before this one, so there is no same-wave conflict).
Output: Demo and Blog routes fully migrated to Section/SectionHeading; bot-tab.tsx's phone-frame chrome fully token-based.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-CONTEXT.md
@.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-RESEARCH.md
@apps/web/shared/components/section.tsx
@apps/web/shared/components/section-heading.tsx
@apps/web/app/demo/page.tsx
@apps/web/modules/demo/bot-tab.tsx
@apps/web/app/blog/page.tsx
@apps/web/app/blog/[slug]/page.tsx
@apps/web/modules/blog/related-posts.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Demo sweep — page Section wrap + bot-tab.tsx phone-frame tokens</name>
  <files>apps/web/app/demo/page.tsx, apps/web/modules/demo/bot-tab.tsx</files>
  <action>
    In apps/web/app/demo/page.tsx: this page has 2 `<section>` blocks. The first (`pb-12`, the DEMO MODE badge + heading + intro paragraph, already carrying the Eyebrow migration from Plan 01 Task 3) is page-hero-shaped — wrap in `<Section className="pb-12">`, preserving its exact current padding. The second (the bare `<section>` wrapping `<DemoTabs />`, no padding className today) — wrap in `<Section>` using its default vertical rhythm, since this section currently has no padding at all and adopting the new default rhythm is the intended fix, not a preserved-override case. Do not add a SectionHeading to either — neither block matches the duplicated heading pattern (the first already has its own custom hero-shaped heading block, the second has no heading at all, just `<DemoTabs />`).

    In apps/web/modules/demo/bot-tab.tsx, migrate the phone-mockup frame chrome (D-16, D-22, D-23, D-04). D-16 context: the brief asks for bot/admin screenshots to be framed in iPhone/browser chrome with a soft shadow and no 3D tilt — RESEARCH.md confirmed this session there is no raw `<img>` screenshot anywhere in the codebase to frame; this phone-mockup is already hand-built device-frame chrome (not a screenshot), so D-16's intent is satisfied by tightening this existing chrome to dt- tokens rather than by adding new framing. Change `rounded-[3rem]` to `rounded-dt-frame` and `rounded-[2.5rem]` to `rounded-dt-frame-inner` (both generated by the tokens Plan 01 added to premium-theme.css). Change `shadow-2xl` to `shadow-[var(--shadow-dt-hover)]` (D-23's default recommendation — reuse the existing heaviest shadow tier rather than adding a new one). Leave `bg-gray-900` exactly as-is — CONTEXT.md's explicit D-21 exception: a near-black phone bezel is a "real-world" semantic color choice with no defensible dt-navy equivalent (navy at `#1a2b3d` is visibly lighter than a realistic phone bezel), not a chrome inconsistency. Migrate this file's three `border-dt-navy/10` occurrences (the bot message bubble border, the typing-indicator bubble border, and the scenario-list top divider) to `border-[var(--dt-border)]`. Do not touch this file's scenario-playback state machine, timers, or the `text-dt-caption` chat-timestamp occurrences (lines 169-170, per Plan 01 Task 3's explicit note — that is the unrelated `--text-dt-caption` type-scale token, not `.dt-caption`).
  </action>
  <verify>
    <automated>cd apps/web && ! grep -q "rounded-\[" modules/demo/bot-tab.tsx && grep -q "rounded-dt-frame" modules/demo/bot-tab.tsx && grep -q "shadow-\[var(--shadow-dt-hover)\]" modules/demo/bot-tab.tsx && grep -q "bg-gray-900" modules/demo/bot-tab.tsx && grep -c "text-dt-caption" modules/demo/bot-tab.tsx | grep -qx 2 && grep -c "Живе демо" app/demo/page.tsx | grep -qx 1 && pnpm exec eslint --max-warnings 0 app/demo/page.tsx modules/demo/bot-tab.tsx && ! pnpm exec tsc --noEmit 2>&1 | grep "error TS" | grep -v "button-group.tsx"</automated>
  </verify>
  <done>Zero rounded-[ arbitrary values remain in bot-tab.tsx; its phone frame uses rounded-dt-frame/rounded-dt-frame-inner and shadow-[var(--shadow-dt-hover)]; bg-gray-900 bezel and both text-dt-caption timestamp occurrences are untouched; Demo page's 2 sections render via Section with the tabs section adopting the new default rhythm.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Blog sweep — list/detail pages + related-posts.tsx anti-pattern fix</name>
  <files>apps/web/app/blog/page.tsx, apps/web/app/blog/[slug]/page.tsx, apps/web/modules/blog/related-posts.tsx</files>
  <action>
    In apps/web/app/blog/page.tsx: this file has up to 3 `<section>` blocks (the empty-state early-return doesn't need touching — leave it as-is). For the main return path: the hero-title `<section className="pb-12">` and the featured-post `<section className="pb-12">` are both page-hero-shaped — wrap each in `<Section className="pb-12">`, preserving current padding. The filters-wrapper `<section className="pb-12">` (only rendered when `remainingPosts.length > 0`) — same treatment, `<Section className="pb-12">`. None of these three carry the duplicated SectionHeading pattern (the hero title is a standalone `<h1>`, the featured post's `<h2>` lives inside a PremiumCard as article content, and BlogFilters owns its own search/filter UI) — do not add SectionHeading to any of them, only swap the outer `<section>` tag for `<Section>`. Do not touch the `fetch()` call, `getServerApiUrl()`, or any post-data logic.

    In apps/web/app/blog/[slug]/page.tsx: the bottom CTA block (`<div className="mb-16 rounded-dt-card bg-dt-navy p-8 text-center text-dt-warm-white lg:p-12">`, "Хочете автоматизувати запис у вашій клініці?") is structurally the same shape as Home's already-migrated CtaBanner (Plan 03) — heading + subtext + two CTA buttons on a navy surface. Leave it as a `<div>` (it is a decorative in-article callout, not a page-level `<section>` landmark, so it should NOT become a `<Section>` element), but do extract its heading+subtext pair into `<SectionHeading tone="navy" title="Хочете автоматизувати запис у вашій клініці?" description="Спробуйте DentaBot безкоштовно протягом 14 днів" />`, keeping the two PremiumButton CTAs in their own sibling block below exactly as they render today. Do not touch this file's `fetch()` calls, `notFound()` handling, or `extractPostBodyBlocks` usage.

    In apps/web/modules/blog/related-posts.tsx (RESEARCH.md's flagged anti-pattern — the one bare `<section>` sitewide with zero padding className): wrap the existing bare `<section>` in `<Section>` using its default vertical rhythm (this is the intentional fix — this section has never had the sitewide `py-8 lg:py-12` convention other sections had, so there is no existing padding value to preserve as an override). Replace `<Reveal><h2 className="mb-8 text-dt-h2 font-dt-heading font-bold text-dt-navy">Схожі статті</h2></Reveal>` with `<Reveal><SectionHeading title="Схожі статті" /></Reveal>` (note this file used `mb-8`, not the usual `mb-12` — SectionHeading's `mb-dt-48` normalizes it to the same spacing every other migrated heading now uses, which is the intended consistency fix, not a scope overreach).
  </action>
  <verify>
    <automated>cd apps/web && grep -c "Корисні матеріали для стоматологій" app/blog/page.tsx | grep -qx 1 && grep -q 'tone="navy"' "app/blog/[slug]/page.tsx" && grep -c "Хочете автоматизувати запис у вашій клініці" "app/blog/[slug]/page.tsx" | grep -qx 1 && grep -c "Схожі статті" modules/blog/related-posts.tsx | grep -qx 1 && pnpm exec eslint --max-warnings 0 app/blog/page.tsx "app/blog/[slug]/page.tsx" modules/blog/related-posts.tsx && ! pnpm exec tsc --noEmit 2>&1 | grep "error TS" | grep -v "button-group.tsx"</automated>
  </verify>
  <done>Blog list/detail pages render via Section for their hero/featured/filters blocks with byte-identical copy and unchanged fetch/data logic; the [slug] page's bottom CTA callout keeps its <div> shape but its heading now goes through SectionHeading tone="navy"; related-posts.tsx's previously-bare section now has the sitewide default vertical rhythm and its heading matches every other migrated section's spacing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Blog async Server Components | app/blog/page.tsx and app/blog/[slug]/page.tsx fetch real post data from apps/server's public API at request/build time — this plan touches only the JSX wrapper markup, never the fetch() calls, notFound() handling, or extractPostBodyBlocks parsing |
| Demo bot-tab.tsx client state | Local scenario-playback state machine (setInterval/setTimeout-driven chat simulation) — this plan touches only the phone-frame chrome's className values, never the runScenario callback, timers, or cleanup effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-06.1-09 | Tampering | app/blog/page.tsx, app/blog/[slug]/page.tsx JSX wrapper restructuring around existing fetch() calls | low | accept | fetch URLs, revalidate options, notFound()/error-throw paths are outside this task's edits (only surrounding <section>/<div> markup and heading composition change), confirmed by reading both files before editing |
| T-06.1-10 | Denial of Service | bot-tab.tsx className changes near the scenario-playback interval/timeout logic | low | accept | This task's edits are scoped to className strings on static wrapper <div>s (frame radius/shadow/border) — the runScenario/useEffect timer logic and its cleanup guards are untouched, confirmed by reading the file before editing |

</threat_model>

<verification>
Run `cd apps/web && pnpm exec eslint --max-warnings 0 app/demo/page.tsx modules/demo/bot-tab.tsx app/blog/page.tsx "app/blog/[slug]/page.tsx" modules/blog/related-posts.tsx` and confirm zero output. Run `cd apps/web && pnpm exec tsc --noEmit 2>&1 | grep "error TS"` and confirm only the known pre-existing packages/ui/button-group.tsx line. Load /demo and click through each chat scenario to confirm the playback still works with the new phone-frame chrome; load /blog and a post detail page and diff against pre-phase screenshots at all 5 breakpoints to confirm no text lost, no horizontal scroll, and Related Posts' new spacing doesn't crowd or overlap the CTA callout above it. <human-check>Confirm no more than two coral elements are visible in any single viewport on /demo and /blog (D-18).</human-check>
</verification>

<success_criteria>
Demo and Blog routes fully adopt Section/SectionHeading where applicable; bot-tab.tsx's phone-frame chrome is fully dt- token-based (radius, shadow, hairline borders) with its deliberate near-black bezel and chat-timestamp token preserved; related-posts.tsx's previously-inconsistent bare section now matches the sitewide rhythm. Combined with Plans 02/03/04, every one of the phase's 5 routes has completed the D-17 primitive sweep.
</success_criteria>

<output>
Create `.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-05-SUMMARY.md` when done
</output>
