---
phase: 06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - apps/web/shared/components/premium-button.tsx
  - apps/web/shared/components/premium-card.tsx
  - apps/web/shared/lib/motion.ts
  - apps/web/shared/components/premium-input.tsx
  - apps/web/shared/components/premium-textarea.tsx
  - apps/web/shared/components/premium-switch.tsx
  - apps/web/shared/components/header.tsx
  - apps/web/shared/components/footer.tsx
  - apps/web/shared/components/premium-dialog.tsx
  - apps/web/shared/components/premium-accordion.tsx
autonomous: true
requirements: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every PremiumButton coral CTA sitewide is at least 48px tall by default (44px minimum on the icon size), has an 800-weight label, a coral drop shadow, and a 6%-darker hover state — without any button's onClick/href/asChild contract changing"
    - "Every scroll-triggered Reveal/StaggerGrid animation sitewide now translates 14px (not 20px) over 200ms (not 500ms) with 60ms stagger (not 80ms), and fully disables under prefers-reduced-motion exactly as it did before this plan"
    - "Every focus-visible ring sitewide (buttons, inputs, textareas, switches) is teal (--dt-ring), not navy"
    - "The sticky Header is 86%-opaque with a 14px backdrop blur and a hairline bottom border once the page is scrolled, matching the brief's exact header spec"
  artifacts:
    - path: "apps/web/shared/components/premium-button.tsx"
      provides: "PremiumButton with brief-spec coral/outline variants and 48px/44px size floor"
    - path: "apps/web/shared/lib/motion.ts"
      provides: "Retuned revealVariants/revealContainerVariants shared by every Reveal/StaggerGrid consumer sitewide"
  key_links:
    - from: "apps/web/shared/components/premium-button.tsx"
      to: "apps/web/app/premium-theme.css"
      via: "focus-visible:ring-[var(--dt-ring)] and border-[var(--dt-border)] arbitrary-value utilities"
      pattern: "var(--dt-ring)"
    - from: "apps/web/shared/components/header.tsx"
      to: "apps/web/app/premium-theme.css"
      via: "border-b border-[var(--dt-border)] hairline utility"
      pattern: "var(--dt-border)"
---

<objective>
Retrofit the existing shared "chrome" components — PremiumButton, PremiumCard, Reveal's shared motion constants, the form-field focus rings, Header, Footer, PremiumDialog, PremiumAccordion — to the brief's exact spec (D-04, D-05, D-06, D-13, D-14, D-15, D-20, D-24, D-26) using the tokens Plan 01 added to premium-theme.css. This is a pure retrofit wave: every component keeps its existing props/API, only className/constant values change.

Purpose: these ten files are imported by every page in the sweep (Waves 2's other four plans), so landing this wave in parallel with the page sweeps means the sweep plans automatically pick up the retrofitted chrome the moment they run, without a second pass.
Output: PremiumButton/PremiumCard/PremiumInput/PremiumTextarea/PremiumSwitch/Header/Footer/PremiumDialog/PremiumAccordion updated in place; motion.ts constants retuned.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-CONTEXT.md
@.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-RESEARCH.md
@apps/web/shared/components/premium-button.tsx
@apps/web/shared/components/premium-card.tsx
@apps/web/shared/lib/motion.ts
@apps/web/shared/components/header.tsx
@apps/web/app/premium-theme.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PremiumButton — coral/outline spec, 48px/44px size floor, teal ring</name>
  <files>apps/web/shared/components/premium-button.tsx</files>
  <action>
    Update premiumButtonVariants in apps/web/shared/components/premium-button.tsx (D-13, D-20, D-05):

    Base classes: change `focus-visible:ring-dt-navy` to `focus-visible:ring-[var(--dt-ring)]` (D-05 — teal focus ring sitewide via the token Plan 01 added; the mechanism resolved there is a raw CSS custom property, so this must stay an arbitrary-value utility, not a `ring-dt-teal` Tailwind-color-scale reference). Also swap the base `duration-200` transition to `duration-[var(--dt-duration-fast)]` (D-06 — 180ms, this component's hover/press micro-interaction is exactly the "fast" tier the token is for).

    `coral` variant: add `font-extrabold` (800 weight, replacing the effective font-medium ≈500 inherited from the base classes — CVA merges variant classes after base classes so an explicit `font-extrabold` here wins), add the exact drop shadow `shadow-[0_8px_16px_rgba(232,107,90,0.28)]`, and change `hover:bg-dt-coral/90` to `hover:bg-dt-coral/[0.94]` (an opacity-based ~6% darken against the coral background, per D-13's explicit note that an opacity-based hover is an acceptable interpretation of "darkens 6%" absent a token-based darken function in this codebase). Keep `hover:-translate-y-0.5`.

    `outline` variant: change `border-dt-navy` to `border-[var(--dt-border)]` (D-04 — the brief's "Secondary: біла з hairline-межею" reads the outline button's border as the same hairline treatment as cards, not the current full-opacity navy).

    `size` variants: change `default: 'h-10 px-5 py-2'` to `default: 'h-12 px-5 py-2'` (40px to 48px, D-13's ≥48px default floor — this also converges `default` and `lg` to the same 48px height, differentiated only by padding/text-size; that convergence is expected and correct per RESEARCH.md's D-13 finding, not a bug). Change `icon: 'size-10'` to `icon: 'size-11'` (40px to 44px, D-20 — the header's mobile burger toggle is the live use case for this floor). Leave `sm: 'h-9 px-4 text-sm'` and `lg: 'h-12 px-6 text-base'` unchanged.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec eslint --max-warnings 0 shared/components/premium-button.tsx && ! pnpm exec tsc --noEmit 2>&1 | grep "error TS" | grep -v "button-group.tsx"</automated>
  </verify>
  <done>coral variant is font-extrabold with the exact rgba coral shadow and a darker hover; outline variant's border references --dt-border; default size is 48px tall, icon size is 44px square; every focus-visible ring on this component is teal via --dt-ring; the component's exported props/API (variant, size, asChild) are unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PremiumCard hairline + motion.ts retune + form-field focus rings</name>
  <files>apps/web/shared/components/premium-card.tsx, apps/web/shared/lib/motion.ts, apps/web/shared/components/premium-input.tsx, apps/web/shared/components/premium-textarea.tsx, apps/web/shared/components/premium-switch.tsx</files>
  <action>
    In apps/web/shared/components/premium-card.tsx (D-04): change `border-dt-navy/10` to `border-[var(--dt-border)]`. Leave the `highlighted` prop's `border-2 border-dt-teal` untouched — that is a distinct semantic highlight border, not a decorative hairline, and D-04's scope is the hairline treatment only.

    In apps/web/shared/lib/motion.ts (D-14): in `revealVariants`, change `hidden: { opacity: 0, y: 20 }` to `hidden: { opacity: 0, y: 14 }`, and change the `visible` transition's `duration: 0.5` to `duration: 0.2` (both within the brief's 180-260ms band; `EASE_DT_EXPO_OUT` stays as-is, it already matches). In `revealContainerVariants`, change `staggerChildren: 0.08` to `staggerChildren: 0.06`. These are shared constants consumed by Reveal, StaggerGrid, and StaggerItem — every Reveal-wrapped section sitewide retunes together, which is the intent (site-wide motion consistency). Do not touch `hoverLift`, `idleBounceAnimate`, or `idleBounceTransition` — D-26's considered decision (documented in Plan 01) is to keep the existing idle-bounce usage in Home's Hero as-is.

    In apps/web/shared/components/premium-input.tsx, premium-textarea.tsx, premium-switch.tsx (D-05): change every `focus-visible:ring-dt-navy` (premium-switch.tsx) and `focus-visible:ring-dt-navy/30` (premium-input.tsx, premium-textarea.tsx) to `focus-visible:ring-[var(--dt-ring)]` and `focus-visible:ring-[var(--dt-ring)]/30` respectively — preserve each file's existing ring *opacity* exactly, only the color source changes from the navy Tailwind color scale to the new teal custom property. Explicit no-change note (D-24): leave premium-input.tsx's and premium-textarea.tsx's `border-dt-navy/20` exactly as-is — form-field borders are a functionally distinct, more-visible use case than the D-04 decorative hairline, confirmed by RESEARCH.md's own recommendation not to fold them into the hairline migration.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec eslint --max-warnings 0 shared/components/premium-card.tsx shared/lib/motion.ts shared/components/premium-input.tsx shared/components/premium-textarea.tsx shared/components/premium-switch.tsx && ! pnpm exec tsc --noEmit 2>&1 | grep "error TS" | grep -v "button-group.tsx"</automated>
  </verify>
  <done>PremiumCard's default border is the --dt-border hairline (highlighted variant unchanged); motion.ts's revealVariants/revealContainerVariants match the brief's y:14/duration:0.2/stagger:0.06 spec with idleBounce untouched; every form-field focus-visible ring is teal via --dt-ring with input/textarea border alpha left at /20.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Header retrofit + remaining shared-component hairline call sites</name>
  <files>apps/web/shared/components/header.tsx, apps/web/shared/components/footer.tsx, apps/web/shared/components/premium-dialog.tsx, apps/web/shared/components/premium-accordion.tsx</files>
  <action>
    In apps/web/shared/components/header.tsx (D-15): in the scrolled-state className branch, change `bg-dt-warm-white/80` to `bg-dt-warm-white/[0.86]` and `backdrop-blur-md` to `backdrop-blur-[14px]`; add a hairline bottom border to that same branch, `border-b border-[var(--dt-border)]` (currently absent — confirmed via RESEARCH.md this session). Also change that branch's `duration-300` to `duration-[var(--dt-duration-base)]` (D-06 — 240ms, within the brief's band; the unscrolled branch has no transition-affecting classes to change). No breakpoint change is needed — RESEARCH.md confirmed this session that the header's existing `lg:` (1024px) burger-menu breakpoint already exactly matches the brief's "<1024px" requirement. While this file is open, migrate its one D-04 hairline occurrence: the mobile-menu-collapse `border-t border-dt-navy/10` (around line 116) to `border-t border-[var(--dt-border)]`.

    Migrate the remaining D-04 hairline-border call sites in shared components not touched by Task 2: apps/web/shared/components/footer.tsx — both occurrences of `border-dt-navy/10` (the outer `<footer>` top border and the bottom copyright-row divider) to `border-[var(--dt-border)]`. apps/web/shared/components/premium-dialog.tsx — the dialog content's `border-dt-navy/10` to `border-[var(--dt-border)]`. apps/web/shared/components/premium-accordion.tsx — PremiumAccordionItem's `border-dt-navy/10` to `border-[var(--dt-border)]`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec eslint --max-warnings 0 shared/components/header.tsx shared/components/footer.tsx shared/components/premium-dialog.tsx shared/components/premium-accordion.tsx && ! pnpm exec tsc --noEmit 2>&1 | grep "error TS" | grep -v "button-group.tsx"</automated>
  </verify>
  <done>Header's scrolled state is 86% opaque with 14px blur and a hairline bottom border; Footer/PremiumDialog/PremiumAccordion's borders reference --dt-border; zero border-dt-navy/10 occurrences remain anywhere in apps/web/shared/components after this plan (Wave 2's page-sweep plans separately migrate the remaining occurrences that live in apps/web/modules).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Form-field chrome (PremiumInput/PremiumTextarea) | Visual border/ring styling only — the underlying react-hook-form + zod validation in contact-form.tsx/demo-lead-form.tsx that these components render inside is untouched by this plan |
| Header scroll listener | Existing `scroll` event listener; this plan only changes the CSS applied when its existing `isScrolled` state flips |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-06.1-04 | Tampering | PremiumButton/PremiumCard/PremiumInput/PremiumTextarea/PremiumSwitch className changes | low | accept | Pure CSS/visual change — no prop, event handler, or validation-schema change; contact-form.tsx/demo-lead-form.tsx's zod schemas and submit handlers are outside this plan's file list and untouched |
| T-06.1-05 | Denial of Service | Header backdrop-blur/opacity/border change | low | accept | No new event listener added (the existing scroll listener from Phase 01.1/02 is reused as-is); a CSS blur-radius/opacity/border value change has no measurable perf implication beyond the existing baseline |

</threat_model>

<verification>
Run `cd apps/web && pnpm exec eslint --max-warnings 0 shared/components/premium-button.tsx shared/components/premium-card.tsx shared/lib/motion.ts shared/components/premium-input.tsx shared/components/premium-textarea.tsx shared/components/premium-switch.tsx shared/components/header.tsx shared/components/footer.tsx shared/components/premium-dialog.tsx shared/components/premium-accordion.tsx` and confirm zero output. Run `cd apps/web && pnpm exec tsc --noEmit 2>&1 | grep "error TS"` and confirm the only line is the known pre-existing packages/ui/button-group.tsx error. Manually load the site and scroll the Header past 20px to confirm the 86%-opacity/14px-blur/hairline-border scrolled state renders correctly, and tab through a form (Contacts) to confirm the focus ring is now teal.
</verification>

<success_criteria>
Every coral CTA sitewide is >=48px tall (>=44px for icon buttons) with the brief's exact shadow/weight/hover; every focus-visible ring sitewide is teal; every hairline border in apps/web/shared/components references --dt-border; scroll-triggered reveals use the brief's 14px/200ms/60ms-stagger motion spec; the Header matches the brief's scrolled-state opacity/blur/border spec.
</success_criteria>

<output>
Create `.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-02-SUMMARY.md` when done
</output>
