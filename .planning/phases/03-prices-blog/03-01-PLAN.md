---
phase: 03-prices-blog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/shared/components/premium-switch.tsx
  - apps/web/shared/components/premium-badge.tsx
  - apps/web/shared/components/premium-card.tsx
  - apps/web/modules/prices/pricing-cards.tsx
  - apps/web/modules/prices/comparison-table.tsx
  - apps/web/modules/prices/faq-accordion.tsx
  - apps/web/app/prices/page.tsx
autonomous: true
requirements: [PRICE-01]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "User can view /prices with all 3 pricing tiers (Старт, Бізнес, Клініка) rendered in that fixed order, sourced from static mock data, matching the design archive's plans array order."
    - "All 3 pricing tiers render unconditionally on every page load — no empty-state UI exists or is needed since the tier list is never empty, single-element, or null."
    - "Exactly one pricing tier (Бізнес) displays the 'Популярний' badge and the highlighted teal-border treatment; Старт and Клініка never show it."
    - "The billing toggle (PremiumSwitch) switches all 3 tiers' displayed price between monthly and yearly figures without a page reload, and the '-20%' discount badge is visible next to the yearly label."
    - "The 14-row comparison table and 7-item FAQ accordion render fully, matching the design archive's comparisonFeatures/faqs arrays with no rows or items dropped."
  artifacts:
    - apps/web/shared/components/premium-switch.tsx
    - apps/web/shared/components/premium-badge.tsx
    - apps/web/modules/prices/pricing-cards.tsx
    - apps/web/modules/prices/comparison-table.tsx
    - apps/web/modules/prices/faq-accordion.tsx
    - apps/web/app/prices/page.tsx
  key_links:
    - "prices/page.tsx -> pricing-cards.tsx billing-toggle state (isYearly) -> conditional price string per tier"
    - "pricing-cards.tsx -> PremiumCard(highlighted) + PremiumBadge('Популярний') -> applied only to the Бізнес entry"
    - "pricing-cards.tsx CTA buttons -> routes.contacts ('/contacts') via PremiumButton asChild Link"
    - "prices/page.tsx closing CTA ('Напишіть нам') -> routes.contacts"
  prohibitions:
    - "Must not fabricate pricing tier names, prices, features, comparison-table rows, or FAQ questions/answers beyond what 03-CONTEXT.md's code_context transcribes verbatim from the design archive."
---

<objective>
Ship the Prices page at `/prices` — hero with a functional monthly/yearly billing toggle, a 3-tier pricing card grid (Старт / Бізнес / Клініка, with Бізнес highlighted and marked "Популярний"), a 14-row feature comparison table, a 7-item FAQ accordion, and a closing CTA — ported verbatim from the design archive (see `.planning/phases/03-prices-blog/03-CONTEXT.md`'s `<code_context>` "Design source — Prices page") onto the Phase 01.1 premium `dt-*` design system. This plan also builds two new premium primitives (`PremiumSwitch`, `PremiumBadge`) that the sibling Blog plan (03-02, wave 2) depends on for its category badges — hence this plan runs first.

Purpose: Completes PRICE-01 (D-based on the locked design source, no new decisions) — the fifth of the site's six routes.
Output: `/prices` route rendering fully; `PremiumSwitch` and `PremiumBadge` primitives available for reuse; `PremiumCard` gains a `highlighted` variant.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-prices-blog/03-CONTEXT.md
@.planning/phases/03-prices-blog/03-PATTERNS.md
@apps/web/app/contacts/page.tsx
@apps/web/app/demo/page.tsx
@apps/web/shared/components/premium-card.tsx
@apps/web/shared/components/premium-button.tsx
@apps/web/shared/components/premium-accordion.tsx
@apps/web/shared/components/premium-input.tsx
@apps/web/modules/contacts/faq-accordion.tsx
@apps/web/modules/home/features.tsx
@apps/web/shared/lib/routes.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Wire /prices end-to-end — billing toggle + pricing cards</name>
  <files>apps/web/shared/components/premium-switch.tsx, apps/web/modules/prices/pricing-cards.tsx, apps/web/app/prices/page.tsx</files>
  <read_first>
    .planning/phases/03-prices-blog/03-CONTEXT.md (code_context "Design source — Prices page" — plans array, hero copy, exact prices 499/599, 999/1199, 1999/2499)
    .planning/phases/03-prices-blog/03-PATTERNS.md (PremiumSwitch pattern section, pricing-cards.tsx section, "Root wrapper" snippet)
    apps/web/shared/components/premium-input.tsx (plain-cn styling convention, no CVA needed for a 2-state control)
    apps/web/shared/components/premium-accordion.tsx (radix-ui import path convention: `import { X as XPrimitive } from 'radix-ui'`)
    apps/web/app/demo/page.tsx (root wrapper class `min-h-screen pb-16 pt-24 lg:pt-32`)
    apps/web/app/contacts/page.tsx (section/Container/centered-hero pattern)
    apps/web/shared/components/premium-button.tsx (coral vs outline variant usage)
    apps/web/shared/lib/routes.ts (routes.contacts)
  </read_first>
  <action>
    Create apps/web/shared/components/premium-switch.tsx exporting `PremiumSwitch`, built on `Switch as SwitchPrimitive` imported from the `radix-ui` package (same consolidated-package convention as premium-accordion.tsx, not `@radix-ui/react-switch`). Root gets `data-slot="premium-switch"` and `dt-*` token classes for on/off state (`data-[state=checked]:bg-dt-teal`, `data-[state=unchecked]:bg-dt-navy/20`); Thumb gets `data-slot="premium-switch-thumb"` with a translate-x transform driven by `data-[state=checked]`/`data-[state=unchecked]`. No CVA needed — plain `cn()` composition, matching premium-input.tsx's simplicity, since this control only needs two states.

    Create apps/web/modules/prices/pricing-cards.tsx as a `'use client'` component. Define a local `plans` array inline (not a separate data file — page-specific, small) with exactly 3 entries in this order: Старт (price 599 monthly / 499 yearly, description "Для невеликих клінік", features: До 100 записів/місяць, 1 лікар, Telegram бот, Автонагадування, Базова аналітика, Email підтримка), Бізнес (price 1199 monthly / 999 yearly, description "Для середніх клінік", `popular: true`, features: До 500 записів/місяць, До 5 лікарів, Telegram бот, Автонагадування, Розширена аналітика, Збір відгуків, Інтеграції, Пріоритетна підтримка), Клініка (price 2499 monthly / 1999 yearly, description "Для великих клінік", features: Необмежено записів, Необмежено лікарів, Telegram бот, Автонагадування, Повна аналітика, Збір відгуків, Всі інтеграції, API доступ, Декілька локацій, Персональний менеджер) — transcribed verbatim from 03-CONTEXT.md. Hold `isYearly` via `React.useState(false)`. Render a centered hero block (`max-w-2xl mx-auto text-center`) with h1 "Прості та прозорі ціни", subhead p "Починайте безкоштовно. Платіть тільки коли відчуєте цінність.", and a billing-toggle row: label "Щомісяця" + `PremiumSwitch checked={isYearly} onCheckedChange={setIsYearly}` + label "Щороку". Below, render a 3-column grid (`grid gap-6 lg:grid-cols-3 max-w-6xl mx-auto`) of `PremiumCard` (plain, no `highlighted` prop yet — Task 2 adds that), each showing: plan name (h3), description, price block (`{isYearly ? plan.yearlyPrice : plan.monthlyPrice} ₴` + `/міс` in `text-dt-graphite`), features list (map with `Check` icon from `@phosphor-icons/react/ssr` in `text-green-500`, matching the archive's icon color exactly), and a footer `PremiumButton` (`asChild`, wrapping `<Link href={routes.contacts}>Обрати план</Link>`) — `variant="coral" size="lg"` for the Бізнес card, `variant="outline" size="lg"` for Старт/Клініка.

    Create apps/web/app/prices/page.tsx: root div class `min-h-screen pb-16 pt-24 lg:pt-32` (copy from apps/web/app/demo/page.tsx), rendering `<PricingCards />` (which itself owns and renders the hero + grid, per the component built above). This file's existence at `app/prices/page.tsx` is the route registration — no separate route config needed.
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web exec tsc --noEmit` exits 0.
    - apps/web/app/prices/page.tsx exists and default-exports a component rendering PricingCards.
    - apps/web/modules/prices/pricing-cards.tsx renders exactly 3 PremiumCard entries in Старт, Бізнес, Клініка order.
    - Toggling the PremiumSwitch changes the displayed price text for all 3 cards (verified by reading the component's price expression uses `isYearly` conditionally, not a hardcoded string).
    - Each card's CTA button links to routes.contacts ('/contacts').
  </acceptance_criteria>
  <done>`/prices` renders h1 + a working billing toggle that switches all 3 tiers' price text, plus 3 PremiumCards in the correct order each with a full feature list and an "Обрати план" button linking to /contacts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PremiumBadge primitive + PremiumCard highlighted variant — apply to Бізнес tier</name>
  <files>apps/web/shared/components/premium-badge.tsx, apps/web/shared/components/premium-card.tsx, apps/web/modules/prices/pricing-cards.tsx</files>
  <read_first>
    .planning/phases/03-prices-blog/03-PATTERNS.md (PremiumBadge pattern section — full cva code excerpt)
    apps/web/shared/components/premium-card.tsx (current shape — plain `cn()` component, not CVA-based)
    apps/web/shared/components/premium-button.tsx (cva + `Slot.Root` asChild convention to mirror)
    .planning/phases/03-prices-blog/03-CONTEXT.md (popular: true / "-top-4 center" badge placement note)
  </read_first>
  <action>
    Create apps/web/shared/components/premium-badge.tsx: a `cva()`-based component named `premiumBadgeVariants` with base classes `inline-flex w-fit shrink-0 items-center justify-center gap-1 rounded-full px-2.5 py-1 text-xs font-medium whitespace-nowrap`, variant key `variant` with options `teal` (`bg-dt-teal/10 text-dt-teal`), `coral` (`bg-dt-coral text-dt-navy`), `navy` (`bg-dt-navy text-dt-warm-white`), `outline` (`border border-dt-navy/20 text-dt-navy`), default `teal`. Export function component `PremiumBadge` accepting `asChild?: boolean` rendered via `Slot.Root` from `radix-ui` when true (span otherwise), `data-slot="premium-badge"`. Export both `PremiumBadge` and `premiumBadgeVariants`.

    Extend apps/web/shared/components/premium-card.tsx: add an optional `highlighted?: boolean` prop to the existing `React.ComponentProps<'div'>` signature. When `highlighted` is true, append `'border-2 border-dt-teal relative'` to the `cn()` call (in addition to the existing base classes) — do not rewrite the component to CVA, keep the existing plain-`cn()` shape per 03-PATTERNS.md's recommendation.

    In apps/web/modules/prices/pricing-cards.tsx: pass `highlighted={plan.popular}` to each `PremiumCard`. Only when `plan.popular` is true, render an absolutely-positioned `PremiumBadge variant="coral"` reading "Популярний", positioned `absolute -top-4 left-1/2 -translate-x-1/2` (matches the archive's centered-above-card placement). Add a second `PremiumBadge variant="teal"` reading "-20%" next to the "Щороку" toggle label in the hero block (always visible, not conditional on `isYearly`, matching the archive's static discount badge).
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit && test "$(grep -v '^import' apps/web/modules/prices/pricing-cards.tsx | grep -c 'Популярний')" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web exec tsc --noEmit` exits 0.
    - `grep -v '^import' apps/web/modules/prices/pricing-cards.tsx | grep -c 'Популярний'` returns exactly 1 — the literal string appears once (the conditional render for the Бізнес card only), proving Старт/Клініка never render it.
    - apps/web/shared/components/premium-card.tsx exposes a `highlighted` prop that conditionally adds `border-2 border-dt-teal` classes.
    - apps/web/shared/components/premium-badge.tsx exports `PremiumBadge` with at least `teal`/`coral`/`navy`/`outline` variants.
  </acceptance_criteria>
  <done>Exactly the Бізнес card shows the teal border + "Популярний" badge; Старт and Клініка never render it; a "-20%" PremiumBadge is visible next to the "Щороку" label.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Comparison table + FAQ accordion + closing CTA</name>
  <files>apps/web/modules/prices/comparison-table.tsx, apps/web/modules/prices/faq-accordion.tsx, apps/web/app/prices/page.tsx</files>
  <read_first>
    .planning/phases/03-prices-blog/03-CONTEXT.md (code_context — comparisonFeatures 14-row array and faqs 7-item array, both transcribed verbatim, to copy exactly)
    .planning/phases/03-prices-blog/03-PATTERNS.md (comparison-table section, faq-accordion section)
    apps/web/modules/contacts/faq-accordion.tsx (full structural source to copy — only the `faqs` array content changes)
    apps/web/app/contacts/page.tsx (lines 31-42 — `bg-dt-navy/5 py-8 lg:py-12` alt-section, Reveal + centered h2 + `max-w-3xl mx-auto` wrapper pattern)
    apps/web/modules/home/features.tsx (Check icon import path: `@phosphor-icons/react/ssr`)
  </read_first>
  <action>
    Create apps/web/modules/prices/comparison-table.tsx as a server component (no `'use client'`): `<section className="py-8 lg:py-12 bg-dt-navy/5">` wrapping `<Container><Reveal><h2 className="text-dt-h2 font-dt-heading font-bold text-dt-navy text-center mb-12">Детальне порівняння планів</h2></Reveal>`, then an `overflow-x-auto` wrapper around a plain semantic `<table className="w-full max-w-5xl mx-auto">`. `<thead>` row: "Функція" / "Старт" / "Бізнес" / "Клініка". `<tbody>` maps the 14-row `comparisonFeatures` array transcribed verbatim from 03-CONTEXT.md (Записи на місяць, Кількість лікарів, Telegram бот, Автонагадування, Базова аналітика, Розширена аналітика, Повна аналітика, Збір відгуків, Інтеграції, API доступ, Декілька локацій, Email підтримка, Пріоритетна підтримка, Персональний менеджер) — string cell values render as plain text, boolean `true` renders a `Check` icon from `@phosphor-icons/react/ssr` in `text-dt-teal`, boolean `false` renders `"—"` in `text-dt-graphite`.

    Create apps/web/modules/prices/faq-accordion.tsx as a distinct new file (do not import Contacts' module cross-page — same `modules/<page>/` convention, separate file) using `PremiumAccordion`/`PremiumAccordionItem`/`PremiumAccordionTrigger`/`PremiumAccordionContent` exactly as apps/web/modules/contacts/faq-accordion.tsx does, with the 7-item `faqs` array transcribed verbatim from 03-CONTEXT.md's code_context (Чи є безкоштовний пробний період? / Як я можу скасувати підписку? / Які методи оплати ви приймаєте? / Чи можу я змінити тариф пізніше? / Чи включена технічна підтримка? / Що станеться якщо я перевищу ліміт записів? / Чи потрібен технічний спеціаліст для налаштування?, each with its full answer text from the source).

    Wire both into apps/web/app/prices/page.tsx, after the pricing-cards section: `<ComparisonTable />`, then an FAQ section mirroring apps/web/app/contacts/page.tsx's FAQ section structure (`bg-dt-navy/5 py-8 lg:py-12` > `Container` > `Reveal` centered h2 "Часті питання" > `max-w-3xl mx-auto` wrapping `<FaqAccordion />`), followed by a closing CTA block inside that same section: centered `p` "Залишились питання?" + `PremiumButton size="lg" asChild` wrapping `<Link href={routes.contacts}>Напишіть нам →</Link>`.
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit && test "$(grep -c '<tr' apps/web/modules/prices/comparison-table.tsx)" = "15" && test "$(grep -c 'question:' apps/web/modules/prices/faq-accordion.tsx)" = "7"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web exec tsc --noEmit` exits 0.
    - apps/web/modules/prices/comparison-table.tsx contains 15 `<tr` occurrences (1 header row + 14 body rows) — no comparison rows dropped.
    - apps/web/modules/prices/faq-accordion.tsx's `faqs` array literal has exactly 7 objects (verify by counting `question:` occurrences = 7).
    - apps/web/app/prices/page.tsx imports and renders both `ComparisonTable` and `FaqAccordion`, plus a closing CTA `Link` to routes.contacts.
  </acceptance_criteria>
  <done>`/prices` renders the full 14-row/3-column comparison table and the 7-item FAQ accordion (expand/collapse works via Radix), plus a closing "Напишіть нам" CTA linking to /contacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossing untrusted input | Every artifact in this plan renders static, hardcoded mock data (plans/comparisonFeatures/faqs arrays) or purely local UI state (`isYearly` boolean); there is no form submission, no external API call, and no persisted user input on `/prices`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01-SC | Tampering | npm/pnpm installs | low | accept | No new packages are installed by this plan — `PremiumSwitch`/`PremiumBadge` are built on `radix-ui`/`class-variance-authority`/`@phosphor-icons/react`, all already-vetted dependencies from Phase 01.1's Package Legitimacy Gate (confirmed present in apps/web/package.json). |
| T-03-01-01 | Information Disclosure | pricing-cards.tsx / comparison-table.tsx static data | low | accept | Pricing tiers, features, and FAQ content are intentionally public marketing copy — no sensitive or user-specific data is rendered. |
| T-03-01-02 | Tampering | PremiumSwitch billing-toggle state | low | accept | `isYearly` is ephemeral client-only React state (not persisted, not submitted to any endpoint) — no tamper surface beyond the user's own browser session, and it only changes which static price string is displayed. |

</threat_model>

<verification>
- `pnpm --filter web exec tsc --noEmit` passes with zero errors after all 3 tasks.
- Manual/end-of-phase human check (per `workflow.human_verify_mode: end-of-phase`): visit `/prices` in a dev server, confirm visual parity with the design archive — hero copy, toggle interaction, highlighted Бізнес card, comparison table, FAQ accordion expand/collapse, and all CTAs routing to `/contacts`.
</verification>

<success_criteria>
- `/prices` route exists and renders without errors.
- All 3 pricing tiers, the 14-row comparison table, and the 7-item FAQ render with content matching 03-CONTEXT.md's transcribed source exactly.
- Only the Бізнес tier shows the "Популярний" badge and highlighted border.
- Billing toggle functionally switches all displayed prices.
- `pnpm --filter web exec tsc --noEmit` passes.
</success_criteria>

## Artifacts This Phase Produces

New files created by this plan:
- `apps/web/shared/components/premium-switch.tsx` — new primitive, `PremiumSwitch` (Radix Switch wrapper, `dt-*` tokens)
- `apps/web/shared/components/premium-badge.tsx` — new primitive, `PremiumBadge` + `premiumBadgeVariants` (cva, teal/coral/navy/outline)
- `apps/web/modules/prices/pricing-cards.tsx` — `PricingCards` client component (billing toggle + 3-tier grid)
- `apps/web/modules/prices/comparison-table.tsx` — `ComparisonTable` server component (14-row feature table)
- `apps/web/modules/prices/faq-accordion.tsx` — `FaqAccordion` server component (7-item Prices FAQ, distinct from Contacts' FAQ)
- `apps/web/app/prices/page.tsx` — `/prices` route

Modified files:
- `apps/web/shared/components/premium-card.tsx` — adds optional `highlighted?: boolean` prop (new symbol on an existing component; back-compatible, no existing callers change behavior)

<output>
Create `.planning/phases/03-prices-blog/03-01-SUMMARY.md` when done
</output>
