---
phase: 02-home-contacts-demo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/shared/components/premium-input.tsx
  - apps/web/shared/components/premium-textarea.tsx
  - apps/web/shared/components/premium-accordion.tsx
  - apps/web/modules/contacts/contact-form.tsx
  - apps/web/modules/contacts/faq-accordion.tsx
  - apps/web/modules/contacts/contact-info.tsx
  - apps/web/app/contacts/page.tsx
autonomous: false
requirements: [CONT-01, CONT-02, CONT-03]

must_haves:
  truths:
    - "User can fill the Contacts form (Ім'я, Назва клініки, Телефон або email, Повідомлення) and sees inline field errors when the name is under 2 characters or the contact field matches neither a phone-number pattern nor an email pattern"
    - "Submitting a valid form shows a toast reading 'Заявку успішно надіслано!' and swaps the form for a 'Дякуємо!' confirmation card; a 'Надіслати ще одну заявку' button resets back to the empty form"
    - "react-hook-form, zod, and @hookform/resolvers are declared as apps/web dependencies only after explicit human approval of their npm registry legitimacy"
    - "User can expand and collapse all 8 FAQ accordion items on /contacts, each showing its exact question and answer from the design archive"
    - "Zero shadcn-package component import appears in any file this plan creates"
  artifacts:
    - path: "apps/web/shared/components/premium-input.tsx"
      provides: "PremiumInput — dt-token-styled text input primitive"
      exports: "PremiumInput"
    - path: "apps/web/shared/components/premium-textarea.tsx"
      provides: "PremiumTextarea — dt-token-styled textarea primitive"
      exports: "PremiumTextarea"
    - path: "apps/web/shared/components/premium-accordion.tsx"
      provides: "Radix-based accordion primitive set styled with dt- tokens"
      exports: "PremiumAccordion, PremiumAccordionItem, PremiumAccordionTrigger, PremiumAccordionContent"
    - path: "apps/web/modules/contacts/contact-form.tsx"
      provides: "react-hook-form + zod validated Contacts form with mocked submit and success-state swap"
      exports: "ContactForm"
    - path: "apps/web/modules/contacts/faq-accordion.tsx"
      provides: "8-item FAQ accordion composed from PremiumAccordion"
      exports: "FaqAccordion"
    - path: "apps/web/modules/contacts/contact-info.tsx"
      provides: "3 contact-method cards + 3 benefit-callout cards"
      exports: "ContactInfo"
    - path: "apps/web/app/contacts/page.tsx"
      provides: "Contacts route composing hero copy, 2-column form/info grid, FAQ section"
      exports: "default function Contacts"
  key_links:
    - from: "apps/web/modules/contacts/contact-form.tsx"
      to: "zod schema via zodResolver"
      via: "useForm({ resolver: zodResolver(contactFormSchema) })"
      pattern: "zodResolver(contactFormSchema)"
    - from: "apps/web/modules/contacts/contact-form.tsx"
      to: "the root layout's already-mounted toast renderer"
      via: "import { toast } from 'sonner'; toast.success(...)"
      pattern: "toast.success"
---

<objective>
Rebuild the Contacts page (`/contacts`) from the design archive on the Phase 01.1 premium design system: a `react-hook-form` + `zod`-validated lead-capture form (mocked submit, inline errors, success-state swap), a 3-contact-method + 3-benefit-callout info column, and an 8-item FAQ accordion. Gates the first-ever install of `react-hook-form`/`zod`/`@hookform/resolvers` into this monorepo behind an explicit human legitimacy check. Satisfies CONT-01, CONT-02, CONT-03.

Purpose: Deliver the site's primary lead-gen form, production-shaped (typed, validated) even though submission stays mocked this milestone.
Output: `apps/web/app/contacts/page.tsx` (new route), 6 new component files, 3 new npm dependencies (plus the already-locked `sonner` declared directly per the established Rule-3 pattern).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-home-contacts-demo/02-CONTEXT.md
@.planning/phases/02-home-contacts-demo/02-PATTERNS.md
@.planning/phases/01.1-premium-design-system/01.1-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package Legitimacy Gate — react-hook-form, zod, @hookform/resolvers</name>
  <files>(none — this checkpoint installs nothing; it only gates Task 2's install)</files>
  <read_first>
    .planning/PROJECT.md (Constraints — react-hook-form + zod are required for all form validation this milestone)
    apps/web/package.json (current `dependencies` block — being extended in Task 2)
  </read_first>
  <what-built>
    Nothing yet — this checkpoint runs before any package install in Task 2. `react-hook-form`, `zod`, and `@hookform/resolvers` have never been installed anywhere in this monorepo (confirmed: zero `pnpm-lock.yaml` entries for `react-hook-form` or `@hookform/resolvers`; a `zod` entry exists but only as an indirect, undeclared transitive dependency of unrelated tooling — none of the three packages is usable from `apps/web` without an explicit, direct install). No RESEARCH.md exists for this phase (skipped per established project preference), so there is no prior audit verdict to cite — this checkpoint is the first legitimacy check for these three packages.
  </what-built>
  <action>
    Present the 3 npmjs.com package listings below to the user and pause for an explicit approval before Task 2 runs any install command. Do not run `pnpm add` or modify any file in this task — this task is verification-only.
  </action>
  <how-to-verify>
    Check each package's npm registry listing before approving Task 2's install:
    1. https://www.npmjs.com/package/react-hook-form — confirm the GitHub repo is `react-hook-form/react-hook-form`, weekly downloads in the millions, active/recent publish history.
    2. https://www.npmjs.com/package/zod — confirm the GitHub repo is `colinhacks/zod`, weekly downloads in the tens of millions, active/recent publish history.
    3. https://www.npmjs.com/package/@hookform/resolvers — confirm the GitHub repo is `react-hook-form/resolvers` (same maintainer org as react-hook-form), weekly downloads in the millions.
  </how-to-verify>
  <acceptance_criteria>
    <criterion>User has responded with an explicit approval (or has identified a concern that halts the plan) for all three packages</criterion>
  </acceptance_criteria>
  <verify>
    <automated>MISSING — this is a human-approval checkpoint, not an automatable check; the resume-signal below is the verification gate</automated>
  </verify>
  <done>User has typed "approved" (or equivalent affirmative), confirming all three packages' npmjs.com listings were reviewed and are legitimate.</done>
  <resume-signal>Type "approved" once all three packages are confirmed legitimate on npmjs.com, or describe concerns to halt the install.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install deps, build input/textarea primitives, build the validated Contact form</name>
  <files>apps/web/package.json, pnpm-lock.yaml, apps/web/shared/components/premium-input.tsx, apps/web/shared/components/premium-textarea.tsx, apps/web/modules/contacts/contact-form.tsx</files>
  <read_first>
    apps/web/shared/components/premium-button.tsx (cva pattern, `cn()` import path, `data-slot` convention this task's new primitives follow)
    apps/web/shared/components/premium-card.tsx (plain-div `React.ComponentProps` + `cn()` pattern for the form's card container — no sub-components exist, use plain `<h2>`/`<p>` inside instead of a Card-family)
    apps/web/shared/lib/cn.ts (`cn()` helper)
    apps/web/app/layout.tsx (already renders the site's toast surface — no changes needed here this task)
    .planning/phases/02-home-contacts-demo/02-CONTEXT.md (D-09 — exact validation rules per field; the Contacts page `<code_context>` source for the form's field set, placeholders, and success-state copy)
    .planning/phases/02-home-contacts-demo/02-PATTERNS.md ("apps/web/modules/contacts/contact-form.tsx" pattern section — submit-button/card-container guidance, the RHF+zod "No Analog Found" note)
  </read_first>
  <action>
    Approval from Task 1 is required before this task runs. Run a single `pnpm --filter web add` installing `react-hook-form`, `zod`, `@hookform/resolvers` (the three newly-approved packages, unpinned caret ranges — `react-hook-form@^7`, `zod@^3`, `@hookform/resolvers@^3` — let pnpm resolve the current stable release within each major) together with `sonner@2.0.7` (an already-locked transitive dependency of the shared component workspace package, being declared directly on `apps/web` per the same "Rule 3" precedent Phase 1 used for `next-themes`/`lucide-react` and Phase 01.1 used for `class-variance-authority`/`clsx`/`tailwind-merge`/`radix-ui` — pnpm's per-package `node_modules` isolation means `apps/web` cannot import `sonner` directly until it is a direct dependency, even though it is already resolved in `pnpm-lock.yaml`).

    Create `apps/web/shared/components/premium-input.tsx`: a plain (non-cva) function component `PremiumInput({ className, ...props }: React.ComponentProps<'input'>)` rendering an `<input data-slot="premium-input" className={cn('flex h-11 w-full rounded-dt-input border border-dt-navy/20 bg-dt-warm-white px-3 py-2 text-sm text-dt-navy placeholder:text-dt-graphite/50 outline-none transition-colors focus-visible:border-dt-navy focus-visible:ring-2 focus-visible:ring-dt-navy/30 disabled:cursor-not-allowed disabled:opacity-50', className)} {...props} />`. Export `{ PremiumInput }`.

    Create `apps/web/shared/components/premium-textarea.tsx`: the same pattern as `PremiumInput` but rendering a `<textarea>`, `data-slot="premium-textarea"`, adding `min-h-[120px] resize-y` to the class list, otherwise identical border/radius/focus treatment. Export `{ PremiumTextarea }`.

    Create `apps/web/modules/contacts/contact-form.tsx` as a `'use client'` module exporting `function ContactForm(): React.JSX.Element`. Define a zod object schema `contactFormSchema` with 4 fields per D-09: `name: z.string().trim().min(2, "Ім'я має містити щонайменше 2 символи")`; `clinic: z.string().trim().optional()`; `contact: z.string().trim().min(1, 'Вкажіть телефон або email').refine((value) => /^\+?[0-9\s\-()]{7,20}$/.test(value) || /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value), { message: 'Введіть коректний номер телефону або email' })`; `message: z.string().trim().optional()`. Export `type ContactFormValues = z.infer<typeof contactFormSchema>`. In the component: `const form = useForm<ContactFormValues>({ resolver: zodResolver(contactFormSchema), defaultValues: { name: '', clinic: '', contact: '', message: '' } })`; local `const [isSubmitted, setIsSubmitted] = React.useState(false)`. `onSubmit = form.handleSubmit((values) => { setTimeout(() => { setIsSubmitted(true); toast.success('Заявку успішно надіслано!'); }, 500); })` (the 500ms `setTimeout` and exact toast copy are ported verbatim from the archive per D-09 — no real network call). Render inside a `<PremiumCard>`: when `!isSubmitted`, an `<h2 className="text-dt-h3 font-dt-heading font-semibold text-dt-navy">Залишити заявку</h2>` + `<p className="text-dt-graphite">Заповніть форму і ми зв'яжемося з вами найближчим часом</p>` above a `<form onSubmit={onSubmit} className="space-y-6">` with 4 labeled fields (`<label>` + `PremiumInput`/`PremiumTextarea` registered via `form.register('name')` etc., `rows={4}` on the textarea) — under each of the `name`/`contact` fields, conditionally render `form.formState.errors.name?.message` / `form.formState.errors.contact?.message` in a `<p className="text-sm text-dt-coral">` when present — and a `PremiumButton type="submit" variant="coral" size="lg" className="w-full"` reading "Надіслати заявку". When `isSubmitted`, render the archived success state instead: a centered `CheckCircle` icon (Phosphor, `weight="fill" className="h-8 w-8 text-dt-teal"`) inside a `flex h-16 w-16 items-center justify-center rounded-full bg-dt-teal/10 mx-auto mb-4`, `<h3 className="text-dt-h3 font-dt-heading font-bold text-dt-navy">Дякуємо!</h3>`, `<p className="text-dt-graphite">Ваша заявка успішно надіслана. Ми зв'яжемося з вами найближчим часом.</p>`, and a `PremiumButton variant="outline" onClick={() => { setIsSubmitted(false); form.reset(); }}` reading "Надіслати ще одну заявку".
  </action>
  <acceptance_criteria>
    <criterion>`apps/web/package.json` `dependencies` includes `react-hook-form`, `zod`, `@hookform/resolvers`, and `sonner`</criterion>
    <criterion>`apps/web/shared/components/premium-input.tsx` and `premium-textarea.tsx` each export exactly one named component, both importing `cn` from `@/shared/lib/cn`</criterion>
    <criterion>`apps/web/modules/contacts/contact-form.tsx` defines `contactFormSchema` with `z.object`, uses `zodResolver`, and calls `useForm`</criterion>
    <criterion>The `contact` field's zod validator uses `.refine(` with both a phone-pattern and an email-pattern check</criterion>
    <criterion>`toast.success('Заявку успішно надіслано!')` appears exactly once, imported from `sonner` directly (not from the shared shadcn package)</criterion>
    <criterion>Both the pre-submit form JSX and the post-submit "Дякуємо!" JSX exist in the same component, gated on `isSubmitted`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c '"react-hook-form"' apps/web/package.json)" = "1" && \
test "$(grep -c '"zod"' apps/web/package.json)" = "1" && \
test "$(grep -c '"@hookform/resolvers"' apps/web/package.json)" = "1" && \
test "$(grep -c '"sonner"' apps/web/package.json)" = "1" && \
test "$(grep -c 'export function PremiumInput\|function PremiumInput' apps/web/shared/components/premium-input.tsx)" -ge "1" && \
test "$(grep -c 'export function PremiumTextarea\|function PremiumTextarea' apps/web/shared/components/premium-textarea.tsx)" -ge "1" && \
test "$(grep -c "@/shared/lib/cn" apps/web/shared/components/premium-input.tsx apps/web/shared/components/premium-textarea.tsx)" -ge "2" && \
test "$(grep -c 'z.object' apps/web/modules/contacts/contact-form.tsx)" -ge "1" && \
test "$(grep -c 'zodResolver' apps/web/modules/contacts/contact-form.tsx)" -ge "1" && \
test "$(grep -c 'useForm' apps/web/modules/contacts/contact-form.tsx)" -ge "1" && \
test "$(grep -c '.refine(' apps/web/modules/contacts/contact-form.tsx)" -ge "1" && \
test "$(grep -c "from 'sonner'" apps/web/modules/contacts/contact-form.tsx)" = "1" && \
test "$(grep -c "toast.success('Заявку успішно надіслано!')" apps/web/modules/contacts/contact-form.tsx)" = "1" && \
test "$(grep -c 'isSubmitted' apps/web/modules/contacts/contact-form.tsx)" -ge "2" && \
echo PASS
    </automated>
  </verify>
  <done>`react-hook-form`/`zod`/`@hookform/resolvers`/`sonner` are installed; `PremiumInput`/`PremiumTextarea` primitives exist; `ContactForm` validates per D-09, mocks submission with the archived toast/delay, and swaps to the "Дякуємо!" confirmation card with a working reset button.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: FAQ accordion, contact-info cards, finish contacts/page.tsx</name>
  <files>apps/web/shared/components/premium-accordion.tsx, apps/web/modules/contacts/faq-accordion.tsx, apps/web/modules/contacts/contact-info.tsx, apps/web/app/contacts/page.tsx</files>
  <read_first>
    apps/web/modules/contacts/contact-form.tsx (Task 2 output — `ContactForm` this task's `page.tsx` renders alongside `ContactInfo`)
    packages/ui/src/components/shadcn-ui/accordion.tsx (structural Radix-usage reference only, per 02-CONTEXT.md's "not this file's import target" note — same `Header`/`Trigger`/`Content` shape and the exact `data-[state=closed]:animate-accordion-up data-[state=open]:animate-accordion-down` utility class names already available via the `tw-animate-css` import in `apps/web/app/globals.css`; do not import anything from this file)
    apps/web/shared/components/premium-card.tsx, apps/web/shared/components/container.tsx, apps/web/shared/components/reveal.tsx
    .planning/phases/02-home-contacts-demo/02-CONTEXT.md (all 8 FAQ question/answer pairs, transcribed verbatim in the Contacts `<code_context>` block; the 3 contact-method + 3 benefit-callout card summaries in the same block's trailing comments)
  </read_first>
  <action>
    Create `apps/web/shared/components/premium-accordion.tsx` using `Accordion as AccordionPrimitive` imported from the `radix-ui` package (the same combined-package import style already used for `Slot` in `premium-button.tsx`). Export 4 components mirroring `packages/ui`'s accordion shape but on `dt-` tokens: `PremiumAccordion` (thin wrapper over `AccordionPrimitive.Root`, `data-slot="premium-accordion"`, passes through all props including `type="single"` `collapsible`); `PremiumAccordionItem({ className, ...props })` (`AccordionPrimitive.Item`, `data-slot="premium-accordion-item"`, `className={cn('rounded-dt-card border border-dt-navy/10 bg-dt-warm-white px-6', className)}`); `PremiumAccordionTrigger({ className, children, ...props })` (`AccordionPrimitive.Header className="flex"` wrapping `AccordionPrimitive.Trigger`, `data-slot="premium-accordion-trigger"`, `className={cn('flex flex-1 items-center justify-between gap-4 py-4 text-left font-dt-heading font-semibold text-dt-navy [&[data-state=open]>svg]:rotate-180', className)}`, rendering `children` followed by a Phosphor `CaretDown` icon at `h-5 w-5 shrink-0 text-dt-navy transition-transform duration-200`); `PremiumAccordionContent({ className, children, ...props })` (`AccordionPrimitive.Content`, `data-slot="premium-accordion-content"`, `className="overflow-hidden text-dt-graphite data-[state=closed]:animate-accordion-up data-[state=open]:animate-accordion-down"`, wrapping `children` in an inner `<div className={cn('pb-4', className)}>`).

    Create `apps/web/modules/contacts/faq-accordion.tsx` exporting `function FaqAccordion(): React.JSX.Element` (server component — the underlying Radix primitive handles its own client interactivity internally, this composing file needs no `'use client'`). Define a local `const faqs` array of the 8 `{ question, answer }` pairs transcribed verbatim in 02-CONTEXT.md's Contacts source. Render `<PremiumAccordion type="single" collapsible className="space-y-4">` mapping `faqs` to `<PremiumAccordionItem key={index} value={`item-${index}`}>` containing `<PremiumAccordionTrigger>{faq.question}</PremiumAccordionTrigger>` and `<PremiumAccordionContent>{faq.answer}</PremiumAccordionContent>`.

    Create `apps/web/modules/contacts/contact-info.tsx` exporting `function ContactInfo(): React.JSX.Element` (server component). A `<div className="space-y-6">` with two groups. Group 1 — 3 contact-method `<PremiumCard>` rows (icon badge `flex h-10 w-10 items-center justify-center rounded-dt-card bg-dt-navy text-dt-warm-white shrink-0` + a label/value text column): `PaperPlaneTilt` icon, "Telegram" label, "@dentabot_support" value; `Envelope` icon, "Email" label, "hello@dentabot.ua" value; `Clock` icon, "Робочі години" label, "Пн-Пт 9:00-18:00 Київ" value — all icons from `@phosphor-icons/react/ssr`. Group 2 — 3 benefit-callout cards, each a small `rounded-dt-card p-4 flex items-center gap-3` tinted with a literal (non-`dt-`) Tailwind background per 02-CONTEXT.md's Specific Ideas precedent: `bg-green-50` + `Clock` icon `text-green-600` + "Відповідаємо за 2 години"; `bg-blue-50` + `Sparkle` icon `text-blue-600` + "Безкоштовна демонстрація"; `bg-purple-50` + `Lightning` icon `text-purple-600` + "Налаштування за 1 день".

    Create `apps/web/app/contacts/page.tsx`: `export default function Contacts(): React.JSX.Element` (server component). Hero: centered `<section>` wrapping `<Container>` with `<h1 className="text-dt-h1 font-dt-heading font-bold text-dt-navy">Зв'яжіться з нами</h1>` and the subhead paragraph from 02-CONTEXT.md. Main: `<section>` wrapping `<Container>`, a `lg:grid-cols-2 gap-12 max-w-6xl mx-auto` grid rendering `<ContactForm />` and `<ContactInfo />` side by side. FAQ: `<section className="bg-dt-navy/5">` wrapping `<Container>`, a `<Reveal>`-wrapped centered `<h2 className="text-dt-h2 font-dt-heading font-bold text-dt-navy">Часті питання</h2>`, then `<div className="mx-auto max-w-3xl"><FaqAccordion /></div>`. Import `ContactForm` from `@/modules/contacts/contact-form`, `ContactInfo` from `@/modules/contacts/contact-info`, `FaqAccordion` from `@/modules/contacts/faq-accordion`.
  </action>
  <acceptance_criteria>
    <criterion>`apps/web/shared/components/premium-accordion.tsx` exports all 4 named components and imports `Accordion as AccordionPrimitive` from the `radix-ui` package</criterion>
    <criterion>`apps/web/modules/contacts/faq-accordion.tsx` defines exactly 8 entries in its `faqs` array</criterion>
    <criterion>`apps/web/modules/contacts/contact-info.tsx` renders exactly 3 `PremiumCard` contact-method rows plus 3 benefit-callout divs (6 total content blocks)</criterion>
    <criterion>`apps/web/app/contacts/page.tsx` imports and renders `ContactForm`, `ContactInfo`, and `FaqAccordion`</criterion>
    <criterion>None of this task's 4 files import a component from the shared shadcn package</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c "Accordion as AccordionPrimitive" apps/web/shared/components/premium-accordion.tsx)" = "1" && \
test "$(grep -c "from 'radix-ui'" apps/web/shared/components/premium-accordion.tsx)" -ge "1" && \
test "$(grep -c "export.*PremiumAccordion\b" apps/web/shared/components/premium-accordion.tsx)" -ge "1" && \
test "$(grep -c "question:" apps/web/modules/contacts/faq-accordion.tsx)" = "8" && \
test "$(grep -c "PremiumCard" apps/web/modules/contacts/contact-info.tsx)" = "3" && \
test "$(grep -c "<ContactForm" apps/web/app/contacts/page.tsx)" = "1" && \
test "$(grep -c "<ContactInfo" apps/web/app/contacts/page.tsx)" = "1" && \
test "$(grep -c "<FaqAccordion" apps/web/app/contacts/page.tsx)" = "1" && \
test "$(grep -Ec '@repo/ui' apps/web/shared/components/premium-accordion.tsx apps/web/modules/contacts/faq-accordion.tsx apps/web/modules/contacts/contact-info.tsx apps/web/app/contacts/page.tsx)" = "0" && \
CT_OUT="$(pnpm --filter web check-types 2>&1)"; \
test -z "$(printf '%s' "$CT_OUT" | grep 'error TS' | grep -v 'button-group.tsx\|calendar.tsx\|sidebar.tsx')" && \
echo PASS
    </automated>
  </verify>
  <done>`/contacts` renders the hero, the validated form + contact-info column, and the working 8-item FAQ accordion — CONT-01, CONT-02, CONT-03 are satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry → `apps/web/package.json` | 3 previously-uninstalled dependencies (`react-hook-form`, `zod`, `@hookform/resolvers`) entering the dependency graph |
| User → Contacts form fields | Untyped free-text input (name/clinic/contact/message), client-validated only, never sent to a real backend this milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-02-SC | Tampering (supply chain) | `react-hook-form`, `zod`, `@hookform/resolvers` npm installs | high | mitigate | Task 1 blocking-human checkpoint verifies each package's npmjs.com listing (maintainer, GitHub repo, download counts) before Task 2 installs anything; `workflow.auto_advance` is ignored for this checkpoint per the Package Legitimacy Gate protocol |
| T-02-02-02 | Tampering (supply chain, low-risk) | `sonner` npm install | low | accept | Already-locked, already-vetted transitive dependency resolved identically via the shared component workspace package's own `pnpm-lock.yaml` entry — no new code enters the dependency graph, only a direct `package.json` declaration (Rule 3 precedent) |
| T-02-02-03 | Tampering / Injection | Contact form free-text fields (name/clinic/contact/message) | medium | mitigate | Values are rendered exclusively as React JSX text content (never via `dangerouslySetInnerHTML` or raw DOM injection), so React's default output-escaping neutralizes stored-XSS via typed input; `zod` additionally rejects malformed `contact` values before the mocked "submit" path runs |
| T-02-02-04 | Information Disclosure | Mocked form "submission" | low | accept | Form data never leaves the client (a `setTimeout`-simulated delay only) — no backend endpoint exists yet this milestone (tracked as v2 `INTEG-01`), so there is no data-in-transit exposure to mitigate |

</threat_model>

<verification>
- `pnpm --filter web check-types` after Task 3 reports zero new errors outside the pre-existing, unrelated `csstype` conflict
- Task 2 and Task 3's grep-based acceptance criteria confirm dependency installation, zod/RHF wiring, accordion structure, and zero shadcn-package leakage
- Manual spot-check (not automated this plan): `pnpm dev:web`, visit `/contacts`, submit the form with an invalid `contact` value and confirm the inline error message, then submit valid values and confirm the toast + "Дякуємо!" swap + reset button; expand/collapse several FAQ items
</verification>

<success_criteria>
`/contacts` renders a fully validated, production-shaped lead-capture form (react-hook-form + zod, mocked submission, inline errors, success-state swap), a working contact-info column, and all 8 FAQ items — CONT-01, CONT-02, CONT-03 and ROADMAP Phase 2 success criteria 3-5 are satisfied.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `apps/web/shared/components/premium-input.tsx` (new) — `PremiumInput`
- `apps/web/shared/components/premium-textarea.tsx` (new) — `PremiumTextarea`
- `apps/web/shared/components/premium-accordion.tsx` (new) — `PremiumAccordion`, `PremiumAccordionItem`, `PremiumAccordionTrigger`, `PremiumAccordionContent`
- `apps/web/modules/contacts/contact-form.tsx` (new) — `ContactForm`, `contactFormSchema`, `ContactFormValues`
- `apps/web/modules/contacts/faq-accordion.tsx` (new) — `FaqAccordion`
- `apps/web/modules/contacts/contact-info.tsx` (new) — `ContactInfo`
- `apps/web/app/contacts/page.tsx` (new) — Contacts route
- `apps/web/package.json` / `pnpm-lock.yaml` (edited) — adds `react-hook-form`, `zod`, `@hookform/resolvers`, `sonner` as direct dependencies

<output>
Create `.planning/phases/02-home-contacts-demo/02-02-SUMMARY.md` when done
</output>
