---
phase: 02-home-contacts-demo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/demo/_data.ts
  - apps/web/app/demo/page.tsx
autonomous: true
requirements: [DEMO-01, DEMO-02]

must_haves:
  truths:
    - "User visiting /demo sees a two-tab interface (🤖 Бот — вид пацієнта / ⚙️ Адмін панель), defaulting to the Bot tab"
    - "User can click any of the 3 scenario buttons in the Bot tab and watch the chat replay from the greeting, with each scripted message appearing roughly 800ms apart"
    - statement: "Triggering a new scenario while a previous one is still mid-playback clears the prior setInterval before starting the new one, so messages from two scenarios never interleave"
      verification: backstop
    - "User can switch between the 5 admin sidebar sections (Dashboard/Записи/Лікарі/Пацієнти/Налаштування) and see each section's distinct content, with the active section's button using the default (filled) Button variant and inactive ones ghost"
    - "Dashboard section shows exactly 4 stat cards (12/47/156/85%) and a 7-day bar chart using the exact mock values [42, 38, 45, 50, 47, 40, 35] with Пн-Нд day labels"
    - "Appointments section shows all 5 mock appointments with a Підтверджено/Очікує badge matching each row's confirmed/pending status"
    - "Doctors section shows all 3 mock doctor cards with name, specialty, experience, and schedule"
    - "Admin banner's 'Підключити свою клініку →' button links to /contacts"
  artifacts:
    - path: "apps/web/app/demo/_data.ts"
      provides: "Typed mock data module: initial chat greeting, 3 chat scenarios, 5 appointments, 3 doctors, 4 dashboard stats, 7-day bar chart values"
      exports: "ChatMessage, initialChatMessages, Scenario, scenarios, Appointment, appointments, Doctor, doctors, dashboardStats, weeklyBookings, weekDays"
    - path: "apps/web/app/demo/page.tsx"
      provides: "Demo route — Tabs-based bot/admin simulation, client component"
      exports: "default (Demo)"
  key_links:
    - from: "apps/web/app/demo/page.tsx"
      to: "apps/web/app/demo/_data.ts"
      via: "named imports of all mock data constants and types"
      pattern: "from '\\./_data'"
    - from: "apps/web/app/demo/page.tsx"
      to: "apps/web/lib/routes.ts"
      via: "routes.contacts used as the admin banner's Link href"
      pattern: "routes\\.contacts"
---

<objective>
Build the `/demo` route: a scripted, entirely client-side (mock data, local state, no real API) two-tab simulation of the DentaBot patient chat and clinic admin panel, ported verbatim from the design archive's `demo.tsx`, fixing the archive's latent unguarded-`setInterval` bug during the port per CONTEXT.md's explicit discretion note.

Purpose: DEMO-01/DEMO-02 — the second lead-gen surface, letting a prospective clinic "try before they buy" without registering.
Output: a working `pnpm dev:web` `/demo` page where scenario buttons replay a chat and the admin sidebar switches between 5 distinct content panels, all backed by a typed local mock-data module.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-home-contacts-demo/02-CONTEXT.md
@.planning/phases/02-home-contacts-demo/02-PATTERNS.md
@.planning/phases/01-theme-site-shell/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mock data module</name>
  <files>apps/web/app/demo/_data.ts</files>
  <read_first>
    apps/web/lib/routes.ts (exact analog — `as const` typed-object-literal module shape to mirror)
    .planning/phases/02-home-contacts-demo/02-CONTEXT.md (lines ~412-417 — verbatim mock data values: appointments, doctors, dashboard stats, bar chart)
    .planning/phases/02-home-contacts-demo/02-PATTERNS.md ("Data extraction pattern" section — `as const` + explicit union literal types, not loose `string`)
  </read_first>
  <action>
    Create `apps/web/app/demo/_data.ts` (no `'use client'` — pure data, no React). Define and export: `export type ChatMessage = { type: 'bot' | 'user'; text: string };`. `export const initialChatMessages: ChatMessage[] = [...]` with exactly 2 entries: `{ type: 'bot', text: 'Вітаю! Я DentaBot 🦷' }`, `{ type: 'bot', text: 'Допоможу записатись на прийом до стоматолога' }`. `export type Scenario = { label: string; messages: ChatMessage[] };` and `export const scenarios: Scenario[] = [...]` with exactly 3 entries, each `messages` array reproducing the exact archive script (multi-line message text uses `\n` inside the string literal, not separate array items — preserve line breaks exactly as written): (1) label `'Записатись на прийом'`, messages: user `'Хочу записатись'` → bot `'Чудово! Оберіть зручну дату:'` → bot `'📅 8 березня (Сб)\n📅 10 березня (Пн)\n📅 11 березня (Вт)'` → user `'10 березня'` → bot `'Оберіть час:\n⏰ 10:00\n⏰ 14:00\n⏰ 16:30'` → user `'14:00'` → bot `'Оберіть лікаря:\n👨‍⚕️ Доктор Іваненко Петро\n👩‍⚕️ Доктор Коваль Олена'` → user `'Доктор Коваль Олена'` → bot `'✅ Готово! Ви записані на 10 березня о 14:00 до лікаря Коваль Олена.\n\nНадішлю нагадування за день до прийому.'`; (2) label `'Перенести запис'`, messages: user `'Хочу перенести запис'` → bot `'Зараз у вас запис на 10 березня о 14:00'` → bot `'Оберіть нову дату:\n📅 11 березня (Вт)\n📅 12 березня (Ср)\n📅 13 березня (Чт)'` → user `'11 березня'` → bot `'Оберіть час:\n⏰ 11:00\n⏰ 15:00\n⏰ 17:00'` → user `'15:00'` → bot `'✅ Запис перенесено! Нова дата: 11 березня о 15:00'`; (3) label `'Скасувати запис'`, messages: user `'Потрібно скасувати запис'` → bot `'Ваш запис: 10 березня о 14:00 до лікаря Коваль Олена'` → bot `'Ви впевнені що хочете скасувати?'` → user `'Так, скасувати'` → bot `'✅ Запис скасовано. Сподіваємось побачити вас іншого разу!'`.
    `export type Appointment = { id: number; patient: string; doctor: string; date: string; time: string; status: 'confirmed' | 'pending' };` and `export const appointments: Appointment[] = [...]` with exactly 5 entries: `{ id: 1, patient: 'Іваненко Марія', doctor: 'Доктор Коваль О.', date: '10 березня', time: '14:00', status: 'confirmed' }`, `{ id: 2, patient: 'Петренко Олександр', doctor: 'Доктор Іваненко П.', date: '10 березня', time: '15:30', status: 'confirmed' }`, `{ id: 3, patient: 'Сидоренко Анна', doctor: 'Доктор Коваль О.', date: '11 березня', time: '10:00', status: 'pending' }`, `{ id: 4, patient: 'Мельник Ігор', doctor: 'Доктор Іваненко П.', date: '11 березня', time: '11:30', status: 'confirmed' }`, `{ id: 5, patient: 'Коваленко Ольга', doctor: 'Доктор Коваль О.', date: '12 березня', time: '09:00', status: 'confirmed' }`.
    `export type Doctor = { name: string; specialty: string; experience: string; schedule: string; avatar: string };` and `export const doctors: Doctor[] = [...]` with exactly 3 entries: `{ name: 'Іваненко Петро Сергійович', specialty: 'Терапевт-стоматолог', experience: '12 років досвіду', schedule: 'Пн, Ср, Пт: 9:00 - 18:00', avatar: '👨‍⚕️' }`, `{ name: 'Коваль Олена Михайлівна', specialty: 'Хірург-стоматолог', experience: '8 років досвіду', schedule: 'Вт, Чт, Сб: 10:00 - 19:00', avatar: '👩‍⚕️' }`, `{ name: 'Сидоренко Андрій Васильович', specialty: 'Ортодонт', experience: '15 років досвіду', schedule: 'Пн-Пт: 10:00 - 17:00', avatar: '👨‍⚕️' }`.
    `export const dashboardStats = [{ label: 'Записів сьогодні', value: '12' }, { label: 'Записів цього тижня', value: '47' }, { label: 'Активних пацієнтів', value: '156' }, { label: 'Завантаженість', value: '85%' }] as const;`. `export const weeklyBookings = [42, 38, 45, 50, 47, 40, 35] as const;`. `export const weekDays = ['Пн', 'Вт', 'Ср', 'Чт', 'Пт', 'Сб', 'Нд'] as const;`.
  </action>
  <acceptance_criteria>
    <criterion>File exports exactly these 11 identifiers: ChatMessage, initialChatMessages, Scenario, scenarios, Appointment, appointments, Doctor, doctors, dashboardStats, weeklyBookings, weekDays</criterion>
    <criterion>`appointments` array has exactly 5 elements and `doctors` has exactly 3</criterion>
    <criterion>`scenarios` array has exactly 3 elements</criterion>
    <criterion>`weeklyBookings` equals exactly `[42, 38, 45, 50, 47, 40, 35]`</criterion>
    <criterion>File contains no `'use client'` directive (pure data module)</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c 'export ' apps/web/app/demo/_data.ts)" -ge "11" && \
test "$(grep -c \"id: 1,\" apps/web/app/demo/_data.ts)" = "1" && \
test "$(grep -c \"id: 5,\" apps/web/app/demo/_data.ts)" = "1" && \
test "$(grep -c '42, 38, 45, 50, 47, 40, 35' apps/web/app/demo/_data.ts)" = "1" && \
test "$(grep -c \"'use client'\" apps/web/app/demo/_data.ts)" = "0" && \
test "$(grep -c \"Іваненко Петро Сергійович\" apps/web/app/demo/_data.ts)" = "1" && \
echo PASS
    </automated>
  </verify>
  <done>apps/web/app/demo/_data.ts exports all typed mock data (chat scenarios, appointments, doctors, dashboard stats, bar chart values) verbatim from the design source, ready for page.tsx to import.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Demo page scaffold and Bot tab</name>
  <files>apps/web/app/demo/page.tsx</files>
  <read_first>
    apps/web/app/demo/_data.ts (Task 1 output — imported here)
    apps/web/components/header.tsx ('use client' + useState/useEffect cleanup-return idiom to mirror for the interval fix)
    packages/ui/src/components/shadcn-ui/tabs.tsx (Tabs/TabsList/TabsTrigger/TabsContent API — full file, no changes needed)
    .planning/phases/02-home-contacts-demo/02-CONTEXT.md (lines ~232-410 — Demo page design source; lines ~36-39 — Claude's Discretion note on the setInterval cleanup fix being in-scope, not a scope change)
    .planning/phases/02-home-contacts-demo/02-PATTERNS.md (Demo page pattern-assignment section — client directive/state pattern, effect cleanup pattern, Tabs composition)
  </read_first>
  <action>
    Create `apps/web/app/demo/page.tsx`, first line `'use client';` (chat state, tab-driven admin section, `setInterval` playback all require it). Import order: third-party (`Clock`, `User` from `'lucide-react'` — these are the ONLY lucide icons actually rendered in this page's JSX; do not import `Calendar`, `CheckCircle`, `X`, or `Phone` even though the design archive's source imports them — they are unused there and would fail this repo's `--max-warnings 0` ESLint config; `Link` from `'next/link'`; `Badge`, `Button`, `Card`, `CardContent`, `CardDescription`, `CardHeader`, `CardTitle`, `Tabs`, `TabsContent`, `TabsList`, `TabsTrigger` from `'@repo/ui'`; `* as React` from `'react'`) then relative (`routes` from `'@/lib/routes'`; `appointments`, `doctors`, `initialChatMessages`, `scenarios`, `type ChatMessage` from `'./_data'` — Task 3 will additionally need `dashboardStats`, `weeklyBookings`, `weekDays`, `type Appointment`, `type Doctor` from the same module, add those to this same import line now so both tasks share one import statement).
    Default export `function Demo(): React.JSX.Element`. State: `const [chatMessages, setChatMessages] = React.useState<ChatMessage[]>(initialChatMessages);` and `const [selectedSection, setSelectedSection] = React.useState<'dashboard' | 'appointments' | 'doctors' | 'patients' | 'settings'>('dashboard');`. Interval-cleanup fix (CONTEXT.md discretion — the archive never clears its interval before starting a new one): `const scenarioIntervalRef = React.useRef<ReturnType<typeof setInterval> | null>(null);` then a `runScenario(scenarioIndex: number)` function that: if `scenarioIntervalRef.current` is non-null, calls `clearInterval(scenarioIntervalRef.current)` first; resets `chatMessages` to `initialChatMessages`; then starts a new `setInterval` at 800ms appending `scenarios[scenarioIndex].messages[currentIndex]` one at a time via the functional `setChatMessages((prev) => [...prev, ...])` form, clearing the interval (and setting `scenarioIntervalRef.current = null`) once `currentIndex` reaches the scenario's message count; store the new interval id in `scenarioIntervalRef.current` immediately after creating it. Add a `React.useEffect(() => { return () => { if (scenarioIntervalRef.current) clearInterval(scenarioIntervalRef.current); }; }, []);` unmount-cleanup effect, mirroring `header.tsx`'s `addEventListener`/`removeEventListener` cleanup-return idiom but for `setInterval`/`clearInterval`.
    Render: root `<div className="min-h-screen pt-24 lg:pt-32 pb-16">`. Hero `<section className="pb-12">` with `container mx-auto px-4 lg:px-8` wrapping a `text-center mb-8`: `<Badge variant="secondary" className="mb-4">DEMO MODE</Badge>`, `<h1 className="text-4xl lg:text-6xl font-bold text-foreground mb-4">Живе демо — спробуйте DentaBot прямо зараз</h1>` (D-03: was `text-gray-900 dark:text-white`), `<p className="text-lg text-muted-foreground max-w-3xl mx-auto">Це тестове середовище клініки "Посмішка". Всі дані фейкові. Реєстрація не потрібна.</p>` (D-03: was `text-gray-600 dark:text-gray-400`).
    Then a `<section>` wrapping `container mx-auto px-4 lg:px-8` with `<Tabs defaultValue="bot" className="w-full">`: `<TabsList className="grid w-full max-w-md mx-auto grid-cols-2 h-14 mb-8">` containing `<TabsTrigger value="bot" className="text-base">🤖 Бот — вид пацієнта</TabsTrigger>` and `<TabsTrigger value="admin" className="text-base">⚙️ Адмін панель</TabsTrigger>`.
    `<TabsContent value="bot">`: `grid lg:grid-cols-3 gap-8`. Left `lg:col-span-2` phone mockup: `max-w-md mx-auto` wrapping `bg-gray-900 rounded-[3rem] p-3 shadow-2xl` (keep this literal `bg-gray-900` — it represents an actual phone-bezel device chrome, not app UI, and is deliberately identical in both themes; do not normalize it) wrapping `bg-card rounded-[2.5rem] overflow-hidden h-[600px] flex flex-col` (D-03: was `bg-white dark:bg-gray-800`). Inside: Telegram-style header `bg-brand text-white px-4 py-3 flex items-center gap-3` (D-02, was `bg-[#1d6be4]`) with a `w-10 h-10 rounded-full bg-white flex items-center justify-center` holding `<span className="text-2xl">🦷</span>`, plus a text block (`font-semibold` "DentaBot" / `text-xs opacity-90` "завжди онлайн"). Chat area `flex-1 overflow-y-auto p-4 space-y-3 bg-muted` (D-03: was `bg-gray-50 dark:bg-gray-900`) — `.map` over `chatMessages`, each a `flex ${message.type === 'user' ? 'justify-end' : 'justify-start'}` wrapping a bubble `max-w-[80%] px-4 py-2 rounded-2xl` with conditional classes: user → `bg-brand text-white` (D-02, was `bg-[#1d6be4] text-white`), bot → `bg-card text-foreground` (D-03, was `bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100`) — render `message.text` directly (React text nodes render literal `\n` as a space, matching the design's own behavior, no `white-space: pre-line` was applied in the source — do not add one). Input placeholder bar `p-3 bg-card border-t border-border` (D-03) wrapping `bg-muted rounded-full px-4 py-2 text-sm text-muted-foreground` (D-03) with text "Оберіть сценарій справа →".
    Right column `space-y-4`: `<h3 className="font-semibold text-lg mb-4">Оберіть сценарій:</h3>`, then `.map` over `scenarios` rendering `<Button key={index} onClick={() => runScenario(index)} variant="outline" className="w-full justify-start h-auto py-4">{scenario.label}</Button>`. Then `pt-4 border-t border-border` (D-03, was `dark:border-gray-800` — normalize to the semantic border token in both modes) with `<p className="text-sm text-muted-foreground mb-3">Або спробуйте в реальному Telegram:</p>` and `<Button variant="default" className="w-full">Відкрити в Telegram</Button>` (no `onClick` — non-functional decorative button in the source, port as-is, no real Telegram deep link exists this milestone).
  </action>
  <acceptance_criteria>
    <criterion>File's first line is exactly `'use client';`</criterion>
    <criterion>File's lucide-react import contains only `Clock` and `User` — grep confirms zero occurrences of `Calendar`, `CheckCircle`, `X`, or `Phone` as import specifiers (avoids unused-import lint failure)</criterion>
    <criterion>File contains a `React.useRef` used to track the running scenario interval, and `clearInterval` is called both inside `runScenario` (guarding against a still-running previous interval) and inside a `useEffect` cleanup on unmount</criterion>
    <criterion>File contains `bg-brand` for the Telegram-style chat header and the user chat bubble, and `text-foreground`/`bg-card` for the bot chat bubble (no `#1d6be4` and no `dark:bg-gray-800`/`dark:text-gray-100` literal utilities in the chat area)</criterion>
    <criterion>File contains exactly 2 `TabsTrigger` elements with values `"bot"` and `"admin"`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(sed -n '1p' apps/web/app/demo/page.tsx)" = "'use client';" && \
IMPORT_LINE="$(grep \"from 'lucide-react'\" apps/web/app/demo/page.tsx)" && \
test "$(echo "$IMPORT_LINE" | grep -Ec 'Calendar|CheckCircle|Phone|\bX\b')" = "0" && \
test "$(grep -c 'useRef' apps/web/app/demo/page.tsx)" -ge "1" && \
test "$(grep -c 'clearInterval' apps/web/app/demo/page.tsx)" -ge "2" && \
test "$(grep -c 'bg-brand' apps/web/app/demo/page.tsx)" -ge "2" && \
test "$(grep -c '#1d6be4' apps/web/app/demo/page.tsx)" = "0" && \
test "$(grep -c 'value=\"bot\"' apps/web/app/demo/page.tsx)" = "1" && \
test "$(grep -c 'value=\"admin\"' apps/web/app/demo/page.tsx)" = "1" && \
echo PASS
    </automated>
  </verify>
  <done>apps/web/app/demo/page.tsx exists as a client component with the hero heading, the two-tab shell, and a fully working Bot tab (phone mockup, scenario-driven chat playback with leak-safe interval cleanup), ready for Task 3 to add the Admin tab.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add Admin tab, verify build</name>
  <files>apps/web/app/demo/page.tsx</files>
  <read_first>
    apps/web/app/demo/page.tsx (Task 2 output — insert the Admin `TabsContent` as a sibling immediately after the Bot `TabsContent`'s closing tag, before `</Tabs>`)
    apps/web/app/demo/_data.ts (dashboardStats/weeklyBookings/weekDays/appointments/doctors — all consumed here)
    packages/ui/src/components/shadcn-ui/table.tsx (Table/TableHeader/TableBody/TableRow/TableHead/TableCell API — used here instead of a raw HTML `<table>`, per this project's "all UI through @repo/ui" constraint; the design source used a raw `<table>` but @repo/ui already ships an equivalent primitive, so this is the intended substitution, not a new component)
    .planning/phases/02-home-contacts-demo/02-CONTEXT.md (lines ~373-380 — Admin tab structure; lines ~76-81 — token normalization rules)
  </read_first>
  <action>
    Edit `apps/web/app/demo/page.tsx`: add `Table, TableBody, TableCell, TableHead, TableHeader, TableRow` to the existing `'@repo/ui'` import line (alphabetized with the existing Badge/Button/Card.../Tabs... specifiers).
    Insert `<TabsContent value="admin">` as the sibling after the Bot tab's `</TabsContent>`. Outer wrapper `bg-card rounded-xl border border-border overflow-hidden` (D-03: was `bg-white dark:bg-gray-800 border dark:border-gray-700`). Demo banner `bg-brand text-white px-6 py-3 flex items-center justify-between` (D-02, was `bg-[#1d6be4]`): left side `flex items-center gap-2` with `<Badge variant="secondary" className="bg-white text-brand">Демо режим</Badge>` (D-02, was `text-[#1d6be4]`) and `<span>Клініка "Посмішка"</span>`; right side `<Button variant="secondary" size="sm" asChild>` wrapping `<Link href={routes.contacts}>Підключити свою клініку →</Link>`.
    Below the banner, `flex`: sidebar `w-64 border-r border-border p-4 space-y-2` (D-03, was `dark:border-gray-700`) with 5 buttons, each `variant={selectedSection === '<key>' ? 'default' : 'ghost'} className="w-full justify-start" onClick={() => setSelectedSection('<key>')}`, in order: `'dashboard'` "📊 Dashboard", `'appointments'` "📅 Записи", `'doctors'` "👨‍⚕️ Лікарі", `'patients'` "🧑‍🤝‍🧑 Пацієнти", `'settings'` "⚙️ Налаштування".
    Content panel `flex-1 p-6`, conditionally rendering per `selectedSection`: `'dashboard'` → `space-y-6` with `<h2 className="text-2xl font-bold">Dashboard</h2>`, a `grid md:grid-cols-4 gap-4` of 4 `Card`s built from `.map` over `dashboardStats` (`CardHeader className="pb-2"` > `CardDescription` = stat.label; `CardContent` > `<div className="text-3xl font-bold">{stat.value}</div>` — for the 4th "Завантаженість" stat only, add `text-green-500` to that value div, matching the source's `text-3xl font-bold text-green-500` on the loading-percentage card only), then a `Card` with `CardHeader > CardTitle` "Записи по днях" and `CardContent > <div className="h-48 flex items-end gap-4">` mapping over `weeklyBookings` (paired with `weekDays[i]`) rendering a `flex-1 flex flex-col items-center gap-2` with a `w-full bg-brand rounded-t` div whose inline `style={{ height: \`${(value / 50) * 100}%\` }}` is computed exactly as the source (D-02, bar color was `bg-[#1d6be4]`) plus a `text-xs text-muted-foreground` day label (D-03, was `text-gray-600 dark:text-gray-400`).
    `'appointments'` → `space-y-6` with a `flex items-center justify-between` header (`h2` "Записи" + `<Button>Новий запис</Button>`, non-functional decorative button per source), then a `border border-border rounded-lg overflow-hidden` (D-03) wrapping a `@repo/ui` `Table`: `TableHeader className="bg-muted"` (D-03, was `bg-gray-50 dark:bg-gray-700`) with a `TableRow` of 5 `TableHead`s ("Пацієнт", "Лікар", "Дата", "Час", "Статус"); `TableBody` mapping `appointments` to a `TableRow key={apt.id}` of `TableCell`s for patient/doctor/date/time, and a final `TableCell` with `<Badge variant={apt.status === 'confirmed' ? 'default' : 'secondary'}>{apt.status === 'confirmed' ? 'Підтверджено' : 'Очікує'}</Badge>`.
    `'doctors'` → `space-y-6` with a `flex items-center justify-between` header (`h2` "Лікарі" + `<Button>Додати лікаря</Button>`, decorative), then a `grid md:grid-cols-2 gap-4` mapping `doctors` to a `Card key={index}`: `CardHeader` with `flex items-center gap-4` — a `w-16 h-16 rounded-full bg-muted flex items-center justify-center text-3xl` (D-03, was `bg-gray-200 dark:bg-gray-700`) holding `{doctor.avatar}`, plus `CardTitle className="text-lg"` = `doctor.name` and `CardDescription` = `doctor.specialty`; `CardContent className="space-y-2"` with two `flex items-center gap-2 text-sm` rows: `<User className="h-4 w-4 text-muted-foreground" />` + `{doctor.experience}` (D-03, was `text-gray-500`), and `<Clock className="h-4 w-4 text-muted-foreground" />` + `{doctor.schedule}`.
    `'patients'` → `space-y-6` with `h2` "Пацієнти" and a `Card > CardContent className="pt-6 text-center text-muted-foreground"` (D-03) containing "База даних пацієнтів з контактами та історією відвідувань". `'settings'` → same shape, `h2` "Налаштування", `CardContent` text "Налаштування клініки, робочих годин, повідомлень та інтеграцій".
    After the edit, run `pnpm --filter web build` to confirm the full page compiles, type-checks, and lints cleanly.
  </action>
  <acceptance_criteria>
    <criterion>File contains exactly 2 `TabsContent` elements (`value="bot"`, `value="admin"`)</criterion>
    <criterion>File contains a `Table` import from `'@repo/ui'` and no raw `<table>`/`<thead>`/`<tbody>` HTML elements anywhere</criterion>
    <criterion>File contains `routes.contacts` used exactly once (admin banner CTA)</criterion>
    <criterion>File contains all 5 sidebar section keys as string literals: `'dashboard'`, `'appointments'`, `'doctors'`, `'patients'`, `'settings'`</criterion>
    <criterion>File contains zero occurrences of `#1d6be4` and zero literal Tailwind gray utilities (`text-gray-`, `bg-gray-`, `dark:bg-gray-`, `dark:text-gray-`) — except the one deliberately-preserved `bg-gray-900` phone-bezel class from Task 2, which must remain exactly once</criterion>
    <criterion>`pnpm --filter web build` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c '<TabsContent' apps/web/app/demo/page.tsx)" = "2" && \
test "$(grep -c \"from '@repo/ui'\" apps/web/app/demo/page.tsx)" -ge "1" && \
test "$(grep -c '<table' apps/web/app/demo/page.tsx)" = "0" && \
test "$(grep -c 'routes.contacts' apps/web/app/demo/page.tsx)" = "1" && \
test "$(grep -c \"'dashboard'\" apps/web/app/demo/page.tsx)" -ge "2" && \
test "$(grep -c \"'patients'\" apps/web/app/demo/page.tsx)" -ge "2" && \
test "$(grep -c \"'settings'\" apps/web/app/demo/page.tsx)" -ge "2" && \
test "$(grep -c '#1d6be4' apps/web/app/demo/page.tsx)" = "0" && \
test "$(grep -Ec 'text-gray-|bg-gray-900|dark:bg-gray-|dark:text-gray-' apps/web/app/demo/page.tsx)" -ge "1" && \
pnpm --filter web build && \
echo PASS
    </automated>
  </verify>
  <done>apps/web/app/demo/page.tsx is complete: Bot tab (scripted chat playback, leak-safe interval) and Admin tab (banner, 5-section sidebar, dashboard/appointments/doctors/patients/settings panels using @repo/ui's Table for appointments) both render correctly, and pnpm --filter web build passes cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side timer state (`setInterval`) | No server round-trip; purely local scripted playback of hardcoded mock messages |
| Admin banner CTA → `/contacts` | Internal navigation only, no external target |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Denial of Service (client-side resource leak) | `apps/web/app/demo/page.tsx` `runScenario`'s `setInterval` | medium | mitigate | `scenarioIntervalRef` tracks the active interval; `runScenario` clears any prior interval before starting a new one, and a `useEffect` cleanup clears it on unmount — fixes the archive's latent unguarded-interval bug (Task 2, CONTEXT.md discretion) |
| T-02-04 | Spoofing (mock data mistaken for real clinic data) | Demo banner "Клініка \"Посмішка\"" / "Демо режим" | low | accept | The design already discloses the mock nature via the "DEMO MODE" badge, the "Демо режим" banner badge, and the hero copy "Всі дані фейкові. Реєстрація не потрібна." — no additional mitigation needed |
| T-02-SC | Tampering (supply chain) | npm installs | high | accept | No new package installs occur in this plan — every import (`lucide-react`, `next/link`, `@repo/ui`, `react`) is a pre-existing, already-locked dependency |
</threat_model>

<verification>
- `pnpm --filter web build` succeeds (Task 3 automated verify)
- Task-level grep-based acceptance criteria above confirm content fidelity, token normalization, and interval-cleanup safety
- <human-check>Run `pnpm dev:web`, visit `/demo`, and confirm: (1) each of the 3 scenario buttons replays its full scripted conversation with visible per-message delay; (2) clicking a second scenario button while the first is still animating does NOT produce interleaved/duplicated messages; (3) switching admin sidebar sections shows the correct distinct content each time, with the active section button visually highlighted; (4) the appointments table renders all 5 rows with correct Підтверджено/Очікує badges. This human-check satisfies workflow.human_verify_mode=end-of-phase.</human-check>
</verification>

<success_criteria>
User can view the Demo page at `/demo`, interact with the scripted chat-bot simulation, and switch between dashboard sections as in the design — client-side, local state, no real API (ROADMAP Phase 2 Success Criterion #2, DEMO-01/DEMO-02).
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `apps/web/app/demo/_data.ts` (new) — typed mock data module: `ChatMessage`, `initialChatMessages`, `Scenario`, `scenarios`, `Appointment`, `appointments`, `Doctor`, `doctors`, `dashboardStats`, `weeklyBookings`, `weekDays`
- `apps/web/app/demo/page.tsx` (new) — Demo route, default export `Demo`, Tabs-based Bot/Admin two-tab simulation

<output>
Create `.planning/phases/02-home-contacts-demo/02-02-SUMMARY.md` when done
</output>
