---
phase: 02-home-contacts-demo
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/modules/home/stagger-grid.tsx
  - apps/web/modules/demo/bot-tab.tsx
autonomous: true
gap_closure: true
requirements: [HOME-01, DEMO-01]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Problem, Features, and Testimonials card grids (rendered via StaggerGrid/StaggerItem) collapse to a static, always-opaque render — matching Reveal's already-established prefers-reduced-motion pattern — instead of animating translateY(20px)->0, when the user's OS-level reduced-motion preference is set"
    - "Retriggering a Demo chat scenario while a previous one is still mid-playback (specifically during the ~400ms typing-indicator-then-reveal window of a bot-message tick) never appends a stale message from the abandoned scenario onto the freshly-reset chat — no interleaved or duplicated messages"
  artifacts:
    - path: "apps/web/modules/home/stagger-grid.tsx"
      provides: "StaggerGrid/StaggerItem grid-entry stagger primitives, now branching to an opacity-only variant pair under prefers-reduced-motion (mirroring apps/web/shared/components/reveal.tsx)"
      exports: "StaggerGrid, StaggerItem"
    - path: "apps/web/modules/demo/bot-tab.tsx"
      provides: "Demo chat scenario playback with both the setInterval and the inner per-message setTimeout tracked and cleared on retrigger and on unmount"
      exports: "BotTab"
  key_links:
    - from: "apps/web/modules/home/stagger-grid.tsx StaggerGrid/StaggerItem"
      to: "apps/web/shared/components/reveal.tsx's useReducedMotion() branch"
      via: "identical opacity-only hidden/visible variant pair pattern"
      pattern: "prefersReducedMotion ? { hidden: { opacity: 1 }, visible: { opacity: 1 } } : revealVariants"
    - from: "apps/web/modules/demo/bot-tab.tsx runScenario's cleanup guard"
      to: "the inner 400ms setTimeout scheduled per bot-message tick"
      via: "messageTimeoutRef, cleared alongside intervalRef both on retrigger and on unmount"
      pattern: "messageTimeoutRef.current"
---

<objective>
Close the 2 code-confirmed gaps from `02-VERIFICATION.md` (status: gaps_found) without touching any already-merged 02-01/02-02/02-03 work:

1. `apps/web/modules/home/stagger-grid.tsx` never calls Motion's `useReducedMotion()`, so `StaggerGrid`/`StaggerItem` always animate regardless of the user's OS-level reduced-motion preference — contradicts 02-01-PLAN.md's must-have. Fix by mirroring `apps/web/shared/components/reveal.tsx`'s existing branching pattern.
2. `apps/web/modules/demo/bot-tab.tsx`'s `runScenario` only clears the `setInterval` on retrigger/unmount; the inner per-tick `setTimeout` (typing-indicator-then-reveal, ~400ms) is never tracked or cleared, so retriggering a scenario mid-playback can leak a stale message into the freshly-reset chat — contradicts 02-03-PLAN.md's must-have. Independently documented as WR-01 in `02-REVIEW.md` with a ready-to-apply fix. Fix by adding a second ref for the inner timeout id, cleared in both the retrigger guard and the unmount cleanup effect.

Purpose: Bring HOME-01 and DEMO-01's already-"SATISFIED (with 1 associated gap)" status in `02-VERIFICATION.md` to fully clean, so Phase 2 re-verification can pass without qualification.
Output: `apps/web/modules/home/stagger-grid.tsx` and `apps/web/modules/demo/bot-tab.tsx` — both edited in place, no new files, no new exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-home-contacts-demo/02-VERIFICATION.md
@.planning/phases/02-home-contacts-demo/02-REVIEW.md
@.planning/phases/02-home-contacts-demo/02-01-PLAN.md
@.planning/phases/02-home-contacts-demo/02-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Collapse Home's staggered grids to opacity-only under prefers-reduced-motion</name>
  <files>apps/web/modules/home/stagger-grid.tsx</files>
  <read_first>
    apps/web/modules/home/stagger-grid.tsx (current file — `StaggerGrid`/`StaggerItem`, both `motion.div`-based, imports `motion` from `motion/react` but not `useReducedMotion`)
    apps/web/shared/components/reveal.tsx (the sibling component with the correct reduced-motion pattern to mirror: imports `useReducedMotion` alongside `motion` from `motion/react`, calls `const prefersReducedMotion = useReducedMotion();`, and sets `variants={prefersReducedMotion ? { hidden: { opacity: 1 }, visible: { opacity: 1 } } : revealVariants}`)
    apps/web/shared/lib/motion.ts (`revealVariants` = `{ hidden: { opacity: 0, y: 20 }, visible: { opacity: 1, y: 0, transition: {...} } }`; `revealContainerVariants` = `{ hidden: {}, visible: { transition: { staggerChildren: 0.08 } } }` — the container itself carries no opacity, only the stagger timing, so its reduced-motion branch only needs to drop the `staggerChildren` transition, not set an opacity value)
  </read_first>
  <action>
    Edit `apps/web/modules/home/stagger-grid.tsx`: add `useReducedMotion` to the existing `import { motion } from 'motion/react';` line so it reads `import { motion, useReducedMotion } from 'motion/react';`.

    In `StaggerGrid`, immediately after the existing `const { ref, inView } = useInView<HTMLDivElement>(0.15);` line, add `const prefersReducedMotion = useReducedMotion();`. Change the `motion.div`'s `variants={revealContainerVariants}` prop to `variants={prefersReducedMotion ? { hidden: {}, visible: {} } : revealContainerVariants}` — when reduced motion is on, the container drops its `staggerChildren` transition entirely (no delay between children) since `StaggerItem` (not the container) owns the opacity value.

    In `StaggerItem`, add `const prefersReducedMotion = useReducedMotion();` as the function's first line — `StaggerItem` calls the hook independently rather than reading it via a prop or context, exactly as `Reveal` calls it standalone with no parent coordination. Change the `motion.div`'s `variants={revealVariants}` prop to `variants={prefersReducedMotion ? { hidden: { opacity: 1 }, visible: { opacity: 1 } } : revealVariants}` — this is the identical opacity-only hidden/visible pair `Reveal` uses (apps/web/shared/components/reveal.tsx lines 25-29), so the translateY(20px)->0 stagger fully collapses to a static, always-fully-opaque render with zero motion.

    Both `StaggerGrid` and `StaggerItem` are the sole shared primitives consumed by Problem's 4-card grid, Features' 8-card grid, and Testimonials' 3-card grid (all in `apps/web/modules/home/`) — no changes needed in those 3 consumer files since they only pass `children`/`className`, never touch `variants` directly.
  </action>
  <acceptance_criteria>
    <criterion>`apps/web/modules/home/stagger-grid.tsx` imports `useReducedMotion` from `motion/react` in the same import statement as `motion`</criterion>
    <criterion>`StaggerGrid` calls `useReducedMotion()` and its `motion.div`'s `variants` prop branches to `{ hidden: {}, visible: {} }` when true, `revealContainerVariants` when false</criterion>
    <criterion>`StaggerItem` calls `useReducedMotion()` independently (its own hook call, not a prop/context read) and its `motion.div`'s `variants` prop branches to `{ hidden: { opacity: 1 }, visible: { opacity: 1 } }` when true — identical to `Reveal`'s pattern — and `revealVariants` when false</criterion>
    <criterion>No new file created; `StaggerGrid`/`StaggerItem`'s exported names, parameter shapes, and existing consumers (`problem.tsx`, `features.tsx`, `testimonials.tsx`) are unchanged</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c "useReducedMotion" apps/web/modules/home/stagger-grid.tsx)" -ge "3" && \
test "$(grep -c "prefersReducedMotion" apps/web/modules/home/stagger-grid.tsx)" -ge "4" && \
test "$(grep -c "hidden: { opacity: 1 }" apps/web/modules/home/stagger-grid.tsx)" -ge "1" && \
test "$(grep -c "visible: { opacity: 1 }" apps/web/modules/home/stagger-grid.tsx)" -ge "1" && \
test "$(grep -c "export function StaggerGrid\|export function StaggerItem" apps/web/modules/home/stagger-grid.tsx)" -ge "2" && \
echo PASS
    </automated>
  </verify>
  <done>`StaggerGrid` and `StaggerItem` both call `useReducedMotion()` and branch to an opacity-only/no-stagger variant pair when true, mirroring `Reveal`'s established pattern — the Problem/Features/Testimonials grids now fully collapse to a static, non-animated render under `prefers-reduced-motion`, closing 02-VERIFICATION.md gap 1.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Track and clear bot-tab's inner message setTimeout to stop scenario-retrigger leaks</name>
  <files>apps/web/modules/demo/bot-tab.tsx</files>
  <read_first>
    apps/web/modules/demo/bot-tab.tsx (current file — `intervalRef` declared at the top of `BotTab`, `runScenario`'s existing cleanup guard clears only `intervalRef.current`, the inner `setTimeout(() => {...}, 400)` inside the bot-message tick branch is a bare unassigned call, and the unmount `React.useEffect` cleanup only clears `intervalRef.current`)
    .planning/phases/02-home-contacts-demo/02-REVIEW.md (WR-01 section — identical root cause, ready-to-apply fix snippet using a `messageTimeoutRef`)
  </read_first>
  <action>
    Edit `apps/web/modules/demo/bot-tab.tsx`: immediately after the existing `const intervalRef = React.useRef<ReturnType<typeof setInterval> | null>(null);` declaration, add a second ref: `const messageTimeoutRef = React.useRef<ReturnType<typeof setTimeout> | null>(null);`.

    In `runScenario`'s existing cleanup guard (the `if (intervalRef.current) { clearInterval(intervalRef.current); intervalRef.current = null; }` block at the top of the callback), add an equivalent guard for the new ref immediately after it: `if (messageTimeoutRef.current) { clearTimeout(messageTimeoutRef.current); messageTimeoutRef.current = null; }` — so retriggering a scenario cancels both the interval AND any in-flight typing-indicator timeout from the previous scenario before resetting `chatMessages`.

    At the inner `setTimeout(() => { setChatMessages(...); setIsTyping(false); }, 400)` call inside the interval tick's bot-message branch (currently a bare, unassigned call), assign its return value to the ref and self-clear it once it fires naturally: `messageTimeoutRef.current = setTimeout(() => { setChatMessages((prev) => [...prev, { ...nextMessage, time: nowTime() }]); setIsTyping(false); messageTimeoutRef.current = null; }, 400);`.

    In the unmount cleanup `React.useEffect`'s return function (the one that currently only clears `intervalRef.current`), add the same `messageTimeoutRef.current` clear-and-null guard alongside the existing `intervalRef.current` clear, so a component unmount during the 400ms window also cancels the pending message reveal.
  </action>
  <acceptance_criteria>
    <criterion>`bot-tab.tsx` declares `messageTimeoutRef` via `React.useRef<ReturnType<typeof setTimeout> | null>(null)` immediately after `intervalRef`'s declaration</criterion>
    <criterion>`runScenario`'s cleanup guard calls `clearTimeout(messageTimeoutRef.current)` and nulls it before resetting `chatMessages`/starting the new interval</criterion>
    <criterion>The inner 400ms `setTimeout` inside the bot-message tick branch is assigned to `messageTimeoutRef.current` and nulls the ref when it fires naturally</criterion>
    <criterion>The unmount cleanup effect also clears `messageTimeoutRef.current` alongside `intervalRef.current`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c "messageTimeoutRef" apps/web/modules/demo/bot-tab.tsx)" -ge "5" && \
test "$(grep -c "clearTimeout(messageTimeoutRef.current)" apps/web/modules/demo/bot-tab.tsx)" -ge "2" && \
test "$(grep -c "messageTimeoutRef.current = setTimeout" apps/web/modules/demo/bot-tab.tsx)" -ge "1" && \
test "$(grep -c "clearInterval(intervalRef.current)" apps/web/modules/demo/bot-tab.tsx)" -ge "2" && \
echo PASS
    </automated>
  </verify>
  <done>`runScenario`'s retrigger guard and the unmount cleanup effect both clear `messageTimeoutRef.current` alongside `intervalRef.current`, so a scenario retriggered mid-playback (or a component unmount mid-playback) can never let a stale message from the abandoned scenario reach `setChatMessages` — closing 02-VERIFICATION.md gap 2 / WR-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client-side chat scenario timer state (`apps/web/modules/demo/bot-tab.tsx`) | Rapid user-triggered scenario retriggering (or navigating away mid-playback) creates untracked async callbacks (`setTimeout`) that mutate component state after the state they were scheduled against has already changed or unmounted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-04-01 | Denial of Service (resource/state leak) | `apps/web/modules/demo/bot-tab.tsx` `runScenario`'s inner `setTimeout` | low | mitigate | Track the inner timeout id in `messageTimeoutRef`, clear it in both the retrigger guard and the unmount cleanup effect — prevents a stale callback from calling `setChatMessages`/`setIsTyping` on a reset or unmounted component (wasted renders / a React "set state on unmounted component" warning in dev, not exploitable cross-user since all state is local-only per DEMO-01's client-side-only design) |

Gap 1 (`apps/web/modules/home/stagger-grid.tsx`'s missing `prefers-reduced-motion` collapse) is a WCAG AA / vestibular-motion-sensitivity accessibility fix with no security trust-boundary implications — not included in the STRIDE register.

</threat_model>

<verification>
- Task 1 and Task 2's grep-based acceptance criteria confirm the exact source-level fixes described in 02-VERIFICATION.md's gaps and 02-REVIEW.md's WR-01
- `pnpm --filter web check-types` reports zero new errors in either modified file (the pre-existing, unrelated `csstype` conflict confined to `packages/ui/src/components/shadcn-ui/{button-group,calendar,sidebar}.tsx` is out of scope and may still fail)
- Manual spot-check (not automated this plan, recommended before phase re-verification): with OS-level "reduce motion" enabled, visit `/`, confirm Problem/Features/Testimonials cards appear instantly with no translateY animation; on `/demo`, click a scenario button, then click a different scenario button within ~400ms of a bot message tick, and confirm no stale message from the first scenario appears in the reset chat
</verification>

<success_criteria>
Both gaps from `02-VERIFICATION.md` (status: gaps_found, score 11/13) are closed at the source level: `StaggerGrid`/`StaggerItem` honor `prefers-reduced-motion` exactly like `Reveal`, and `bot-tab.tsx`'s scenario retrigger guard fully cancels all in-flight async work (interval + inner timeout) before resetting the chat — HOME-01 and DEMO-01 no longer carry an associated gap.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

None — this plan only edits 2 existing files in place (`apps/web/modules/home/stagger-grid.tsx`, `apps/web/modules/demo/bot-tab.tsx`); no new files, no new exported symbols. `StaggerGrid`/`StaggerItem`/`BotTab`'s existing exports and call signatures are unchanged.

<output>
Create `.planning/phases/02-home-contacts-demo/02-04-SUMMARY.md` when done
</output>
