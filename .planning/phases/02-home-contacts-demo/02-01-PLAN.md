---
phase: 02-home-contacts-demo
plan: 01
type: execute
wave: 2
depends_on: ["02-04"]
files_modified:
  - apps/web/app/page.tsx
  - apps/web/next.config.js
autonomous: true
requirements: [HOME-01]

must_haves:
  truths:
    - "User visiting / sees the Hero section's exact headline 'Автоматичний запис пацієнтів через Telegram. Без дзвінків.' with two CTA buttons: 'Спробувати демо' (variant=brand) linking to /demo and 'Дізнатись більше' linking to the same-page #features anchor"
    - "User sees 3 stat callouts (500+ клінік, 15 000+ записів/місяць, 98% задоволених) rendered using the text-brand token, not a raw hex value"
    - "User sees the Problem section's 4 cards (📞 Адміністратор на телефоні / 😔 Пацієнти забувають / 💬 Хаос в месенджерах / 📊 Немає статистики) with their exact Ukrainian titles and descriptions"
    - "User sees the Solution section's two numbered-step columns ('Для пацієнта' / 'Для клініки', 3 steps each, exact copy) plus an outline CTA button 'Спробувати як це працює' linking to /demo"
    - statement: "User sees the Features section's 8 cards rendered inside a section with id='features', reachable by clicking the hero's 'Дізнатись більше' anchor link"
      verification: backstop
    - "User sees the CTA Banner (bg-brand background) with both buttons ('Відкрити демо бот' and 'Переглянути адмін панель') linking to /demo"
    - "User sees the Testimonials section's 3 cards: one with a real next/image Unsplash photo (Олена Ковальчук), two with emoji-in-circle avatars (👨‍⚕️ Андрій Мельник, 👩‍⚕️ Марія Петренко), all showing 5 filled yellow stars"
    - statement: "apps/web/app/page.tsx remains a Server Component (no 'use client' directive) since the only interactivity is a native same-page hash anchor"
      verification: backstop
  artifacts:
    - path: "apps/web/app/page.tsx"
      provides: "Home route — full 6-section landing page (Hero, Problem, Solution, Features, CTA Banner, Testimonials)"
      exports: "default (Home)"
    - path: "apps/web/next.config.js"
      provides: "images.remotePatterns allowlist entry for images.unsplash.com, required for next/image to render the two hotlinked design images"
      contains: "images.unsplash.com"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "apps/web/shared/lib/routes.ts"
      via: "routes.demo imported and used as the Link href for every internal CTA on this page"
      pattern: "routes\\.demo"
    - from: "apps/web/app/page.tsx"
      to: "apps/web/next.config.js"
      via: "next/image Image components pointed at images.unsplash.com URLs, only renderable because of the next.config.js remotePatterns allowlist"
      pattern: "images\\.unsplash\\.com"
---

<objective>
Replace the default create-turbo starter content in `apps/web/app/page.tsx` with the full 6-section Home landing page (Hero, Problem, Solution, Features, CTA Banner, Testimonials), ported verbatim from the design archive's `home.tsx` into `@repo/ui` + Next.js App Router, with the Phase 1 brand-token and semantic-gray-token normalization rules applied throughout (D-02/D-03), and the two Unsplash-hotlinked images wired through `next/image` per D-08.

Purpose: this is the site's primary landing page and the first requirement (HOME-01) of Phase 2 — a real visitor's first impression of the product.
Output: `apps/web/app/page.tsx` renders all 6 sections with the original Ukrainian copy, and `pnpm --filter web build` succeeds with the Unsplash images resolving through the configured remote pattern.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-home-contacts-demo/02-CONTEXT.md
@.planning/phases/02-home-contacts-demo/02-PATTERNS.md
@.planning/phases/02-home-contacts-demo/02-04-SUMMARY.md
@.planning/phases/01-theme-site-shell/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Home page scaffold — Hero and Problem sections</name>
  <files>apps/web/app/page.tsx</files>
  <read_first>
    apps/web/app/page.tsx (current starter content — being fully replaced)
    .planning/phases/02-home-contacts-demo/02-CONTEXT.md (lines ~88-207 — Home page design source for Hero and Problem sections, verbatim JSX; lines ~18-21 — D-08 image decision; lines ~76-81 — brand/gray token normalization rules)
    .planning/phases/02-home-contacts-demo/02-PATTERNS.md (`apps/web/app/page.tsx` pattern-assignment section — analog file, imports pattern, route-file shape, link-to-internal-route pattern, brand-token normalization examples, image pattern)
    apps/web/app/not-found.tsx (analog for import grouping, `React.JSX.Element` explicit return type, `routes` usage, `Button asChild` + `Link` pattern)
    apps/web/shared/lib/routes.ts (confirms `routes.demo` is the only route needed this task — relocated by plan 02-04, which this plan depends on)
    packages/ui/src/components/shadcn-ui/badge.tsx, packages/ui/src/components/shadcn-ui/card.tsx (Badge/Card composition API — no changes needed to either component)
  </read_first>
  <action>
    Overwrite `apps/web/app/page.tsx` entirely. Default export function `Home(): React.JSX.Element` (explicit return type, Phase 1 tsc-workaround convention), no `'use client'` directive — this page has zero client-side hooks. Import order: third-party (`ArrowRight`, `Bell`, `Check` from `'lucide-react'`; `Image` from `'next/image'`; `Link` from `'next/link'`; `Badge`, `Button`, `Card`, `CardContent`, `CardDescription`, `CardHeader`, `CardTitle` from `'@repo/ui'`) then relative (`routes` from `'@/shared/lib/routes'`) — only import the icons actually used in this task's two sections (`ArrowRight`, `Bell`, `Check`); Task 2 and Task 3 will add their own icon imports to this same import block when they extend the file.
    Root element: `<div className="min-h-screen">`.
    Hero `<section>` — original classes `pt-24 lg:pt-32 pb-16 lg:pb-24 bg-gradient-to-b from-blue-50 to-white dark:from-gray-900 dark:to-gray-950`; per D-03 normalize only the literal-gray dark-mode stops to `dark:from-background dark:to-background` (keep `from-blue-50 to-white` as-is — Tailwind's blue-50 palette color is not a literal gray and matches the design's light-mode wash). Inside: `container mx-auto px-4 lg:px-8` wrapping a `grid lg:grid-cols-2 gap-12 items-center`.
    Left column (`space-y-6`): a `Badge variant="secondary" className="inline-flex items-center gap-2"` containing the two-span animated ping-dot (`relative flex h-2 w-2` wrapper; first span `animate-ping absolute inline-flex h-full w-full rounded-full bg-brand opacity-75` — brand token, not `#1d6be4`; second span `relative inline-flex rounded-full h-2 w-2 bg-brand`) followed by the text "Нова платформа для стоматологій". Then `<h1 className="text-4xl lg:text-6xl font-bold text-foreground leading-tight">` with exact text "Автоматичний запис пацієнтів через Telegram. Без дзвінків." (D-03: `text-gray-900 dark:text-white` → `text-foreground`). Then `<p className="text-lg lg:text-xl text-muted-foreground">` with exact text "Підключіть бота за 1 день — пацієнти записуються самі, отримують нагадування і не забувають про прийом." (D-03: `text-gray-600 dark:text-gray-400` → `text-muted-foreground`). Then a `flex flex-col sm:flex-row gap-4` with two buttons: `<Button size="lg" variant="brand" asChild>` wrapping `<Link href={routes.demo}>Спробувати демо <ArrowRight className="ml-2 h-5 w-5" /></Link>` (brand variant per Pattern Map's explicit "hero button → variant=brand" rule — this is a discretionary normalization, the source has no explicit variant here), and `<Button size="lg" variant="outline" asChild>` wrapping a plain `<a href="#features">Дізнатись більше</a>` (same-page hash anchor, NOT `next/link` — per CONTEXT.md discretion, `#features` is not a `routes` entry). Then the 3-stat `grid grid-cols-3 gap-4 pt-8`: each stat is a `space-y-1` div with a `text-2xl lg:text-3xl font-bold text-brand` value line and a `text-sm text-muted-foreground` label line — exact pairs: "500+"/"клінік", "15 000+"/"записів/місяць", "98%"/"задоволених".
    Right column (`relative`): a `relative rounded-2xl overflow-hidden shadow-2xl` div wrapping a `next/image` `Image` for the hero dashboard mockup — `src="https://images.unsplash.com/photo-1766171359875-73155eff7f66?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&ixid=M3w3Nzg4Nzd8MHwxfHNlYXJjaHwxfHxsYXB0b3AlMjBkYXNoYm9hcmQlMjBtb2NrdXAlMjBzY3JlZW58ZW58MXx8fHwxNzcyOTA1MjU0fDA&ixlib=rb-4.1.0&q=80&w=1080&utm_source=figma&utm_medium=referral"`, `alt="DentaBot Dashboard"`, `width={1080}`, `height={720}`, `className="h-auto w-full"`, `priority` (above-the-fold hero image). No `ImageWithFallback` wrapper — per CONTEXT.md discretion, drop it and rely on `next/image`'s own behavior. Below the image, two floating cards, both `absolute` positioned `bg-card rounded-lg shadow-lg p-4` (D-03: `bg-white dark:bg-gray-800` → `bg-card`): first `-top-4 -right-4 animate-bounce` containing a `flex items-center gap-2` with `<Check className="h-5 w-5 text-green-500" />` and `<span className="text-sm font-medium">Новий запис від Олени Коваль</span>`; second `-bottom-4 -left-4` containing `<Bell className="h-5 w-5 text-brand" />` (D-02 brand token, was `text-[#1d6be4]`) and `<span className="text-sm font-medium">Нагадування відправлено 24 пацієнтам</span>`.
    Problem `<section className="py-16 lg:py-24 bg-background">` (D-03: `bg-white dark:bg-gray-950` → `bg-background`). Inside `container mx-auto px-4 lg:px-8`: a centered header block (`text-center mb-12`) with `<h2 className="text-3xl lg:text-5xl font-bold text-foreground mb-4">Знайомо?</h2>` and `<p className="text-lg text-muted-foreground max-w-2xl mx-auto">Як зараз виглядає запис у більшості клінік</p>`. Then a `grid md:grid-cols-2 lg:grid-cols-4 gap-6` of exactly 4 `Card`s, each `CardHeader` containing a `w-12 h-12 rounded-full flex items-center justify-center mb-4` icon badge (keep these literal red/orange/yellow/purple `-100`/`dark:*-900/20` Tailwind tint pairs exactly as designed — they are not part of the D-03 gray-normalization rule, and no brand-token equivalent exists for them) holding an emoji `<span className="text-2xl">`, followed by a `CardTitle`; then `CardContent > CardDescription`. Exact 4 pairs, in order: (1) `bg-red-100 dark:bg-red-900/20`, "📞", "Адміністратор на телефоні", "Весь день відповідає на дзвінки, записує в блокнот або Excel"; (2) `bg-orange-100 dark:bg-orange-900/20`, "😔", "Пацієнти забувають", "Немає автоматичних нагадувань — люди просто не приходять"; (3) `bg-yellow-100 dark:bg-yellow-900/20`, "💬", "Хаос в месенджерах", "Записи через WhatsApp, Viber, Instagram — все розкидано"; (4) `bg-purple-100 dark:bg-purple-900/20`, "📊", "Немає статистики", "Неможливо проаналізувати завантаженість та оптимізувати роботу".
  </action>
  <acceptance_criteria>
    <criterion>`apps/web/app/page.tsx` first non-import line is `export default function Home(): React.JSX.Element {` — no `'use client'` directive anywhere in the file</criterion>
    <criterion>File contains exactly one `import { routes } from '@/shared/lib/routes';` and uses `routes.demo` at least once</criterion>
    <criterion>File contains `variant="brand"` on the hero's "Спробувати демо" button and `href="#features"` on a plain anchor (not `Link`) for "Дізнатись більше"</criterion>
    <criterion>File contains no raw hex literal `#1d6be4` anywhere (brand color only via `bg-brand`/`text-brand`)</criterion>
    <criterion>File contains no literal Tailwind gray utility (`text-gray-`, `bg-gray-`, `dark:bg-gray-`, `dark:text-gray-`) in the Hero or Problem sections</criterion>
    <criterion>File contains exactly 4 `<Card>` elements so far (Problem section) and the exact Ukrainian strings "Адміністратор на телефоні", "Пацієнти забувають", "Хаос в месенджерах", "Немає статистики"</criterion>
    <criterion>File contains a `next/image` `Image` import and one `<Image` usage with `src` pointing at `images.unsplash.com`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(sed -n '1p' apps/web/app/page.tsx | grep -c 'use client')" = "0" && \
test "$(grep -c \"from '@/shared/lib/routes'\" apps/web/app/page.tsx)" = "1" && \
test "$(grep -c 'routes.demo' apps/web/app/page.tsx)" -ge "1" && \
test "$(grep -c 'variant=\"brand\"' apps/web/app/page.tsx)" -ge "1" && \
test "$(grep -c 'href=\"#features\"' apps/web/app/page.tsx)" = "1" && \
test "$(grep -c '#1d6be4' apps/web/app/page.tsx)" = "0" && \
test "$(grep -Ec 'text-gray-|bg-gray-|dark:bg-gray-|dark:text-gray-' apps/web/app/page.tsx)" = "0" && \
test "$(grep -c '<Card>' apps/web/app/page.tsx)" = "4" && \
test "$(grep -c 'Адміністратор на телефоні' apps/web/app/page.tsx)" = "1" && \
test "$(grep -c 'images.unsplash.com' apps/web/app/page.tsx)" -ge "1" && \
echo PASS
    </automated>
  </verify>
  <done>apps/web/app/page.tsx exists as a Server Component with the Hero section (badge, headline, subhead, brand+outline CTA buttons, 3 brand-colored stats, hero image via next/image, 2 floating cards) and the Problem section (4-card grid), all tokens normalized per D-02/D-03, ready for Task 2 to append Solution and Features.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Solution and Features sections</name>
  <files>apps/web/app/page.tsx</files>
  <read_first>
    apps/web/app/page.tsx (Task 1 output — insert new sections after the closing `</section>` of Problem, before the file's final closing `</div>`)
    .planning/phases/02-home-contacts-demo/02-CONTEXT.md (lines ~209-216 — Solution and Features section descriptions; lines ~76-81 — token normalization rules)
    .planning/phases/02-home-contacts-demo/02-PATTERNS.md (Card grid pattern, brand-token normalization shared pattern)
  </read_first>
  <action>
    Edit `apps/web/app/page.tsx`: extend the top import block to also pull `Calendar`, `MessageSquare`, `Bell` (already imported in Task 1 — do not duplicate), `BarChart`, `Users`, `Settings`, `Star`, `Check` (already imported) from `'lucide-react'` — final combined lucide-react import list across the whole file must be exactly `ArrowRight, BarChart, Bell, Calendar, Check, MessageSquare, Settings, Star, Users` (alphabetized per the project's `@trivago/prettier-plugin-sort-imports` convention), each used at least once by the time this task and Task 3 are both done.
    Insert a Solution `<section className="py-16 lg:py-24 bg-muted">` (D-03: `bg-gray-50 dark:bg-gray-900` → `bg-muted`) immediately after Problem's closing `</section>`. Centered header: `<h2 className="text-3xl lg:text-5xl font-bold text-foreground mb-4">DentaBot бере це на себе</h2>`. Then `grid lg:grid-cols-2 gap-12` with two `<Card className="p-8">` columns. Column 1 heading `<h3 className="text-2xl font-bold mb-6 text-brand">Для пацієнта</h3>` (D-02 brand token) followed by a `space-y-6` of 3 numbered steps, each a `flex gap-4` with a `flex-shrink-0 w-10 h-10 rounded-full bg-brand text-white flex items-center justify-center font-bold` circle containing the step number (1/2/3) and a text block (`h4.font-semibold.mb-1` + `p.text-sm.text-muted-foreground`). Exact 3 pairs: (1) "Відкриває Telegram бота" / "Один клік — бот вітається і показує вільні часи"; (2) "Обирає час і лікаря" / "Зручний календар з реальними вільними слотами"; (3) "Отримує підтвердження" / "Автоматичне нагадування за день і годину до прийому". Column 2 heading `<h3 className="text-2xl font-bold mb-6 text-brand">Для клініки</h3>` with the same numbered-circle structure, exact 3 pairs: (1) "Запис з'являється в системі" / "Миттєво видно в адмін панелі та календарі лікаря"; (2) "Бот надсилає нагадування" / "Автоматично, без участі адміністратора"; (3) "Аналітика та звіти" / "Бачите статистику, завантаженість, можете оптимізувати роботу". Below both columns, a `text-center mt-8` with `<Button size="lg" variant="outline" asChild>` wrapping `<Link href={routes.demo}>Спробувати як це працює <ArrowRight className="ml-2 h-5 w-5" /></Link>`.
    Insert a Features `<section id="features" className="py-16 lg:py-24 bg-background">` immediately after Solution's closing `</section>` (the `id="features"` attribute is the hero anchor's scroll target — do not omit it). Centered header `<h2 className="text-3xl lg:text-5xl font-bold text-foreground mb-4">Все що потрібно для автоматизації запису</h2>`. Then `grid md:grid-cols-2 lg:grid-cols-4 gap-6` of exactly 8 `Card`s, each `CardHeader` with a `<{Icon} className="h-10 w-10 text-brand mb-4" />` (D-02 brand token, was `text-[#1d6be4]`) followed by `CardTitle`, then `CardContent > CardDescription`. Exact 8 triples (icon/title/description), in order: (1) `MessageSquare`, "Telegram бот", "Пацієнти записуються через звичний месенджер"; (2) `Calendar`, "Розклад лікарів", "Налаштування робочих годин та вихідних"; (3) `Bell`, "Автонагадування", "Автоматичні повідомлення перед прийомом"; (4) `BarChart`, "Аналітика", "Статистика записів та завантаженості"; (5) `Star`, "Відгуки", "Збір відгуків після відвідування"; (6) `Settings`, "Адмін панель", "Зручне управління всіма записами"; (7) `Users`, "Управління персоналом", "Додавання лікарів та налаштування прав"; (8) `Check`, "Скасування запису", "Пацієнти можуть перенести або скасувати".
  </action>
  <acceptance_criteria>
    <criterion>File's `id="features"` section wraps exactly 8 `<Card>` elements (grep count of `<Card>` across the whole file is now 12: 4 Problem + 8 Features)</criterion>
    <criterion>File contains the literal string `id="features"` exactly once</criterion>
    <criterion>File contains the exact Ukrainian strings "Для пацієнта", "Для клініки", "DentaBot бере це на себе", "Все що потрібно для автоматизації запису"</criterion>
    <criterion>File still contains zero occurrences of `#1d6be4` and zero literal Tailwind gray utilities</criterion>
    <criterion>File's lucide-react import line lists exactly: ArrowRight, BarChart, Bell, Calendar, Check, MessageSquare, Settings, Star, Users (no unused imports, avoids `--max-warnings 0` lint failure)</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c '<Card>' apps/web/app/page.tsx)" = "12" && \
test "$(grep -c 'id=\"features\"' apps/web/app/page.tsx)" = "1" && \
test "$(grep -c 'Для пацієнта' apps/web/app/page.tsx)" -ge "1" && \
test "$(grep -c 'Для клініки' apps/web/app/page.tsx)" -ge "1" && \
test "$(grep -c '#1d6be4' apps/web/app/page.tsx)" = "0" && \
test "$(grep -Ec 'text-gray-|bg-gray-|dark:bg-gray-|dark:text-gray-' apps/web/app/page.tsx)" = "0" && \
echo PASS
    </automated>
  </verify>
  <done>apps/web/app/page.tsx now contains Hero, Problem, Solution (2-column numbered steps), and Features (8-card grid, id=features) sections, all with verbatim Ukrainian copy and normalized brand/semantic tokens.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add CTA Banner and Testimonials sections, wire Unsplash images, verify build</name>
  <files>apps/web/app/page.tsx, apps/web/next.config.js</files>
  <read_first>
    apps/web/app/page.tsx (Task 2 output — append after Features' closing `</section>`, before the file's final closing `</div>`)
    apps/web/next.config.js (current 4-line config — being edited additively)
    .planning/phases/02-home-contacts-demo/02-CONTEXT.md (lines ~18-21 — D-08 image decision; lines ~217-229 — CTA Banner and Testimonials descriptions)
    .planning/phases/02-home-contacts-demo/02-PATTERNS.md ("Brand-blue Button/token normalization" shared pattern — exact CTA-banner outline-button override rule)
  </read_first>
  <action>
    Edit `apps/web/next.config.js`: add an `images` key to the exported `nextConfig` object alongside the existing `transpilePackages`, with `remotePatterns: [{ protocol: 'https', hostname: 'images.unsplash.com' }]` — do not use a wildcard hostname, pin exactly to `images.unsplash.com` (T-02-02 mitigation, no broader SSRF-style allowlist surface than the two images actually used).
    Edit `apps/web/app/page.tsx`: insert a CTA Banner `<section className="py-16 lg:py-24 bg-brand text-white">` (D-02: was `bg-[#1d6be4] text-white`) immediately after Features' closing `</section>`. Inside `container mx-auto px-4 lg:px-8 text-center`: `<h2 className="text-3xl lg:text-5xl font-bold mb-4">Спробуйте як це працює прямо зараз</h2>`, `<p className="text-xl mb-8 opacity-90">Живий демо бот. Без реєстрації. Займає 2 хвилини.</p>`, then `flex flex-col sm:flex-row gap-4 justify-center` with two buttons: `<Button size="lg" variant="secondary" asChild>` wrapping `<Link href={routes.demo}>Відкрити демо бот</Link>` (variant stays `secondary`, unchanged — already contrasts correctly against the brand background), and `<Button size="lg" variant="outline" className="border-white text-white hover:bg-white hover:text-brand" asChild>` wrapping `<Link href={routes.demo}>Переглянути адмін панель</Link>` — per the Pattern Map's exact rule, only the `hover:text-[#1d6be4]` piece becomes `hover:text-brand`; `border-white text-white hover:bg-white` are intentionally literal (on-brand-background contrast, no semantic token applies) and must NOT be changed.
    Insert a Testimonials `<section className="py-16 lg:py-24 bg-background">` immediately after CTA Banner's closing `</section>`. Centered header `<h2 className="text-3xl lg:text-5xl font-bold text-foreground mb-4">Що кажуть клініки які вже користуються</h2>`. Then `grid md:grid-cols-3 gap-8` of exactly 3 `Card`s. Card 1 `CardHeader`: a `flex items-center gap-4 mb-4` with a `next/image` `Image` (`src="https://images.unsplash.com/photo-1631596577204-53ad0d6e6978?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&ixid=M3w3Nzg4Nzd8MHwxfHNlYXJjaHwxfHxkZW50aXN0JTIwcHJvZmVzc2lvbmFsJTIwcG9ydHJhaXR8ZW58MXx8fHwxNzcyODM1MzgyfDA&ixlib=rb-4.1.0&q=80&w=1080&utm_source=figma&utm_medium=referral"`, `alt="Доктор"`, `width={64}`, `height={64}`, `className="h-16 w-16 rounded-full object-cover"`) plus a name/clinic text block ("Олена Ковальчук" / "Клініка Посмішка", clinic line using `text-sm text-muted-foreground`), followed by a `flex gap-1 mb-2` of exactly 5 `<Star className="h-5 w-5 fill-yellow-400 text-yellow-400" />` icons; `CardContent > <p className="text-muted-foreground">` with the exact quote `"Після впровадження DentaBot кількість пропущених прийомів зменшилась на 70%. Пацієнти в захваті!"`. Card 2 (emoji avatar, per CONTEXT.md — do NOT source a real photo): `w-16 h-16 rounded-full bg-muted flex items-center justify-center` (D-03: `bg-gray-200 dark:bg-gray-700` → `bg-muted`) containing `<span className="text-2xl">👨‍⚕️</span>`, name/clinic "Андрій Мельник" / "Denta Plus", same 5-star row, quote `"Адміністратор тепер може зосередитись на інших задачах. Все працює автоматично!"`. Card 3 (emoji avatar): same `bg-muted` circle with `<span className="text-2xl">👩‍⚕️</span>`, name/clinic "Марія Петренко" / "Стоматологія Люкс", same 5-star row, quote `"Налаштування зайняло менше години. Дуже інтуїтивно і просто. Рекомендую!"`.
    After both edits, run `pnpm --filter web build` to confirm the full page compiles, type-checks, and lints cleanly, and that both Unsplash images resolve against the new `remotePatterns` entry with zero build errors.
  </action>
  <acceptance_criteria>
    <criterion>`apps/web/next.config.js` contains an `images` key with `remotePatterns` listing `hostname: 'images.unsplash.com'` (single-quoted or double-quoted, exact hostname, no wildcard)</criterion>
    <criterion>`apps/web/app/page.tsx` contains `bg-brand text-white` on the CTA Banner section and `hover:text-brand` on its outline button</criterion>
    <criterion>File contains exactly 2 `next/image` `<Image` usages total (hero + 1 testimonial photo) and exactly 15 `<Card>` elements total (4 Problem + 8 Features + 3 Testimonials)</criterion>
    <criterion>File contains the exact strings "Олена Ковальчук", "Андрій Мельник", "Марія Петренко" and exactly 15 `<Star` icon usages total (3 testimonials × 5 stars = 15, plus the 1 Features-section Star icon reference is a separate `Star` import usage, not a rendered star row — count only literal `<Star className="h-5 w-5 fill-yellow-400 text-yellow-400"` occurrences, which must equal 15)</criterion>
    <criterion>Zero occurrences of `#1d6be4` and zero literal Tailwind gray utilities anywhere in the final file</criterion>
    <criterion>`pnpm --filter web build` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c 'images.unsplash.com' apps/web/next.config.js)" -ge "1" && \
test "$(grep -c 'bg-brand text-white' apps/web/app/page.tsx)" = "1" && \
test "$(grep -c 'hover:text-brand' apps/web/app/page.tsx)" = "1" && \
test "$(grep -c '<Image' apps/web/app/page.tsx)" = "2" && \
test "$(grep -c '<Card>' apps/web/app/page.tsx)" = "15" && \
test "$(grep -c 'Олена Ковальчук' apps/web/app/page.tsx)" = "1" && \
test "$(grep -c 'Андрій Мельник' apps/web/app/page.tsx)" = "1" && \
test "$(grep -c 'Марія Петренко' apps/web/app/page.tsx)" = "1" && \
test "$(grep -c 'fill-yellow-400 text-yellow-400' apps/web/app/page.tsx)" = "15" && \
test "$(grep -c '#1d6be4' apps/web/app/page.tsx)" = "0" && \
test "$(grep -Ec 'text-gray-|bg-gray-|dark:bg-gray-|dark:text-gray-' apps/web/app/page.tsx)" = "0" && \
pnpm --filter web build && \
echo PASS
    </automated>
  </verify>
  <done>apps/web/app/page.tsx renders the complete 6-section Home page (Hero/Problem/Solution/Features/CTA Banner/Testimonials) with faithful Ukrainian copy, normalized brand/semantic tokens, both Unsplash images rendering via next/image against the configured remotePatterns allowlist, and pnpm --filter web build passes cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `next/image` optimizer → `images.unsplash.com` | Server-side image fetch/proxy to an external, third-party-controlled host, allowlisted via `next.config.js` `remotePatterns` |
| Browser → `#features` same-page anchor | Native browser hash navigation, no JS, no server round-trip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | External Unsplash-hosted hero/testimonial images | low | accept | Publicly-hosted marketing imagery with no user/security impact if the host swaps the underlying asset; cosmetic-only risk, matches D-08's explicit decision to keep these as external hotlinks this milestone |
| T-02-02 | Information Disclosure / SSRF-adjacent | `apps/web/next.config.js` `images.remotePatterns` | medium | mitigate | `remotePatterns` pinned to the exact `hostname: 'images.unsplash.com'` (no wildcard `*` subdomain or protocol), preventing the Next.js image optimizer from being usable as an open proxy to arbitrary hosts later |
| T-02-SC | Tampering (supply chain) | npm installs | high | accept | No new package installs occur in this plan — every import (`lucide-react`, `next/image`, `next/link`, `@repo/ui`) is a pre-existing, already-locked dependency |
</threat_model>

<verification>
- `pnpm --filter web build` succeeds (Task 3 automated verify — proves the full page compiles, type-checks, lints at `--max-warnings 0`, and both Unsplash images resolve against the remotePatterns allowlist)
- Task-level grep-based acceptance criteria above confirm content fidelity, token normalization, and card/image counts
- <human-check>Run `pnpm dev:web`, visit `/`, and confirm: (1) the hero image and both floating cards render correctly in both light and dark theme; (2) clicking "Дізнатись більше" scrolls to the Features section; (3) both testimonial photo and emoji avatars render; (4) all brand-blue accents (badge dot, stats, step circles, feature icons, CTA banner) use a consistent blue matching the header/footer from Phase 1. This human-check satisfies workflow.human_verify_mode=end-of-phase.</human-check>
</verification>

<success_criteria>
User can view the Home page at `/` with all sections from the design (hero, features, etc.) ported with `@repo/ui` components and the original Ukrainian copy (ROADMAP Phase 2 Success Criterion #1, HOME-01).
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `apps/web/app/page.tsx` (rewritten) — Home route, default export `Home`, 6 sections: Hero, Problem, Solution, Features (`id="features"`), CTA Banner, Testimonials
- `apps/web/next.config.js` (edited) — `images.remotePatterns` allowlist entry for `images.unsplash.com`

<output>
Create `.planning/phases/02-home-contacts-demo/02-01-SUMMARY.md` when done
</output>
