---
phase: 02-home-contacts-demo
plan: 03
type: execute
wave: 2
depends_on: ["02-04"]
files_modified:
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/app/contacts/page.tsx
autonomous: false
requirements: [CONT-01, CONT-02, CONT-03]

must_haves:
  truths:
    - "User visiting /contacts sees the lead form (Ім'я/Назва клініки/Телефон або email/Повідомлення) alongside the 3 contact-method cards (Telegram/Email/Робочі години) and 3 benefit callout cards"
    - "Submitting the form with an empty name shows an inline Ukrainian error under the name field and does not proceed to the confirmation view"
    - statement: "Submitting the form with a contact value matching neither a phone pattern nor an email pattern shows the specific inline error 'Введіть коректний телефон або email' and does not proceed"
      verification: backstop
    - "Submitting the form with a valid name and a valid phone-or-email contact value replaces the form with the 'Дякуємо!' confirmation card after a short simulated delay, and a sonner success toast reading 'Заявку успішно надіслано!' appears"
    - "Clicking the confirmation card's 'Надіслати ще одну заявку' button resets the view back to the empty form"
    - "User can expand and collapse all 8 FAQ accordion items, each showing the exact verbatim Ukrainian question and answer from the design source"
  artifacts:
    - path: "apps/web/app/contacts/page.tsx"
      provides: "Contacts route — react-hook-form + zod validated lead form, mocked submit/confirmation, contact-method + benefit cards, 8-item FAQ accordion"
      exports: "default (Contacts)"
    - path: "apps/web/package.json"
      provides: "sonner (pinned to the already-locked @repo/ui version), react-hook-form, zod, @hookform/resolvers declared as direct apps/web dependencies"
      contains: "react-hook-form"
  key_links:
    - from: "apps/web/app/contacts/page.tsx"
      to: "zodResolver(contactSchema) via @hookform/resolvers/zod"
      via: "useForm({ resolver: zodResolver(contactSchema) }) wiring react-hook-form's validation lifecycle to the zod schema"
      pattern: "zodResolver\\(contactSchema\\)"
    - from: "apps/web/app/contacts/page.tsx"
      to: "sonner's toast"
      via: "toast.success(...) called from the mocked onSubmit handler, rendered by the Toaster already mounted in apps/web/app/layout.tsx (Phase 1)"
      pattern: "toast\\.success"
---

<objective>
Build the `/contacts` route: a `react-hook-form` + `zod` validated lead form (CONT-01), a mocked success confirmation (CONT-02), and the FAQ accordion (CONT-03), ported verbatim from the design archive's `contacts.tsx` — replacing the archive's zero-validation raw `useState` form with real client-side format validation per D-09, while keeping the mocked `setTimeout` submission and `sonner` toast exactly as designed (no real backend call this milestone).

Purpose: CONT-01/02/03 — the site's primary lead-capture surface.
Output: a working `pnpm dev:web` `/contacts` page where invalid input shows inline Ukrainian errors, valid submission shows the mocked confirmation + toast, and all 8 FAQ items are browsable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-home-contacts-demo/02-CONTEXT.md
@.planning/phases/02-home-contacts-demo/02-PATTERNS.md
@.planning/phases/02-home-contacts-demo/02-04-SUMMARY.md
@.planning/phases/01-theme-site-shell/01-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package Legitimacy Gate — react-hook-form, zod, @hookform/resolvers</name>
  <files>(none — this checkpoint installs nothing; it only gates Task 2's install)</files>
  <read_first>
    .planning/STATE.md (Blockers/Concerns section — notes this exact deferral from Phase 1)
    .planning/phases/01-theme-site-shell/01-01-SUMMARY.md (key-decisions: "shadcn form primitive NOT added this plan — react-hook-form is not yet an installed dependency; adding it requires the Package Legitimacy Gate, deferred to Phase 2 (CONT-01)")
  </read_first>
  <what-built>
    Nothing yet — this checkpoint runs BEFORE any package install in Task 2. This phase's RESEARCH.md was intentionally skipped (design-port phase, not novel research), so no `## Package Legitimacy Audit` table exists. Per the Package Legitimacy Gate fallback policy, `react-hook-form`, `zod`, and `@hookform/resolvers` are treated as `[ASSUMED]` and require explicit human verification before install — they are genuinely new to this monorepo (confirmed via `pnpm-lock.yaml` grep: zero existing entries for any of the three). `sonner` is NOT part of this gate — it is already a pinned, locked dependency of `@repo/ui` (version `^2.0.7` in `packages/ui/package.json`) and only needs declaring directly on `apps/web`, matching the exact precedent set by Phase 1's `next-themes`/`lucide-react` fix (01-02-SUMMARY.md).
  </what-built>
  <action>
    Present the 3 npmjs.com package listings below to the user and pause for an explicit approval before Task 2 runs any install command. Do not run `pnpm add` or modify any file in this task — this task is verification-only.
  </action>
  <how-to-verify>
    Check each package's npm registry listing before approving Task 2's install:
    1. https://www.npmjs.com/package/react-hook-form — confirm recent/active publish history, GitHub repo `react-hook-form/react-hook-form`, weekly downloads in the millions.
    2. https://www.npmjs.com/package/zod — confirm recent/active publish history, GitHub repo `colinhacks/zod`, weekly downloads in the tens of millions.
    3. https://www.npmjs.com/package/@hookform/resolvers — confirm recent/active publish history, GitHub repo `react-hook-form/resolvers` (the official react-hook-form-org adapter package, not a similarly-named typosquat).
  </how-to-verify>
  <acceptance_criteria>
    <criterion>User has responded with an explicit approval (or has identified a concern that halts the plan) for all 3 packages</criterion>
  </acceptance_criteria>
  <verify>
    <automated>MISSING — this is a human-approval checkpoint, not an automatable check; the resume-signal below is the verification gate</automated>
  </verify>
  <done>User has typed "approved" (or equivalent affirmative), confirming all 3 packages' npmjs.com listings were reviewed and are legitimate.</done>
  <resume-signal>Type "approved" once all 3 packages are confirmed legitimate on npmjs.com, or describe concerns to halt the install.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install form dependencies, build the validated lead form</name>
  <files>apps/web/package.json, pnpm-lock.yaml, apps/web/app/contacts/page.tsx</files>
  <read_first>
    apps/web/package.json (current dependency block — being extended)
    .planning/phases/02-home-contacts-demo/02-CONTEXT.md (lines ~23-32 — D-09 full validation spec: field rules, error message wording, mocked-submit behavior, confirmation-card + reset behavior)
    .planning/phases/02-home-contacts-demo/02-PATTERNS.md (Contacts page pattern-assignment section — the full react-hook-form + zod + shadcn scaffold example, field-error-display convention using `text-destructive`)
    packages/ui/src/components/shadcn-ui/input.tsx, packages/ui/src/components/shadcn-ui/textarea.tsx, packages/ui/src/components/shadcn-ui/label.tsx (confirm these are plain native-element passthrough components — `react-hook-form`'s `register()` spread, including its `ref`, attaches correctly with no `Controller`/`forwardRef` wrapper needed)
    apps/web/app/layout.tsx (confirms `<Toaster />` is already mounted site-wide from Phase 1 — no new provider needed for `sonner`'s `toast.success` to render)
  </read_first>
  <action>
    Approval from Task 1 is required before this task runs. Run `pnpm --filter web add sonner@^2.0.7 react-hook-form zod @hookform/resolvers` — this both declares `sonner` directly on `apps/web` (pinned to the exact version `@repo/ui` already locks, matching Phase 1's Rule-3 precedent for a transitive-but-already-vetted dependency) and installs the 3 newly-approved packages at their latest resolved versions. If `pnpm install` resolves a `zod` major version that `@hookform/resolvers`' `zodResolver` does not yet support (a `tsc` type error on `zodResolver(contactSchema)` surfaces this), re-run `pnpm --filter web add zod@^3` to pin to the latest 3.x line, which `@hookform/resolvers` is guaranteed to support.
    Create `apps/web/app/contacts/page.tsx`, first line `'use client';`. Import order: third-party (`zodResolver` from `'@hookform/resolvers/zod'`; `CheckCircle`, `Clock`, `Mail`, `MessageSquare` from `'lucide-react'` — `CheckCircle` used in this task's confirmation view, `Clock`/`Mail`/`MessageSquare` reserved for Task 3's contact-method cards, import all 4 now so Task 3 only edits JSX, not imports; `useForm` from `'react-hook-form'`; `toast` from `'sonner'`; `z` from `'zod'`; `Accordion`, `AccordionContent`, `AccordionItem`, `AccordionTrigger`, `Button`, `Card`, `CardContent`, `CardDescription`, `CardHeader`, `CardTitle`, `Input`, `Label`, `Textarea` from `'@repo/ui'` — `Accordion*` reserved for Task 3; `* as React` from `'react'`).
    Define `const contactSchema = z.object({...})` with exactly these fields per D-09: `name: z.string().min(2, "Ім'я має містити щонайменше 2 символи")`; `clinic: z.string().optional()`; `contact: z.string().min(1, 'Вкажіть телефон або email').refine((val) => /^\+?[0-9\s()-]{7,}$/.test(val) || /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(val), 'Введіть коректний телефон або email')`; `message: z.string().optional()`. Then `type ContactFormValues = z.infer<typeof contactSchema>;`.
    Default export `function Contacts(): React.JSX.Element`. State: `const [isSubmitted, setIsSubmitted] = React.useState(false);` and `const form = useForm<ContactFormValues>({ resolver: zodResolver(contactSchema), defaultValues: { name: '', clinic: '', contact: '', message: '' } });`. `const onSubmit = (values: ContactFormValues) => { setTimeout(() => { setIsSubmitted(true); toast.success('Заявку успішно надіслано!'); }, 500); };` (D-09: keep the exact `setTimeout` delay and toast copy — `values` is intentionally unused beyond validation since there is no real backend call this milestone, prefix with `_` or reference it in a comment-free no-op if the linter flags unused-parameter; simplest is `// eslint-disable-next-line @typescript-eslint/no-unused-vars` is NOT preferred — instead just accept the parameter without using it, since react-hook-form always calls `handleSubmit(onSubmit)` with the validated values and most ESLint configs don't flag unused function parameters by default, only unused variables; if this repo's `--max-warnings 0` lint does flag it, prefix the parameter name with an underscore: `_values`).
    Render: root `<div className="min-h-screen pt-24 lg:pt-32 pb-16">`. Hero `<section className="pb-12 lg:pb-16">` wrapping `container mx-auto px-4 lg:px-8 text-center`: `<h1 className="text-4xl lg:text-6xl font-bold text-foreground mb-4">Зв'яжіться з нами</h1>` (D-03, was `text-gray-900 dark:text-white`), `<p className="text-lg lg:text-xl text-muted-foreground max-w-2xl mx-auto">Відповімо протягом 2 годин у робочий час. Або напишіть нам у Telegram — там швидше.</p>` (D-03).
    Then `<section className="pb-16 lg:pb-24">` wrapping `container mx-auto px-4 lg:px-8` wrapping `<div className="grid lg:grid-cols-2 gap-12 max-w-6xl mx-auto">` containing ONLY the form `Card` for now (Task 3 adds the sibling contact-info column inside this same grid div): `<Card>` with `CardHeader` (`CardTitle className="text-2xl"` "Залишити заявку", `CardDescription` "Заповніть форму і ми зв'яжемося з вами найближчим часом"), `CardContent` conditionally rendering: when `!isSubmitted`, `<form onSubmit={form.handleSubmit(onSubmit)} className="space-y-6">` with 4 `space-y-2` field blocks — (1) `<Label htmlFor="name">Ім'я *</Label>` + `<Input id="name" placeholder="Ваше ім'я" {...form.register('name')} />` + `{form.formState.errors.name && <p className="text-sm text-destructive">{form.formState.errors.name.message}</p>}`; (2) `<Label htmlFor="clinic">Назва клініки</Label>` + `<Input id="clinic" placeholder="Назва вашої клініки" {...form.register('clinic')} />`; (3) `<Label htmlFor="contact">Телефон або email *</Label>` + `<Input id="contact" placeholder="+380 XX XXX XX XX або email@example.com" {...form.register('contact')} />` + `{form.formState.errors.contact && <p className="text-sm text-destructive">{form.formState.errors.contact.message}</p>}`; (4) `<Label htmlFor="message">Повідомлення</Label>` + `<Textarea id="message" placeholder="Розкажіть про ваші потреби..." rows={4} {...form.register('message')} />`; then `<Button type="submit" size="lg" className="w-full">Надіслати заявку</Button>`. When `isSubmitted`, a `text-center py-12` block: `w-16 h-16 rounded-full bg-green-100 dark:bg-green-900/20 flex items-center justify-center mx-auto mb-4` wrapping `<CheckCircle className="h-8 w-8 text-green-500" />` (keep this literal green — matches the Home page's existing green success-color usage, no brand-token equivalent, not part of D-02/D-03's scope), `<h3 className="text-2xl font-bold mb-2">Дякуємо!</h3>`, `<p className="text-muted-foreground mb-6">Ваша заявка успішно надіслана. Ми зв'яжемося з вами найближчим часом.</p>` (D-03), `<Button variant="outline" onClick={() => { setIsSubmitted(false); form.reset(); }}>Надіслати ще одну заявку</Button>` (D-09: resets the view — `form.reset()` is react-hook-form's equivalent of the archive's manual `setFormData` reset).
  </action>
  <acceptance_criteria>
    <criterion>`apps/web/package.json` `dependencies` includes `sonner`, `react-hook-form`, `zod`, and `@hookform/resolvers`</criterion>
    <criterion>`apps/web/app/contacts/page.tsx` first line is exactly `'use client';`</criterion>
    <criterion>File contains `zodResolver(contactSchema)` and exactly 4 `z.object` field keys: `name`, `clinic`, `contact`, `message`</criterion>
    <criterion>File contains the exact error message strings "Ім'я має містити щонайменше 2 символи" and "Введіть коректний телефон або email"</criterion>
    <criterion>File contains `toast.success('Заявку успішно надіслано!')` and a `setTimeout` wrapping it</criterion>
    <criterion>File contains `form.reset()` inside the "Надіслати ще одну заявку" button's `onClick`</criterion>
    <criterion>File contains no raw hex literal `#1d6be4` and no literal Tailwind gray utility (`text-gray-`, `bg-gray-`, `dark:bg-gray-`, `dark:text-gray-`)</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c '\"react-hook-form\"' apps/web/package.json)" = "1" && \
test "$(grep -c '\"zod\"' apps/web/package.json)" = "1" && \
test "$(grep -c '\"@hookform/resolvers\"' apps/web/package.json)" = "1" && \
test "$(grep -c '\"sonner\"' apps/web/package.json)" = "1" && \
test "$(sed -n '1p' apps/web/app/contacts/page.tsx)" = "'use client';" && \
test "$(grep -c 'zodResolver(contactSchema)' apps/web/app/contacts/page.tsx)" = "1" && \
test "$(grep -c \"Ім'я має містити щонайменше 2 символи\" apps/web/app/contacts/page.tsx)" = "1" && \
test "$(grep -c 'Введіть коректний телефон або email' apps/web/app/contacts/page.tsx)" = "1" && \
test "$(grep -c \"toast.success('Заявку успішно надіслано!')\" apps/web/app/contacts/page.tsx)" = "1" && \
test "$(grep -c 'form.reset()' apps/web/app/contacts/page.tsx)" -ge "1" && \
test "$(grep -c '#1d6be4' apps/web/app/contacts/page.tsx)" = "0" && \
test "$(grep -Ec 'text-gray-|bg-gray-|dark:bg-gray-|dark:text-gray-' apps/web/app/contacts/page.tsx)" = "0" && \
echo PASS
    </automated>
  </verify>
  <done>apps/web/app/contacts/page.tsx exists with a fully validated (react-hook-form + zod) lead form showing inline Ukrainian errors, and a mocked setTimeout + sonner toast submission flow that swaps to a "Дякуємо!" confirmation card with a working reset button; sonner/react-hook-form/zod/@hookform/resolvers are declared as direct apps/web dependencies.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Contact-method cards, benefit cards, FAQ accordion, verify build</name>
  <files>apps/web/app/contacts/page.tsx</files>
  <read_first>
    apps/web/app/contacts/page.tsx (Task 2 output — insert the contact-info column as a sibling immediately after the form `</Card>`, inside the same `grid lg:grid-cols-2` div; insert the FAQ section as a new `<section>` immediately after that grid's enclosing `</section>`, before the file's final closing `</div>`)
    packages/ui/src/components/shadcn-ui/accordion.tsx (Accordion/AccordionItem/AccordionTrigger/AccordionContent API — full file, no changes needed)
    .planning/phases/02-home-contacts-demo/02-CONTEXT.md (lines ~443-452 — all 8 FAQ question/answer pairs, transcribed verbatim, port exactly; lines ~76-81, ~526 — token normalization and the "green/purple benefit cards stay literal, only the blue one normalizes" rule)
  </read_first>
  <action>
    Edit `apps/web/app/contacts/page.tsx`: insert `<div className="space-y-6">` as a sibling immediately after the form `Card`'s closing `</Card>` tag (still inside the `grid lg:grid-cols-2 gap-12 max-w-6xl mx-auto` div). Inside it, 3 contact-method `Card`s, each `CardHeader` with a `w-12 h-12 rounded-full bg-brand flex items-center justify-center mb-2` icon badge (D-02, was `bg-[#1d6be4]`) then `CardTitle` then `CardDescription`, and a `CardContent`: (1) `<MessageSquare className="h-6 w-6 text-white" />`, "Telegram", "Найшвидший спосіб зв'язку", `CardContent` = `<a href="https://t.me/dentabot_support" target="_blank" rel="noopener noreferrer" className="text-brand hover:underline">@dentabot_support</a>` (D-02; external link security — `target="_blank" rel="noopener noreferrer"` required, matching Phase 1's T-01-03 reverse-tabnabbing mitigation precedent); (2) `<Mail className="h-6 w-6 text-white" />`, "Email", "Для детальних запитів", `CardContent` = `<a href="mailto:hello@dentabot.ua" className="text-brand hover:underline">hello@dentabot.ua</a>` (no `target="_blank"` — `mailto:` is not a same-origin-bypassing new-tab external site); (3) `<Clock className="h-6 w-6 text-white" />`, "Робочі години", "Коли ми на зв'язку", `CardContent` = two `<p>` lines: "Понеділок - П'ятниця" and "9:00 - 18:00 (Київ)".
    Below the 3 cards, a `space-y-4 pt-4` wrapping 3 benefit-callout `Card`s (per CONTEXT.md: only the blue one's icon normalizes to the brand token — the green/purple cards' background/border tint classes stay as literal Tailwind utilities, matching their sibling): (1) `className="bg-green-50 dark:bg-green-900/10 border-green-200 dark:border-green-900/20"`, `CardContent className="pt-6"` with `flex items-start gap-3`: `<CheckCircle className="h-5 w-5 text-green-500 flex-shrink-0 mt-0.5" />` + a text block (`font-semibold mb-1` "Відповідаємо за 2 години", `text-sm text-muted-foreground` "У робочий час гарантуємо швидку відповідь"); (2) `className="bg-blue-50 dark:bg-blue-900/10 border-blue-200 dark:border-blue-900/20"` (literal blue-tint container, unchanged — only its icon gets the brand token), `CardContent className="pt-6"` with `<CheckCircle className="h-5 w-5 text-brand flex-shrink-0 mt-0.5" />` (D-02, was `text-[#1d6be4]`) + text block ("Безкоштовна демонстрація" / "Покажемо як працює система для вашої клініки"); (3) `className="bg-purple-50 dark:bg-purple-900/10 border-purple-200 dark:border-purple-900/20"`, `CardContent className="pt-6"` with `<CheckCircle className="h-5 w-5 text-purple-500 flex-shrink-0 mt-0.5" />` + text block ("Налаштування за 1 день" / "Допоможемо швидко запустити систему"). Close this `space-y-6` div, matching the grid's existing closing structure.
    Insert a new FAQ `<section className="py-16 lg:py-24 bg-muted">` (D-03, was `bg-gray-50 dark:bg-gray-900`) immediately after the form-and-info section's closing `</section>`, before the file's final `</div>`. Inside `container mx-auto px-4 lg:px-8`: centered header `text-center mb-12` with `<h2 className="text-3xl lg:text-5xl font-bold text-foreground mb-4">Часті питання</h2>` (D-03). Then `max-w-3xl mx-auto` wrapping `<Accordion type="single" collapsible className="space-y-4">`. Define a local `const faqs = [...]` array (inside the component function body, mirroring the archive's structure) with exactly these 8 `{ question, answer }` pairs, verbatim: (1) "Скільки коштує підключення?" / "Підключення безкоштовне. Ви платите тільки за обраний тарифний план. Перші 14 днів — безкоштовний пробний період."; (2) "Чи складно налаштувати систему?" / "Налаштування дуже просте і займає близько години. Ми надаємо відео інструкції та особисту підтримку на кожному етапі."; (3) "Чи потрібен технічний спеціаліст?" / "Ні, система розроблена для зручності звичайних користувачів. Якщо виникнуть питання — наша підтримка завжди на зв'язку."; (4) "Як швидко можна почати користуватись?" / "Після реєстрації ви можете налаштувати бота за 1 день та одразу почати приймати записи від пацієнтів."; (5) "Чи безпечна система?" / "Так, ми використовуємо сучасні методи шифрування даних та відповідаємо всім стандартам безпеки медичних даних."; (6) "Які гарантії якості?" / "Ми надаємо 14 днів безкоштовного тестування. Якщо система вам не підійде — ви можете скасувати в будь-який момент."; (7) "Чи можна інтегрувати з існуючою системою?" / "Так, ми маємо API та інтеграції з популярними CRM системами. Зв'яжіться з нами для деталей."; (8) "Які методи оплати доступні?" / "Ми приймаємо оплату картками Visa/Mastercard онлайн, а також банківський переказ для юридичних осіб." `.map` over `faqs` rendering `<AccordionItem key={index} value={\`item-${index}\`} className="border border-border rounded-lg px-6 bg-card">` (D-03, was `border rounded-lg px-6 bg-white dark:bg-gray-950 dark:border-gray-700`) with `<AccordionTrigger className="text-left font-semibold hover:no-underline">{faq.question}</AccordionTrigger>` and `<AccordionContent className="text-muted-foreground">{faq.answer}</AccordionContent>` (D-03).
    After the edit, run `pnpm --filter web build` to confirm the full page compiles, type-checks, and lints cleanly.
  </action>
  <acceptance_criteria>
    <criterion>File contains exactly 6 contact-info `<Card>` elements added this task (3 contact-method + 3 benefit), for a total of 7 `<Card>` elements in the whole file (1 form card + 6)</criterion>
    <criterion>File contains exactly 8 `<AccordionItem` elements</criterion>
    <criterion>File contains the exact strings "Скільки коштує підключення?" and "Які методи оплати доступні?" (first and last FAQ questions, confirming all 8 are present and in order)</criterion>
    <criterion>File contains exactly 2 `target="_blank"` + `rel="noopener noreferrer"` occurrences (only the Telegram link)</criterion>
    <criterion>File contains zero occurrences of `#1d6be4` and zero literal Tailwind gray utilities in the final file</criterion>
    <criterion>`pnpm --filter web build` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c '<Card>' apps/web/app/contacts/page.tsx)" = "7" && \
test "$(grep -c '<AccordionItem' apps/web/app/contacts/page.tsx)" = "8" && \
test "$(grep -c 'Скільки коштує підключення?' apps/web/app/contacts/page.tsx)" = "1" && \
test "$(grep -c 'Які методи оплати доступні?' apps/web/app/contacts/page.tsx)" = "1" && \
test "$(grep -c 'target=\"_blank\"' apps/web/app/contacts/page.tsx)" = "1" && \
test "$(grep -c 'rel=\"noopener noreferrer\"' apps/web/app/contacts/page.tsx)" = "1" && \
test "$(grep -c '#1d6be4' apps/web/app/contacts/page.tsx)" = "0" && \
test "$(grep -Ec 'text-gray-|bg-gray-|dark:bg-gray-|dark:text-gray-' apps/web/app/contacts/page.tsx)" = "0" && \
pnpm --filter web build && \
echo PASS
    </automated>
  </verify>
  <done>apps/web/app/contacts/page.tsx is complete: the lead form is flanked by 3 contact-method cards and 3 benefit cards, an 8-item FAQ accordion renders below with verbatim Ukrainian copy, and pnpm --filter web build passes cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form input (name/clinic/contact/message) → client-side zod validation | No server round-trip this phase — validation is entirely client-side, submission is a mocked `setTimeout`, nothing is persisted or transmitted |
| Browser → `t.me/dentabot_support` (external anchor) | User-initiated navigation to a third-party origin via `target="_blank"` |
| npm registry → `apps/web/package.json` | New, previously-uninstalled dependencies entering the dependency graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering (client-side validation bypass) | `apps/web/app/contacts/page.tsx` zod schema | low | accept | Validation is entirely client-side and there is no real backend endpoint receiving this data this phase (mocked `setTimeout`, no `fetch` call, no persistence) — there is no server-side trust boundary to violate yet. This disposition is explicitly scoped to change when v2's INTEG-01 (real backend) ships, at which point server-side re-validation becomes mandatory before this can remain "accept" |
| T-02-06 | Injection (XSS via rendered form input) | `apps/web/app/contacts/page.tsx` confirmation view | low | accept | Form values (name/clinic/contact/message) are captured via `react-hook-form`'s local state but never interpolated back into rendered JSX — the "Дякуємо!" confirmation view is 100% static copy with no `{values.name}`-style reflection, so no XSS surface exists in this phase's UI |
| T-01-03 (reused) | Tampering / Spoofing (reverse tabnabbing) | Telegram contact-method anchor (`t.me/dentabot_support`) | medium | mitigate | `target="_blank" rel="noopener noreferrer"` required (Task 3 acceptance criteria greps for exactly 1 occurrence each) — same mitigation pattern already established and verified in Phase 1's footer social links |
| T-02-SC | Tampering (supply chain) | `react-hook-form`, `zod`, `@hookform/resolvers` npm installs | high | mitigate | Task 1 blocking-human checkpoint verifies each package's npmjs.com listing (maintainer, GitHub repo, download counts) before Task 2 installs anything; `workflow.auto_advance` is ignored for this checkpoint per the Package Legitimacy Gate protocol. `sonner` is excluded from this gate — already a locked, already-vetted transitive dependency via `@repo/ui` (Phase 1 Rule-3 precedent) |
</threat_model>

<verification>
- `pnpm --filter web build` succeeds (Task 3 automated verify)
- Task-level grep-based acceptance criteria above confirm schema fields, error copy, toast wiring, card counts, FAQ content fidelity, and external-link security attributes
- <human-check>Run `pnpm dev:web`, visit `/contacts`, and confirm: (1) submitting with an empty name shows an inline error and does not submit; (2) typing a contact value like "abc" (matches neither phone nor email) shows the specific "Введіть коректний телефон або email" error; (3) submitting valid values (e.g. name "Тест", contact "+380501234567") shows the "Дякуємо!" confirmation card plus a toast notification after ~500ms; (4) clicking "Надіслати ще одну заявку" returns to the empty form; (5) all 8 FAQ items expand/collapse correctly. This human-check satisfies workflow.human_verify_mode=end-of-phase.</human-check>
</verification>

<success_criteria>
User can fill and submit the Contacts request form with react-hook-form + zod validation, seeing inline field errors for invalid input (CONT-01); user sees a mocked success confirmation after submitting a valid form (CONT-02); user can view and expand the FAQ accordion with all FAQ items from the design (CONT-03) — ROADMAP Phase 2 Success Criteria #3-5.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `apps/web/app/contacts/page.tsx` (new) — Contacts route, default export `Contacts`: validated lead form, mocked submit + confirmation, contact-method cards, benefit cards, 8-item FAQ accordion
- `apps/web/package.json` (edited) — adds `sonner`, `react-hook-form`, `zod`, `@hookform/resolvers` as direct dependencies
- `pnpm-lock.yaml` (regenerated) — new dependency resolutions for the above

<output>
Create `.planning/phases/02-home-contacts-demo/02-03-SUMMARY.md` when done
</output>
