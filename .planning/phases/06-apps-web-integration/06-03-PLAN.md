---
phase: 06-apps-web-integration
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - apps/server/src/pricing-plans/public-pricing-plans.controller.ts
  - apps/server/src/pricing-plans/pricing-plans.service.ts
  - apps/server/src/pricing-plans/pricing-plans.module.ts
  - apps/web/modules/prices/types.ts
  - apps/web/modules/prices/pricing-cards.tsx
  - apps/web/app/prices/page.tsx
  - apps/web/modules/prices/comparison-table.tsx
  - apps/web/app/prices/error.tsx
autonomous: true
requirements: [CMS-04]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "apps/web's Prices page fetches real, published-only pricing plans from GET /public/pricing-plans, replacing the hardcoded plans array"
    - "Zero published pricing plans renders a dedicated empty state ('Тарифи тимчасово недоступні' / 'Ми оновлюємо тарифні плани. Зв'яжіться з нами, щоб дізнатися актуальні умови.') with a 'Зв'язатися з нами' CTA linking to routes.contacts, and PricingCards/ComparisonTable are not rendered"
    - "Pricing card grid layout flexes to the actual published-plan count: 1 plan renders a single centered card (max-w-md mx-auto), 2 plans render lg:grid-cols-2, 3+ plans render lg:grid-cols-3"
    - "The comparison table renders only when 2 or more published plans exist; 0 or 1 plan omits it entirely (no comparative value)"
    - "comparison-table.tsx's matrix rows are derived from a union of all PricingPlan.features[] strings across the fetched plans, with a check/dash per plan based on whether that plan's features[] includes the row — no hand-curated rows, no schema change (D-05/D-06)"
    - { statement: "Comparison table rows have no truncation on long feature phrases — the existing overflow-x-auto wrapper handles narrow-viewport horizontal scroll", verification: backstop }
    - "The comparison table wrapper (overflow-x-auto around the <table>) handles narrow-viewport horizontal scroll, unchanged and reused as-is"
    - "Pricing card feature-list checkmark icon uses the dt-teal palette color, fixed from the off-palette green Tailwind default"
    - "pricing-cards.tsx reads plan.isPopular (the real Prisma field), not the mock data's plan.popular"
    - "Prices page fetch failures (5xx/network) render the app/prices/error.tsx boundary: heading 'Щось пішло не так', body 'Не вдалося завантажити дані. Спробуйте оновити сторінку.', retry button 'Оновити' calling reset()"
    - { statement: "Prices page read failures have no shared mutable state to corrupt under concurrent reads — each request is an independent fetch", verification: backstop }
    - "The existing protected GET /pricing-plans (admin, apps/platform-admin) still requires a Bearer token — unaffected by the new public route"
    - "GET /public/pricing-plans never returns unpublished (draft) plans, verified server-side (Prisma where clause), never trusted to apps/web to filter"
  artifacts:
    - path: "apps/server/src/pricing-plans/public-pricing-plans.controller.ts"
      provides: "@Public() class, GET /public/pricing-plans"
    - path: "apps/web/modules/prices/types.ts"
      provides: "PricingPlan type shared by pricing-cards.tsx and comparison-table.tsx"
  key_links:
    - from: "apps/web/app/prices/page.tsx"
      to: "apps/server/src/pricing-plans/public-pricing-plans.controller.ts"
      via: "server-side fetch() to {getServerApiUrl()}/public/pricing-plans"
      pattern: "fetch.*\\/public\\/pricing-plans"
    - from: "apps/server/src/pricing-plans/public-pricing-plans.controller.ts"
      to: "apps/server/src/pricing-plans/pricing-plans.service.ts"
      via: "findAllPublished() delegation"
      pattern: "pricingPlansService\\.findAllPublished"
    - from: "apps/web/app/prices/page.tsx"
      to: "apps/web/modules/prices/comparison-table.tsx"
      via: "plans prop, conditionally rendered when plans.length >= 2"
      pattern: "ComparisonTable plans="
---

<objective>
Add a published-only public read route for Pricing content on `apps/server`, and rewire `apps/web`'s Prices page to fetch real data through it — collapsing the `pricing-cards.tsx`/`comparison-table.tsx` hardcoded-data duplication into a single fetched `PricingPlan[]` source.

Purpose: CMS-04 requires the Prices page to render real pricing plans created via `apps/platform-admin` (Phase 5), with the comparison table's matrix derived from `features[]` rather than hand-duplicated per plan.
Output: `PublicPricingPlansController` (`GET /public/pricing-plans`, published-only, unauthenticated); `apps/web/app/prices/page.tsx`, `pricing-cards.tsx`, `comparison-table.tsx` all consuming fetched data.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-apps-web-integration/06-CONTEXT.md
@.planning/phases/06-apps-web-integration/06-RESEARCH.md
@.planning/phases/06-apps-web-integration/06-PATTERNS.md
@.planning/phases/06-apps-web-integration/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PublicPricingPlansController — published-only reads, least-privilege fields</name>
  <precondition>Plan 06-02's Task 1 (PublicBlogPostsController) is committed — proves the per-module PublicController + least-privilege-select pattern this task mirrors</precondition>
  <files>apps/server/src/pricing-plans/public-pricing-plans.controller.ts, apps/server/src/pricing-plans/pricing-plans.service.ts, apps/server/src/pricing-plans/pricing-plans.module.ts</files>
  <reversibility rating="costly">New public route shape (/public/pricing-plans) becomes a contract apps/web depends on; changing it later requires updating both the backend route and every apps/web fetch call site (D-01).</reversibility>
  <read_first>
    apps/server/src/pricing-plans/pricing-plans.controller.ts, apps/server/src/pricing-plans/pricing-plans.service.ts, apps/server/src/pricing-plans/pricing-plans.module.ts (current state — protected sibling controller, untouched)
    apps/server/src/blog-posts/public-blog-posts.controller.ts, apps/server/src/blog-posts/blog-posts.service.ts (Plan 06-02's output — the exact PublicController + select-clause pattern this task replicates)
    apps/server/src/pricing-plans/dto/pricing-plan-response.dto.ts (PricingPlanResponseDto — reused for @ApiOkResponse typing)
    packages/db/prisma/schema.prisma (PricingPlan model — isPopular, features String[], sortOrder)
  </read_first>
  <action>
    1. Add to `pricing-plans.service.ts`: `findAllPublished()` returns `this.prisma.pricingPlan.findMany({ where: { published: true }, orderBy: { sortOrder: 'asc' }, select: { id: true, name: true, monthlyPrice: true, yearlyPrice: true, description: true, features: true, isPopular: true, sortOrder: true, published: true } })` — reuses the existing `sortOrder: 'asc'` ordering; the explicit `select` excludes `updatedById`/`updatedBy`/`createdAt`/`updatedAt` (least-privilege, T-06-11).
    2. Create `apps/server/src/pricing-plans/public-pricing-plans.controller.ts`: class-level `@Public()`, `@ApiTags('public-pricing-plans')`, `@Controller('public/pricing-plans')`, constructor injects `PricingPlansService`. `@Get() @ApiOkResponse({ type: PricingPlanResponseDto, isArray: true }) findAllPublished() { return this.pricingPlansService.findAllPublished(); }`. No write handlers.
    3. Register `PublicPricingPlansController` in `pricing-plans.module.ts`'s `controllers` array alongside the existing `PricingPlansController`.
  </action>
  <acceptance_criteria>
    <criterion>GET /public/pricing-plans returns 200 and includes a published plan, never a draft plan</criterion>
    <criterion>GET /pricing-plans (existing admin route) still returns 401 without a Bearer token</criterion>
    <criterion>pricing-plans.service.ts's findAllPublished() uses an explicit select clause, not a bare findMany</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
docker compose up -d postgres
for i in $(seq 1 30); do docker compose exec -T postgres pg_isready -U postgres -d denta_bot_dev >/dev/null 2>&1 && break; sleep 1; done
cd packages/db && npx dotenv -e ../../.env -- npx prisma migrate deploy && cd ../..
pnpm --filter server run build
( pnpm --filter server run start > /tmp/denta-server-06-03.log 2>&1 & echo $! > /tmp/denta-server-06-03.pid )
for i in $(seq 1 30); do curl -s http://localhost:4000/api/docs-json >/dev/null 2>&1 && break; sleep 1; done
UNAUTH=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:4000/pricing-plans)
test "$UNAUTH" = "401"
LOGIN=$(curl -s -X POST http://localhost:4000/auth/login -H "Content-Type: application/json" -d '{"email":"platformadmin@dentabot.dev","password":"DevAdmin!2026"}')
ACCESS=$(echo "$LOGIN" | grep -o '"accessToken":"[^"]*"' | head -1 | cut -d'"' -f4)
curl -s -X POST http://localhost:4000/pricing-plans -H "Authorization: Bearer $ACCESS" -H "Content-Type: application/json" -d '{"name":"E2E Draft Plan 06-03","monthlyPrice":"1","yearlyPrice":"1","description":"d","features":["f1"],"published":false}' > /dev/null
curl -s -X POST http://localhost:4000/pricing-plans -H "Authorization: Bearer $ACCESS" -H "Content-Type: application/json" -d '{"name":"E2E Published Plan 06-03","monthlyPrice":"1","yearlyPrice":"1","description":"d","features":["f1"],"published":true}' > /dev/null
LIST=$(curl -s http://localhost:4000/public/pricing-plans)
echo "$LIST" | grep -q "E2E Published Plan 06-03"
if echo "$LIST" | grep -q "E2E Draft Plan 06-03"; then echo "FAIL: draft plan leaked in public list"; exit 1; fi
kill "$(cat /tmp/denta-server-06-03.pid)" 2>/dev/null || true
test "$(grep -c 'select: {' apps/server/src/pricing-plans/pricing-plans.service.ts)" -ge "1"
echo PASS
    </automated>
  </verify>
  <done>GET /public/pricing-plans returns published-only content with least-privilege fields; the admin route is untouched — backend half of CMS-04 complete.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prices page fetch + PricingCards wiring — isPopular rename, color fix, flexible grid</name>
  <precondition>Task 1's GET /public/pricing-plans route is committed and returns published-only plans</precondition>
  <files>apps/web/modules/prices/types.ts, apps/web/modules/prices/pricing-cards.tsx, apps/web/app/prices/page.tsx</files>
  <read_first>
    apps/web/modules/prices/pricing-cards.tsx (current file — local hardcoded `plans` array, reads plan.popular, checkmark uses text-green-500)
    apps/web/app/prices/page.tsx (current file — synchronous, unconditionally renders `<PricingCards />` and `<ComparisonTable />` with no props)
    apps/web/modules/prices/comparison-table.tsx (current file — still has NO props this task; its own signature change is Task 3's job — do not touch this file in this task, leave `<ComparisonTable />`'s call site in page.tsx completely unchanged so the two files' signatures stay matched until Task 3 updates both together)
    apps/web/shared/lib/api-url.ts (Plan 06-01's output — getServerApiUrl())
    .planning/phases/06-apps-web-integration/06-UI-SPEC.md (Copywriting Contract — Prices empty-state exact copy; UI Considerations — Prices zero-one-many card grid rule; Color section — text-green-500 → text-dt-teal fix)
  </read_first>
  <action>
    1. Create `apps/web/modules/prices/types.ts` exporting `PricingPlan` = `{ id: string; name: string; monthlyPrice: string; yearlyPrice: string; description: string; features: string[]; isPopular: boolean }`.
    2. In `pricing-cards.tsx`: remove the local `const plans = [...]` array; import `PricingPlan` type from `./types`; change the function signature to `PricingCards({ plans }: { plans: PricingPlan[] }): React.JSX.Element`. Compute a grid className based on `plans.length`: 1 → `'mx-auto grid max-w-md gap-6'`, 2 → `'mx-auto grid max-w-4xl gap-6 lg:grid-cols-2'`, 3+ → `'mx-auto grid max-w-6xl gap-6 lg:grid-cols-3'` (replacing the hardcoded `lg:grid-cols-3` div className). Rename every `plan.popular` reference to `plan.isPopular` (the real Prisma field name — badge condition, `highlighted={plan.isPopular}`, `variant={plan.isPopular ? 'coral' : 'outline'}`). Change the checkmark icon's className from the off-palette green to `text-dt-teal` (matches comparison-table.tsx's `CheckCell`). Change `key={plan.name}` to `key={plan.id}` (real unique id now available).
    3. Rewrite `app/prices/page.tsx` as `export default async function Prices(): Promise<React.JSX.Element>`: import `getServerApiUrl` from `@/shared/lib/api-url` and `PricingPlan` type from `@/modules/prices/types`. `const res = await fetch(\`${getServerApiUrl()}/public/pricing-plans\`, { next: { revalidate: 60 } }); if (!res.ok) throw new Error(\`Failed to fetch pricing plans: ${res.status}\`); const plans = (await res.json()) as PricingPlan[];`. If `plans.length === 0`, return a dedicated empty-state block (heading `Тарифи тимчасово недоступні`, body `Ми оновлюємо тарифні плани. Зв'яжіться з нами, щоб дізнатися актуальні умови.`, a `PremiumButton asChild` wrapping a `Link` to `routes.contacts` reading `Зв'язатися з нами`) instead of the normal layout. Otherwise render `<PricingCards plans={plans} />` (pass the new prop) followed by the existing unchanged `<ComparisonTable />` call (no props yet — Task 3 updates this line) and the unchanged FAQ section.
  </action>
  <acceptance_criteria>
    <criterion>pricing-cards.tsx no longer has a local hardcoded plans array — it receives plans via a prop</criterion>
    <criterion>pricing-cards.tsx reads plan.isPopular, not plan.popular</criterion>
    <criterion>pricing-cards.tsx's checkmark icon uses text-dt-teal</criterion>
    <criterion>app/prices/page.tsx is an async function fetching from getServerApiUrl() + '/public/pricing-plans'</criterion>
    <criterion>app/prices/page.tsx's zero-plans branch never renders PricingCards or ComparisonTable</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
pnpm --filter web exec tsc --noEmit > /tmp/denta-web-tsc-06-03a.log 2>&1; true
NEW_ERRORS=$(grep 'error TS' /tmp/denta-web-tsc-06-03a.log | grep -v 'shadcn-ui/button-group.tsx\|shadcn-ui/calendar.tsx\|shadcn-ui/sidebar.tsx' || true)
test -z "$NEW_ERRORS"
test "$(grep -c 'export type PricingPlan' apps/web/modules/prices/types.ts)" -ge "1"
test "$(grep -c 'isPopular' apps/web/modules/prices/pricing-cards.tsx)" -ge "1"
test "$(grep -c 'text-dt-teal' apps/web/modules/prices/pricing-cards.tsx)" -ge "1"
test "$(grep -c 'getServerApiUrl' apps/web/app/prices/page.tsx)" -ge "1"
echo PASS
    </automated>
  </verify>
  <done>Prices page fetches real published plans; PricingCards renders them with the correct grid layout, isPopular field, and palette-correct checkmark; zero-plans shows a dedicated empty state.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ComparisonTable derived-matrix rewrite (D-05/D-06) + conditional render + error boundary</name>
  <precondition>Task 2's PricingCards/page.tsx fetch wiring is committed</precondition>
  <files>apps/web/modules/prices/comparison-table.tsx, apps/web/app/prices/page.tsx, apps/web/app/prices/error.tsx</files>
  <read_first>
    apps/web/modules/prices/comparison-table.tsx (current file — CheckCell/DashCell helpers reused as-is; the fully hand-written `<table>` body this task replaces with a derived matrix)
    apps/web/app/prices/page.tsx (Task 2's output — the unconditional `<ComparisonTable />` line this task changes)
    apps/web/modules/prices/types.ts (Task 2's output — PricingPlan type this task imports)
    apps/web/app/blog/error.tsx (Plan 06-02's output — the exact error.tsx shape to replicate for Prices, same copy per UI-SPEC)
  </read_first>
  <action>
    1. Rewrite `comparison-table.tsx`: keep the `CheckCell`/`DashCell` helper functions unchanged. Import `PricingPlan` type from `./types`. Change the function signature to `ComparisonTable({ plans }: { plans: PricingPlan[] }): React.JSX.Element`. Compute `const featureRows = Array.from(new Set(plans.flatMap((plan) => plan.features)));` (D-05 — union of all plans' `features[]`, each distinct phrase becomes its own row per D-06, no numeric-limit parsing). Replace the hardcoded `<thead>` row with `{plans.map((plan) => (<th key={plan.id} className="py-4 text-center text-dt-navy">{plan.name}</th>))}` after the fixed `Функція` header cell. Replace the fully hand-written `<tbody>` rows with `{featureRows.map((row) => (<tr key={row} className="border-b border-dt-navy/10"><td className="py-4 text-dt-navy">{row}</td>{plans.map((plan) => (<td key={plan.id} className="py-4 text-center">{plan.features.includes(row) ? <CheckCell /> : <DashCell />}</td>))}</tr>))}`. The `<div className="overflow-x-auto">` wrapper stays unchanged.
    2. In `app/prices/page.tsx`, change the `<ComparisonTable />` line to `{plans.length >= 2 && <ComparisonTable plans={plans} />}` (UI-SPEC zero-one-many rule — 0 or 1 plan has no comparative value).
    3. Create `apps/web/app/prices/error.tsx` — identical shape/copy to Plan 06-02's `apps/web/app/blog/error.tsx` (same heading/body/`Оновити` button, same `'use client'` + `{ error, reset }` props pattern), scoped to the `/prices` route segment.
  </action>
  <acceptance_criteria>
    <criterion>comparison-table.tsx derives featureRows via Array.from(new Set(...flatMap...)) — no hardcoded Ukrainian row labels remain</criterion>
    <criterion>app/prices/page.tsx only renders ComparisonTable when plans.length >= 2</criterion>
    <criterion>apps/web/app/prices/error.tsx exists with the same heading/body/retry-button shape as blog/error.tsx</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
pnpm --filter web exec tsc --noEmit > /tmp/denta-web-tsc-06-03b.log 2>&1; true
NEW_ERRORS=$(grep 'error TS' /tmp/denta-web-tsc-06-03b.log | grep -v 'shadcn-ui/button-group.tsx\|shadcn-ui/calendar.tsx\|shadcn-ui/sidebar.tsx' || true)
test -z "$NEW_ERRORS"
test "$(grep -c 'flatMap' apps/web/modules/prices/comparison-table.tsx)" -ge "1"
test "$(grep -c 'plans.length >= 2' apps/web/app/prices/page.tsx)" -ge "1"
test -f apps/web/app/prices/error.tsx
echo PASS
    </automated>
  </verify>
  <done>Comparison table derives its full matrix from fetched PricingPlan.features[]; renders only for 2+ plans; error boundary matches Blog's pattern — CMS-04 fully satisfied.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

New backend symbols: `PublicPricingPlansController` (apps/server/src/pricing-plans/public-pricing-plans.controller.ts, `@Public()` class-level), `PricingPlansService.findAllPublished()`.

New frontend symbols: `PricingPlan` type (apps/web/modules/prices/types.ts), `PricesError` (apps/web/app/prices/error.tsx default export).

New files: `apps/server/src/pricing-plans/public-pricing-plans.controller.ts`, `apps/web/modules/prices/types.ts`, `apps/web/app/prices/error.tsx`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → apps/server GET /public/pricing-plans | Unauthenticated read access to admin-authored pricing content; must never leak drafts |
| apps/web Server Component → apps/server | Server-side fetch() at request time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Information Disclosure | PublicPricingPlansController | high | mitigate | `published: true` filter enforced server-side in `PricingPlansService.findAllPublished()`'s `where` clause — never relies on apps/web to filter client-side |
| T-06-11 | Information Disclosure | PublicPricingPlansController response fields | low | mitigate | Explicit Prisma `select` excludes `updatedById`/`updatedBy`/`createdAt`/`updatedAt` — least-privilege field selection |
| T-06-12 | Elevation of Privilege | PublicPricingPlansController route surface | high | mitigate | The controller declares only a single `@Get()` handler — no write methods exist on this class; the existing protected `PricingPlansController` (create/update/delete) is untouched |
| T-06-13 | Denial of Service | GET /public/pricing-plans (unrate-limited) | low | accept | Read-only public content endpoint, same risk profile as any public marketing page; rate limiting intentionally scoped to the one write endpoint only (D-03) |

No new prohibitions minted for this plan — considered and dropped: pricing staleness from aggressive caching (routine engineering, `revalidate: 60` already bounds it); comparison-table feature-derivation being "gamed" by an admin (low signal, not actionable).
</threat_model>

<verification>
Task 1 proves published-only filtering live via a real draft-vs-published round trip. Tasks 2-3 are frontend-only, verified via filtered TypeScript compilation (same known pre-existing csstype exclusion as Plans 06-01/06-02) plus structural grep assertions.
</verification>

<success_criteria>
- `GET /public/pricing-plans` exists, is unauthenticated, and never returns unpublished content.
- `/prices` renders real fetched plans through both PricingCards and the derived ComparisonTable; the old hardcoded arrays in both files are gone.
- Empty/zero-one-many/error states match UI-SPEC exactly.
</success_criteria>

<output>
Create `.planning/phases/06-apps-web-integration/06-03-SUMMARY.md` when done
</output>
