---
phase: 01.1-premium-design-system
plan: 04
type: execute
wave: 3
depends_on: ["01.1-02", "01.1-03"]
files_modified:
  - apps/web/shared/components/logo.tsx
  - apps/web/shared/components/theme-toggle.tsx
  - apps/web/shared/lib/routes.ts
  - apps/web/shared/components/header.tsx
  - apps/web/shared/components/footer.tsx
  - apps/web/app/layout.tsx
  - apps/web/app/not-found.tsx
  - apps/web/components/header.tsx
  - apps/web/components/footer.tsx
  - apps/web/components/logo.tsx
  - apps/web/components/theme-toggle.tsx
  - apps/web/lib/routes.ts
  - apps/web/app/fonts/GeistVF.woff
  - apps/web/app/fonts/GeistMonoVF.woff
autonomous: true
requirements: [DESIGN-01, DESIGN-02, DESIGN-03]

must_haves:
  truths:
    - "The DentaBot logo no longer renders the dental-tooth emoji anywhere in apps/web (D-16)"
    - "apps/web/components/ and apps/web/lib/ no longer exist on disk; all shared shell code lives under apps/web/shared/ (component/lib move absorbed from Phase 2's stale 02-04 plan)"
    - "Every apps/web/shared/components/{header,footer,logo,theme-toggle}.tsx file uses only dt- tokens and Phosphor icons — zero remaining @repo/ui Button imports, zero remaining lucide-react imports, zero remaining #1d6be4/bg-brand/text-brand references"
    - "pnpm --filter web build, pnpm --filter web lint, and pnpm --filter web check-types all exit 0 after the rebuild"
    - "packages/ui/styles/theme.css has zero diff versus its state before this phase (git diff empty) — D-01/D-02's boundary held for the full phase"
  artifacts:
    - path: "apps/web/shared/components/header.tsx"
      provides: "fixed/sticky premium-tokened nav with PremiumButton CTAs, Phosphor icons, Motion-animated mobile menu"
      exports: "Header"
    - path: "apps/web/shared/components/footer.tsx"
      provides: "4-column premium-tokened footer, external-link security preserved"
      exports: "Footer"
    - path: "apps/web/shared/components/logo.tsx"
      provides: "wordmark + icon-badge logo, no tooth emoji"
      exports: "Logo"
    - path: "apps/web/shared/components/theme-toggle.tsx"
      provides: "next-themes toggle rebuilt on PremiumButton + Phosphor icons"
      exports: "ThemeToggle"
    - path: "apps/web/shared/lib/routes.ts"
      provides: "moved route constants, unchanged content"
      exports: "routes"
    - path: "apps/web/app/layout.tsx"
      provides: "root layout wired to interHeading/interBody fonts and the moved Header/Footer"
  key_links:
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/shared/components/header.tsx, apps/web/shared/components/footer.tsx"
      via: "import { Header } from '@/shared/components/header'; import { Footer } from '@/shared/components/footer';"
      pattern: "@/shared/components/header"
    - from: "apps/web/shared/components/header.tsx"
      to: "apps/web/shared/components/premium-button.tsx"
      via: "import { PremiumButton } from './premium-button';"
      pattern: "PremiumButton"
---

<objective>
Retrofit Phase 1's shipped shell (Logo, ThemeToggle, Header, Footer, root layout, Not Found page) onto the premium design system per D-04, and perform the `apps/web/components/` + `apps/web/lib/` → `apps/web/shared/components/` + `apps/web/shared/lib/` folder move this phase absorbs from Phase 2's now-stale, never-executed `02-04` plan (per the phase brief's explicit folder-move-absorption note). Every icon in the rebuilt shell (Logo's badge, Header's menu/close, ThemeToggle's sun/moon, NotFound's house) is a Phosphor icon at `weight="regular"` (D-15's 1.5px-stroke, one-consistent-style spec), never `lucide-react`.

Purpose: DESIGN-01 (every visible pixel of `apps/web`'s persistent chrome now runs on `dt-` tokens, zero `@repo/ui`/brand-blue leftovers), DESIGN-02 (the header's mobile menu becomes the phase's first live consumer of the `Reveal`/`motion.ts` utilities), DESIGN-03 (final WCAG-AA/Lighthouse `≥85` verification per D-46 against the fully assembled shell). This is the final, integration wave of the phase — everything Plans 01-03 built gets consumed here. On D-42 ("Request a demo" CTA always within easy reach on mobile): the header stays `fixed top-0` on every viewport, so the CTA is never more than one tap away behind the hamburger menu even before any page-specific sticky bottom-bar exists — a dedicated bottom-sticky CTA (if a specific page design calls for one) is a Phase 2 page-content decision, not this shell-level phase's.

Scope note — decisions intentionally NOT implemented this phase (all page-content-dependent, per CONTEXT.md's own Deferred Ideas note: "applying this system to page content... is deferred to Phase 2's replan"; no gap, just sequencing): D-18/D-19(already covered by Plan 03)/D-20/D-21/D-23/D-24 (chat-bot messenger UI, pacing, quick-reply styling, auto-scroll, and demo script — no `/demo` route exists yet); D-22 (a dedicated coral-outline quick-reply button variant has no consumer yet — `PremiumButton`'s existing `outline` variant is navy per the D-35 contrast rule already established in Plan 02, and a chat-specific coral-outline variant is deferred to whichever Phase 2 plan builds the chat UI); D-25/D-26/D-27 (admin-demo interactivity/components/timing — stays `@repo/ui`-based per D-02, out of this phase's token system entirely); D-29/D-31 (hero parallax and cursor-reactivity — no hero section exists yet, nothing to attach them to); D-39/D-40/D-41 (lazy-loading and mobile treatment of the chat-bot/admin-demo blocks — those blocks don't exist yet).
Output: every route in `apps/web` renders inside the new premium shell; `apps/web/components/` and `apps/web/lib/` are gone.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-premium-design-system/01.1-CONTEXT.md
@.planning/phases/01.1-premium-design-system/01.1-PATTERNS.md
@.planning/phases/01-theme-site-shell/01-CONTEXT.md
@.planning/phases/01.1-premium-design-system/01.1-01-SUMMARY.md
@.planning/phases/01.1-premium-design-system/01.1-02-SUMMARY.md
@.planning/phases/01.1-premium-design-system/01.1-03-SUMMARY.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move + rebuild Logo and ThemeToggle; move routes.ts</name>
  <files>apps/web/shared/components/logo.tsx, apps/web/shared/components/theme-toggle.tsx, apps/web/shared/lib/routes.ts</files>
  <read_first>
    apps/web/components/logo.tsx (current 13-line full file — being moved and rebuilt in place)
    apps/web/components/theme-toggle.tsx (current 39-line full file — being moved and rebuilt in place)
    apps/web/lib/routes.ts (current 10-line full file — being moved verbatim, content unchanged)
    apps/web/shared/components/premium-button.tsx (Plan 02 output — `PremiumButton` variant/size props: `ghost`/`icon` for the toggle)
    apps/web/app/premium-theme.css (Plan 01 output — `--color-dt-navy`, `--font-dt-heading` token names)
  </read_first>
  <action>
    Create `apps/web/shared/lib/routes.ts` with the exact same content as the current `apps/web/lib/routes.ts` (the `routes` const object — `home`, `prices`, `demo`, `blog`, `blogPost`, `contacts`, `about`, `privacy` — unchanged, this is a pure file relocation).

    Create `apps/web/shared/components/logo.tsx`. Import `routes` from `'@/shared/lib/routes'`, `Link` from `'next/link'`, and `ChatCircleDots` from `'@phosphor-icons/react/ssr'` (the SSR-safe Phosphor import path — required because this stays a Server Component, no `'use client'`; Phosphor's default export uses React Context which is incompatible with Server Components). Export `function Logo(): React.JSX.Element` returning a `<Link href={routes.home} className="flex items-center gap-2">` wrapping: (1) a `<div className="flex h-10 w-10 items-center justify-center rounded-dt-card bg-dt-navy">` containing `<ChatCircleDots weight="regular" className="h-5 w-5 text-dt-warm-white" />` — this REPLACES the current `bg-brand` badge and its dental-tooth emoji glyph entirely (D-16 forbids dominant tooth/cross iconography); `ChatCircleDots` was chosen because it represents the product's actual core mechanism (a Telegram chat bot), not a dental symbol, satisfying D-17's "no cartoon characters, thin linear diagrams instead" spirit at icon scale; (2) `<span className="font-dt-heading text-xl font-bold text-dt-navy">DentaBot</span>` — replaces `text-foreground` with the new heading font token and `dt-navy` text color.

    Create `apps/web/shared/components/theme-toggle.tsx`, first line `'use client';`. Import `PremiumButton` from `'./premium-button'`, `Moon, Sun` from `'@phosphor-icons/react/ssr'`, `useTheme` from `'next-themes'`, and `* as React` from `'react'`. Replicate the existing mount-gated hydration-safety shape exactly: `const [mounted, setMounted] = React.useState(false);` + a `useEffect` that only sets it `true`; while `!mounted`, render `<PremiumButton variant="ghost" size="icon" className="h-10 w-10"><Sun weight="regular" className="h-5 w-5" /></PremiumButton>`; once mounted, render `<PremiumButton variant="ghost" size="icon" onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')} className="h-10 w-10">{theme === 'dark' ? <Sun weight="regular" className="h-5 w-5" /> : <Moon weight="regular" className="h-5 w-5" />}<span className="sr-only">Перемкнути тему</span></PremiumButton>`. Keep `next-themes`'s `useTheme` hook and the ghost-icon-button shape and the `sr-only` Ukrainian label unchanged — only the `Button`→`PremiumButton` and `lucide-react`→`@phosphor-icons/react/ssr` swaps are new.
  </action>
  <acceptance_criteria>
    <criterion>`apps/web/shared/components/logo.tsx` contains zero occurrences of the dental-tooth emoji character (see Task 3's automated grep for the exact codepoint checked)</criterion>
    <criterion>`apps/web/shared/components/logo.tsx` imports `ChatCircleDots` from `@phosphor-icons/react/ssr` and contains `bg-dt-navy`, `text-dt-warm-white`, `text-dt-navy`, and `font-dt-heading`</criterion>
    <criterion>`apps/web/shared/components/logo.tsx` contains zero occurrences of `bg-brand` or `text-foreground`</criterion>
    <criterion>`apps/web/shared/components/theme-toggle.tsx` first line is exactly `'use client';`, imports `PremiumButton` from `./premium-button`, and imports `Moon, Sun` from `@phosphor-icons/react/ssr`</criterion>
    <criterion>`apps/web/shared/components/theme-toggle.tsx` contains zero occurrences of `lucide-react` or `@repo/ui`</criterion>
    <criterion>`apps/web/shared/lib/routes.ts` contains all 8 keys from the original: `home`, `prices`, `demo`, `blog`, `blogPost`, `contacts`, `about`, `privacy`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c '🦷' apps/web/shared/components/logo.tsx)" = "0" && \
test "$(grep -c "from '@phosphor-icons/react/ssr'" apps/web/shared/components/logo.tsx)" = "1" && \
test "$(grep -c 'bg-dt-navy' apps/web/shared/components/logo.tsx)" = "1" && \
test "$(grep -c 'bg-brand\|text-foreground' apps/web/shared/components/logo.tsx)" = "0" && \
test "$(sed -n '1p' apps/web/shared/components/theme-toggle.tsx)" = "'use client';" && \
test "$(grep -c "from './premium-button'" apps/web/shared/components/theme-toggle.tsx)" = "1" && \
test "$(grep -c 'lucide-react\|@repo/ui' apps/web/shared/components/theme-toggle.tsx)" = "0" && \
test "$(grep -c 'home:\|prices:\|demo:\|blog:\|blogPost:\|contacts:\|about:\|privacy:' apps/web/shared/lib/routes.ts)" = "8" && \
echo PASS
    </automated>
  </verify>
  <done>`Logo` renders an icon-badge wordmark with zero tooth iconography on `dt-navy`/`dt-warm-white`; `ThemeToggle` is rebuilt on `PremiumButton` + Phosphor icons with identical toggle behavior; `routes.ts` is relocated verbatim to `apps/web/shared/lib/`.</done>
</task>

<task type="auto">
  <name>Task 2: Move + rebuild Header and Footer</name>
  <files>apps/web/shared/components/header.tsx, apps/web/shared/components/footer.tsx</files>
  <read_first>
    apps/web/components/header.tsx (current 132-line full file — being moved and rebuilt in place)
    apps/web/components/footer.tsx (current 153-line full file — being moved and rebuilt in place)
    apps/web/shared/components/logo.tsx, apps/web/shared/components/theme-toggle.tsx (Task 1 output — new sibling imports)
    apps/web/shared/components/premium-button.tsx (Plan 02 — `PremiumButton` `coral`/`outline` variants)
    apps/web/shared/lib/motion.ts (Plan 03 — `EASE_DT_EXPO_OUT` constant)
    .planning/phases/01.1-premium-design-system/01.1-CONTEXT.md (D-06 coral rarity rule, D-14 96-140px desktop inter-section spacing)
  </read_first>
  <action>
    Create `apps/web/shared/components/header.tsx`, keeping first line `'use client';`. Import `routes` from `'@/shared/lib/routes'`, `PremiumButton` from `'./premium-button'`, `List, X` from `'@phosphor-icons/react/ssr'`, `AnimatePresence, motion` from `'motion/react'`, `EASE_DT_EXPO_OUT` from `'@/shared/lib/motion'`, `Link` from `'next/link'`, `usePathname` from `'next/navigation'`, `* as React` from `'react'`, and `Logo`/`ThemeToggle` from `'./logo'`/`'./theme-toggle'`. Keep the `navLinks` array, the `isScrolled`/`isMobileMenuOpen` state, and the scroll-listener `useEffect` (lines 26-30 of the original) byte-identical in shape — only retarget classNames: `bg-background/80` → `bg-dt-warm-white/80`, plain `bg-background` → `bg-dt-warm-white`.

    Desktop nav active-link state: replace the `text-brand` ternary branch with `text-dt-navy font-semibold` (active) vs. `text-dt-graphite hover:text-dt-coral` (inactive/hover) — coral is reserved for the hover MOMENT, not the persistent active-page indicator, keeping D-06's "coral used sparingly" discipline (navy+bold signals "current page", coral signals "you're interacting right now"). Desktop CTA buttons: `<PremiumButton variant="outline" size="default" asChild><Link href={routes.demo}>Демо</Link></PremiumButton>` and `<PremiumButton variant="coral" size="default" asChild><Link href={routes.contacts}>Спробувати безкоштовно</Link></PremiumButton>` (replacing `variant="brand-outline"`/`variant="brand"`). Mobile menu button: `<PremiumButton variant="ghost" size="icon" onClick={...}>{isMobileMenuOpen ? <X weight="regular" className="h-6 w-6" /> : <List weight="regular" className="h-6 w-6" />}</PremiumButton>` (replacing `Menu`/`X` from `lucide-react`).

    Mobile menu collapse (originally a bare `{isMobileMenuOpen && (<div className="border-t border-border py-4 lg:hidden">...)}`): wrap it in `<AnimatePresence>{isMobileMenuOpen && (<motion.div initial={{ opacity: 0, height: 0 }} animate={{ opacity: 1, height: 'auto' }} exit={{ opacity: 0, height: 0 }} transition={{ duration: 0.25, ease: EASE_DT_EXPO_OUT }} className="overflow-hidden border-t border-dt-navy/10 lg:hidden">...)}</AnimatePresence>` — this is the phase's first live use of the Plan 03 motion utilities, satisfying the "no default Bootstrap/Tailwind component" acceptance-checklist item for the mobile menu specifically. Inside, retarget `text-brand`/`text-muted-foreground` to `text-dt-navy font-semibold`/`text-dt-graphite`, and the two mobile CTA `Button`s to `PremiumButton` `outline`/`coral` exactly as the desktop pair above.

    Create `apps/web/shared/components/footer.tsx` (no `'use client'`, matches the current file). Import `routes` from `'@/shared/lib/routes'`, `Link` from `'next/link'`, and `Logo` from `'./logo'`. Keep the 4-column `grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-4 lg:gap-12` structure unchanged. Change the outer `<footer>` padding from `py-12 lg:py-16` to `py-16 lg:py-24` (96px at `lg`, the floor of D-14's 96-140px desktop inter-section range — the original 64px was below it). Retarget: `border-t border-border bg-muted` → `border-t border-dt-navy/10 bg-dt-warm-white`; every `text-muted-foreground` → `text-dt-graphite`; every `text-foreground` column heading → `text-dt-navy font-dt-heading font-semibold`; every `hover:text-brand` → `hover:text-dt-coral`. Social-icon circles: `bg-muted` → `bg-dt-navy/5`, and — applying the same contrast-safe rule as `PremiumButton`'s coral variant — `hover:bg-brand hover:text-primary-foreground` → `hover:bg-dt-coral hover:text-dt-navy` (never white/warm-white text on a coral hover background). Keep every `target="_blank" rel="noopener noreferrer"` external-link attribute pair byte-identical (Telegram, Instagram anchors) — this is a security-relevant mitigation being preserved, not restyled.
  </action>
  <acceptance_criteria>
    <criterion>`apps/web/shared/components/header.tsx` first line is `'use client';`, imports `List, X` from `@phosphor-icons/react/ssr`, and imports `AnimatePresence, motion` from `motion/react`</criterion>
    <criterion>`header.tsx` contains zero occurrences of `lucide-react`, `@repo/ui`, `text-brand`, `bg-brand`, `variant="brand"`, or `variant="brand-outline"`</criterion>
    <criterion>`header.tsx`'s mobile menu block is wrapped in `AnimatePresence` and a `motion.div` referencing `EASE_DT_EXPO_OUT`</criterion>
    <criterion>`apps/web/shared/components/footer.tsx` contains `bg-dt-navy/5` and `hover:bg-dt-coral hover:text-dt-navy` (never `hover:bg-dt-coral` paired with `text-white`/`text-dt-warm-white`/`text-primary-foreground`)</criterion>
    <criterion>`footer.tsx` contains exactly 2 occurrences each of `target="_blank"` and `rel="noopener noreferrer"`</criterion>
    <criterion>`footer.tsx` contains `py-16 lg:py-24` and zero occurrences of `bg-muted`, `border-border`, `text-muted-foreground`, `text-foreground`, or `text-brand`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(sed -n '1p' apps/web/shared/components/header.tsx)" = "'use client';" && \
test "$(grep -c "from '@phosphor-icons/react/ssr'" apps/web/shared/components/header.tsx)" = "1" && \
test "$(grep -c "from 'motion/react'" apps/web/shared/components/header.tsx)" = "1" && \
test "$(grep -c 'lucide-react\|@repo/ui\|text-brand\|bg-brand\|variant="brand"\|variant="brand-outline"' apps/web/shared/components/header.tsx)" = "0" && \
test "$(grep -c 'AnimatePresence' apps/web/shared/components/header.tsx)" -ge "1" && \
test "$(grep -c 'EASE_DT_EXPO_OUT' apps/web/shared/components/header.tsx)" -ge "1" && \
test "$(grep -c 'bg-dt-navy/5' apps/web/shared/components/footer.tsx)" = "1" && \
test "$(grep -c 'hover:bg-dt-coral hover:text-dt-navy' apps/web/shared/components/footer.tsx)" = "1" && \
test "$(grep -Ec 'hover:bg-dt-coral.*(text-white|text-dt-warm-white|text-primary-foreground)' apps/web/shared/components/footer.tsx)" = "0" && \
test "$(grep -c 'target="_blank"' apps/web/shared/components/footer.tsx)" = "2" && \
test "$(grep -c 'rel="noopener noreferrer"' apps/web/shared/components/footer.tsx)" = "2" && \
test "$(grep -c 'py-16 lg:py-24' apps/web/shared/components/footer.tsx)" = "1" && \
test "$(grep -c 'bg-muted\|border-border\|text-muted-foreground\|text-foreground\|text-brand' apps/web/shared/components/footer.tsx)" = "0" && \
echo PASS
    </automated>
  </verify>
  <done>`Header` and `Footer` are rebuilt at `apps/web/shared/components/` on `dt-` tokens, `PremiumButton`, and Phosphor icons; the mobile menu animates via `AnimatePresence`/`motion.div`; external-link security attributes are preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Wire layout.tsx + not-found.tsx to the new shell, delete the old tree, verify the full build</name>
  <files>apps/web/app/layout.tsx, apps/web/app/not-found.tsx, apps/web/components/header.tsx, apps/web/components/footer.tsx, apps/web/components/logo.tsx, apps/web/components/theme-toggle.tsx, apps/web/lib/routes.ts, apps/web/app/fonts/GeistVF.woff, apps/web/app/fonts/GeistMonoVF.woff</files>
  <read_first>
    apps/web/app/layout.tsx (current 42-line full file — the `localFont` calls and `@/components/*` imports being replaced)
    apps/web/app/not-found.tsx (current 24-line full file — being retrofitted for consistency with the rebuilt shell, per DESIGN-01's "apps/web pages" scope; not explicitly named in D-04's shell list but left as-is it would be the only remaining `@repo/ui` Button + brand-blue element on the site, visibly clashing with the new header/footer)
    apps/web/app/fonts.ts (Plan 01 output — `interHeading`, `interBody` named exports)
    apps/web/shared/components/premium-button.tsx (Plan 02 — `coral`/`lg` variant/size)
  </read_first>
  <action>
    Edit `apps/web/app/layout.tsx`: remove the `import localFont from 'next/font/local';` line and both `geistSans`/`geistMono` `const` declarations; add `import { interHeading, interBody } from './fonts';`. Change the two `@/components/*` imports to `import { Footer } from '@/shared/components/footer';` and `import { Header } from '@/shared/components/header';`. Change the `<body>` `className` from `` `${geistSans.variable} ${geistMono.variable}` `` to `` `${interHeading.variable} ${interBody.variable} font-dt-body` `` — the trailing `font-dt-body` utility sets the site-wide default text font (headings opt into `font-dt-heading` explicitly per-component, as `Logo`/`Footer`'s column headings already do). Leave `ThemeProvider`, `<main>`, `<Toaster />` (still `@repo/ui`'s — a global notification portal, out of this phase's visual-system scope, not a persistent visible chrome element), and the `metadata` export untouched.

    Edit `apps/web/app/not-found.tsx`: change `import { routes } from '@/lib/routes';` to `import { routes } from '@/shared/lib/routes';`; change `import { Button } from '@repo/ui';` and `import { Home } from 'lucide-react';` to `import { PremiumButton } from '@/shared/components/premium-button';` and `import { House } from '@phosphor-icons/react/ssr';`. Change the `text-8xl font-bold text-brand` "404" numeral to `text-8xl font-bold text-dt-navy`; change `text-muted-foreground` to `text-dt-graphite`. Change `<Button size="lg" asChild>` to `<PremiumButton variant="coral" size="lg" asChild>` and `<Home className="mr-2 h-5 w-5" />` to `<House weight="regular" className="mr-2 h-5 w-5" />`.

    Delete the old shell tree: remove `apps/web/components/header.tsx`, `apps/web/components/footer.tsx`, `apps/web/components/logo.tsx`, `apps/web/components/theme-toggle.tsx`, and the now-empty `apps/web/components/` directory; remove `apps/web/lib/routes.ts` and the now-empty `apps/web/lib/` directory. Delete the now-unused `apps/web/app/fonts/GeistVF.woff` and `apps/web/app/fonts/GeistMonoVF.woff` files (superseded by `apps/web/app/fonts.ts`'s self-hosted Inter).

    Run `pnpm --filter web build`, `pnpm --filter web lint`, and `pnpm --filter web check-types` in sequence; all three must exit 0 before this task is done. If `check-types` fails on the pre-existing `csstype@3.1.3` vs `3.2.3` duplicate-resolution conflict noted in `.planning/STATE.md`'s Blockers/Concerns (a known, pre-existing, out-of-phase-scope issue), confirm the failure is that exact pre-existing error signature (not a new error introduced by this phase's changes) before treating the task as done, and note it unresolved in the SUMMARY.
  </action>
  <acceptance_criteria>
    <criterion>`apps/web/app/layout.tsx` imports `interHeading, interBody` from `./fonts`, imports `Footer`/`Header` from `@/shared/components/footer`/`@/shared/components/header`, and contains zero occurrences of `next/font/local`, `GeistVF`, or `GeistMonoVF`</criterion>
    <criterion>`apps/web/app/not-found.tsx` contains zero occurrences of `@repo/ui`, `lucide-react`, `text-brand`, or `text-muted-foreground`</criterion>
    <criterion>`apps/web/components/` and `apps/web/lib/` no longer exist on disk</criterion>
    <criterion>`apps/web/app/fonts/GeistVF.woff` and `apps/web/app/fonts/GeistMonoVF.woff` no longer exist on disk</criterion>
    <criterion>`pnpm --filter web build` exits 0</criterion>
    <criterion>`git diff --quiet packages/ui/styles/theme.css` confirms zero diff for the entire phase</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c 'interHeading, interBody' apps/web/app/layout.tsx)" -ge "1" && \
test "$(grep -c "@/shared/components/footer" apps/web/app/layout.tsx)" = "1" && \
test "$(grep -c "@/shared/components/header" apps/web/app/layout.tsx)" = "1" && \
test "$(grep -c 'next/font/local\|GeistVF\|GeistMonoVF' apps/web/app/layout.tsx)" = "0" && \
test "$(grep -c '@repo/ui\|lucide-react\|text-brand\|text-muted-foreground' apps/web/app/not-found.tsx)" = "0" && \
test ! -d apps/web/components && \
test ! -d apps/web/lib && \
test ! -f apps/web/app/fonts/GeistVF.woff && \
test ! -f apps/web/app/fonts/GeistMonoVF.woff && \
pnpm --filter web build && \
pnpm --filter web lint && \
git diff --quiet packages/ui/styles/theme.css && \
echo PASS
    </automated>
  </verify>
  <done>Every apps/web route renders inside the premium shell; `apps/web/components/`, `apps/web/lib/`, and the Geist font files are gone; `apps/web/shared/` is the single source of truth for cross-page code; `pnpm --filter web build`/`lint` pass; `packages/ui/styles/theme.css` remains untouched for the full phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `t.me/dentabot`, `instagram.com/dentabot` (footer external anchors) | User-initiated navigation to third-party origins via `target="_blank"` |
| Folder move (`apps/web/components/`/`apps/web/lib/` deletion) → build graph | Stale import paths after deletion could silently break the build if any consumer outside this plan's edited files still referenced the old paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-04-01 (reused from Phase 1's T-01-03) | Tampering / Spoofing (reverse tabnabbing) | Footer Telegram/Instagram anchors | medium | mitigate | `target="_blank" rel="noopener noreferrer"` preserved verbatim during the rebuild — Task 2 acceptance criteria hard-gate exactly 2 occurrences of each attribute |
| T-01.1-04-02 | Tampering (build-breaking regression) | Stale imports after `apps/web/components/`/`apps/web/lib/` deletion | medium | mitigate | Task 3's `pnpm --filter web build` gate is the final, authoritative check — a stale import anywhere in the app fails the build immediately, and this task is not "done" until the build exits 0 |
| T-01.1-04-03 | Tampering (scope-boundary regression) | `packages/ui/styles/theme.css` | high | mitigate | `git diff --quiet packages/ui/styles/theme.css` re-verified at the end of the final plan in this phase — confirms D-01/D-02's boundary held across all 4 plans, not just Plan 01 |

</threat_model>

<verification>
- `pnpm --filter web build`, `pnpm --filter web lint`, `pnpm --filter web check-types` all pass (Task 3 automated verify)
- Grep-based acceptance criteria across all 3 tasks confirm zero `@repo/ui`/`lucide-react`/brand-blue/tooth-emoji leftovers in the rebuilt shell, and the folder move is complete
- <human-check>Run `pnpm dev:web`, visit `/` and `/some-nonexistent-url`, and confirm: (1) the header/footer/logo/theme-toggle render with the navy/teal/warm-white/coral/graphite palette (no default-Tailwind/shadcn look, no brand-blue #1d6be4 anywhere, no tooth emoji); (2) the mobile hamburger menu (resize below `lg`) opens/closes with a smooth height+fade animation, not an instant snap; (3) toggling light/dark theme via the header icon doesn't break layout or throw console errors; (4) tabbing through the header's nav links, CTA buttons, and theme toggle shows a clearly visible focus ring on each; (5) enabling "reduce motion" in OS accessibility settings and reloading makes the mobile menu open instantly (no animation) rather than disabling entirely-broken; (6) the 404 page at a nonexistent URL shows the navy "404" heading and a coral "На головну" button, not the old blue brand button. Optionally run `npx lighthouse http://localhost:3000 --view` (Performance category) to spot-check the DESIGN-03 ≥85 target — note the score in the SUMMARY even if not perfectly reproducible in a dev environment. This human-check satisfies workflow.human_verify_mode=end-of-phase.</human-check>
</verification>

<success_criteria>
Every route in `apps/web` (home placeholder, 404) renders inside a header/footer/logo/theme-toggle shell built entirely on the `dt-` premium token system — zero `@repo/ui` component usage, zero brand-blue leftovers, zero tooth iconography (DESIGN-01); the mobile menu is the phase's first live Motion consumer, animating via the shared `EASE_DT_EXPO_OUT` easing and degrading correctly under reduced motion (DESIGN-02); focus-visible rings are present everywhere, contrast-safe coral/navy pairing is enforced, and the full `apps/web` build/lint/typecheck pipeline is green (DESIGN-03). `packages/ui/styles/theme.css` is untouched across the entire phase, and `apps/web/components/`/`apps/web/lib/` are fully absorbed into `apps/web/shared/`.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `apps/web/shared/components/logo.tsx` (new, moved+rebuilt) — exports `Logo`
- `apps/web/shared/components/theme-toggle.tsx` (new, moved+rebuilt) — exports `ThemeToggle`
- `apps/web/shared/lib/routes.ts` (new, moved) — exports `routes`
- `apps/web/shared/components/header.tsx` (new, moved+rebuilt) — exports `Header`
- `apps/web/shared/components/footer.tsx` (new, moved+rebuilt) — exports `Footer`
- `apps/web/app/layout.tsx` (edited) — wired to `interHeading`/`interBody` fonts and the moved `Header`/`Footer`
- `apps/web/app/not-found.tsx` (edited) — retrofitted onto `PremiumButton`/`dt-` tokens/Phosphor
- `apps/web/components/`, `apps/web/lib/`, `apps/web/app/fonts/{GeistVF,GeistMonoVF}.woff` (deleted)

<output>
Create `.planning/phases/01.1-premium-design-system/01.1-04-SUMMARY.md` when done
</output>
