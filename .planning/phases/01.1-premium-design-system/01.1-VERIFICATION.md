---
phase: 01.1-premium-design-system
verified: 2026-08-09T00:00:00Z
status: human_needed
score: 17/17 must-haves verified (1 via documented override)
behavior_unverified: 0
overrides_applied: 1
overrides:
  - must_have: "pnpm --filter web build, pnpm --filter web lint, and pnpm --filter web check-types all exit 0 after the rebuild (01.1-04-PLAN.md must_haves.truths)"
    reason: "pnpm --filter web lint exits 0 cleanly (re-verified live). pnpm --filter web build/check-types fail on a pre-existing csstype@3.1.3/3.2.3 duplicate-resolution conflict confined to packages/ui/src/components/shadcn-ui/{button-group,calendar,sidebar}.tsx — a file set this phase never touched. Confirmed by re-running the build live: the single TS2322 error is in button-group.tsx:50, identical to the signature documented in STATE.md/PROJECT.md since Phase 1 and in this phase's own deferred-items.md across all 4 plans. Per the orchestrator's known_context_for_verifier instruction, this predates the phase and is explicitly not to be flagged as a phase regression."
    accepted_by: "gsd-verifier (per explicit known_context_for_verifier instruction from orchestrator, corroborated by STATE.md/PROJECT.md/deferred-items.md pre-existing documentation)"
    accepted_at: "2026-08-09T00:00:00Z"
human_verification:
  - test: "Run `pnpm dev:web`, visit `/` and a nonexistent URL. Confirm the header/footer/logo render with the navy(#1A2B3D)/teal(#2C7A7B)/warm-white(#FAFAF8)/coral(#E86B5A)/graphite(#2D3436) palette, no default-Tailwind/shadcn look, no brand-blue #1d6be4 anywhere, no tooth emoji/iconography."
    expected: "Site reads as a bespoke premium visual system, not a default component-library look."
    why_human: "Visual/subjective appearance judgment; cannot be verified by grep or type-check."
  - test: "Resize below the `lg` breakpoint and open/close the mobile hamburger menu."
    expected: "Menu opens/closes with a smooth height+opacity animation (not an instant snap); with OS 'reduce motion' enabled, it opens instantly with no height animation."
    why_human: "Runtime animation behavior (Motion/AnimatePresence) cannot be observed via static analysis; code inspection confirms the reduced-motion branch exists (apps/web/shared/components/header.tsx:96-116) but not that it renders correctly in a browser."
  - test: "Tab through the header's nav links, CTA buttons, and mobile menu toggle."
    expected: "A clearly visible focus ring appears on each focusable element (PremiumButton's focus-visible:ring-dt-navy, nav links' browser-default outline since no global outline reset was found)."
    why_human: "Focus-ring visibility/legibility is a rendered, not static, property."
  - test: "Load the 404 page at a nonexistent URL."
    expected: "Navy '404' heading, coral 'На головну' PremiumButton with a House icon — no leftover @repo/ui Button or brand-blue styling."
    why_human: "Visual confirmation of the rebuilt not-found.tsx page; code inspection already confirms the source (no @repo/ui/lucide-react/text-brand/text-muted-foreground references)."
  - test: "Run `npx lighthouse http://localhost:3000 --view` (or equivalent) against the built/served site and check the Performance category score."
    expected: "Score ≥85 (DESIGN-03 / D-46), even with the mobile-menu animation and reveal/motion utilities active."
    why_human: "Performance scoring requires a running server and a real browser audit; not reproducible via static grep/type-check in this verification pass. Note: `pnpm --filter web build` currently fails on the pre-existing csstype issue (see overrides above), so a production Lighthouse run may currently require a workaround (e.g. `next dev` or a local override) until that pre-existing blocker is fixed."
---

# Phase 01.1: Premium Design System (apps/web) Verification Report

**Phase Goal:** Establish a bespoke premium visual system for the marketing site (apps/web) per the client's new ТЗ — own color palette, typography, spacing/shadow/radius scale, motion/animation primitives, icon system — scoped to apps/web only, with Phase 1's shipped shell (header/footer/logo/theme-toggle) retrofitted onto it. packages/ui (and its consumers) stays on the existing theme; this phase does not touch it. No page content ships here.
**Verified:** 2026-08-09
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `globals.css` imports a new `dt`-namespaced `premium-theme.css` after `@repo/ui`'s theme.css, zero token collision | ✓ VERIFIED | `apps/web/app/globals.css` lines 1-3: `@repo/ui/styles/theme.css` then `./premium-theme.css`; `packages/ui/styles/theme.css` grep for `--color-dt-`/`--radius-dt-`/`--shadow-dt-`/`--text-dt-`/`--font-dt-` returns 0 |
| 2 | `motion`/`@phosphor-icons/react` declared as direct `apps/web` deps only after human approval | ✓ VERIFIED | `apps/web/package.json` lines 14-26 list both; 01.1-01-SUMMARY.md documents the Package Legitimacy Gate approval |
| 3 | `fonts.ts` exports two `next/font/google` Inter instances (600/700 heading, 400/500 body), latin+cyrillic | ✓ VERIFIED | `apps/web/app/fonts.ts` — `interHeading`/`interBody`, both `subsets: ['latin', 'cyrillic']` |
| 4 | Global `prefers-reduced-motion: reduce` CSS rule collapses transition/animation durations | ✓ VERIFIED | `apps/web/app/premium-theme.css` lines 66-75 |
| 5 | `PremiumButton`'s coral variant never pairs `bg-dt-coral` with white/warm-white text | ✓ VERIFIED | `apps/web/shared/components/premium-button.tsx:12` — `bg-dt-coral text-dt-navy`, no white/warm-white pairing anywhere in file |
| 6 | Interactive premium primitives show a visible `focus-visible` ring | ✓ VERIFIED | `premium-button.tsx:8` — `focus-visible:ring-2 focus-visible:ring-dt-navy focus-visible:ring-offset-2` |
| 7 | No file under `apps/web/shared/` imports from `@repo/ui`/`packages/ui` | ✓ VERIFIED | `grep -rn "@repo/ui" apps/web/shared` returns 0 matches |
| 8 | `PremiumCard`'s hover uses `translateY` + shadow swap, never `scale()` | ✓ VERIFIED | `premium-card.tsx:10` — `hover:-translate-y-1 hover:shadow-[var(--shadow-dt-hover)]`, zero `scale(` occurrences |
| 9 | `Reveal` shows children at full opacity immediately under reduced motion, fades+translates otherwise | ✓ VERIFIED | `reveal.tsx:25-29` — ternary on `prefersReducedMotion` |
| 10 | `motion.ts`'s easing matches D-32 exactly; variants animate only opacity/y | ✓ VERIFIED | `motion.ts:1` — `[0.16, 1, 0.3, 1] as const`; no `top:`/`left:`/`width:`/`height:` in variant objects |
| 11 | `useInView` starts `false` on server/first render, flips only inside `useEffect` | ✓ VERIFIED | `use-in-view.ts:7` `useState(false)`; observer created/observed inside `useEffect` only |
| 12 | `SignatureMark` renders an `aria-hidden` coral dot, pulse gated by `motion-safe:` | ✓ VERIFIED | `signature-mark.tsx:14,17` — `aria-hidden="true"`, `pulse && 'motion-safe:animate-pulse'` |
| 13 | No dental-tooth emoji anywhere in `apps/web` | ✓ VERIFIED | `grep -rn "🦷" apps/web/shared apps/web/app` returns 0 |
| 14 | `apps/web/components/` and `apps/web/lib/` no longer exist; shell lives under `apps/web/shared/` | ✓ VERIFIED | `ls apps/web/components` / `apps/web/lib` → "No such file or directory"; `apps/web/shared/{components,lib,hooks}` populated |
| 15 | Rebuilt shell files use only `dt-` tokens/Phosphor icons — zero `@repo/ui` Button, zero `lucide-react`, zero brand-blue refs | ✓ VERIFIED | `grep -rn "🦷\|bg-brand\|text-brand\|#1d6be4\|text-foreground\|bg-muted\|border-border\|text-muted-foreground" apps/web/shared apps/web/app` returns 0; `grep "@repo/ui\|lucide-react" apps/web/shared` returns 0 |
| 16 | `pnpm --filter web build`/`lint`/`check-types` all exit 0 | ✗ FAILED → PASSED (override) | `lint` exits 0 (re-run live, clean). `build`/`check-types` fail on the pre-existing, out-of-scope `csstype@3.1.3`/`3.2.3` conflict in `packages/ui/src/components/shadcn-ui/*` (re-run live, confirmed identical error signature to STATE.md/deferred-items.md). See `overrides` in frontmatter. |
| 17 | `packages/ui/styles/theme.css` has zero diff for the entire phase | ✓ VERIFIED | `git log --follow -- packages/ui/styles/theme.css` shows last touch at Phase 1 commit `c430198`; no phase-01.1 commit touches this file |

**Score:** 17/17 truths verified (16 directly, 1 via documented override) — 0 present-but-behavior-unverified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/web/app/premium-theme.css` | `dt-` token set (colors/radius/shadows/type/grid/motion) | ✓ VERIFIED | All 7 colors, 2 radii, 3 shadows, 5 type-scale + line-heights, `.dt-caption`, `--font-dt-*` mappings, `--dt-container-max/section-y/ease-expo-out`, reduced-motion rule all present |
| `apps/web/app/fonts.ts` | `interHeading`/`interBody` exports | ✓ VERIFIED | Both present, correct weights/variables |
| `apps/web/shared/lib/cn.ts` | local `clsx`+`tailwind-merge` helper | ✓ VERIFIED | `export function cn(...)` present, no `@repo/ui` import |
| `apps/web/shared/components/premium-button.tsx` | CVA button, coral/outline/ghost × 4 sizes | ✓ VERIFIED | All variants/sizes present, WCAG-safe coral pairing, focus ring |
| `apps/web/shared/components/premium-card.tsx` | layered-shadow card, hover-lift | ✓ VERIFIED | translateY hover, arbitrary-value shadow tokens |
| `apps/web/shared/components/container.tsx` | max-width 1280px wrapper | ✓ VERIFIED | `max-w-[var(--dt-container-max)]` |
| `apps/web/shared/hooks/use-in-view.ts` | SSR-safe `useInView` | ✓ VERIFIED | Hydration-safe, disconnects on intersect + cleanup |
| `apps/web/shared/lib/motion.ts` | easing/variant constants | ✓ VERIFIED | `EASE_DT_EXPO_OUT`, `revealVariants`, `revealContainerVariants`, `hoverLift` |
| `apps/web/shared/components/reveal.tsx` | scroll-reveal wrapper | ✓ VERIFIED | Composes `useInView` + Motion + `useReducedMotion` |
| `apps/web/shared/components/signature-mark.tsx` | recurring coral action-marker | ✓ VERIFIED | `aria-hidden`, `motion-safe:` gated pulse |
| `apps/web/shared/components/{logo,theme-toggle,header,footer}.tsx` | rebuilt shell | ✓ VERIFIED | All exist, `dt-`-tokened, Phosphor icons, zero `@repo/ui`/`lucide-react` |
| `apps/web/shared/lib/routes.ts` | relocated route constants | ✓ VERIFIED | All 8 keys present |
| `apps/web/app/layout.tsx` | wired to new fonts/shell | ✓ VERIFIED | Imports `interHeading`/`interBody`, `Header`/`Footer` from `@/shared/components/*`; `pt-16 lg:pt-20` on `<main>` (CR-1 fix) |
| `apps/web/app/not-found.tsx` | retrofitted 404 page | ✓ VERIFIED | `PremiumButton` (coral) + Phosphor `House`, `dt-navy`/`dt-graphite` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `globals.css` | `premium-theme.css` | `@import './premium-theme.css'` after `@repo/ui`'s theme | ✓ WIRED | Confirmed, correct order |
| `header.tsx` | `premium-button.tsx` | `import { PremiumButton } from './premium-button'` | ✓ WIRED | Both desktop and mobile CTAs use it |
| `layout.tsx` | `header.tsx`/`footer.tsx` | `@/shared/components/{header,footer}` | ✓ WIRED | Confirmed in layout.tsx |
| `reveal.tsx` | `use-in-view.ts` | `useInView<HTMLDivElement>(threshold)` | ✓ WIRED | Confirmed |
| `reveal.tsx` | `motion.ts` | `revealVariants` | ✓ WIRED | Confirmed |
| `header.tsx` mobile menu | `motion.ts` | `EASE_DT_EXPO_OUT` in `AnimatePresence`/`motion.div` | ✓ WIRED | Confirmed, plus reduced-motion branch |

### Data-Flow Trace (Level 4)

Not applicable — this phase ships no dynamic/DB-backed data. All artifacts are static design-system primitives and token definitions (no fetch/query/store consumers exist yet; that's explicitly Phase 2's scope).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `pnpm --filter web lint` exits 0 | `pnpm --filter web lint` | `eslint --max-warnings 0` completed, no output/errors | ✓ PASS |
| `pnpm --filter web build` exits 0 | `pnpm --filter web build` | Fails at `tsc` type-check step on pre-existing `csstype` conflict in `packages/ui/src/components/shadcn-ui/button-group.tsx:50` (and calendar/sidebar) — zero relation to any file this phase touched | ✗ FAIL (excused via override — pre-existing, documented since Phase 1) |
| Browser-rendered visual/animation/focus/Lighthouse checks | — | Not run (requires dev server + browser) | ? SKIP → routed to Human Verification |

### Probe Execution

No `scripts/*/tests/probe-*.sh` files found in the repository and none referenced in this phase's PLAN/SUMMARY files. Step 7c: SKIPPED (no probes declared or discovered).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|------------|--------------|--------|----------|
| DESIGN-01 | 01.1-01, 01.1-02, 01.1-04 | Bespoke premium palette/typography/spacing/shadow system on `apps/web`, not built on `packages/ui` | ✓ SATISFIED | `dt-` token file, primitives, full shell rebuild; `packages/ui/styles/theme.css` untouched |
| DESIGN-02 | 01.1-03, 01.1-04 | Site-wide motion system (scroll reveals, hover micro-interactions, signature coral marker), `prefers-reduced-motion` respected | ✓ SATISFIED (code-level) — Lighthouse/visual runtime confirmation still pending, see Human Verification | `Reveal`, `SignatureMark`, `motion.ts`, header mobile-menu `AnimatePresence` all present and correctly gated for reduced motion at the code level |
| DESIGN-03 | 01.1-01, 01.1-02, 01.1-04 | WCAG AA contrast, visible `focus-visible` states, Lighthouse Performance ≥85 | ⚠️ PARTIALLY SATISFIED — needs human | Contrast fixes (WR-2, IN-1) and focus-visible rings verified in code; Lighthouse ≥85 was never run (SUMMARY 04 explicitly flags this as outstanding) |

**Cross-phase note (not this phase's own requirement, but a real side effect):** THEME-03 (Phase 1, "user can toggle light/dark theme from the header") is regressed on the premium `apps/web` site by this phase's own code-review fix (commit `c9f3fc5`, WR-1) — `ThemeToggle` was removed from `Header` entirely because no dark-mode `dt-*` token values exist. Confirmed in the codebase: `apps/web/shared/components/theme-toggle.tsx` still exists and exports `ThemeToggle`, but `grep -rn "ThemeToggle" apps/web` shows only its own definition — zero consumers. `REQUIREMENTS.md` already documents this regression accurately (2026-08-09 entry under THEME-03) and flags it as needing a follow-up decision (design dark-mode `dt-*` values, or formally descope). This is **not counted as a gap against DESIGN-01/02/03** (Phase 01.1's own requirements don't cover dark mode) but is carried forward here as a known, disclosed deviation per the orchestrator's explicit instruction.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `grep -rn "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across `apps/web/shared`/`apps/web/app` | — | Zero matches — no debt markers found |
| `apps/web/shared/components/footer.tsx` | 84 | Comment cites "D-05" for the `/about`/`/privacy` 404-until-later rationale, but no D-05 in either CONTEXT.md matches this (IN-2, unfixed by design — Info severity, excluded from fix scope) | Info | Misleading decision-ID citation for future readers; no functional impact |
| `apps/web/shared/components/premium-button.tsx` | 12 | Coral-variant contrast margin ~4.59:1 vs 4.5:1 AA floor (IN-1, unfixed by design — Info severity, excluded from fix scope) | Info | Passes AA but with a thin margin; documented and accepted as a follow-up-optional item in 01.1-REVIEW.md |

No Critical or Warning-severity anti-patterns remain — all 6 in-scope findings from `01.1-REVIEW.md` (CR-1, WR-1 through WR-5) were fixed and independently re-verified against the current codebase in this pass (CR-1: `pt-16 lg:pt-20` present; WR-1: `ThemeToggle` removed from `Header`; WR-2: zero `text-dt-coral` hover in header/footer; WR-3: `Container` used in both; WR-4: `aria-label`/`aria-expanded` present; WR-5: `useReducedMotion` branch present in mobile menu).

### Human Verification Required

See `human_verification` in frontmatter — 5 items: visual palette/iconography check, mobile-menu animation (incl. reduced-motion), focus-ring visibility, 404 page visual check, and a Lighthouse Performance ≥85 spot-check. All require a running dev server and/or browser, which this verification pass could not execute.

### Gaps Summary

No structural gaps. All must-have truths, artifacts, and key links from the four plans (01.1-01 through 01.1-04) are present and correctly wired in the current codebase. The one truth that literally fails as written (`build`/`check-types` exit 0) is excused via a documented override — it is a pre-existing, project-wide `csstype` version-duplication issue confined to `packages/ui/src/components/shadcn-ui/*`, first flagged in Phase 1, tracked continuously through every one of this phase's four plans' `deferred-items.md` entries, and not caused by any file this phase created or modified; `lint` (this phase's own gate) passes cleanly.

The phase's only genuine, disclosed deviation is the THEME-03 regression (dark-mode toggle removed from the premium header) — already accurately reflected in `REQUIREMENTS.md`, not a failure of this phase's own DESIGN-01/02/03 scope, but flagged here per the orchestrator's explicit instruction so it isn't silently lost.

Status is `human_needed` rather than `passed` solely because runtime/visual/Lighthouse verification (mobile-menu animation quality, focus-ring visibility, actual rendered palette, Performance ≥85) requires a browser session this verification pass cannot provide — all of it was also flagged as outstanding by the phase's own 01.1-04-SUMMARY.md.

---

*Verified: 2026-08-09*
*Verifier: Claude (gsd-verifier)*
