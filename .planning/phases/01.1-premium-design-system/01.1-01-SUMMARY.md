---
phase: 01.1-premium-design-system
plan: 01
subsystem: ui
tags: [tailwind-v4, css-tokens, next-font, motion, phosphor-icons, design-system]

# Dependency graph
requires:
  - phase: 01-theme-site-shell
    provides: apps/web app shell (globals.css, layout.tsx) and @repo/ui theme.css this plan layers on top of without modifying
provides:
  - apps/web-scoped dt-prefixed Tailwind v4 token set (colors, radius, shadows, type scale, grid/motion constants)
  - Global prefers-reduced-motion CSS baseline
  - Self-hosted Inter heading/body font instances bound to the token set's font variables
  - motion and @phosphor-icons/react installed as direct apps/web dependencies (user-approved)
affects: [01.1-02-premium-primitives, 01.1-03-motion-system, 01.1-04-shell-retrofit]

# Tech tracking
tech-stack:
  added: [motion@13.0.0, "@phosphor-icons/react@2.1.10", "class-variance-authority (direct dep, Rule 3)", "clsx (direct dep, Rule 3)", "tailwind-merge (direct dep, Rule 3)", "radix-ui (direct dep, Rule 3)"]
  patterns:
    - "Dual Tailwind v4 @theme token layering — apps/web's dt-prefixed tokens coexist with @repo/ui's theme.css via a distinctly-namespaced second @theme block, imported after @repo/ui's theme.css in globals.css, zero property-name collisions"
    - "next/font/google Inter (plain, not manually self-hosted 'Inter Display') differentiated by weight/letter-spacing for heading vs body registers, per D-08's explicit fallback allowance"

key-files:
  created: [apps/web/app/premium-theme.css, apps/web/app/fonts.ts]
  modified: [apps/web/app/globals.css, apps/web/package.json, pnpm-lock.yaml]

key-decisions:
  - "Used plain next/font/google Inter for both heading and body registers instead of manually self-hosting 'Inter Display' woff2 files from rsms/inter — avoids unverified Cyrillic/weight-coverage risk (RESEARCH.md A1), D-08 explicitly allows plain Inter as acceptable"
  - "Task 1's Package Legitimacy Gate (motion, @phosphor-icons/react) was pre-approved by the user in a prior run that was torn down before any commits landed; re-executed without re-prompting per explicit orchestrator instruction"

patterns-established:
  - "New premium/dt- Tailwind tokens always live in apps/web/app/premium-theme.css, imported after @repo/ui's theme.css — never edit packages/ui/styles/theme.css for apps/web-only styling (D-01/D-02 boundary)"

requirements-completed: [DESIGN-01, DESIGN-03]

coverage:
  - id: D1
    description: "apps/web-scoped dt-prefixed Tailwind v4 token set (colors D-05, radius D-12, shadows D-13, type scale D-09/D-10) with zero namespace collision against packages/ui/styles/theme.css"
    requirement: "DESIGN-01"
    verification:
      - kind: other
        ref: "Task 2 automated verify block (grep-based acceptance criteria + git diff --quiet packages/ui/styles/theme.css) — see 01.1-01-PLAN.md Task 2"
        status: pass
    human_judgment: false
  - id: D2
    description: "Global prefers-reduced-motion CSS baseline collapsing transition/animation durations"
    requirement: "DESIGN-03"
    verification:
      - kind: other
        ref: "grep 'prefers-reduced-motion' apps/web/app/premium-theme.css (Task 2 automated verify)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Self-hosted Inter heading (600/700) and body (400/500) font instances covering latin+cyrillic, bound to --font-dt-heading-raw/--font-dt-body-raw"
    requirement: "DESIGN-03"
    verification:
      - kind: other
        ref: "Task 3 grep-based acceptance criteria — see 01.1-01-PLAN.md Task 3"
        status: pass
      - kind: other
        ref: "pnpm --filter web check-types (next typegen step) — cyrillic subset accepted, no subset-validation error"
        status: pass
    human_judgment: false
  - id: D4
    description: "motion and @phosphor-icons/react installed as direct apps/web dependencies only after explicit human approval of npm registry legitimacy"
    requirement: "DESIGN-01"
    verification: []
    human_judgment: true
    rationale: "Package legitimacy approval is an explicit human judgment call (npmjs.com listing review); this plan run relied on the pre-approval documented in the orchestrator's checkpoint_pre_approval instructions rather than re-prompting, so there is no fresh verification artifact from this run to cite."

# Metrics
duration: ~20min
completed: 2026-08-08
status: complete
---

# Phase 01.1 Plan 01: Design System Foundation (Tokens, Fonts, Deps) Summary

**apps/web-scoped `dt-`-prefixed Tailwind v4 token set (navy/teal/coral/graphite palette, radius, layered shadows, type scale) plus self-hosted Inter heading/body fonts and the `motion`/`@phosphor-icons/react` dependencies, all coexisting with an untouched `packages/ui/styles/theme.css`.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-08-08T19:18:00Z (approx)
- **Completed:** 2026-08-08T19:38:00Z
- **Tasks:** 3 (1 pre-approved checkpoint + 2 auto tasks)
- **Files modified:** 6 (apps/web/package.json, pnpm-lock.yaml, apps/web/app/premium-theme.css, apps/web/app/globals.css, apps/web/app/fonts.ts, .planning/phases/01.1-premium-design-system/deferred-items.md)

## Accomplishments

- Installed `motion@13.0.0` and `@phosphor-icons/react@2.1.10` (user-approved Package Legitimacy Gate) plus 4 Rule-3 transitive dependencies (`class-variance-authority`, `clsx`, `tailwind-merge`, `radix-ui`) as direct `apps/web` dependencies
- Created `apps/web/app/premium-theme.css` — a second, distinctly-namespaced Tailwind v4 `@theme` block (7 `--color-dt-*` colors, 2 `--radius-dt-*`, 3 `--shadow-dt-*`, 5 `--text-dt-*` type-scale tokens with line-height companions, `.dt-caption` utility, `--font-dt-*` mappings, grid/motion custom properties, global `prefers-reduced-motion: reduce` rule) — imported into `globals.css` after `@repo/ui`'s theme, with zero property-name collisions
- Created `apps/web/app/fonts.ts` exporting `interHeading` (600/700) and `interBody` (400/500), both self-hosted via `next/font/google` Inter covering latin+cyrillic subsets, bound to the exact CSS variable names `premium-theme.css` expects
- Verified `packages/ui/styles/theme.css` has zero diff — the D-01/D-02 "packages/ui untouched" boundary holds

## Task Commits

Each task was committed atomically:

1. **Task 1: Package Legitimacy Gate — motion, @phosphor-icons/react** — pre-approved by the user in a prior run (no commit — verification-only task, installs nothing)
2. **Task 2: Install dependencies, create the premium token file, wire it into globals.css** - `8300b90` (feat)
3. **Task 3: Self-host Inter heading/body fonts** - `45d37d3` (feat)

**Plan metadata:** (pending — final docs commit follows this SUMMARY)

## Files Created/Modified

- `apps/web/app/premium-theme.css` - New apps/web-scoped `dt-`-prefixed Tailwind v4 token set (colors, radius, shadows, type scale, grid/motion constants, reduced-motion baseline)
- `apps/web/app/fonts.ts` - New `interHeading`/`interBody` self-hosted Inter font exports
- `apps/web/app/globals.css` - Added `@import './premium-theme.css';` after `@repo/ui`'s theme import
- `apps/web/package.json` - Added `motion`, `@phosphor-icons/react`, `class-variance-authority`, `clsx`, `tailwind-merge`, `radix-ui` as direct dependencies
- `pnpm-lock.yaml` - Lockfile updated for the above installs
- `.planning/phases/01.1-premium-design-system/deferred-items.md` - New file logging the pre-existing, out-of-scope `csstype` check-types failure

## Decisions Made

- Used plain `next/font/google` Inter for both heading and body registers (differentiated by weight/letter-spacing) instead of manually self-hosting "Inter Display" woff2 files from `rsms/inter` — RESEARCH.md Open Question 2's recommended simpler path; D-08 explicitly allows plain Inter, avoiding the unverified Cyrillic/weight-coverage risk flagged in RESEARCH.md Assumption A1
- Re-executed Task 1's checkpoint as pre-approved (per orchestrator instruction) rather than re-prompting the user — a prior worktree reached this same checkpoint and was torn down before any commits landed, so no work was lost or duplicated

## Deviations from Plan

None — plan executed exactly as written. Task 1 was pre-approved per explicit orchestrator instruction rather than re-presented to the user (see `<checkpoint_pre_approval>` in the executor's prompt); this is a resumption of prior-run state, not a deviation from the plan's substance.

## Issues Encountered

- `pnpm --filter web check-types` fails on a pre-existing `csstype@3.1.3`/`3.2.3` duplicate-resolution conflict in `packages/ui/src/components/shadcn-ui/{button-group,calendar,sidebar}.tsx` — all 5 errors are confined to files this plan did not touch; `apps/web/app/fonts.ts` itself is error-free and the `cyrillic` Google Fonts subset was accepted without a subset-validation error (confirming RESEARCH.md's A1 risk did not materialize). This is a known, already-documented blocker (STATE.md "Blockers/Concerns", PROJECT.md "Active" — flagged since Phase 1, requires a monorepo-wide `pnpm.overrides` fix). Logged to `.planning/phases/01.1-premium-design-system/deferred-items.md`; not auto-fixed per the executor's scope-boundary rule (pre-existing, unrelated to this plan's changes).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `apps/web/app/premium-theme.css` and `apps/web/app/fonts.ts` are ready for Plan 02 (premium primitives) and Plan 04 (shell retrofit) to consume — `dt-` token utilities (`bg-dt-navy`, `text-dt-coral`, `rounded-dt-card`, `shadow-dt-card`, `text-dt-h1`, etc.) and the `interHeading`/`interBody` font exports are all in place.
- `motion` and `@phosphor-icons/react` are installed and available for Plan 03 (motion system) and any plan needing new iconography.
- Blocker: the pre-existing `csstype` duplicate-resolution conflict still blocks a clean `pnpm --filter web check-types`/`build` — unrelated to this plan, tracked project-wide, recommended to land before/during Phase 2 per PROJECT.md.

---
*Phase: 01.1-premium-design-system*
*Completed: 2026-08-08*
