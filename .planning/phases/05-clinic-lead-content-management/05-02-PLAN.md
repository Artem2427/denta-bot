---
phase: 05-clinic-lead-content-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/package.json
  - packages/ui/src/components/shadcn-ui/form.tsx
  - packages/ui/src/components/shadcn-ui/data-table.tsx
  - packages/ui/index.tsx
autonomous: true
requirements: [INFRA-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "packages/ui exports a Form/FormField/FormItem/FormLabel/FormControl/FormDescription/FormMessage set built on react-hook-form's Controller/FormProvider + this package's existing Label/Slot conventions, so every Phase 5 create/edit form (Clinic, Lead status, BlogPost, PricingPlan) binds through one shared primitive instead of 4 hand-rolled wirings (per UI-SPEC Gap 1)"
    - "packages/ui exports a DataTable + DataTableColumnHeader (sortable header button) + DataTablePagination (wraps the existing Pagination primitive) composed entirely from the existing Table/TableHeader/TableBody/TableRow/TableHead/TableCell primitives and @tanstack/react-table — no bespoke <table> markup duplicated elsewhere (per UI-SPEC Gap 2)"
    - "Neither new primitive introduces a new color token or overrides packages/ui/styles/theme.css — both inherit --primary/--border/--muted/--destructive exactly as every other packages/ui/src/components/shadcn-ui/* primitive does"
    - "DataTable's scope this phase is exactly DataTable + DataTableColumnHeader + DataTablePagination — no column-visibility toggle, row-selection, or drag-reorder (UI-SPEC's explicit 'do not over-build' scope)"
  artifacts:
    - path: "packages/ui/src/components/shadcn-ui/form.tsx"
      provides: "Form (FormProvider), FormField, FormItem, FormLabel, FormControl, FormDescription, FormMessage, useFormField"
      exports: "Form, FormField, FormItem, FormLabel, FormControl, FormDescription, FormMessage, useFormField"
    - path: "packages/ui/src/components/shadcn-ui/data-table.tsx"
      provides: "TanStack-Table-backed table composition + sortable column header + pagination footer"
      exports: "DataTable, DataTableColumnHeader, DataTablePagination"
    - path: "packages/ui/index.tsx"
      provides: "barrel re-exports for both new primitives"
      contains: "shadcn-ui/form"
  key_links:
    - from: "packages/ui/src/components/shadcn-ui/form.tsx"
      to: "packages/ui/src/components/shadcn-ui/label.tsx"
      via: "FormLabel wraps Label, marking aria-invalid via useFormField's error state"
      pattern: "FormLabel"
    - from: "packages/ui/src/components/shadcn-ui/data-table.tsx"
      to: "packages/ui/src/components/shadcn-ui/table.tsx"
      via: "composition — Table/TableHeader/TableBody/TableRow/TableHead/TableCell"
      pattern: "flexRender"
    - from: "packages/ui/src/components/shadcn-ui/data-table.tsx"
      to: "packages/ui/src/components/shadcn-ui/pagination.tsx"
      via: "DataTablePagination wraps Pagination/PaginationContent/PaginationItem/PaginationPrevious/PaginationNext"
      pattern: "DataTablePagination"
    - from: "packages/ui/index.tsx"
      to: "packages/ui/src/components/shadcn-ui/form.tsx, data-table.tsx"
      via: "export * from"
      pattern: "export * from './src/components/shadcn-ui/form'"
  prohibitions:
    - statement: "MUST NOT add react-hook-form or @tanstack/react-table as a dependency of apps/platform-admin directly for these primitives — react-hook-form is added to packages/ui/package.json (the primitive's own package); @tanstack/react-table is already a packages/ui dependency"
      status: resolved
      verification: test
    - statement: "MUST NOT introduce a new third-party form/table registry or library (e.g. react-table's standalone v7, Formik) — Form is hand-ported from shadcn's own official, unmodified registry source; DataTable is hand-composed from primitives already in this package"
      status: resolved
      verification: test
---

<objective>
Add the two `@repo/ui` primitives every Phase 5 screen depends on — `Form` (react-hook-form binding) and `DataTable` (TanStack Table composition) — before any CRUD screen work begins, per UI-SPEC's explicit "build these before any CRUD screen task" directive (Gaps 1 and 2). This plan has no dependency on the backend and can run fully in parallel with Plan 05-01.

Purpose: Give every later frontend plan (05-05 through 05-07) one shared, already-audited form-binding and data-table primitive instead of 4 independent hand-rolled implementations.
Output: `packages/ui`'s `form.tsx` and `data-table.tsx`, barrel-exported alongside the package's other 35 shadcn primitives.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-clinic-lead-content-management/05-UI-SPEC.md
@.planning/phases/05-clinic-lead-content-management/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Form primitive (react-hook-form binding)</name>
  <files>packages/ui/package.json, packages/ui/src/components/shadcn-ui/form.tsx, packages/ui/index.tsx</files>
  <read_first>
    packages/ui/src/components/shadcn-ui/label.tsx (full file — this package's cn()/Radix Label composition convention this task's FormLabel follows)
    packages/ui/src/components/shadcn-ui/button.tsx (data-slot + cn() convention every primitive in this directory follows)
    packages/ui/src/lib/utils.ts (cn() helper — `import { cn } from '../../lib/utils'`)
    packages/ui/package.json (current dependencies — react-hook-form is NOT present yet)
    packages/ui/index.tsx (current alphabetized export list — insertion point for the new `form` line, between `dropdown-menu` and `hover-card`)
  </read_first>
  <action>
    Run `pnpm --filter @repo/ui add react-hook-form@^7` (matches `apps/web`'s existing pin per CLAUDE.md's forms constraint; not `^8` or unpinned).

    Create packages/ui/src/components/shadcn-ui/form.tsx as a hand-port of shadcn's official, unmodified `form.tsx` registry source (standard `Form`/`FormField`/`FormItem`/`FormLabel`/`FormControl`/`FormDescription`/`FormMessage` set), adapted only to match this package's local conventions: import `Slot` from `'radix-ui'` (use `Slot.Root`, matching button.tsx's `Slot.Root` usage — not the standalone `@radix-ui/react-slot` package) and `Label as LabelPrimitive` or the local `Label` component from `./label` for `FormLabel`'s underlying element; import `cn` from `'../../lib/utils'`. The module: `Form` is a direct re-export of react-hook-form's `FormProvider`. A `FormFieldContext` (React context carrying the current field's `name`) and a `FormField` component that wraps react-hook-form's `Controller`, providing that context. A `useFormField()` hook reading both `FormFieldContext` and `FormItemContext` plus `useFormContext()`/`useFormState()` to compute `{ id, name, formItemId, formDescriptionId, formMessageId, ...fieldState }`. `FormItem` provides a `FormItemContext` (carrying a generated `id` via `React.useId()`) and renders a `<div data-slot="form-item" className={cn('grid gap-2', className)}>`. `FormLabel` renders the local `Label` component, `data-slot="form-label"`, `htmlFor={formItemId}`, and `data-error={!!error}` driving a `text-destructive` className when `useFormField()`'s `error` is set. `FormControl` renders `<Slot.Root data-slot="form-control" id={formItemId} aria-describedby={...} aria-invalid={!!error} {...props} />`. `FormDescription` renders `<p data-slot="form-description" id={formDescriptionId} className={cn('text-sm text-muted-foreground', className)} />`. `FormMessage` renders the field's error message (or `children`) as `<p data-slot="form-message" id={formMessageId} className={cn('text-sm text-destructive', className)} />`, or `null` if there is no error and no children.

    Edit packages/ui/index.tsx: insert `export * from './src/components/shadcn-ui/form';` in alphabetical position (between the `dropdown-menu` and `empty` lines).
  </action>
  <acceptance_criteria>
    <criterion>packages/ui/package.json's dependencies include `"react-hook-form": "^7"`</criterion>
    <criterion>form.tsx exports exactly: Form, FormField, FormItem, FormLabel, FormControl, FormDescription, FormMessage, useFormField</criterion>
    <criterion>form.tsx imports `cn` from `'../../lib/utils'` and `Slot` from `'radix-ui'`, matching every other primitive in this directory</criterion>
    <criterion>`pnpm --filter @repo/ui run check-types` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
test "$(grep -c 'react-hook-form' packages/ui/package.json)" -ge "1"
test "$(grep -c 'export.*Form\b' packages/ui/src/components/shadcn-ui/form.tsx)" -ge "1"
test "$(grep -c 'FormField' packages/ui/src/components/shadcn-ui/form.tsx)" -ge "1"
test "$(grep -c 'useFormField' packages/ui/src/components/shadcn-ui/form.tsx)" -ge "1"
test "$(grep -c \"shadcn-ui/form\" packages/ui/index.tsx)" -ge "1"
pnpm --filter @repo/ui run check-types
echo PASS
    </automated>
  </verify>
  <done>`@repo/ui` exports a complete, working `Form` binding primitive matching shadcn's standard shape, ready for every Phase 5 create/edit form.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DataTable composition (TanStack Table wrapper)</name>
  <files>packages/ui/src/components/shadcn-ui/data-table.tsx, packages/ui/index.tsx</files>
  <read_first>
    packages/ui/src/components/shadcn-ui/table.tsx (full file — Table/TableHeader/TableBody/TableRow/TableHead/TableCell this task composes)
    packages/ui/src/components/shadcn-ui/pagination.tsx (full file — Pagination/PaginationContent/PaginationItem/PaginationLink/PaginationPrevious/PaginationNext this task's DataTablePagination wraps)
    packages/ui/src/components/shadcn-ui/button.tsx (buttonVariants — DataTableColumnHeader's sort-toggle button uses the `ghost` variant + `size="sm"`)
    packages/ui/package.json (confirms `@tanstack/react-table@^8.21.3` is already a dependency — no install needed)
  </read_first>
  <action>
    Create packages/ui/src/components/shadcn-ui/data-table.tsx with 3 exports, scoped exactly per UI-SPEC's Gap 2 (no column-visibility, no row-selection, no drag-reorder):

    `DataTable<TData, TValue>({ columns, data, isLoading }: { columns: ColumnDef<TData, TValue>[]; data: TData[]; isLoading?: boolean })` — calls `useReactTable({ data, columns, getCoreRowModel: getCoreRowModel(), getSortedRowModel: getSortedRowModel() })` (both from `@tanstack/react-table`), renders the existing `Table`/`TableHeader`/`TableBody`/`TableRow`/`TableHead`/`TableCell` primitives, mapping `table.getHeaderGroups()` to `TableHead` cells via `flexRender(header.column.columnDef.header, header.getContext())` and `table.getRowModel().rows` to `TableRow`/`TableCell` via `flexRender(cell.column.columnDef.cell, cell.getContext())`. When `data.length === 0` and not `isLoading`, render a single full-width `TableCell` with `colSpan={columns.length}` — the caller is responsible for rendering the actual `Empty`/`Skeleton` states around `DataTable` (this primitive does not own empty/loading UI itself, keeping it a pure table renderer).

    `DataTableColumnHeader<TData, TValue>({ column, title }: { column: Column<TData, TValue>; title: string })` — if `!column.getCanSort()`, renders plain `title` text; otherwise renders a `Button` (`variant="ghost"`, `size="sm"`, `onClick={() => column.toggleSorting(column.getIsSorted() === 'asc')}`) containing `title` plus a `lucide-react` `ChevronsUpDownIcon`/`ArrowUpIcon`/`ArrowDownIcon` (chosen by `column.getIsSorted()`'s value: `false` -> `ChevronsUpDownIcon`, `'asc'` -> `ArrowUpIcon`, `'desc'` -> `ArrowDownIcon`).

    `DataTablePagination<TData>({ table }: { table: Table<TData> })` — wraps the existing `Pagination`/`PaginationContent`/`PaginationItem`/`PaginationPrevious`/`PaginationNext` primitives: a page-position label (`Page {table.getState().pagination.pageIndex + 1} of {table.getPageCount()}`), `PaginationPrevious` `onClick={() => table.previousPage()}` disabled via `!table.getCanPreviousPage()`, `PaginationNext` `onClick={() => table.nextPage()}` disabled via `!table.getCanNextPage()`.

    Edit packages/ui/index.tsx: insert `export * from './src/components/shadcn-ui/data-table';` in alphabetical position (between `checkbox` and `dialog`).
  </action>
  <acceptance_criteria>
    <criterion>data-table.tsx exports exactly: DataTable, DataTableColumnHeader, DataTablePagination</criterion>
    <criterion>DataTable imports Table/TableHeader/TableBody/TableRow/TableHead/TableCell from `./table`, not a re-implementation</criterion>
    <criterion>DataTablePagination imports Pagination/PaginationContent/PaginationItem/PaginationPrevious/PaginationNext from `./pagination`</criterion>
    <criterion>no column-visibility, row-selection, or drag-reorder code exists in data-table.tsx</criterion>
    <criterion>`pnpm --filter @repo/ui run check-types` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
test "$(grep -c 'export function DataTable\|export.*DataTable' packages/ui/src/components/shadcn-ui/data-table.tsx)" -ge "1"
test "$(grep -c 'DataTableColumnHeader' packages/ui/src/components/shadcn-ui/data-table.tsx)" -ge "1"
test "$(grep -c 'DataTablePagination' packages/ui/src/components/shadcn-ui/data-table.tsx)" -ge "1"
test "$(grep -c \"from './table'\" packages/ui/src/components/shadcn-ui/data-table.tsx)" -ge "1"
test "$(grep -c \"from './pagination'\" packages/ui/src/components/shadcn-ui/data-table.tsx)" -ge "1"
test "$(grep -c 'getRowSelectionModel\|onRowSelectionChange\|columnVisibility' packages/ui/src/components/shadcn-ui/data-table.tsx)" = "0"
test "$(grep -c \"shadcn-ui/data-table\" packages/ui/index.tsx)" -ge "1"
pnpm --filter @repo/ui run check-types
echo PASS
    </automated>
  </verify>
  <done>`@repo/ui` exports a working, minimally-scoped `DataTable` composition ready for every Phase 5 list screen (Clinics, Leads, Blog Posts, Pricing Plans).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry → packages/ui install | 1 new package (`react-hook-form`) — not in RESEARCH.md's SUS/ASSUMED list (already an established repo-wide dependency of `apps/web`, just newly added to this package) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-05-02-01 | Tampering (supply chain) | `react-hook-form` install into `packages/ui` | low | accept | Already a vetted, actively-used dependency elsewhere in this monorepo (`apps/web/package.json`); adding it to a second workspace package carries no new trust exposure |
| T-05-02-02 | Information Disclosure | `FormMessage` rendering field-level validation errors | low | accept | Validation error strings are developer-authored (zod schema messages), never raw server error text or stack traces |

</threat_model>

<verification>
- Task 1's automated script proves `Form`'s full export surface and type-checks cleanly
- Task 2's automated script proves `DataTable`'s scope stays exactly to spec (no column-visibility/row-selection/drag-reorder) and type-checks cleanly
</verification>

<success_criteria>
`@repo/ui` ships working `Form` and `DataTable` primitives, indistinguishable in convention from the package's other 35 shadcn primitives, ready for every Phase 5 frontend plan to consume — INFRA-04's "typed client + TanStack Query" screens have their rendering primitives in place before any screen work starts.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `packages/ui/src/components/shadcn-ui/form.tsx` (new) — `Form`, `FormField`, `FormItem`, `FormLabel`, `FormControl`, `FormDescription`, `FormMessage`, `useFormField`
- `packages/ui/src/components/shadcn-ui/data-table.tsx` (new) — `DataTable`, `DataTableColumnHeader`, `DataTablePagination`
- `packages/ui/package.json` (edited) — adds `react-hook-form@^7`
- `packages/ui/index.tsx` (edited) — barrel exports for both new primitives

<output>
Create `.planning/phases/05-clinic-lead-content-management/05-02-SUMMARY.md` when done
</output>
</output>
