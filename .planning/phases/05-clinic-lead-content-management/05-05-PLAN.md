---
phase: 05-clinic-lead-content-management
plan: 05
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - apps/platform-admin/package.json
  - apps/platform-admin/.env.example
  - apps/platform-admin/src/index.css
  - apps/platform-admin/src/App.tsx
  - apps/platform-admin/src/App.css
  - apps/platform-admin/src/main.tsx
  - apps/platform-admin/src/router.tsx
  - apps/platform-admin/src/lib/api/schema.d.ts
  - apps/platform-admin/src/lib/api/client.ts
  - apps/platform-admin/src/lib/auth/auth-store.ts
  - apps/platform-admin/src/modules/auth/login-page.tsx
  - apps/platform-admin/src/shared/components/app-shell.tsx
  - apps/platform-admin/src/modules/clinics/use-clinics.ts
  - apps/platform-admin/src/modules/clinics/clinics-list-page.tsx
  - apps/platform-admin/src/modules/clinics/clinic-form-dialog.tsx
  - apps/platform-admin/src/modules/clinics/clinic-detail-page.tsx
  - package.json
autonomous: false

requirements: [CLINIC-01, CLINIC-02, CLINIC-03, CLINIC-04, CLINIC-05, INFRA-04, INFRA-05]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "apps/platform-admin bootstraps QueryClientProvider -> RouterProvider in main.tsx; every list/detail screen fetches via TanStack Query against a client typed from openapi-typescript's generated schema.d.ts, never a hand-maintained interface (INFRA-04)"
    - "A PlatformAdmin who is not logged in and requests any / route is redirected to /login; the access token lives only in memory (never localStorage) and a page reload triggers one silent POST /auth/refresh via the httpOnly cookie before falling back to a login redirect"
    - "The frontend never issues two concurrent POST /auth/refresh calls — every concurrent 401 handler awaits one shared in-flight refresh promise (RESEARCH.md Pitfall 2), verified by triggering 2 simultaneous 401-triggering requests and confirming exactly 1 refresh call fires"
    - "apps/platform-admin/src/index.css no longer contains the Vite-starter #root{width:1126px;text-align:center;...} rule or the unused --text/--accent/--shadow/--code-bg custom properties; the dashboard shell is full-width, top-left-aligned, min-height:100svh"
    - "Clinics list screen: zero clinics ever renders Empty/EmptyTitle/EmptyDescription with 'No clinics yet' + an 'Add Clinic' CTA; initial fetch renders Skeleton rows; a fetch failure renders 'Couldn't load clinics.' + a Retry button that re-triggers the query (CLINIC-01, UI Considerations)"
    - "Clinics list uses the new @repo/ui DataTable with a status column rendered via Badge (trial=outline, active=default, suspended=destructive, cancelled=secondary per UI-SPEC's Color section) and a status Select filter wired to GET /clinics?status= server-side, not client-side array filtering (CLINIC-05)"
    - "Filtering the Clinics list to a status with zero matches shows a distinct 'No clinics match these filters' message + 'Clear filters' link, never conflated with the true-zero-clinics-ever empty state"
    - "Long clinic names in DataTable cells truncate with a native title tooltip on hover; full text is always visible on the clinic's own detail page (UI Considerations: overflow)"
    - "Add Clinic (Dialog + Form, per @repo/ui's new Form primitive) validates client-side via zod mirroring CreateClinicDto's class-validator rules field-for-field (name/email/plan required, phone optional); a 409 from a duplicate email surfaces as an inline/toast error, never a silent failure (CLINIC-03)"
    - "Clinic detail page inline-edits name/email/phone/status/plan via 'Save Changes' (PATCH), and always renders messageCount/bookingsCount as literal 0 and lastActiveAt as 'Never active' when null, never blank/undefined (CLINIC-02, CLINIC-04)"
    - "Every Clinic detail view renders 'Last updated by {admin email} on {date}' (date-fns formatted MMM d, yyyy 'at' h:mm a) below the title in text-sm text-muted-foreground; when updatedById is null it renders 'Not yet updated' instead of a blank/broken name (INFRA-05)"
    - "Save/Convert/Delete-style mutation buttons show an inline Spinner and disable themselves for the duration of the mutation's isPending state (UI Considerations: loading)"
    - "No apps/web dt-* class or Premium* component is imported anywhere under apps/platform-admin/src; no --brand color token is used anywhere in apps/platform-admin (UI-SPEC's locked directive)"
    - statement: "AppShell's Sidebar nav labels ('Clinics', 'Leads', 'Content') and any Breadcrumb labels are fixed, short, static strings with no dynamic overflow risk at spec time — not independently overflow-tested until the executor builds the actual nav copy"
      verification: backstop
  artifacts:
    - path: "apps/platform-admin/src/lib/api/client.ts"
      provides: "openapi-fetch typed client with Authorization-header injection + single-in-flight-promise 401 refresh-retry"
      exports: "api"
    - path: "apps/platform-admin/src/lib/auth/auth-store.ts"
      provides: "in-memory access token holder, login(), logout(), refreshAccessToken() (shared in-flight promise)"
      exports: "getAccessToken, setAccessToken, login, logout, refreshAccessToken"
    - path: "apps/platform-admin/src/router.tsx"
      provides: "createBrowserRouter tree: /login (public) + / (authLoader-guarded AppShell layout) with a clinics child route"
      exports: "router"
    - path: "apps/platform-admin/src/shared/components/app-shell.tsx"
      provides: "Sidebar nav (Clinics/Leads/Content) + header + logout, composed entirely from @repo/ui Sidebar primitives"
      exports: "AppShell"
    - path: "apps/platform-admin/src/modules/clinics/use-clinics.ts"
      exports: "useClinics, useClinic, useCreateClinic, useUpdateClinic"
    - path: "apps/platform-admin/src/modules/clinics/clinics-list-page.tsx"
      exports: "ClinicsListPage"
    - path: "apps/platform-admin/src/modules/clinics/clinic-detail-page.tsx"
      exports: "ClinicDetailPage"
  key_links:
    - from: "apps/platform-admin/src/main.tsx"
      to: "apps/platform-admin/src/router.tsx"
      via: "RouterProvider(router) inside QueryClientProvider"
      pattern: "RouterProvider"
    - from: "apps/platform-admin/src/lib/api/client.ts"
      to: "apps/platform-admin/src/lib/auth/auth-store.ts"
      via: "onRequest reads getAccessToken(); onResponse 401-handler calls refreshAccessToken()"
      pattern: "refreshAccessToken"
    - from: "apps/platform-admin/src/modules/clinics/use-clinics.ts"
      to: "apps/platform-admin/src/lib/api/client.ts"
      via: "api.GET('/clinics', ...) / api.POST('/clinics', ...) wrapped in useQuery/useMutation"
      pattern: "api.GET"
    - from: "apps/platform-admin/src/modules/clinics/clinics-list-page.tsx"
      to: "packages/ui (DataTable)"
      via: "import { DataTable, DataTableColumnHeader } from '@repo/ui'"
      pattern: "DataTable"
  prohibitions:
    - statement: "MUST NOT install react-router unpinned/at latest (resolves to v8, which needs Node >=22.22.0, above this repo's floor) — pinned to ^7 in package.json"
      status: resolved
      verification: test
    - statement: "MUST NOT store the access token in localStorage/sessionStorage — it lives only in an in-memory module variable in auth-store.ts, lost on reload by design (refreshed silently via the httpOnly cookie instead)"
      status: resolved
      verification: test
    - statement: "MUST NOT issue a second concurrent POST /auth/refresh while one is already in flight — every 401 handler awaits the SAME shared promise (RESEARCH.md Pitfall 2); a naive per-request refresh call would trigger Phase 4's reuse-detection and force-logout the admin mid-session"
      status: resolved
      verification: test
    - statement: "MUST NOT hand-write a TypeScript interface for any /clinics response shape instead of consuming the openapi-typescript-generated schema.d.ts types"
      status: resolved
      verification: test
    - statement: "MUST NOT use apps/web's dt-* classes, Premium* components, or the --brand color token anywhere under apps/platform-admin/src — UI-SPEC's locked directive confines this app to @repo/ui only"
      status: resolved
      verification: test
    - statement: "MUST NOT build a one-off table/form component inside apps/platform-admin duplicating what @repo/ui's DataTable/Form primitives (Plan 05-02) already provide"
      status: resolved
      verification: test
---

<objective>
Bootstrap `apps/platform-admin` from an untouched Vite scaffold into a working authenticated SPA — React Router (auth-guarded layout route), TanStack Query, an `openapi-typescript`-generated typed client, and a shared single-in-flight-promise refresh interceptor — then build the phase's first fully working screen: the Clinics list + detail + create/edit, using `@repo/ui`'s new `DataTable`/`Form` primitives against the real `/clinics` API. This is the phase's tracer bullet: the one path that proves every new architectural layer (DB -> API -> typed client -> TanStack Query -> auth-guarded routing -> `@repo/ui` primitives -> real screen) wired together end-to-end.

Purpose: Prove the riskiest, entirely-new-to-this-repo architecture on the simplest resource before Plans 05-06/05-07 replicate the same pattern for Leads and Content.
Output: A logged-in-only `apps/platform-admin` with a working Clinics list (filterable, paginated via `DataTable`), detail (inline-editable, trace field), and create dialog.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-clinic-lead-content-management/05-RESEARCH.md
@.planning/phases/05-clinic-lead-content-management/05-PATTERNS.md
@.planning/phases/05-clinic-lead-content-management/05-UI-SPEC.md
@.planning/phases/05-clinic-lead-content-management/05-01-PLAN.md
@.planning/phases/05-clinic-lead-content-management/05-02-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint — react-router, @tanstack/react-query, @tanstack/react-query-devtools</name>
  <what-built>Nothing yet — this checkpoint runs BEFORE Task 2 installs 3 packages RESEARCH.md's Package Legitimacy Audit flagged `[SUS]` (too-new-publish-date heuristic only, not a real trust signal): `react-router`, `@tanstack/react-query`, `@tanstack/react-query-devtools`.</what-built>
  <how-to-verify>
    Open each URL and confirm the package resolves to its expected official org with a multi-million weekly download count:
    - https://www.npmjs.com/package/react-router — expect `remix-run/react-router`, pin `^7` (NOT `^8`/`latest` — Node floor mismatch, see RESEARCH.md Pitfall 1)
    - https://www.npmjs.com/package/@tanstack/react-query — expect `TanStack/query`
    - https://www.npmjs.com/package/@tanstack/react-query-devtools — expect `TanStack/query`
  </how-to-verify>
  <resume-signal>Type "approved" once all 3 are confirmed, or describe any discrepancy found.</resume-signal>
</task>

<task type="tracer" tdd="false">
  <name>Task 2: Bootstrap — router, TanStack Query, typed client, auth store, AppShell, Clinics list</name>
  <precondition>Task 1's package-legitimacy checkpoint is approved; apps/server is running locally with ClinicsModule/LeadsModule/BlogPostsModule/PricingPlansModule all registered (Plans 05-01/05-03/05-04), since codegen reads the live full OpenAPI surface</precondition>
  <files>apps/platform-admin/package.json, apps/platform-admin/.env.example, apps/platform-admin/src/index.css, apps/platform-admin/src/App.tsx, apps/platform-admin/src/App.css, apps/platform-admin/src/main.tsx, apps/platform-admin/src/router.tsx, apps/platform-admin/src/lib/api/schema.d.ts, apps/platform-admin/src/lib/api/client.ts, apps/platform-admin/src/lib/auth/auth-store.ts, apps/platform-admin/src/modules/auth/login-page.tsx, apps/platform-admin/src/shared/components/app-shell.tsx, apps/platform-admin/src/modules/clinics/use-clinics.ts, apps/platform-admin/src/modules/clinics/clinics-list-page.tsx, package.json</files>
  <read_first>
    apps/platform-admin/src/main.tsx, App.tsx, index.css, package.json, vite.config.ts (current scaffold — everything this task replaces)
    packages/ui/index.tsx (confirms Sidebar*, DataTable, Table, Badge, Select, Skeleton, Empty*, Button, Avatar, DropdownMenu are all available imports from `@repo/ui`)
    packages/ui/src/components/shadcn-ui/data-table.tsx (Plan 05-02 output — DataTable/DataTableColumnHeader/DataTablePagination's exact prop shapes)
    .planning/phases/05-clinic-lead-content-management/05-RESEARCH.md (System Architecture Diagram, Pattern 2, Pitfall 1/2/4/5/6, "React Router v7 auth-guarded layout route" code example)
    .planning/phases/05-clinic-lead-content-management/05-UI-SPEC.md (Color section's status->Badge mapping, Copywriting Contract, Component Inventory)
    root package.json (current `dev:admin` script — stale filter, RESEARCH.md Pitfall 5)
  </read_first>
  <action>
    **A. Root/package fixes.** Edit root package.json: add `"dev:platform-admin": "turbo dev --filter=platform-admin"`; remove or fix the stale `"dev:admin": "turbo dev --filter=admin-panel"` entry (RESEARCH.md Pitfall 5 — no workspace package is named `admin-panel` anymore).

    **B. Install dependencies.** Run `pnpm --filter platform-admin add react-router@^7 @tanstack/react-query openapi-fetch react-hook-form@^7 zod@^3 @hookform/resolvers@^3` and `pnpm --filter platform-admin add -D openapi-typescript @tanstack/react-query-devtools`.

    **C. index.css cleanup (RESEARCH.md Pitfall 4).** Delete the `#root { width: 1126px; max-width: 100%; margin: 0 auto; text-align: center; border-inline: ...; }` block and the unused `--text`/`--text-h`/`--bg`/`--border`/`--code-bg`/`--accent`/`--accent-bg`/`--accent-border`/`--social-bg`/`--shadow`/`--sans`/`--heading`/`--mono` custom properties, plus the dark-mode `@media (prefers-color-scheme: dark)` block and the `h1`/`h2`/`code`/`.counter` rules that reference them — all leftover from the old `apps/admin-panel` scaffold. Replace `#root` with a minimal `#root { min-height: 100svh; display: flex; flex-direction: column; }` (no width cap, no text-align). Keep line 1 (`@import 'tailwindcss';`), line 2 (`@import '@repo/ui/styles/theme.css';`), the `@source` line, the `*,*::before,*::after { box-sizing: border-box; }` rule, and the `body { margin: 0; font-family: system-ui, ...; }` rule exactly as-is — these are already correct.

    Delete apps/platform-admin/src/App.tsx and App.css entirely (superseded by router.tsx/AppShell).

    **D. Typed API client.** Create apps/platform-admin/.env.example documenting `VITE_API_URL=http://localhost:4000`. Add a `"generate:api": "openapi-typescript http://localhost:4000/api/docs-json -o src/lib/api/schema.d.ts"` script to apps/platform-admin/package.json. With apps/server running locally (all 4 resource modules registered), run it once to generate apps/platform-admin/src/lib/api/schema.d.ts (committed, not gitignored — regenerate via this script whenever the backend's DTOs change).

    Create apps/platform-admin/src/lib/api/client.ts: `export const api = createClient<paths>({ baseUrl: import.meta.env.VITE_API_URL ?? 'http://localhost:4000', credentials: 'include' })` (import `createClient` default-export from `openapi-fetch`, `paths` type from `./schema`). Register two middleware via `api.use({...})`: `onRequest({ request })` sets `Authorization: Bearer ${token}` from `getAccessToken()` when a token exists; `onResponse({ request, response })` — if `response.status === 401` and the request URL does not already target `/auth/login` or `/auth/refresh`, calls `await refreshAccessToken()`; if it returns a token, retries the original request once with the new `Authorization` header (`fetch(request)` after mutating the header) and returns that response instead; otherwise returns the original 401 response unchanged.

    Create apps/platform-admin/src/lib/auth/auth-store.ts: a module-level `let accessToken: string | null = null` plus `getAccessToken()`/`setAccessToken(token)`. A module-level `let refreshPromise: Promise<string | null> | null = null` and `async function refreshAccessToken(): Promise<string | null>` — if `refreshPromise` is already set, return it as-is (do NOT start a second call); otherwise assign `refreshPromise = fetch(\`${baseUrl}/auth/refresh\`, { method: 'POST', credentials: 'include' })...` parsing the JSON body's `accessToken` on success, calling `setAccessToken(null)` on any non-2xx response, and in a `.finally()` clearing `refreshPromise = null` so a future call can start fresh — this single-shared-in-flight-promise shape is RESEARCH.md Pitfall 2's exact required mitigation. Export `async function login(email: string, password: string)` calling `api.POST('/auth/login', { body: { email, password } })`, setting the access token from the response on success and throwing on failure. Export `async function logout()` calling `api.POST('/auth/logout')` then `setAccessToken(null)`.

    **E. Router + AppShell + LoginPage.** Create apps/platform-admin/src/router.tsx per RESEARCH.md's "React Router v7 auth-guarded layout route" example: an `authLoader` that calls `getAccessToken()`; if falsy, `await refreshAccessToken()`; if STILL falsy after that, `throw redirect('/login')`. `createBrowserRouter([{ path: '/login', Component: LoginPage }, { path: '/', loader: authLoader, Component: AppShell, children: [{ index: true, loader: () => redirect('/clinics') }, { path: 'clinics', Component: ClinicsListPage }] }])`.

    Create apps/platform-admin/src/shared/components/app-shell.tsx: `AppShell` composes `SidebarProvider`/`Sidebar`/`SidebarHeader`/`SidebarContent`/`SidebarMenu`/`SidebarMenuItem`/`SidebarMenuButton`/`SidebarFooter`/`SidebarInset`/`SidebarTrigger` from `@repo/ui`. `SidebarHeader` shows "DentaBot Admin" (no logo asset this phase). `SidebarContent` renders 3 `SidebarMenuButton`s as React Router `NavLink`s: "Clinics" -> `/clinics`, "Leads" -> `/leads`, "Content" -> `/content/blog` (the last two routes don't exist yet until Plans 05-06/05-07 land — that's expected, this task only wires the nav shell). `SidebarFooter` shows an `Avatar`/`DropdownMenu` with a "Log out" item calling `logout()` then `navigate('/login', { replace: true })`. `SidebarInset` renders `<Outlet />` (React Router) for the active child route.

    Create apps/platform-admin/src/modules/auth/login-page.tsx: a centered `Card` containing a `Form` (the new `@repo/ui` primitive) with `email`/`password` fields validated by a zod schema (`z.object({ email: z.string().email(), password: z.string().min(1) })`), `useForm({ resolver: zodResolver(schema) })`. On submit, calls `login(values.email, values.password)`; on success `navigate('/clinics', { replace: true })`; on failure, shows a `FormMessage`/toast: "Invalid credentials."

    **F. Clinics list (the tracer's actual proof point).** Create apps/platform-admin/src/modules/clinics/use-clinics.ts: `useClinics(status?: string)` — `useQuery({ queryKey: ['clinics', { status }], queryFn: async () => { const { data, error } = await api.GET('/clinics', { params: { query: { status } } }); if (error) throw error; return data; } })`. `useCreateClinic()` — `useMutation` wrapping `api.POST('/clinics', { body })`, `onSuccess` invalidates the `['clinics']` query key via `useQueryClient()`.

    Create apps/platform-admin/src/modules/clinics/clinics-list-page.tsx: page header ("Clinics", Display size per UI-SPEC Typography) + "Add Clinic" `Button`; a status `Select` filter (All / trial / active / suspended / cancelled) driving `useClinics(status)`'s argument; renders `@repo/ui`'s `DataTable` with columns for name, email, a status `Badge` (mapped per UI-SPEC's Color table: trial=outline, active=default, suspended=destructive, cancelled=secondary), and plan — `DataTableColumnHeader` for the sortable `name` column. While `isPending`, renders `Skeleton` rows matching the column count. On `isError`, renders "Couldn't load clinics." + "Something went wrong loading this data." + a "Retry" `Button` calling `refetch()`. When `data` is an empty array AND no status filter is active, renders `Empty`/`EmptyTitle`("No clinics yet")/`EmptyDescription`("Add your first clinic to start tracking accounts.")/`EmptyContent` with an "Add Clinic" CTA. When `data` is empty AND a status filter IS active, renders the distinct "No clinics match these filters" + "Clear filters" message instead. Long `name`/`email` cell values get a `truncate` className + native `title` attribute.
  </action>
  <acceptance_criteria>
    <criterion>apps/platform-admin/src/index.css no longer contains the string `width: 1126px`</criterion>
    <criterion>apps/platform-admin/package.json's dependencies include `"react-router": "^7"` (not `^8` or unpinned)</criterion>
    <criterion>a live, unauthenticated GET to the SPA's `/clinics` route (no access token, no valid refresh cookie) redirects to `/login`</criterion>
    <criterion>a live login followed by GET /clinics renders real data fetched via the generated `paths` types (schema.d.ts references `/clinics` as a path key)</criterion>
    <criterion>triggering 2 simultaneous requests that both 401 results in exactly 1 POST /auth/refresh network call, not 2</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
test "$(grep -c 'width: 1126px' apps/platform-admin/src/index.css)" = "0"
test "$(grep -c '\"react-router\": \"\^7\"' apps/platform-admin/package.json)" -ge "1"
test "$(grep -c \"'/clinics'\" apps/platform-admin/src/lib/api/schema.d.ts)" -ge "1"
test "$(grep -c 'refreshPromise' apps/platform-admin/src/lib/auth/auth-store.ts)" -ge "1"
pnpm --filter platform-admin exec tsc -b
pnpm --filter platform-admin run build
echo PASS
    </automated>
    <human-check>
      <test>With apps/server running (docker compose up -d postgres, pnpm --filter server run start) and apps/platform-admin running (pnpm --filter platform-admin run dev), open http://localhost:5173 in a browser.</test>
      <expected>Unauthenticated: redirected to /login. Logging in with platformadmin@dentabot.dev/DevAdmin!2026 lands on /clinics, showing a real (possibly empty) DataTable, full-width layout (no centered 1126px box). A hard page reload while logged in stays logged in (silent refresh via the cookie), not bounced to /login.</expected>
      <why_human>Visual/interactive confirmation of routing, auth-guard behavior across a real reload, and DataTable rendering — not mechanically provable by a build-only check.</why_human>
    </human-check>
  </verify>
  <done>apps/platform-admin boots as an authenticated SPA with a working, real-data Clinics list rendered through the new DataTable primitive and the generated typed client — the phase's architecture is proven end-to-end.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Clinic create dialog, detail page (inline edit + trace field), status filter completion</name>
  <precondition>Task 2's Clinics list renders live data and the auth-guarded router works</precondition>
  <files>apps/platform-admin/src/modules/clinics/use-clinics.ts, apps/platform-admin/src/modules/clinics/clinics-list-page.tsx, apps/platform-admin/src/modules/clinics/clinic-form-dialog.tsx, apps/platform-admin/src/modules/clinics/clinic-detail-page.tsx, apps/platform-admin/src/router.tsx</files>
  <read_first>
    apps/platform-admin/src/modules/clinics/use-clinics.ts, clinics-list-page.tsx (Task 2 output — this task extends both)
    apps/web/modules/contacts/contact-form.tsx (RHF+zod wiring pattern only — schema shape/useForm/handleSubmit; NOT the JSX/styling, per PATTERNS.md's explicit "reuse the RHF/zod wiring pattern only" guidance)
    .planning/phases/05-clinic-lead-content-management/05-UI-SPEC.md (Copywriting Contract's "Clinics list -> create" and "Clinic detail -> edit" rows; "Trace field display" row)
  </read_first>
  <action>
    Extend use-clinics.ts with `useClinic(id: string)` (`useQuery({ queryKey: ['clinics', id], queryFn: ... api.GET('/clinics/{id}', { params: { path: { id } } }) })`) and `useUpdateClinic()` (`useMutation` wrapping `api.PATCH('/clinics/{id}', { params: { path: { id } }, body })`, invalidating both `['clinics']` and `['clinics', id]` on success).

    Create apps/platform-admin/src/modules/clinics/clinic-form-dialog.tsx: `ClinicFormDialog` renders a `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle`("Add Clinic") wrapping a `Form` (the `@repo/ui` primitive) with fields name/email/phone/plan/status (`Select`, default `trial`), validated by a zod schema mirroring `CreateClinicDto`'s class-validator rules field-for-field (name/email/plan required strings, email format-validated, phone optional, status one of the 4 `ClinicStatus` values). On submit, calls `useCreateClinic()`'s mutate; on a 409 (duplicate email) response, shows an inline `FormMessage` under the email field ("A clinic with this email already exists."); on success, closes the dialog and shows a success toast. The mutation button shows a `Spinner` and disables itself while `isPending`.

    Create apps/platform-admin/src/modules/clinics/clinic-detail-page.tsx: `ClinicDetailPage` reads `:id` from `useParams()`, calls `useClinic(id)`. While pending, renders `Skeleton` blocks matching the `Card` layout. Renders a `Card` with `CardHeader`/`CardTitle`(clinic name)/`CardContent` showing email/phone/plan as inline-editable fields (bound through the same `Form` primitive as the create dialog, defaultValues from the fetched clinic) with a "Save Changes" `Button` calling `useUpdateClinic()`'s mutate — PATCH only sends the fields present in the form's dirty state (react-hook-form's `formState.dirtyFields`), matching CLINIC-04's partial-update semantics. A separate "Bot Usage" section renders `messageCount`/`bookingsCount` as literal numbers (defaulting to `0`, never blank) and `lastActiveAt` as a formatted date via `date-fns`, or "Never active" when `null`. Below the title, renders the trace-field line: "Last updated by {updatedBy?.email ?? 'Not yet updated'}{updatedBy?.email ? \` on ${format(updatedAt, "MMM d, yyyy 'at' h:mm a")}\` : ''}" in `text-sm text-muted-foreground` — note the `/clinics/:id` response must include the related admin's email for this to render (extend `ClinicsService.findOne`/`findAll` in a follow-up if the raw Prisma object doesn't already include it via `include: { updatedBy: { select: { email: true } } }`; if Plan 05-01's `findOne`/`findAll` don't already `include` this relation, add it now as a small service-layer fix in this task, since the frontend contract requires it).

    Wire "Add Clinic" (from Task 2's list page) to open `ClinicFormDialog`. Add a `clinics/:id` child route to router.tsx pointing at `ClinicDetailPage`, and make each `DataTable` row in `clinics-list-page.tsx` navigate to `/clinics/${row.id}` on click.
  </action>
  <acceptance_criteria>
    <criterion>Add Clinic dialog validates required fields client-side before submit and surfaces a 409 duplicate-email error inline</criterion>
    <criterion>Clinic detail page's Bot Usage section never renders a blank/undefined value for messageCount/bookingsCount/lastActiveAt</criterion>
    <criterion>Clinic detail page's trace-field line renders "Not yet updated" when the fetched clinic's updatedBy relation is null, and the admin's email + a formatted date otherwise</criterion>
    <criterion>PATCH from the detail page's Save Changes only includes fields the admin actually changed</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
test "$(grep -c 'Not yet updated' apps/platform-admin/src/modules/clinics/clinic-detail-page.tsx)" -ge "1"
test "$(grep -c 'Never active' apps/platform-admin/src/modules/clinics/clinic-detail-page.tsx)" -ge "1"
test "$(grep -c 'dirtyFields' apps/platform-admin/src/modules/clinics/clinic-detail-page.tsx)" -ge "1"
test "$(grep -c \"clinics/:id\" apps/platform-admin/src/router.tsx)" -ge "1"
pnpm --filter platform-admin exec tsc -b
pnpm --filter platform-admin run build
echo PASS
    </automated>
    <human-check>
      <test>In the running app, click "Add Clinic", submit a valid clinic, then click into its detail row, change only the phone number, and click "Save Changes".</test>
      <expected>The new clinic appears in the list immediately (no manual refresh, TanStack Query invalidation). Detail page shows correct name/email/plan/status, "0"/"0"/"Never active" bot-usage fields, and "Last updated by platformadmin@dentabot.dev on {today's date}". Saving only the phone number does not clear name/email/plan.</expected>
      <why_human>Visual confirmation of the full create->list-refresh->detail->edit round trip.</why_human>
    </human-check>
  </verify>
  <done>Clinics is a fully working CRUD screen — create, list (filterable), and detail (inline-editable with trace field) — CLINIC-01 through CLINIC-05 and the frontend half of INFRA-05 are satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `apps/platform-admin` browser origin (localhost:5173 dev) -> `apps/server` (localhost:4000) | Cross-origin, cookie-credentialed (`credentials: 'include'`) — relies entirely on Phase 4's CORS allowlist and httpOnly refresh cookie; the SPA's own route guard (`authLoader`) is UX-only, never a real security boundary by itself (RESEARCH.md's Architectural Responsibility Map) |
| npm registry -> `apps/platform-admin` installs | 3 packages flagged `SUS` by the legitimacy checker (all false positives per RESEARCH.md), human-reconfirmed via Task 1's checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-05-05-SC | Tampering (supply chain) | `react-router`, `@tanstack/react-query`, `@tanstack/react-query-devtools` npm installs | high | mitigate | Task 1's blocking `checkpoint:human-verify` re-confirms each package's npm registry listing before Task 2 installs them |
| T-05-05-01 | Information Disclosure | Access token storage location | high | mitigate | In-memory only (`auth-store.ts` module variable), never `localStorage`/`sessionStorage` — immune to XSS-driven token exfiltration via storage APIs (though not immune to a live in-page XSS reading the variable directly; no user-controlled HTML is rendered anywhere in this plan's scope) |
| T-05-05-02 | Denial of Service (self-inflicted) | Naive concurrent-401 refresh handling | medium | mitigate | Single shared `refreshPromise` in `auth-store.ts` — every concurrent 401 awaits the same promise instead of independently calling `/auth/refresh`, avoiding Phase 4's reuse-detection force-logout (RESEARCH.md Pitfall 2) |
| T-05-05-03 | Spoofing | The SPA's `authLoader` route guard, treated as a security boundary | medium | mitigate | Documented as UX-only; every actual write still requires a valid access token validated server-side by the unchanged global `AccessTokenGuard` |

</threat_model>

<verification>
- Task 2's automated script proves the scaffold cleanup, the react-router version pin, generated-schema presence, and the shared-refresh-promise code shape, plus a full `tsc -b`/`vite build` pass; its human-check confirms live routing/auth-guard/reload behavior
- Task 3's automated script proves the trace-field null-fallback and dirty-fields-only PATCH logic exist in source; its human-check confirms the full create/list/detail/edit round trip live
</verification>

<success_criteria>
`apps/platform-admin` is a working, authenticated SPA with a complete Clinics CRUD screen built on the new typed-client + TanStack Query + `@repo/ui` DataTable/Form architecture — CLINIC-01 through CLINIC-05, INFRA-04, and the frontend half of INFRA-05 are satisfied, and the pattern is proven for Plans 05-06/05-07 to replicate.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `apps/platform-admin/src/lib/api/schema.d.ts` (new, generated) — `paths` types from the live OpenAPI spec
- `apps/platform-admin/src/lib/api/client.ts` (new) — `api` (typed `openapi-fetch` client + auth/refresh middleware)
- `apps/platform-admin/src/lib/auth/auth-store.ts` (new) — `getAccessToken`, `setAccessToken`, `login`, `logout`, `refreshAccessToken`
- `apps/platform-admin/src/router.tsx` (new) — `router`
- `apps/platform-admin/src/main.tsx` (edited) — `QueryClientProvider` + `RouterProvider`
- `apps/platform-admin/src/shared/components/app-shell.tsx` (new) — `AppShell`
- `apps/platform-admin/src/modules/auth/login-page.tsx` (new) — `LoginPage`
- `apps/platform-admin/src/modules/clinics/use-clinics.ts` (new) — `useClinics`, `useClinic`, `useCreateClinic`, `useUpdateClinic`
- `apps/platform-admin/src/modules/clinics/clinics-list-page.tsx` (new) — `ClinicsListPage`
- `apps/platform-admin/src/modules/clinics/clinic-form-dialog.tsx` (new) — `ClinicFormDialog`
- `apps/platform-admin/src/modules/clinics/clinic-detail-page.tsx` (new) — `ClinicDetailPage`
- `apps/platform-admin/src/index.css` (edited) — scaffold cleanup
- `apps/platform-admin/src/App.tsx`, `App.css` (deleted)
- `package.json` (root, edited) — `dev:platform-admin` script, stale `dev:admin` removed/fixed

<output>
Create `.planning/phases/05-clinic-lead-content-management/05-05-SUMMARY.md` when done
</output>
</output>
