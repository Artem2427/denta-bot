---
phase: 05-clinic-lead-content-management
plan: 06
type: execute
wave: 5
depends_on: ["05-05", "05-03"]
files_modified:
  - apps/platform-admin/src/router.tsx
  - apps/platform-admin/src/modules/leads/use-leads.ts
  - apps/platform-admin/src/modules/leads/leads-inbox-page.tsx
  - apps/platform-admin/src/modules/leads/lead-detail-page.tsx
  - apps/platform-admin/src/modules/leads/date-range-picker.tsx
autonomous: true
requirements: [LEAD-03, LEAD-04, LEAD-05, LEAD-06, LEAD-07, INFRA-04, INFRA-05]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Leads inbox screen: zero leads ever renders 'No leads yet' with no CTA (leads only arrive from apps/web's forms, wired in Phase 6); zero leads matching the active filters renders the distinct 'No leads match these filters' + 'Clear filters' link (LEAD-03)"
    - "Leads inbox DataTable shows a source tag (contacts/demo, Badge variant secondary — visually quieter than the status Badge per UI-SPEC's Color section) alongside a status Badge (new=default, contacted=secondary, converted=outline)"
    - "Leads inbox status filter (Select) and date-range filter (Popover+Calendar composed as a page-level DateRangePicker, not a new @repo/ui primitive) both drive GET /leads query params server-side, not client-side filtering (LEAD-06)"
    - "Lead detail view renders 'Not provided' for any null optional field (email, phone, message, clinicName) — never a blank/undefined display; long Lead.message content renders full-width with whitespace-pre-wrap, no truncation (LEAD-04)"
    - "Lead detail's status Select updates via PATCH /leads/:id/status on change, with no separate Save button per transition; selecting 'converted' triggers the Convert flow (AlertDialog) instead of a plain status write (LEAD-05)"
    - "Convert to Clinic button is disabled with a Tooltip ('Add an email to this lead before converting') when the lead has no email; otherwise clicking it opens a non-destructive AlertDialog ('Convert this lead to a clinic? This creates a new Clinic account from the lead's details.') whose Convert action calls POST /leads/:id/convert (LEAD-07)"
    - "A 409 from POST /leads/:id/convert (duplicate Clinic email) renders inline: 'A clinic with this email already exists.' — never a silent failure or an unhandled toast-only error (LEAD-07, RESEARCH.md Pitfall 3)"
    - "On successful conversion, the leads list and the lead's own detail view both reflect the new 'converted' status and linked clinicId without a manual page refresh (TanStack Query invalidation of both ['leads'] and the specific lead's query key, plus ['clinics'] since a new Clinic now exists)"
    - "Lead detail's trace-field line renders 'Not yet updated' when updatedById is null (the common case for a freshly-arrived, never-touched-by-an-admin Lead) and the admin's email + formatted date once any admin action (status change or convert) has occurred (INFRA-05)"
    - "The Convert AlertDialog's confirm action and the status Select show an inline Spinner and disable themselves for the duration of their mutation's isPending state, matching Plan 05-05's established loading-state pattern (UI Considerations: loading)"
  artifacts:
    - path: "apps/platform-admin/src/modules/leads/use-leads.ts"
      exports: "useLeads, useLead, useUpdateLeadStatus, useConvertLead"
    - path: "apps/platform-admin/src/modules/leads/leads-inbox-page.tsx"
      exports: "LeadsInboxPage"
    - path: "apps/platform-admin/src/modules/leads/lead-detail-page.tsx"
      exports: "LeadDetailPage"
    - path: "apps/platform-admin/src/modules/leads/date-range-picker.tsx"
      exports: "DateRangePicker"
  key_links:
    - from: "apps/platform-admin/src/modules/leads/leads-inbox-page.tsx"
      to: "apps/platform-admin/src/modules/leads/date-range-picker.tsx"
      via: "page-level composition of Popover + Calendar (both @repo/ui primitives)"
      pattern: "DateRangePicker"
    - from: "apps/platform-admin/src/modules/leads/lead-detail-page.tsx"
      to: "apps/platform-admin/src/modules/leads/use-leads.ts"
      via: "useConvertLead() mutation, onSuccess invalidates ['leads'], ['leads', id], and ['clinics']"
      pattern: "useConvertLead"
    - from: "apps/platform-admin/src/router.tsx"
      to: "apps/platform-admin/src/modules/leads/leads-inbox-page.tsx, lead-detail-page.tsx"
      via: "child routes under the authLoader-guarded layout route"
      pattern: "leads"
  prohibitions:
    - statement: "MUST NOT implement the DateRangePicker or the status filter as new @repo/ui primitives — both are page-level composition of existing primitives (Popover+Calendar, Select) per CLAUDE.md's app-specific-composition-only allowance and UI-SPEC's explicit 'compose in apps/platform-admin, not packages/ui' directive"
      status: resolved
      verification: test
    - statement: "MUST NOT allow the Convert-to-Clinic action to fire when the lead has no email — the button is disabled client-side (Tooltip explaining why) in addition to the backend's own 400 rejection (defense in depth, not a substitute for the server-side check from Plan 05-03)"
      status: resolved
      verification: test
    - statement: "MUST NOT silently swallow a 409 from POST /leads/:id/convert — it renders an explicit inline error, never a generic toast-only failure that hides the actual reason"
      status: resolved
      verification: test
---

<objective>
Build the Leads inbox screens — unified list (filterable by status + date), full detail view, status transitions, and the Lead-to-Clinic convert flow — replicating Plan 05-05's established typed-client/TanStack-Query/`@repo/ui` pattern against `LeadsModule` (Plan 05-03).

Purpose: Give PlatformAdmin a working, filterable inbox and a safe conversion path from the frontend, completing LEAD-03 through LEAD-07's UI half.
Output: `/leads` (inbox) and `/leads/:id` (detail) routes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-clinic-lead-content-management/05-RESEARCH.md
@.planning/phases/05-clinic-lead-content-management/05-UI-SPEC.md
@.planning/phases/05-clinic-lead-content-management/05-05-PLAN.md
@.planning/phases/05-clinic-lead-content-management/05-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Leads inbox list (filters) + read-only detail view</name>
  <precondition>Plan 05-05's router.tsx, api client, and DataTable-based Clinics list are live; Plan 05-03's LeadsModule is registered</precondition>
  <files>apps/platform-admin/src/router.tsx, apps/platform-admin/src/modules/leads/use-leads.ts, apps/platform-admin/src/modules/leads/leads-inbox-page.tsx, apps/platform-admin/src/modules/leads/lead-detail-page.tsx, apps/platform-admin/src/modules/leads/date-range-picker.tsx</files>
  <read_first>
    apps/platform-admin/src/modules/clinics/use-clinics.ts, clinics-list-page.tsx, clinic-detail-page.tsx (Plan 05-05 output — the exact hook/list/detail pattern this task mirrors for Leads)
    apps/platform-admin/src/lib/api/schema.d.ts (Plan 05-05 output, regenerated to include `/leads` paths — confirm `/leads`, `/leads/{id}`, `/leads/{id}/status`, `/leads/{id}/convert` are present; if not, re-run `pnpm --filter platform-admin run generate:api` with apps/server running first)
    .planning/phases/05-clinic-lead-content-management/05-UI-SPEC.md (Color section's LeadStatus/LeadSource Badge mapping; Copywriting Contract's Leads rows; Component Inventory's DateRangePicker row)
  </read_first>
  <action>
    If apps/platform-admin/src/lib/api/schema.d.ts doesn't yet include `/leads` paths, regenerate it via `pnpm --filter platform-admin run generate:api` (apps/server must be running with LeadsModule registered).

    Create apps/platform-admin/src/modules/leads/use-leads.ts: `useLeads(status?: string, from?: string, to?: string)` — `useQuery({ queryKey: ['leads', { status, from, to }], queryFn: async () => { const { data, error } = await api.GET('/leads', { params: { query: { status, from, to } } }); if (error) throw error; return data; } })`. `useLead(id: string)` — same shape as `useClinic`. Both mirror `use-clinics.ts`'s exact structure.

    Create apps/platform-admin/src/modules/leads/date-range-picker.tsx: `DateRangePicker({ from, to, onChange }: { from?: Date; to?: Date; onChange: (range: { from?: Date; to?: Date }) => void })` — a `Popover`/`PopoverTrigger` `Button` showing the formatted range (or "Pick a date range"), `PopoverContent` containing a `Calendar` (`mode="range"`, from `react-day-picker`, already a `@repo/ui` dependency) — page-level composition, not a new `packages/ui` primitive.

    Create apps/platform-admin/src/modules/leads/leads-inbox-page.tsx: page header ("Lead Inbox") + status `Select` (All/new/contacted/converted) + `DateRangePicker`, both driving `useLeads(status, from, to)`. `DataTable` columns: name, a source `Badge` (secondary, "Contacts"/"Demo"), a status `Badge` (new=default, contacted=secondary, converted=outline), createdAt (formatted via `date-fns`). Loading -> `Skeleton` rows. Error -> "Couldn't load leads." + Retry. Empty (zero leads ever, no filters active) -> `Empty`/"No leads yet" (no CTA per UI-SPEC). Empty (filtered to zero) -> distinct "No leads match these filters" + "Clear filters" link resetting both filters. Row click navigates to `/leads/${row.id}`.

    Create apps/platform-admin/src/modules/leads/lead-detail-page.tsx: reads `:id` via `useParams()`, calls `useLead(id)`. While pending, `Skeleton` blocks. Renders a `Card` showing name, source `Badge`, and every submitted field (clinicName/email/phone/message) — any null optional field renders literal text "Not provided", never blank. `message` renders full-width with `whitespace-pre-wrap`, no truncation. Below the title, the trace-field line ("Last updated by {updatedBy?.email ?? 'Not yet updated'}..."), matching Plan 05-05's Clinic detail page's exact pattern (if `/leads/:id`'s backend response doesn't already `include: { updatedBy: { select: { email: true } } }`, add that as a small fix to `LeadsService.findOne` in this task, mirroring whatever fix Plan 05-05 applied to `ClinicsService`).

    Add `leads` and `leads/:id` child routes to router.tsx's existing `/` layout route (alongside the `clinics` routes Plan 05-05 added), pointing at `LeadsInboxPage`/`LeadDetailPage`.
  </action>
  <acceptance_criteria>
    <criterion>Leads inbox's true-zero-leads empty state and filtered-to-zero empty state render visibly different copy</criterion>
    <criterion>Lead detail page never renders the literal string "undefined" or a blank cell for a null optional field — "Not provided" appears instead</criterion>
    <criterion>router.tsx contains both `leads` and `leads/:id` routes</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
test "$(grep -c \"'leads'\" apps/platform-admin/src/router.tsx)" -ge "1"
test "$(grep -c \"leads/:id\" apps/platform-admin/src/router.tsx)" -ge "1"
test "$(grep -c 'Not provided' apps/platform-admin/src/modules/leads/lead-detail-page.tsx)" -ge "1"
test "$(grep -c 'No leads match these filters' apps/platform-admin/src/modules/leads/leads-inbox-page.tsx)" -ge "1"
test "$(grep -c 'No leads yet' apps/platform-admin/src/modules/leads/leads-inbox-page.tsx)" -ge "1"
pnpm --filter platform-admin exec tsc -b
pnpm --filter platform-admin run build
echo PASS
    </automated>
  </verify>
  <done>Leads inbox list (filterable by status + date) and detail view work live against real data — LEAD-03, LEAD-04, and LEAD-06's frontend satisfied.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Lead status update + Convert-to-Clinic flow</name>
  <precondition>Task 1's LeadsInboxPage/LeadDetailPage render live data</precondition>
  <files>apps/platform-admin/src/modules/leads/use-leads.ts, apps/platform-admin/src/modules/leads/lead-detail-page.tsx</files>
  <read_first>
    apps/platform-admin/src/modules/leads/lead-detail-page.tsx, use-leads.ts (Task 1 output — this task extends both)
    .planning/phases/05-clinic-lead-content-management/05-UI-SPEC.md (Copywriting Contract's "Lead detail -> status update" and "Lead detail -> convert to Clinic" rows, verbatim copy to use)
    .planning/phases/05-clinic-lead-content-management/05-RESEARCH.md (Pitfall 3 — email-missing / duplicate-email edge cases this task's UI must visibly handle)
  </read_first>
  <action>
    Extend use-leads.ts with `useUpdateLeadStatus()` (`useMutation` wrapping `api.PATCH('/leads/{id}/status', { params: { path: { id } }, body: { status } })`, invalidating `['leads']` and `['leads', id]` on success) and `useConvertLead()` (`useMutation` wrapping `api.POST('/leads/{id}/convert', { params: { path: { id } } })`, `onSuccess` invalidating `['leads']`, `['leads', id]`, AND `['clinics']` — a new Clinic now exists).

    Extend lead-detail-page.tsx: a status `Select` (new/contacted/converted) bound to the lead's current status. On change to `new`/`contacted`, calls `useUpdateLeadStatus()`'s mutate directly. On change to `converted`, instead of writing immediately, opens an `AlertDialog` ("Convert this lead to a clinic? This creates a new Clinic account from the lead's details." / Cancel / "Convert") — only the dialog's "Convert" action calls `useConvertLead()`'s mutate; Cancel resets the Select back to the lead's actual current status (no optimistic status change before the mutation confirms). The Convert entry point itself (and the Select's `converted` option) is disabled with a `Tooltip` ("Add an email to this lead before converting") whenever `lead.email` is null/empty. On a 409 response from the convert mutation, render an inline banner or `FormMessage`-style text: "A clinic with this email already exists." — not just a toast. On success, show a success toast and keep the admin on the (now-converted) lead detail view reflecting the new status and a link to the newly created clinic (`/clinics/${result.clinicId}`) if the response includes it. The `AlertDialogAction`("Convert") button and the status `Select` both show an inline `Spinner` and disable themselves for the duration of their respective mutation's `isPending` state, matching Plan 05-05's Clinic form's loading-state pattern.
  </action>
  <acceptance_criteria>
    <criterion>Selecting "converted" without confirming the AlertDialog does not call the convert mutation</criterion>
    <criterion>The convert entry point is disabled (with a Tooltip) when the lead's email is null</criterion>
    <criterion>A simulated 409 response renders the exact inline text "A clinic with this email already exists."</criterion>
    <criterion>A successful convert invalidates both the leads and clinics query keys</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
test "$(grep -c 'AlertDialog' apps/platform-admin/src/modules/leads/lead-detail-page.tsx)" -ge "1"
test "$(grep -c 'A clinic with this email already exists' apps/platform-admin/src/modules/leads/lead-detail-page.tsx)" -ge "1"
test "$(grep -c \"Add an email to this lead before converting\" apps/platform-admin/src/modules/leads/lead-detail-page.tsx)" -ge "1"
test "$(grep -c \"'clinics'\" apps/platform-admin/src/modules/leads/use-leads.ts)" -ge "1"
pnpm --filter platform-admin exec tsc -b
pnpm --filter platform-admin run build
echo PASS
    </automated>
    <human-check>
      <test>In the running app, open a Lead with an email, change its status to Contacted (should apply immediately), then change to Converted and confirm the AlertDialog.</test>
      <expected>Status changes reflect immediately without a manual refresh; converting shows a success toast, the lead's status badge updates to "converted", and the Clinics list (in another tab or on revisit) shows the newly created clinic.</expected>
      <why_human>Visual confirmation of the full status-update and convert round trip against live data.</why_human>
    </human-check>
  </verify>
  <done>Lead status transitions and the full Convert-to-Clinic flow work live, including the email-missing and duplicate-email edge cases — LEAD-05 and LEAD-07's frontend satisfied. Phase's entire Leads requirement set (LEAD-03 through LEAD-07) is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| PlatformAdmin (authenticated SPA) -> POST /leads/:id/convert | Same authenticated-admin-only boundary as Plan 05-03's backend; the frontend's own email-missing check is UX-only, the real enforcement is server-side (Plan 05-03 Task 2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-05-06-01 | Tampering | Client-side "disable Convert when no email" check | low | accept | Explicitly documented as defense-in-depth UX only — the real validation is Plan 05-03's server-side 400/409, which this plan's frontend surfaces rather than duplicates as the sole guard |
| T-05-06-02 | Information Disclosure | Lead detail page rendering submitted PII (name/email/phone/message) | medium | accept | Same disposition as Plan 05-03's backend equivalent threat — only reachable by an authenticated PlatformAdmin, which is exactly the intended "view a Lead's full submitted detail" requirement (LEAD-04) |

</threat_model>

<verification>
- Task 1's automated script proves the two distinct empty states, the null-field "Not provided" fallback, and both new routes exist
- Task 2's automated script proves the AlertDialog gate, the disabled-when-no-email Tooltip, the 409 inline-error copy, and the cross-resource query invalidation all exist in source; its human-check confirms the live round trip
</verification>

<success_criteria>
Leads inbox (filterable list, detail, status transitions, and a safe convert-to-Clinic flow) works end-to-end against real data — LEAD-03 through LEAD-07 fully satisfied.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `apps/platform-admin/src/modules/leads/use-leads.ts` (new) — `useLeads`, `useLead`, `useUpdateLeadStatus`, `useConvertLead`
- `apps/platform-admin/src/modules/leads/leads-inbox-page.tsx` (new) — `LeadsInboxPage`
- `apps/platform-admin/src/modules/leads/lead-detail-page.tsx` (new) — `LeadDetailPage`
- `apps/platform-admin/src/modules/leads/date-range-picker.tsx` (new) — `DateRangePicker`
- `apps/platform-admin/src/router.tsx` (edited) — adds `leads`, `leads/:id` routes

<output>
Create `.planning/phases/05-clinic-lead-content-management/05-06-SUMMARY.md` when done
</output>
</output>
