---
phase: 05-clinic-lead-content-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/migrations/**
  - apps/server/src/clinics/clinics.controller.ts
  - apps/server/src/clinics/clinics.service.ts
  - apps/server/src/clinics/clinics.module.ts
  - apps/server/src/clinics/dto/create-clinic.dto.ts
  - apps/server/src/clinics/dto/update-clinic.dto.ts
  - apps/server/src/clinics/dto/clinic-query.dto.ts
  - apps/server/src/app.module.ts
autonomous: true
requirements: [CLINIC-01, CLINIC-02, CLINIC-03, CLINIC-04, CLINIC-05, INFRA-05]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "packages/db/prisma/schema.prisma's Clinic, Lead, BlogPost, and PricingPlan models each gain a nullable `updatedById String?` field + a named `@relation` to PlatformAdmin, added via one committed `prisma migrate dev` migration under packages/db/prisma/migrations/ — no manual DB edit anywhere (INFRA-05)"
    - "GET /clinics returns all clinics ordered by createdAt descending (newest first) by default, stable across repeated calls when no new writes occur (CLINIC-01 ordering)"
    - "GET /clinics with zero clinics in the DB returns an empty array `[]`, not an error or null (CLINIC-01 empty)"
    - "GET /clinics?status=<value> returns only clinics whose status exactly matches one of ClinicStatus's 4 enum values; an invalid status string is rejected 400 by class-validator's @IsEnum, never silently ignored or treated as 'no filter' (CLINIC-05)"
    - "GET /clinics/:id for a non-existent id returns 404, not a 200 with null/undefined fields (CLINIC-02)"
    - "GET /clinics/:id always returns messageCount and bookingsCount as 0 (never null) and lastActiveAt as null when never set — the stubbed bot-usage fields render as real zero/null values, not undefined (CLINIC-02)"
    - "POST /clinics with an email matching an existing Clinic's email returns 409, not a duplicate row — enforced by Clinic.email's DB-level @unique constraint, not just an application pre-check, so it also holds under two genuinely concurrent POST /clinics requests with the same email (CLINIC-03 idempotency + concurrency)"
    - "PATCH /clinics/:id accepts any subset of name/email/phone/status/plan; fields omitted from the request body are left unchanged in the DB row, never nulled out (CLINIC-04)"
    - "updatedById is set exclusively server-side from the authenticated admin's @CurrentUser().sub on every Clinic create/update; a request body that includes an `updatedById` field is rejected 400 by the app's already-global `ValidationPipe({ whitelist: true, forbidNonWhitelisted: true })`, since no Clinic DTO ever declares that field"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "updatedById nullable FK + named @relation on Clinic/Lead/BlogPost/PricingPlan, plus the 4 inverse relations on PlatformAdmin"
      contains: "updatedById String?"
    - path: "apps/server/src/clinics/clinics.controller.ts"
      provides: "GET /clinics, GET /clinics/:id, POST /clinics, PATCH /clinics/:id"
      exports: "ClinicsController"
    - path: "apps/server/src/clinics/clinics.service.ts"
      provides: "findAll/findOne/create/update — Prisma-backed, server-side status filter, updatedById always sourced from the caller"
      exports: "ClinicsService"
    - path: "apps/server/src/clinics/clinics.module.ts"
      exports: "ClinicsModule"
    - path: "apps/server/src/clinics/dto/create-clinic.dto.ts"
      exports: "CreateClinicDto"
    - path: "apps/server/src/clinics/dto/update-clinic.dto.ts"
      exports: "UpdateClinicDto"
    - path: "apps/server/src/clinics/dto/clinic-query.dto.ts"
      exports: "ClinicQueryDto"
  key_links:
    - from: "apps/server/src/clinics/clinics.controller.ts"
      to: "apps/server/src/clinics/clinics.service.ts"
      via: "constructor DI"
      pattern: "constructor(private readonly clinicsService: ClinicsService)"
    - from: "apps/server/src/clinics/clinics.service.ts"
      to: "apps/server/src/prisma/prisma.service.ts"
      via: "constructor DI, this.prisma.clinic.*"
      pattern: "this.prisma.clinic"
    - from: "apps/server/src/app.module.ts"
      to: "apps/server/src/clinics/clinics.module.ts"
      via: "imports array"
      pattern: "ClinicsModule"
    - from: "apps/server/src/clinics/dto/update-clinic.dto.ts"
      to: "apps/server/src/clinics/dto/create-clinic.dto.ts"
      via: "PartialType(CreateClinicDto) from @nestjs/swagger"
      pattern: "PartialType"
  prohibitions:
    - statement: "MUST NOT accept updatedById (or id/createdAt) as a writable field on CreateClinicDto/UpdateClinicDto — it is set exclusively server-side from @CurrentUser().sub in ClinicsService"
      status: resolved
      verification: test
    - statement: "MUST NOT implement CLINIC-05's status filter as an in-memory .filter() over a full findMany() result — the where clause is built server-side in ClinicsService.findAll() and passed to Prisma"
      status: resolved
      verification: test
    - statement: "MUST NOT apply this migration via `prisma db push` — per this project's INFRA-01 threat model (Phase 4), schema changes are committed migrations only, via `prisma migrate dev`/`migrate deploy`"
      status: resolved
      verification: test
    - statement: "Generic SQL injection / secrets-in-URL canon items"
      status: dismissed
      verification: null
      reason: "Covered by Prisma's parameterized client (no raw queries anywhere in this plan); not minted as bespoke prohibitions here, matching Phase 4's precedent."
---

<objective>
Add the `updatedById` trace-field migration (INFRA-05) shared by all 4 of this phase's content models, then build the first of 4 identically-shaped NestJS resource modules — `ClinicsModule` — covering CLINIC-01 through CLINIC-05. This plan establishes the exact resource-module pattern (`AuthModule`'s constructor-DI shape minus the auth-specific pieces) that Plans 05-03 and 05-04 replicate for Leads/BlogPosts/PricingPlans.

Purpose: Close the one real backend schema gap this phase introduces before any resource module needs it, and prove the CRUD-module pattern once, cleanly, on the simplest of the 4 resources.
Output: A migrated schema with trace fields on all 4 content models, plus a fully working `GET/POST/PATCH /clinics(/:id)` API.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-clinic-lead-content-management/05-RESEARCH.md
@.planning/phases/05-clinic-lead-content-management/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: updatedById migration (all 4 models) + ClinicsModule: list/filter/detail/create</name>
  <precondition>Docker daemon is running and packages/db/prisma/migrations/20260810205834_init is already applied to the local Postgres DB (Phase 4's committed init migration)</precondition>
  <reversibility rating="reversible">Adding a nullable FK column is a safe, reversible schema change — a future migration can drop it with no data loss, since no row currently has a non-null value for it.</reversibility>
  <files>packages/db/prisma/schema.prisma, packages/db/prisma/migrations/**, apps/server/src/clinics/clinics.controller.ts, apps/server/src/clinics/clinics.service.ts, apps/server/src/clinics/clinics.module.ts, apps/server/src/clinics/dto/create-clinic.dto.ts, apps/server/src/clinics/dto/clinic-query.dto.ts, apps/server/src/app.module.ts</files>
  <read_first>
    packages/db/prisma/schema.prisma (current — 6 models, no updatedById anywhere; Clinic.email @unique)
    apps/server/src/auth/auth.controller.ts, auth.service.ts, auth.module.ts (Phase 4 — the exact constructor-DI/module shape this task's ClinicsController/Service/Module mirror, minus JwtService/ConfigService/PassportModule)
    apps/server/src/auth/dto/login.dto.ts (class-validator + @ApiProperty DTO shape to mirror for CreateClinicDto)
    apps/server/src/auth/decorators/current-user.decorator.ts, apps/server/src/auth/strategies/access-token.strategy.ts (AccessTokenPayload shape — `{ sub: string; email?: string }` — this task's @CurrentUser() usage)
    apps/server/src/app.module.ts (current imports array — this task appends ClinicsModule)
    .planning/phases/05-clinic-lead-content-management/05-RESEARCH.md (Pattern 1, Pattern 4)
    .planning/phases/05-clinic-lead-content-management/05-PATTERNS.md ("Backend: apps/server/src/clinics/*" section)
  </read_first>
  <action>
    **A. Schema migration.** Edit packages/db/prisma/schema.prisma: add `updatedById String?` and `updatedBy PlatformAdmin? @relation("ClinicUpdatedBy", fields: [updatedById], references: [id])` to the Clinic, Lead, BlogPost, and PricingPlan models (identical shape on all 4, with relation names `"ClinicUpdatedBy"`, `"LeadUpdatedBy"`, `"BlogPostUpdatedBy"`, `"PricingPlanUpdatedBy"`). Add the 4 corresponding back-relations to PlatformAdmin: `updatedClinics Clinic[] @relation("ClinicUpdatedBy")`, `updatedLeads Lead[] @relation("LeadUpdatedBy")`, `updatedBlogPosts BlogPost[] @relation("BlogPostUpdatedBy")`, `updatedPricingPlans PricingPlan[] @relation("PricingPlanUpdatedBy")`. Run `docker compose up -d postgres`, wait for healthy, then `pnpm --filter @repo/db run db:migrate -- --name add_updated_by_trace_fields` (wraps `dotenv -e ../../.env -- prisma migrate dev`) — this is `prisma migrate dev`, never `db push`, per this project's INFRA-01 threat-model override (a real, non-interactive `--name` flag is required so the command doesn't hang waiting for stdin). Confirm a new timestamped migration directory appears under packages/db/prisma/migrations/. Run `pnpm --filter @repo/db run db:generate` afterward so the regenerated Prisma client's types include `updatedById`/`updatedBy` on all 4 models.

    **B. ClinicsModule scaffolding.** Create apps/server/src/clinics/dto/create-clinic.dto.ts: `CreateClinicDto` with `@ApiProperty() @IsString() @IsNotEmpty() name: string`, `@ApiProperty() @IsEmail() email: string`, `@ApiPropertyOptional() @IsOptional() @IsString() phone?: string`, `@ApiPropertyOptional({ enum: ClinicStatus }) @IsOptional() @IsEnum(ClinicStatus) status?: ClinicStatus` (import `ClinicStatus` from `@repo/db`), `@ApiProperty() @IsString() @IsNotEmpty() plan: string`. Never declare an `updatedById` field on this class.

    Create apps/server/src/clinics/dto/clinic-query.dto.ts: `ClinicQueryDto` with `@ApiPropertyOptional({ enum: ClinicStatus }) @IsOptional() @IsEnum(ClinicStatus) status?: ClinicStatus`.

    Create apps/server/src/clinics/clinics.service.ts: `ClinicsService` constructor-injects `PrismaService` only (no JwtService/ConfigService — those are auth-specific per PATTERNS.md). `findAll(query: ClinicQueryDto)`: `this.prisma.clinic.findMany({ where: query.status ? { status: query.status } : undefined, orderBy: { createdAt: 'desc' } })`. `findOne(id: string)`: `this.prisma.clinic.findUnique({ where: { id } })`; if the result is `null`, throw `new NotFoundException('Clinic not found')`. `create(dto: CreateClinicDto, adminId: string)`: wrap `this.prisma.clinic.create({ data: { ...dto, updatedById: adminId } })` in a try/catch; on a Prisma error with `code === 'P2002'` throw `new ConflictException('A clinic with this email already exists')`, rethrow anything else unchanged.

    Create apps/server/src/clinics/clinics.controller.ts per PATTERNS.md's Core CRUD controller pattern: `@ApiTags('clinics') @ApiBearerAuth('access-token') @Controller('clinics')`, constructor-injects `ClinicsService`. `@Get() findAll(@Query() query: ClinicQueryDto)`. `@Get(':id') findOne(@Param('id') id: string)`. `@Post() create(@Body() dto: CreateClinicDto, @CurrentUser() user: AccessTokenPayload)` calling `this.clinicsService.create(dto, user.sub)`. No `@Public()` on any route — protected by the existing global `AccessTokenGuard` by default (AUTH-04 from Phase 4, already active app-wide).

    Create apps/server/src/clinics/clinics.module.ts: `@Module({ controllers: [ClinicsController], providers: [ClinicsService] }) export class ClinicsModule {}` — no `PrismaModule` import needed (it's `@Global()`).

    **C. Wire into app.module.ts.** Append `ClinicsModule` (imported from `./clinics/clinics.module`) to the existing `imports` array, after `AuthModule`.
  </action>
  <acceptance_criteria>
    <criterion>packages/db/prisma/schema.prisma contains `updatedById String?` on Clinic, Lead, BlogPost, and PricingPlan (4 occurrences)</criterion>
    <criterion>packages/db/prisma/migrations/ contains a new migration directory (beyond the existing `20260810205834_init`) whose SQL adds `updatedById` columns</criterion>
    <criterion>apps/server/src/app.module.ts's imports array includes `ClinicsModule`</criterion>
    <criterion>a live GET /clinics call (empty DB) returns `[]` with HTTP 200</criterion>
    <criterion>a live POST /clinics call with a valid body returns 201 and the created row's `updatedById` matches the authenticated admin's id, not any client-supplied value</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
test "$(grep -c 'updatedById String?' packages/db/prisma/schema.prisma)" = "4"
test "$(ls packages/db/prisma/migrations | grep -c add_updated_by_trace_fields)" -ge "1"
test "$(grep -c 'ClinicsModule' apps/server/src/app.module.ts)" -ge "1"
docker compose up -d postgres
for i in $(seq 1 30); do docker compose exec -T postgres pg_isready -U postgres -d denta_bot_dev >/dev/null 2>&1 && break; sleep 1; done
cd packages/db && npx dotenv -e ../../.env -- npx prisma migrate deploy && cd ../..
pnpm --filter @repo/db run build
pnpm --filter server run build
( pnpm --filter server run start > /tmp/denta-server-05-01.log 2>&1 & echo $! > /tmp/denta-server-05-01.pid )
for i in $(seq 1 30); do curl -s http://localhost:4000/api/docs-json >/dev/null 2>&1 && break; sleep 1; done
LOGIN=$(curl -s -X POST http://localhost:4000/auth/login -H "Content-Type: application/json" -d '{"email":"platformadmin@dentabot.dev","password":"DevAdmin!2026"}')
ACCESS=$(echo "$LOGIN" | grep -o '"accessToken":"[^"]*"' | head -1 | cut -d'"' -f4)
EMPTY=$(curl -s -H "Authorization: Bearer $ACCESS" http://localhost:4000/clinics)
test "$EMPTY" = "[]"
CREATE=$(curl -s -X POST http://localhost:4000/clinics -H "Authorization: Bearer $ACCESS" -H "Content-Type: application/json" -d '{"name":"Test Clinic","email":"testclinic@example.com","plan":"trial","updatedById":"forged-id"}')
echo "$CREATE" | grep -q '"name":"Test Clinic"'
echo "$CREATE" | grep -qv 'forged-id'
kill "$(cat /tmp/denta-server-05-01.pid)" 2>/dev/null || true
echo PASS
    </automated>
  </verify>
  <done>Schema migrated with updatedById on all 4 content models; GET /clinics (empty + filtered), GET /clinics/:id, and POST /clinics all work live, with updatedById always server-derived.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ClinicsModule: update (PATCH) + conflict/404 hardening</name>
  <precondition>Task 1's ClinicsModule is wired into app.module.ts and a live POST /clinics succeeds</precondition>
  <files>apps/server/src/clinics/clinics.controller.ts, apps/server/src/clinics/clinics.service.ts, apps/server/src/clinics/dto/update-clinic.dto.ts</files>
  <read_first>
    apps/server/src/clinics/clinics.controller.ts, clinics.service.ts, clinics.module.ts (Task 1 output — this task extends both)
    .planning/phases/05-clinic-lead-content-management/05-RESEARCH.md (Pattern 1's UpdateClinicDto — `PartialType` from `@nestjs/swagger`, not `@nestjs/mapped-types`)
  </read_first>
  <action>
    Create apps/server/src/clinics/dto/update-clinic.dto.ts: `export class UpdateClinicDto extends PartialType(CreateClinicDto) {}`, importing `PartialType` from `@nestjs/swagger` (already installed) and `CreateClinicDto` from `./create-clinic.dto`.

    Extend clinics.service.ts with `update(id: string, dto: UpdateClinicDto, adminId: string)`: wrap `this.prisma.clinic.update({ where: { id }, data: { ...dto, updatedById: adminId } })` in a try/catch; on `code === 'P2025'` (record to update not found) throw `new NotFoundException('Clinic not found')`; on `code === 'P2002'` throw `new ConflictException('A clinic with this email already exists')`; rethrow anything else. Because `dto` only ever carries the fields the caller actually sent (Prisma's partial `data` object only updates provided keys), omitted fields are left untouched in the row — no manual field-presence bookkeeping needed.

    Add `@Patch(':id') update(@Param('id') id: string, @Body() dto: UpdateClinicDto, @CurrentUser() user: AccessTokenPayload)` to clinics.controller.ts, calling `this.clinicsService.update(id, dto, user.sub)`.
  </action>
  <acceptance_criteria>
    <criterion>PATCH /clinics/:id with `{ "status": "active" }` only changes `status`, leaving `name`/`email`/`phone`/`plan` unchanged on the row</criterion>
    <criterion>PATCH /clinics/:id on a non-existent id returns 404</criterion>
    <criterion>PATCH /clinics/:id changing email to one already used by another clinic returns 409</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
docker compose up -d postgres
for i in $(seq 1 30); do docker compose exec -T postgres pg_isready -U postgres -d denta_bot_dev >/dev/null 2>&1 && break; sleep 1; done
cd packages/db && npx dotenv -e ../../.env -- npx prisma migrate deploy && cd ../..
pnpm --filter server run build
( pnpm --filter server run start > /tmp/denta-server-05-01b.log 2>&1 & echo $! > /tmp/denta-server-05-01b.pid )
for i in $(seq 1 30); do curl -s http://localhost:4000/api/docs-json >/dev/null 2>&1 && break; sleep 1; done
LOGIN=$(curl -s -X POST http://localhost:4000/auth/login -H "Content-Type: application/json" -d '{"email":"platformadmin@dentabot.dev","password":"DevAdmin!2026"}')
ACCESS=$(echo "$LOGIN" | grep -o '"accessToken":"[^"]*"' | head -1 | cut -d'"' -f4)
CREATE=$(curl -s -X POST http://localhost:4000/clinics -H "Authorization: Bearer $ACCESS" -H "Content-Type: application/json" -d '{"name":"Update Test","email":"updatetest@example.com","plan":"trial"}')
ID=$(echo "$CREATE" | grep -o '"id":"[^"]*"' | head -1 | cut -d'"' -f4)
PATCHED=$(curl -s -X PATCH "http://localhost:4000/clinics/$ID" -H "Authorization: Bearer $ACCESS" -H "Content-Type: application/json" -d '{"status":"active"}')
echo "$PATCHED" | grep -q '"status":"active"'
echo "$PATCHED" | grep -q '"name":"Update Test"'
NOTFOUND_CODE=$(curl -s -o /dev/null -w "%{http_code}" -X PATCH "http://localhost:4000/clinics/nonexistent-id" -H "Authorization: Bearer $ACCESS" -H "Content-Type: application/json" -d '{"status":"active"}')
test "$NOTFOUND_CODE" = "404"
kill "$(cat /tmp/denta-server-05-01b.pid)" 2>/dev/null || true
echo PASS
    </automated>
  </verify>
  <done>PATCH /clinics/:id does partial updates only, 404s on unknown ids, and 409s on email collisions — CLINIC-01 through CLINIC-05 are fully satisfied by the combined output of Tasks 1-2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| PlatformAdmin (access-token-authenticated) → Clinic write endpoints | Every write is already gated by the existing global AccessTokenGuard (Phase 4); the new risk this plan introduces is mass-assignment on the new DTOs |
| Docker Postgres → packages/db migration | Local-only, dev-default credentials, no production topology yet (unchanged from Phase 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-05-01-01 | Tampering | CreateClinicDto / UpdateClinicDto | high | mitigate | `updatedById` is never declared on either DTO; the global `ValidationPipe({ whitelist: true, forbidNonWhitelisted: true })` rejects any client-supplied `updatedById` in the request body — verified live in Task 1's curl check |
| T-05-01-02 | Tampering | Schema migration mechanism | high | mitigate | `prisma migrate dev` only, committed under `packages/db/prisma/migrations/` — never `db push`, per this project's INFRA-01 threat-model override |
| T-05-01-03 | Information Disclosure | ClinicsController responses (raw Prisma Clinic objects) | low | accept | `Clinic` has no secret-equivalent field (unlike `PlatformAdmin.passwordHash`) — returning the raw model is acceptable, no DTO-mapping layer needed |

</threat_model>

<verification>
- Task 1's automated script proves the migration lands on all 4 models and ClinicsModule's list/detail/create work live, including the mass-assignment rejection of a forged `updatedById`
- Task 2's automated script proves PATCH does true partial updates and correctly 404s/409s
</verification>

<success_criteria>
`packages/db`'s schema carries `updatedById` on Clinic/Lead/BlogPost/PricingPlan via one committed migration; `GET/POST/PATCH /clinics(/:id)` all work end-to-end against the real DB, with server-derived `updatedById` and DB-enforced email uniqueness — CLINIC-01 through CLINIC-05 satisfied.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `packages/db/prisma/schema.prisma` (edited) — `updatedById`/`updatedBy` on Clinic/Lead/BlogPost/PricingPlan + 4 inverse relations on PlatformAdmin
- `packages/db/prisma/migrations/<timestamp>_add_updated_by_trace_fields/` (new)
- `apps/server/src/clinics/clinics.controller.ts` (new) — `ClinicsController`
- `apps/server/src/clinics/clinics.service.ts` (new) — `ClinicsService`
- `apps/server/src/clinics/clinics.module.ts` (new) — `ClinicsModule`
- `apps/server/src/clinics/dto/create-clinic.dto.ts` (new) — `CreateClinicDto`
- `apps/server/src/clinics/dto/update-clinic.dto.ts` (new) — `UpdateClinicDto`
- `apps/server/src/clinics/dto/clinic-query.dto.ts` (new) — `ClinicQueryDto`
- `apps/server/src/app.module.ts` (edited) — registers `ClinicsModule`

<output>
Create `.planning/phases/05-clinic-lead-content-management/05-01-SUMMARY.md` when done
</output>
</output>
