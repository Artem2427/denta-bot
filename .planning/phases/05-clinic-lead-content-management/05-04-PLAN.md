---
phase: 05-clinic-lead-content-management
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-03"]
files_modified:
  - apps/server/src/blog-posts/blog-posts.controller.ts
  - apps/server/src/blog-posts/blog-posts.service.ts
  - apps/server/src/blog-posts/blog-posts.module.ts
  - apps/server/src/blog-posts/dto/create-blog-post.dto.ts
  - apps/server/src/blog-posts/dto/update-blog-post.dto.ts
  - apps/server/src/pricing-plans/pricing-plans.controller.ts
  - apps/server/src/pricing-plans/pricing-plans.service.ts
  - apps/server/src/pricing-plans/pricing-plans.module.ts
  - apps/server/src/pricing-plans/dto/create-pricing-plan.dto.ts
  - apps/server/src/pricing-plans/dto/update-pricing-plan.dto.ts
  - apps/server/src/app.module.ts
autonomous: true
requirements: [CMS-01, CMS-03, INFRA-05]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GET /blog-posts, GET /blog-posts/:id, POST /blog-posts, PATCH /blog-posts/:id, DELETE /blog-posts/:id all work end-to-end so PlatformAdmin can fully create/edit/delete posts (CMS-01)"
    - "Creating a BlogPost with a slug matching an existing post's slug returns 409, not a duplicate row — BlogPost.slug is @unique (CMS-01 idempotency)"
    - "DELETE /blog-posts/:id on an already-deleted (or never-existed) id returns 404, not a silent 200 — deletion is a hard Prisma delete, no soft-delete this phase (CMS-01 concurrency)"
    - "GET/POST/PATCH/DELETE /pricing-plans(/:id) all work end-to-end so PlatformAdmin can fully create/edit/delete plans (CMS-03)"
    - "PricingPlan.name has no uniqueness constraint — creating two plans with the same name is allowed, each gets its own id (CMS-03 idempotency)"
    - "DELETE /pricing-plans/:id on an already-deleted id returns 404 (CMS-03 concurrency)"
    - "updatedById on both BlogPost and PricingPlan is set to the creating/editing admin's id on every create and update — both are always admin-created, so unlike Lead, updatedById is never null after the first write (INFRA-05)"
  artifacts:
    - path: "apps/server/src/blog-posts/blog-posts.controller.ts"
      provides: "GET /blog-posts, GET /blog-posts/:id, POST /blog-posts, PATCH /blog-posts/:id, DELETE /blog-posts/:id"
      exports: "BlogPostsController"
    - path: "apps/server/src/blog-posts/blog-posts.service.ts"
      exports: "BlogPostsService"
    - path: "apps/server/src/pricing-plans/pricing-plans.controller.ts"
      provides: "GET /pricing-plans, GET /pricing-plans/:id, POST /pricing-plans, PATCH /pricing-plans/:id, DELETE /pricing-plans/:id"
      exports: "PricingPlansController"
    - path: "apps/server/src/pricing-plans/pricing-plans.service.ts"
      exports: "PricingPlansService"
  key_links:
    - from: "apps/server/src/blog-posts/blog-posts.controller.ts"
      to: "apps/server/src/blog-posts/blog-posts.service.ts"
      via: "constructor DI"
      pattern: "constructor(private readonly blogPostsService: BlogPostsService)"
    - from: "apps/server/src/pricing-plans/pricing-plans.controller.ts"
      to: "apps/server/src/pricing-plans/pricing-plans.service.ts"
      via: "constructor DI"
      pattern: "constructor(private readonly pricingPlansService: PricingPlansService)"
    - from: "apps/server/src/app.module.ts"
      to: "apps/server/src/blog-posts/blog-posts.module.ts, apps/server/src/pricing-plans/pricing-plans.module.ts"
      via: "imports array"
      pattern: "BlogPostsModule, PricingPlansModule"
  prohibitions:
    - statement: "MUST NOT accept updatedById as a writable field on any BlogPost/PricingPlan DTO — set exclusively server-side from @CurrentUser().sub"
      status: resolved
      verification: test
    - statement: "MUST NOT soft-delete BlogPost/PricingPlan rows (e.g. a `deletedAt` flag) — DELETE performs a real Prisma `.delete()`; no soft-delete field exists on either model's schema"
      status: resolved
      verification: test
    - statement: "MUST NOT render/execute BlogPost.body (a Json rich-text-shaped field) as HTML anywhere in this plan's own admin UI — rendering to end users is Phase 6's concern on apps/web; this phase's backend only stores/returns it as opaque JSON"
      status: dismissed
      verification: null
      reason: "This plan is backend-only (no rendering surface); the prohibition is carried forward as a note for Plan 05-07's frontend editor task, not applicable to this plan's own scope."
---

<objective>
Build `BlogPostsModule` and `PricingPlansModule` — the two remaining CRUD resource modules this phase's backend needs, both mirroring `ClinicsModule`'s (Plan 05-01) shape exactly, now including a `DELETE` route (neither Clinic nor Lead has one, since neither is deletable per this phase's requirements).

Purpose: Close CMS-01 and CMS-03's backend half — full create/edit/delete for both content types the marketing site will eventually read from (Phase 6).
Output: `GET/POST/PATCH/DELETE /blog-posts(...)` and `GET/POST/PATCH/DELETE /pricing-plans(...)`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-clinic-lead-content-management/05-RESEARCH.md
@.planning/phases/05-clinic-lead-content-management/05-PATTERNS.md
@.planning/phases/05-clinic-lead-content-management/05-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BlogPostsModule — full CRUD incl. DELETE</name>
  <precondition>Plan 05-01's add_updated_by_trace_fields migration is applied (BlogPost.updatedById exists) and apps/server/src/app.module.ts already registers ClinicsModule and LeadsModule</precondition>
  <files>apps/server/src/blog-posts/blog-posts.controller.ts, apps/server/src/blog-posts/blog-posts.service.ts, apps/server/src/blog-posts/blog-posts.module.ts, apps/server/src/blog-posts/dto/create-blog-post.dto.ts, apps/server/src/blog-posts/dto/update-blog-post.dto.ts, apps/server/src/app.module.ts</files>
  <read_first>
    apps/server/src/clinics/clinics.controller.ts, clinics.service.ts, clinics.module.ts (Plan 05-01 output — the exact resource-module shape this task mirrors, extended with a DELETE route)
    packages/db/prisma/schema.prisma (BlogPost model — `slug String @unique`, `body Json`, `published Boolean @default(false)`)
  </read_first>
  <action>
    Create apps/server/src/blog-posts/dto/create-blog-post.dto.ts: `CreateBlogPostDto` with `@ApiProperty() @IsString() @IsNotEmpty() slug: string`, `@ApiProperty() @IsString() @IsNotEmpty() title: string`, `@ApiProperty() @IsString() @IsNotEmpty() excerpt: string`, `@ApiProperty() @IsString() @IsNotEmpty() category: string`, `@ApiProperty() @IsString() @IsNotEmpty() date: string`, `@ApiProperty() @IsString() @IsNotEmpty() readTime: string`, `@ApiProperty() @IsString() @IsNotEmpty() image: string`, `@ApiProperty() @IsObject() body: Record<string, unknown>` (matches the schema's `Json` type — accept any JSON object, no further shape validation this phase), `@ApiPropertyOptional() @IsOptional() @IsBoolean() published?: boolean`.

    Create apps/server/src/blog-posts/dto/update-blog-post.dto.ts: `export class UpdateBlogPostDto extends PartialType(CreateBlogPostDto) {}`.

    Create apps/server/src/blog-posts/blog-posts.service.ts: `BlogPostsService` constructor-injects `PrismaService`. `findAll()`: `this.prisma.blogPost.findMany({ orderBy: { createdAt: 'desc' } })`. `findOne(id)`: `findUnique`, `NotFoundException` if null. `create(dto, adminId)`: `this.prisma.blogPost.create({ data: { ...dto, updatedById: adminId } })`, catching `P2002` → `ConflictException('A blog post with this slug already exists')`. `update(id, dto, adminId)`: `this.prisma.blogPost.update({ where: { id }, data: { ...dto, updatedById: adminId } })`, catching `P2025` → `NotFoundException`, `P2002` → `ConflictException`. `remove(id)`: `this.prisma.blogPost.delete({ where: { id } })`, catching `P2025` → `NotFoundException`.

    Create apps/server/src/blog-posts/blog-posts.controller.ts: `@ApiTags('blog-posts') @ApiBearerAuth('access-token') @Controller('blog-posts')`, constructor-injects `BlogPostsService`. `@Get() findAll()`. `@Get(':id') findOne(@Param('id') id: string)`. `@Post() create(@Body() dto: CreateBlogPostDto, @CurrentUser() user: AccessTokenPayload)`. `@Patch(':id') update(@Param('id') id: string, @Body() dto: UpdateBlogPostDto, @CurrentUser() user: AccessTokenPayload)`. `@Delete(':id') @HttpCode(HttpStatus.NO_CONTENT) remove(@Param('id') id: string)` calling `this.blogPostsService.remove(id)`. No `@Public()` anywhere.

    Create apps/server/src/blog-posts/blog-posts.module.ts mirroring ClinicsModule's shape. Append `BlogPostsModule` to app.module.ts's `imports` array.
  </action>
  <acceptance_criteria>
    <criterion>POST /blog-posts with a slug already used by another post returns 409</criterion>
    <criterion>DELETE /blog-posts/:id followed by a second DELETE on the same id returns 404 the second time</criterion>
    <criterion>apps/server/src/app.module.ts's imports array includes BlogPostsModule</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
test "$(grep -c 'BlogPostsModule' apps/server/src/app.module.ts)" -ge "1"
docker compose up -d postgres
for i in $(seq 1 30); do docker compose exec -T postgres pg_isready -U postgres -d denta_bot_dev >/dev/null 2>&1 && break; sleep 1; done
cd packages/db && npx dotenv -e ../../.env -- npx prisma migrate deploy && cd ../..
pnpm --filter server run build
( pnpm --filter server run start > /tmp/denta-server-05-04a.log 2>&1 & echo $! > /tmp/denta-server-05-04a.pid )
for i in $(seq 1 30); do curl -s http://localhost:4000/api/docs-json >/dev/null 2>&1 && break; sleep 1; done
LOGIN=$(curl -s -X POST http://localhost:4000/auth/login -H "Content-Type: application/json" -d '{"email":"platformadmin@dentabot.dev","password":"DevAdmin!2026"}')
ACCESS=$(echo "$LOGIN" | grep -o '"accessToken":"[^"]*"' | head -1 | cut -d'"' -f4)
CREATE=$(curl -s -X POST http://localhost:4000/blog-posts -H "Authorization: Bearer $ACCESS" -H "Content-Type: application/json" -d '{"slug":"test-post","title":"Test","excerpt":"Ex","category":"News","date":"2026-08-14","readTime":"3 min","image":"/x.png","body":{"blocks":[]}}')
ID=$(echo "$CREATE" | grep -o '"id":"[^"]*"' | head -1 | cut -d'"' -f4)
DUP_CODE=$(curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:4000/blog-posts -H "Authorization: Bearer $ACCESS" -H "Content-Type: application/json" -d '{"slug":"test-post","title":"T2","excerpt":"Ex","category":"News","date":"2026-08-14","readTime":"3 min","image":"/x.png","body":{}}')
test "$DUP_CODE" = "409"
curl -s -o /dev/null -X DELETE "http://localhost:4000/blog-posts/$ID" -H "Authorization: Bearer $ACCESS"
SECOND_DELETE_CODE=$(curl -s -o /dev/null -w "%{http_code}" -X DELETE "http://localhost:4000/blog-posts/$ID" -H "Authorization: Bearer $ACCESS")
test "$SECOND_DELETE_CODE" = "404"
kill "$(cat /tmp/denta-server-05-04a.pid)" 2>/dev/null || true
echo PASS
    </automated>
  </verify>
  <done>BlogPostsModule's full CRUD (incl. DELETE) works live, with slug-uniqueness 409 and double-delete 404 both proven — CMS-01's backend satisfied.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PricingPlansModule — full CRUD incl. DELETE</name>
  <precondition>Task 1's BlogPostsModule is wired into app.module.ts</precondition>
  <files>apps/server/src/pricing-plans/pricing-plans.controller.ts, apps/server/src/pricing-plans/pricing-plans.service.ts, apps/server/src/pricing-plans/pricing-plans.module.ts, apps/server/src/pricing-plans/dto/create-pricing-plan.dto.ts, apps/server/src/pricing-plans/dto/update-pricing-plan.dto.ts, apps/server/src/app.module.ts</files>
  <read_first>
    apps/server/src/blog-posts/blog-posts.controller.ts, blog-posts.service.ts, blog-posts.module.ts (Task 1 output — this task's near-identical twin)
    packages/db/prisma/schema.prisma (PricingPlan model — `features String[]`, `isPopular Boolean @default(false)`, `sortOrder Int @default(0)`, `published Boolean @default(false)`, no @unique fields)
  </read_first>
  <action>
    Create apps/server/src/pricing-plans/dto/create-pricing-plan.dto.ts: `CreatePricingPlanDto` with `@ApiProperty() @IsString() @IsNotEmpty() name: string`, `@ApiProperty() @IsString() @IsNotEmpty() monthlyPrice: string`, `@ApiProperty() @IsString() @IsNotEmpty() yearlyPrice: string`, `@ApiProperty() @IsString() @IsNotEmpty() description: string`, `@ApiProperty({ type: [String] }) @IsArray() @IsString({ each: true }) features: string[]`, `@ApiPropertyOptional() @IsOptional() @IsBoolean() isPopular?: boolean`, `@ApiPropertyOptional() @IsOptional() @IsInt() sortOrder?: number`, `@ApiPropertyOptional() @IsOptional() @IsBoolean() published?: boolean`.

    Create apps/server/src/pricing-plans/dto/update-pricing-plan.dto.ts: `export class UpdatePricingPlanDto extends PartialType(CreatePricingPlanDto) {}`.

    Create apps/server/src/pricing-plans/pricing-plans.service.ts mirroring blog-posts.service.ts exactly, minus the P2002 slug-collision path (`PricingPlan.name` has no `@unique` constraint, so `create`/`update` never need a `P2002`-to-409 translation — only `P2025`-to-404 on update/remove). `findAll()`: `this.prisma.pricingPlan.findMany({ orderBy: { sortOrder: 'asc' } })` (sortOrder is the model's own intended display ordering, unlike Clinic/Lead/BlogPost's createdAt-desc default).

    Create apps/server/src/pricing-plans/pricing-plans.controller.ts mirroring blog-posts.controller.ts exactly (`@Controller('pricing-plans')`, same 5 routes).

    Create apps/server/src/pricing-plans/pricing-plans.module.ts mirroring blog-posts.module.ts. Append `PricingPlansModule` to app.module.ts's `imports` array.
  </action>
  <acceptance_criteria>
    <criterion>POST /pricing-plans with a name identical to an existing plan's name succeeds (201), producing two distinct rows</criterion>
    <criterion>GET /pricing-plans returns results ordered by sortOrder ascending</criterion>
    <criterion>DELETE /pricing-plans/:id on a non-existent id returns 404</criterion>
    <criterion>apps/server/src/app.module.ts's imports array includes PricingPlansModule</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
test "$(grep -c 'PricingPlansModule' apps/server/src/app.module.ts)" -ge "1"
docker compose up -d postgres
for i in $(seq 1 30); do docker compose exec -T postgres pg_isready -U postgres -d denta_bot_dev >/dev/null 2>&1 && break; sleep 1; done
cd packages/db && npx dotenv -e ../../.env -- npx prisma migrate deploy && cd ../..
pnpm --filter server run build
( pnpm --filter server run start > /tmp/denta-server-05-04b.log 2>&1 & echo $! > /tmp/denta-server-05-04b.pid )
for i in $(seq 1 30); do curl -s http://localhost:4000/api/docs-json >/dev/null 2>&1 && break; sleep 1; done
LOGIN=$(curl -s -X POST http://localhost:4000/auth/login -H "Content-Type: application/json" -d '{"email":"platformadmin@dentabot.dev","password":"DevAdmin!2026"}')
ACCESS=$(echo "$LOGIN" | grep -o '"accessToken":"[^"]*"' | head -1 | cut -d'"' -f4)
CREATE1=$(curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:4000/pricing-plans -H "Authorization: Bearer $ACCESS" -H "Content-Type: application/json" -d '{"name":"Starter","monthlyPrice":"$29","yearlyPrice":"$290","description":"d","features":["a"]}')
CREATE2=$(curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:4000/pricing-plans -H "Authorization: Bearer $ACCESS" -H "Content-Type: application/json" -d '{"name":"Starter","monthlyPrice":"$29","yearlyPrice":"$290","description":"d2","features":["b"]}')
test "$CREATE1" = "201"
test "$CREATE2" = "201"
DELETE_404=$(curl -s -o /dev/null -w "%{http_code}" -X DELETE "http://localhost:4000/pricing-plans/nonexistent-id" -H "Authorization: Bearer $ACCESS")
test "$DELETE_404" = "404"
kill "$(cat /tmp/denta-server-05-04b.pid)" 2>/dev/null || true
echo PASS
    </automated>
  </verify>
  <done>PricingPlansModule's full CRUD (incl. DELETE) works live, ordered by sortOrder, allowing duplicate names — CMS-03's backend satisfied. Both CMS-01 and CMS-03's backend are complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| PlatformAdmin → Content write endpoints | Both resources are always admin-authored (no public write path exists or will exist — apps/web only ever reads them, Phase 6); the trust boundary is identical to Clinics: authenticated-admin-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-05-04-01 | Tampering | CreateBlogPostDto/UpdateBlogPostDto/CreatePricingPlanDto/UpdatePricingPlanDto mass assignment | high | mitigate | `updatedById` never declared on any of the 4 DTOs; global `ValidationPipe` whitelist rejects it |
| T-05-04-02 | Tampering | `BlogPost.body` (Json field, holds rich-text-shaped content) | medium | mitigate | Accepted as an opaque `Record<string, unknown>` and validated only for JSON-object shape (`@IsObject()`) this phase; no HTML rendering happens anywhere in this plan's scope — sanitization-at-render is Phase 6's concern when `apps/web` actually displays it |
| T-05-04-03 | Repudiation / Data Loss | `DELETE` on BlogPost/PricingPlan (hard delete, no soft-delete) | low | accept | Matches REQUIREMENTS.md's explicit v2-deferred "Full audit-log with diff history" scope — acceptable for a 1-3 person internal staff tool this milestone |

</threat_model>

<verification>
- Task 1's automated script proves BlogPostsModule's slug-uniqueness 409 and double-delete 404 live
- Task 2's automated script proves PricingPlansModule allows duplicate names, orders by sortOrder, and 404s on delete of a non-existent id
</verification>

<success_criteria>
Both remaining backend resource modules (BlogPosts, PricingPlans) work end-to-end with full create/edit/delete — CMS-01 and CMS-03 satisfied, completing this phase's entire backend (Plans 05-01, 05-03, 05-04).
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `apps/server/src/blog-posts/blog-posts.controller.ts` (new) — `BlogPostsController`
- `apps/server/src/blog-posts/blog-posts.service.ts` (new) — `BlogPostsService`
- `apps/server/src/blog-posts/blog-posts.module.ts` (new) — `BlogPostsModule`
- `apps/server/src/blog-posts/dto/create-blog-post.dto.ts` (new) — `CreateBlogPostDto`
- `apps/server/src/blog-posts/dto/update-blog-post.dto.ts` (new) — `UpdateBlogPostDto`
- `apps/server/src/pricing-plans/pricing-plans.controller.ts` (new) — `PricingPlansController`
- `apps/server/src/pricing-plans/pricing-plans.service.ts` (new) — `PricingPlansService`
- `apps/server/src/pricing-plans/pricing-plans.module.ts` (new) — `PricingPlansModule`
- `apps/server/src/pricing-plans/dto/create-pricing-plan.dto.ts` (new) — `CreatePricingPlanDto`
- `apps/server/src/pricing-plans/dto/update-pricing-plan.dto.ts` (new) — `UpdatePricingPlanDto`
- `apps/server/src/app.module.ts` (edited) — registers `BlogPostsModule`, `PricingPlansModule`

<output>
Create `.planning/phases/05-clinic-lead-content-management/05-04-SUMMARY.md` when done
</output>
</output>
