# Deferred Items — Phase 01

Out-of-scope issues discovered during execution. Not fixed per the Scope Boundary rule (pre-existing, unrelated to the current task's changes).

## From Plan 01-01

1. **`pnpm --filter ui check-types` fails** — pre-existing `csstype` version-resolution mismatch (`csstype@3.1.3` vs `csstype@3.2.3` both present in the pnpm dependency graph) causes `CSSProperties`/`alignmentBaseline` type errors in `calendar.tsx`, `sonner.tsx`, and `spinner.tsx`. Confirmed pre-existing: the errors reference `style` props unrelated to this plan's changes (the `'use client'` directive added to `sonner.tsx` cannot affect this), and identical errors appear in `calendar.tsx`/`spinner.tsx`, neither of which was touched this plan. Root cause is a lockfile/dependency-resolution issue, not application code — fixing it would require touching `pnpm-lock.yaml` dependency resolution, out of scope for a theme-token plan.
2. **`pnpm --filter ui lint` fails with 4 warnings** — pre-existing `react/prop-types` warnings in `calendar.tsx` (lines 135, 145) for `className`/`rootRef`/`orientation` props missing prop-type validation. `calendar.tsx` was not touched by this plan.

Both were present before this plan's commits and are unrelated to `theme.css` or `sonner.tsx`'s `'use client'` fix.

## Post-Wave-1 Build Gate (orchestrator-level, before Plan 01-02)

3. **`npm run build` (turbo) fails on `apps/admin-panel`** — same root-cause `csstype@3.1.3` vs `csstype@3.2.3` version mismatch as item 1, surfacing at the admin-panel build's stricter type check.
4. **`pnpm --filter web build` fails type-check on `apps/web/app/layout.tsx:19`** — "inferred type of 'RootLayout' cannot be named without a reference to '.pnpm/@types+react@19.2.17/...'" — a duplicate `@types/react` version in the pnpm tree. Confirmed pre-existing: `git log -- apps/web/app/layout.tsx` shows only the original `91f9732 Initial commit`; this file has not been touched by any Phase 1 plan yet. Both are monorepo dependency-deduplication issues (would require `pnpm-lock.yaml`/`overrides` changes), out of scope for Phase 1's theming/shell work.

## From Plan 01-02 (Task 3)

5. **`pnpm --filter web build` still fails type-check after the `layout.tsx` "portable type" fix (item 4) is applied — this time on `../../packages/ui/src/components/shadcn-ui/button-group.tsx:50`** — same root-cause `csstype@3.1.3` vs `csstype@3.2.3` duplicate-resolution mismatch as item 1 (`Type 'React.CSSProperties' ... alignmentBaseline ... not assignable`), just surfacing through a different file than item 1's `calendar.tsx`/`sonner.tsx`/`spinner.tsx` list. Confirmed pre-existing and unrelated to Plan 01-02's changes: `git log -- packages/ui/src/components/shadcn-ui/button-group.tsx` and `git log -- packages/ui/index.tsx` both show only `91f9732 Initial commit` — neither file has ever been touched by any Phase 1 plan. Root cause: `apps/web/app/page.tsx` already imported `Button` from `@repo/ui` before this plan started, which resolves to `packages/ui/index.tsx`'s full barrel `export *` across all ~38 shadcn primitives (including `button-group.tsx`) — since `@repo/ui`'s package.json points `main`/`exports` at raw `.tsx` source (not a built `.d.ts`), TypeScript must type-check every barrel-exported file whenever any single `@repo/ui` export is imported, so this failure mode was already latent in the pristine pre-Phase-1 starter and is not something Task 3's `ThemeProvider`/`Header`/`Footer`/`Toaster` wiring introduced. Fixing it for real requires a `pnpm.overrides`/lockfile-level `csstype` version pin — a monorepo-wide dependency-resolution change affecting every `@repo/ui` consumer (`apps/web`, `apps/docs`, `apps/admin-panel`), consistent with items 1 and 3's already-established out-of-scope disposition for a theme/shell phase.
6. **Verification performed instead of a passing `pnpm --filter web build`:** Turbopack's own bundling/compile step reports `✓ Compiled successfully` for every Task 3 file (`layout.tsx`, `header.tsx`, `footer.tsx`, `theme-toggle.tsx`, `logo.tsx`, `not-found.tsx`) — confirming the `ThemeProvider`/`Header`/`main`/`Footer`/`Toaster` RSC wiring itself is structurally correct with no client-boundary errors. Only the subsequent, separate `tsc`-based type-check step (which walks the full `@repo/ui` barrel graph) fails, and only on the pre-existing `csstype` conflict. `pnpm --filter web exec eslint` (0 warnings) and the task's own grep-based acceptance criteria were used as the passing verification gate for this plan's actual file changes instead.

## execute:wave:post `ui.safety-gate` (Wave 2)

7. **`ui.safety-gate` flagged "no UI-SPEC.md" after Wave 2** — expected and already resolved at plan time: the user explicitly chose to skip UI-SPEC.md generation for Phase 01 (`/gsd-plan-phase 1` research/UI-SPEC gate), since this phase is a 1:1 port of an existing Figma-exported design archive and `01-CONTEXT.md`'s `<code_context>` section (full transcribed header/footer/theme-toggle/not-found source + exact token values) already serves as the design contract. Verified instead via `pnpm --filter web exec eslint` (clean), a live `pnpm --filter web dev` smoke test (`/` → 200 with "DentaBot" rendered, `/does-not-exist` → 404), and 1:1 comparison against CONTEXT.md's transcribed source during the folder-structure refactor. Not treated as blocking.
