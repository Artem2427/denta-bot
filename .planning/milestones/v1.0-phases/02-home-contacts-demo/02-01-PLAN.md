---
phase: 02-home-contacts-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/next.config.js
  - apps/web/app/page.tsx
  - apps/web/modules/home/hero.tsx
  - apps/web/modules/home/problem.tsx
  - apps/web/modules/home/solution.tsx
  - apps/web/modules/home/stagger-grid.tsx
  - apps/web/modules/home/features.tsx
  - apps/web/modules/home/cta-banner.tsx
  - apps/web/modules/home/testimonials.tsx
autonomous: true
requirements: [HOME-01]

must_haves:
  truths:
    - "User visiting / sees all 6 Home sections (Hero, Problem, Solution, Features, CTA Banner, Testimonials) rendered in order with the exact Ukrainian copy transcribed in 02-CONTEXT.md"
    - "Zero @repo/ui component or #1d6be4 brand-blue literal appears anywhere in the new Home files — every visible color/spacing/radius comes from the dt- premium token system or an explicitly-accepted literal Tailwind color (problem-card icon circles, star ratings, per 02-CONTEXT.md's Specific Ideas)"
    - "Hero's primary CTA and the CTA Banner's first button both navigate to /demo via the routes constant, never a hardcoded path string"
    - "The Problem, Features, and Testimonials card grids fade+translateY into view with an 80ms-staggered children animation on scroll entry, collapsing to a simple opacity fade when prefers-reduced-motion is set"
    - "The Hero's secondary CTA scrolls to the Features section via the #features same-page anchor"
  artifacts:
    - path: "apps/web/app/page.tsx"
      provides: "Home route composing all 6 section components in document order"
      exports: "default function Home"
    - path: "apps/web/modules/home/hero.tsx"
      provides: "Hero section: badge, h1, subhead, 2 CTAs, 3 stats, hero image, 2 floating cards"
      exports: "Hero"
    - path: "apps/web/modules/home/problem.tsx"
      provides: "Problem section: 4-card staggered-reveal grid"
      exports: "Problem"
    - path: "apps/web/modules/home/solution.tsx"
      provides: "Solution section: 2-column numbered-step tracks + CTA"
      exports: "Solution"
    - path: "apps/web/modules/home/stagger-grid.tsx"
      provides: "Reusable StaggerGrid/StaggerItem motion primitives for grid-entry stagger animation"
      exports: "StaggerGrid, StaggerItem"
    - path: "apps/web/modules/home/features.tsx"
      provides: "Features section (id=features): 8-card staggered-reveal grid"
      exports: "Features"
    - path: "apps/web/modules/home/cta-banner.tsx"
      provides: "Navy CTA banner: 2 buttons both routing to /demo"
      exports: "CtaBanner"
    - path: "apps/web/modules/home/testimonials.tsx"
      provides: "Testimonials section: 3-card staggered-reveal grid"
      exports: "Testimonials"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "apps/web/modules/home/*.tsx"
      via: "direct import and render, one component per section, in the order Hero, Problem, Solution, Features, CtaBanner, Testimonials"
      pattern: "import { Hero } from '@/modules/home/hero'"
    - from: "apps/web/modules/home/hero.tsx and cta-banner.tsx"
      to: "/demo"
      via: "next/link href={routes.demo}"
      pattern: "routes.demo"
    - from: "apps/web/next.config.js images.remotePatterns"
      to: "next/image Image components in hero.tsx and testimonials.tsx"
      via: "hostname images.unsplash.com"
      pattern: "images.unsplash.com"
---

<objective>
Port the Home page (`/`) from the design archive into `apps/web/app/page.tsx` as 6 section components under `apps/web/modules/home/`, built entirely on the Phase 01.1 premium design system (`dt-` tokens, `PremiumButton`, `PremiumCard`, `Container`, `Reveal`, `SignatureMark`, `@phosphor-icons/react`) — zero reliance on the shadcn-based workspace component package or the deprecated brand-blue hex literal. Satisfies HOME-01 and ROADMAP Phase 2 success criterion 1.

Purpose: Replace the current `create-turbo` placeholder at `apps/web/app/page.tsx` with the real, production-shaped marketing homepage.
Output: `apps/web/app/page.tsx` (rewritten), 8 new `apps/web/modules/home/*.tsx` files, `apps/web/next.config.js` (image remote pattern added).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-home-contacts-demo/02-CONTEXT.md
@.planning/phases/02-home-contacts-demo/02-PATTERNS.md
@.planning/phases/01.1-premium-design-system/01.1-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: next.config.js image host + Hero, Problem, Solution sections</name>
  <files>apps/web/next.config.js, apps/web/app/page.tsx, apps/web/modules/home/hero.tsx, apps/web/modules/home/problem.tsx, apps/web/modules/home/solution.tsx, apps/web/modules/home/stagger-grid.tsx</files>
  <read_first>
    apps/web/next.config.js (current 6-line file — only `transpilePackages` set)
    apps/web/app/page.tsx (current 10-line placeholder, being fully replaced)
    apps/web/shared/components/premium-button.tsx (PremiumButton: variant coral/outline/ghost, size default/sm/lg/icon, `asChild`)
    apps/web/shared/components/premium-card.tsx (PremiumCard: plain div, `rounded-dt-card`, built-in `hover:-translate-y-1` lift — no sub-components)
    apps/web/shared/components/container.tsx (Container: max-width + gutter wrapper, replaces raw `container mx-auto px-4 lg:px-8`)
    apps/web/shared/components/reveal.tsx (Reveal: single-element scroll-fade wrapper, props `children`/`className`/`threshold`)
    apps/web/shared/components/signature-mark.tsx (SignatureMark: props `className`/`pulse`, renders an `aria-hidden` coral dot)
    apps/web/shared/hooks/use-in-view.ts (`useInView<T>(threshold)` returns `{ ref, inView }`, SSR-safe)
    apps/web/shared/lib/motion.ts (`EASE_DT_EXPO_OUT`, `revealVariants`, `revealContainerVariants` — `revealContainerVariants` is not yet consumed anywhere in the codebase; this task is its first consumer)
    apps/web/shared/lib/routes.ts (`routes.demo`, `routes.home`)
    apps/web/shared/lib/cn.ts (`cn()` helper import path `@/shared/lib/cn`)
    .planning/phases/02-home-contacts-demo/02-CONTEXT.md (`<code_context>` "Design source — Home page" section — full Hero JSX with exact copy/classes/icon choices; Problem/Solution section content described in the trailing comment blocks; D-08 decision on the two Unsplash hotlinks)
    .planning/phases/01.1-premium-design-system/01.1-CONTEXT.md (D-05 palette role table, D-06 coral-rarity rule, D-28 scroll-reveal stagger spec, D-34 SignatureMark discipline)
  </read_first>
  <action>
    Edit `apps/web/next.config.js`: add an `images: { remotePatterns: [{ protocol: 'https', hostname: 'images.unsplash.com' }] }` key to the exported config object, alongside the existing `transpilePackages` key — do not remove or reorder `transpilePackages`.

    Create `apps/web/modules/home/stagger-grid.tsx` as a `'use client'` module exporting two components: `StaggerGrid({ children, className }: { children: React.ReactNode; className?: string })` — a `motion.div` (from `motion/react`) using `useInView<HTMLDivElement>(0.15)` for its `ref`, `variants={revealContainerVariants}`, `initial="hidden"`, `animate={inView ? 'visible' : 'hidden'}`, `className` passed through; and `StaggerItem({ children }: { children: React.ReactNode })` — a plain `motion.div` with `variants={revealVariants}` (no own `initial`/`animate`, inherits from the parent `StaggerGrid`'s variant propagation). This pair is the reusable 80ms-staggered-children grid-entry pattern (D-28) consumed by this task's Problem section and Task 2's Features/Testimonials sections.

    Create `apps/web/modules/home/hero.tsx` exporting `function Hero(): React.JSX.Element` (server component, no `'use client'`). Structure per 02-CONTEXT.md's Hero JSX: a `<section>` with top/bottom padding wrapping a `<Container>`, inside a `lg:grid-cols-2` grid. Left column, wrapped in `<Reveal>`: a badge (`inline-flex items-center gap-2 rounded-full border border-dt-navy/10 bg-dt-navy/5 px-4 py-1.5 text-sm font-medium text-dt-navy`) containing a small live-indicator dot (two overlapping `span`s — one `motion-safe:animate-ping absolute h-full w-full rounded-full bg-dt-teal opacity-75`, one solid `relative h-2 w-2 rounded-full bg-dt-teal`) plus the text "Нова платформа для стоматологій"; an `<h1 className="text-dt-h1 font-dt-heading font-bold text-dt-navy">` with "Автоматичний запис пацієнтів через Telegram. Без дзвінків."; a `<p className="text-dt-body text-dt-graphite">` with the subhead copy from 02-CONTEXT.md; a button row with `PremiumButton variant="coral" size="lg" asChild` wrapping `<Link href={routes.demo}>Спробувати демо<ArrowRight/></Link>` (Phosphor `ArrowRight`, `weight="regular"`, `className="ml-2 h-5 w-5"`) and `PremiumButton variant="outline" size="lg" asChild` wrapping a plain `<a href="#features">Дізнатись більше</a>` (a raw anchor, not `next/link`, since this is a same-page hash target per 02-CONTEXT.md's Claude's Discretion note); a 3-column stats row (`grid grid-cols-3 gap-4 pt-8`), each stat a `<div className="space-y-1">` with a `text-dt-h2 font-dt-heading font-bold text-dt-teal` number ("500+", "15 000+", "98%") and a `text-sm text-dt-graphite` label ("клінік", "записів/місяць", "задоволених"). Right column: a `relative` wrapper containing a `next/image` `<Image>` (src the exact Unsplash URL transcribed in 02-CONTEXT.md's Hero source, `alt="DentaBot Dashboard"`, `width={800}` `height={600}` `priority` `sizes="(min-width: 1024px) 50vw, 100vw"` `className="h-auto w-full rounded-dt-card shadow-[var(--shadow-dt-card)]"`), plus 2 absolutely-positioned floating cards: one at `-top-4 -right-4` containing a Phosphor `Check` icon (`weight="bold" className="h-5 w-5 text-dt-teal"`), a `<SignatureMark pulse />`, and the text "Новий запис від Олени Коваль" (this is the confirmation-moment SignatureMark placement per D-34); one at `-bottom-4 -left-4` containing a Phosphor `Bell` icon (`text-dt-teal`) and "Нагадування відправлено 24 пацієнтам" — both floating cards styled `absolute rounded-dt-card bg-dt-warm-white p-4 shadow-[var(--shadow-dt-hover)] flex items-center gap-2`.

    Create `apps/web/modules/home/problem.tsx` as `'use client'` exporting `function Problem(): React.JSX.Element`. `<section>` wrapping `<Container>`: a `<Reveal>`-wrapped centered header (`<h2 className="text-dt-h2 font-dt-heading font-bold text-dt-navy">Знайомо?</h2>` + `<p className="text-dt-body text-dt-graphite">Як зараз виглядає запис у більшості клінік</p>`), then a `<StaggerGrid className="grid gap-6 md:grid-cols-2 lg:grid-cols-4">` containing 4 `<StaggerItem>` items, each wrapping a `<PremiumCard>` with: an icon-circle div (`flex h-12 w-12 items-center justify-center rounded-full mb-4`, background literal Tailwind color per card — `bg-red-100`/`bg-orange-100`/`bg-yellow-100`/`bg-purple-100`, matching 02-CONTEXT.md's Specific Ideas precedent that these non-brand semantic tints stay as literal Tailwind classes) containing the card's emoji (📞/😔/💬/📊) at `text-2xl`; a `<h3 className="text-dt-h3 font-dt-heading font-semibold text-dt-navy">` title; a `<p className="text-dt-graphite">` description. All 4 titles/descriptions ported verbatim from 02-CONTEXT.md's Problem section source ("Адміністратор на телефоні", "Пацієнти забувають", "Хаос в месенджерах", "Немає статистики" with their exact description text).

    Create `apps/web/modules/home/solution.tsx` exporting `function Solution(): React.JSX.Element` (server component). `<section>` wrapping `<Container>`: two `<Reveal>`-wrapped columns in a `lg:grid-cols-2` grid — "Для пацієнта" (3 steps: "Відкриває Telegram бота", "Обирає час і лікаря", "Отримує підтвердження") and "Для клініки" (3 steps: "Запис з'являється в системі", "Бот надсилає нагадування", "Аналітика та звіти") — each column has an `<h3 className="text-dt-h3 font-dt-heading font-semibold text-dt-navy mb-6">` heading, then 3 numbered rows (`flex gap-4 items-start`), each row a number-circle (`flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-dt-navy font-dt-heading font-bold text-dt-warm-white`, containing "1"/"2"/"3") plus the step's short label in `text-dt-navy font-medium`. Below both columns, centered: `PremiumButton variant="outline" size="lg" asChild` wrapping `<Link href={routes.demo}>Спробувати як це працює</Link>`.

    Rewrite `apps/web/app/page.tsx`: `export default function Home(): React.JSX.Element` returning a `<div>` rendering `<Hero />`, `<Problem />`, `<Solution />` in that order (Task 2 appends the remaining 3 sections after these). Import all three from `@/modules/home/hero`, `@/modules/home/problem`, `@/modules/home/solution`. Remove the current placeholder markup and the shared shadcn-ui `Button` import entirely.
  </action>
  <acceptance_criteria>
    <criterion>`apps/web/next.config.js` contains a `remotePatterns` array entry with `hostname: 'images.unsplash.com'`, and the pre-existing `transpilePackages` key is still present</criterion>
    <criterion>`apps/web/modules/home/stagger-grid.tsx` exports both `StaggerGrid` and `StaggerItem`, both consuming `revealContainerVariants`/`revealVariants` from `@/shared/lib/motion`</criterion>
    <criterion>`apps/web/modules/home/hero.tsx` imports `PremiumButton` from `@/shared/components/premium-button`, `Reveal` from `@/shared/components/reveal`, `SignatureMark` from `@/shared/components/signature-mark`, and `routes` from `@/shared/lib/routes`</criterion>
    <criterion>`apps/web/modules/home/hero.tsx` and `apps/web/modules/home/testimonials.tsx` (Task 2) are the only files this plan creates that import from `next/image`</criterion>
    <criterion>`apps/web/modules/home/problem.tsx` renders exactly 4 `StaggerItem`/`PremiumCard` pairs</criterion>
    <criterion>`apps/web/app/page.tsx` renders `Hero`, `Problem`, `Solution` in that order and contains zero shadcn-package imports</criterion>
    <criterion>None of this task's 6 files contain the deprecated brand-blue hex literal used before Phase 01.1</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c "images.unsplash.com" apps/web/next.config.js)" -ge "1" && \
test "$(grep -c "transpilePackages" apps/web/next.config.js)" = "1" && \
test "$(grep -c "export function StaggerGrid\|function StaggerGrid" apps/web/modules/home/stagger-grid.tsx)" -ge "1" && \
test "$(grep -c "export function StaggerItem\|function StaggerItem" apps/web/modules/home/stagger-grid.tsx)" -ge "1" && \
test "$(grep -c "revealContainerVariants" apps/web/modules/home/stagger-grid.tsx)" -ge "1" && \
test "$(grep -c "revealVariants" apps/web/modules/home/stagger-grid.tsx)" -ge "1" && \
test "$(grep -c "@/shared/components/premium-button" apps/web/modules/home/hero.tsx)" = "1" && \
test "$(grep -c "@/shared/components/signature-mark" apps/web/modules/home/hero.tsx)" = "1" && \
test "$(grep -c "next/image" apps/web/modules/home/hero.tsx)" = "1" && \
test "$(grep -c "StaggerItem" apps/web/modules/home/problem.tsx)" = "4" && \
test "$(grep -c "PremiumCard" apps/web/modules/home/problem.tsx)" -ge "4" && \
test "$(grep -c "<Hero" apps/web/app/page.tsx)" = "1" && \
test "$(grep -c "<Problem" apps/web/app/page.tsx)" = "1" && \
test "$(grep -c "<Solution" apps/web/app/page.tsx)" = "1" && \
test "$(grep -Ec '1d6be4' apps/web/next.config.js apps/web/app/page.tsx apps/web/modules/home/hero.tsx apps/web/modules/home/problem.tsx apps/web/modules/home/solution.tsx apps/web/modules/home/stagger-grid.tsx)" = "0" && \
test "$(grep -Ec '@repo/ui' apps/web/app/page.tsx apps/web/modules/home/hero.tsx apps/web/modules/home/problem.tsx apps/web/modules/home/solution.tsx apps/web/modules/home/stagger-grid.tsx)" = "0" && \
echo PASS
    </automated>
  </verify>
  <done>`apps/web/next.config.js` allows Unsplash images; `apps/web/app/page.tsx` renders Hero, Problem, Solution (all premium-system, zero shadcn-package/brand-blue leftovers); `stagger-grid.tsx` is a working, reusable D-28-compliant staggered-reveal primitive ready for Task 2's grids.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Features, CTA Banner, Testimonials sections + finish page.tsx</name>
  <files>apps/web/app/page.tsx, apps/web/modules/home/features.tsx, apps/web/modules/home/cta-banner.tsx, apps/web/modules/home/testimonials.tsx</files>
  <read_first>
    apps/web/modules/home/stagger-grid.tsx (Task 1 output — `StaggerGrid`/`StaggerItem` API this task reuses verbatim)
    apps/web/modules/home/problem.tsx (Task 1 output — the exact `StaggerGrid`/`StaggerItem`/`PremiumCard` composition pattern to replicate for Features and Testimonials)
    apps/web/shared/components/premium-button.tsx (variant coral/outline, `asChild`)
    apps/web/shared/components/reveal.tsx, apps/web/shared/components/container.tsx
    .planning/phases/02-home-contacts-demo/02-CONTEXT.md (Features/CTA-Banner/Testimonials section comments in the Home `<code_context>` block — icon choices, button copy, testimonial content)
    .planning/phases/01.1-premium-design-system/01.1-CONTEXT.md (D-06 coral-rarity rule — informs the CTA banner's two-button treatment)
  </read_first>
  <action>
    Create `apps/web/modules/home/features.tsx` as `'use client'` exporting `function Features(): React.JSX.Element`. `<section id="features">` wrapping `<Container>`: a `<Reveal>`-wrapped header ("Можливості" or an equivalent short section title consistent with the design's features intent — 02-CONTEXT.md's comment block does not transcribe an explicit heading string for this section, so use "Все що потрібно для роботи клініки" as the `<h2 className="text-dt-h2 font-dt-heading font-bold text-dt-navy">` and leave no subheading paragraph), then `<StaggerGrid className="grid gap-6 md:grid-cols-2 lg:grid-cols-4">` with 8 `<StaggerItem>`/`<PremiumCard>` pairs, each card: a Phosphor icon at `h-10 w-10 text-dt-teal mb-3` and a `<h3 className="text-dt-h3 font-dt-heading font-semibold text-dt-navy">` title, no description paragraph (matches the design's compact 8-card layout). The 8 icon/title pairs, exactly as listed in 02-CONTEXT.md's Features comment block: `ChatCircleText` "Telegram бот", `Calendar` "Розклад лікарів", `Bell` "Автонагадування", `ChartBar` "Аналітика", `Star` "Відгуки", `Gear` "Адмін панель", `Users` "Управління персоналом", `Check` "Скасування запису" — all imported from `@phosphor-icons/react/ssr`, `weight="regular"`.

    Create `apps/web/modules/home/cta-banner.tsx` exporting `function CtaBanner(): React.JSX.Element` (server component). `<section className="bg-dt-navy py-16 lg:py-24">` wrapping `<Container>`, content wrapped in `<Reveal>`, centered: `<h2 className="text-dt-h2 font-dt-heading font-bold text-dt-warm-white">Спробуйте як це працює прямо зараз</h2>`, `<p className="text-dt-body text-dt-warm-white/80">Живий демо бот. Без реєстрації. Займає 2 хвилини.</p>`, a button row with `PremiumButton variant="coral" size="lg" asChild` wrapping `<Link href={routes.demo}>Відкрити демо бот</Link>` and a second `PremiumButton variant="outline" size="lg" asChild className="border-dt-warm-white text-dt-warm-white hover:bg-dt-warm-white hover:text-dt-navy"` (the `className` override replaces the base outline variant's navy border/text via `cn()`'s tailwind-merge, since this button sits on the navy background) wrapping `<Link href={routes.demo}>Переглянути адмін панель</Link>` — both buttons route to `/demo` via `routes.demo`, matching the design source.

    Create `apps/web/modules/home/testimonials.tsx` as `'use client'` exporting `function Testimonials(): React.JSX.Element`. `<section>` wrapping `<Container>`: `<Reveal>`-wrapped centered header ("Що кажуть клініки" as `<h2 className="text-dt-h2 font-dt-heading font-bold text-dt-navy">`), then `<StaggerGrid className="grid gap-6 md:grid-cols-3">` with 3 `<StaggerItem>`/`<PremiumCard>` pairs. Each card: 5-star row (5x Phosphor `Star weight="fill" className="h-4 w-4 text-yellow-400"` — a literal Tailwind yellow per 02-CONTEXT.md's Specific Ideas precedent, since `dt-amber` is reserved for the Demo admin-simulation only), a `<p className="text-dt-graphite italic">` quote, then an avatar row (`flex items-center gap-3 mt-4`). Card 1 (Олена Ковальчук, Клініка Посмішка, quote about a 70% reduction in missed appointments): avatar is a `next/image` `<Image>` (a stable Unsplash portrait URL depicting a smiling healthcare/clinic professional, following the same `images.unsplash.com` query-string style as the Hero image — this specific photo ID is not transcribed in 02-CONTEXT.md's source, so select a plausible professional-portrait Unsplash photo; if it 404s at runtime, `next/image`'s default broken-image fallback is explicitly acceptable per 02-CONTEXT.md's Claude's Discretion note) `width={48} height={48} className="h-12 w-12 rounded-full object-cover"`. Cards 2 and 3 (Андрій Мельник / Denta Plus, quote about the admin being freed up; Марія Петренко / Стоматологія Люкс, quote about easy setup) use emoji avatars (👨‍⚕️ / 👩‍⚕️) inside a `flex h-12 w-12 items-center justify-center rounded-full bg-dt-navy/10 text-2xl` div instead of an `Image`. Each avatar is followed by a `<div>` with the name (`font-dt-heading font-semibold text-dt-navy`) and clinic (`text-sm text-dt-graphite`).

    Edit `apps/web/app/page.tsx`: import `Features`, `CtaBanner`, `Testimonials` from their new module paths and append `<Features />`, `<CtaBanner />`, `<Testimonials />` after the existing `<Solution />` render, preserving the 3 sections Task 1 already wired.
  </action>
  <acceptance_criteria>
    <criterion>`apps/web/modules/home/features.tsx` renders exactly 8 `StaggerItem` elements and imports all 8 named Phosphor icons (`ChatCircleText`, `Calendar`, `Bell`, `ChartBar`, `Star`, `Gear`, `Users`, `Check`)</criterion>
    <criterion>`apps/web/modules/home/cta-banner.tsx` renders exactly 2 `PremiumButton` elements, both `asChild`-wrapping a `Link` to `routes.demo`</criterion>
    <criterion>`apps/web/modules/home/testimonials.tsx` renders exactly 3 `StaggerItem`/`PremiumCard` pairs and exactly 1 `next/image` `Image` element (only the first testimonial uses a photo)</criterion>
    <criterion>`apps/web/app/page.tsx` renders all 6 sections (`Hero`, `Problem`, `Solution`, `Features`, `CtaBanner`, `Testimonials`) in that exact order</criterion>
    <criterion>None of this task's 3 new files contain the deprecated brand-blue hex literal or a shadcn-package import</criterion>
    <criterion>`pnpm --filter web check-types` reports zero new errors in any file this plan created or modified (the pre-existing, unrelated `csstype` conflict in `packages/ui/src/components/shadcn-ui/*` is out of scope and may still fail — confirm the failing files are exactly `button-group.tsx`/`calendar.tsx`/`sidebar.tsx`, none of which this plan touches)</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c "StaggerItem" apps/web/modules/home/features.tsx)" = "8" && \
test "$(grep -Ec "ChatCircleText|Calendar|Bell|ChartBar|Star|Gear|Users|Check" apps/web/modules/home/features.tsx)" -ge "8" && \
test "$(grep -c "PremiumButton" apps/web/modules/home/cta-banner.tsx)" = "2" && \
test "$(grep -c "routes.demo" apps/web/modules/home/cta-banner.tsx)" -ge "2" && \
test "$(grep -c "StaggerItem" apps/web/modules/home/testimonials.tsx)" = "3" && \
test "$(grep -c "next/image" apps/web/modules/home/testimonials.tsx)" = "1" && \
test "$(grep -c "<Features" apps/web/app/page.tsx)" = "1" && \
test "$(grep -c "<CtaBanner" apps/web/app/page.tsx)" = "1" && \
test "$(grep -c "<Testimonials" apps/web/app/page.tsx)" = "1" && \
test "$(grep -Ec '1d6be4' apps/web/app/page.tsx apps/web/modules/home/features.tsx apps/web/modules/home/cta-banner.tsx apps/web/modules/home/testimonials.tsx)" = "0" && \
test "$(grep -Ec '@repo/ui' apps/web/app/page.tsx apps/web/modules/home/features.tsx apps/web/modules/home/cta-banner.tsx apps/web/modules/home/testimonials.tsx)" = "0" && \
CT_OUT="$(pnpm --filter web check-types 2>&1)"; \
test -z "$(printf '%s' "$CT_OUT" | grep 'error TS' | grep -v 'button-group.tsx\|calendar.tsx\|sidebar.tsx')" && \
echo PASS
    </automated>
  </verify>
  <done>All 6 Home sections render in `apps/web/app/page.tsx` in the correct order, fully on the premium design system; the Home page is feature-complete per HOME-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `images.unsplash.com` (external) → `next/image` proxy | Two hotlinked Unsplash photos rendered through Next.js's built-in image optimizer |
| Same-page anchor navigation | Hero's secondary CTA (`#features`) — no external navigation, no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-01-01 | Tampering / SSRF | `apps/web/next.config.js` `images.remotePatterns` | medium | mitigate | Scope the pattern to exactly `protocol: 'https'`, `hostname: 'images.unsplash.com'` — no wildcard subdomain, no other host added — so Next.js's image-optimization proxy cannot be pointed at an arbitrary attacker-controlled origin |
| T-02-01-02 | Tampering | Hotlinked hero/testimonial photo content | low | accept | Image bytes are served by Unsplash, not derived from any user input; if a URL 404s, `next/image`'s default broken-image behavior applies (explicitly accepted per 02-CONTEXT.md's Claude's Discretion note) — no fallback-wrapper component is required |
| T-02-01-03 | Information Disclosure | 3 hardcoded stat numbers (500+, 15 000+, 98%) | low | accept | Static marketing copy transcribed from the design archive, not derived from any real data source this milestone |

</threat_model>

<verification>
- `pnpm --filter web check-types` after Task 2 reports zero new errors outside the pre-existing, unrelated `csstype` conflict confined to `packages/ui/src/components/shadcn-ui/{button-group,calendar,sidebar}.tsx`
- Task 1 and Task 2's grep-based acceptance criteria confirm section composition, icon usage, and zero brand-blue/shadcn-package leftovers
- Manual spot-check (not automated this plan): `pnpm dev:web`, visit `/`, confirm all 6 sections render, scroll-reveal stagger fires on Problem/Features/Testimonials, both `/demo`-routed CTAs navigate correctly, `#features` anchor scrolls to the Features section
</verification>

<success_criteria>
`apps/web/app/page.tsx` renders the complete, 6-section Home page from the design archive, entirely on the Phase 01.1 premium design system, with the original Ukrainian copy and zero `@repo/ui`/brand-blue leftovers — HOME-01 and ROADMAP Phase 2 success criterion 1 are satisfied.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `apps/web/app/page.tsx` (rewritten) — Home route, composes all 6 section components
- `apps/web/modules/home/hero.tsx` (new) — `Hero`
- `apps/web/modules/home/problem.tsx` (new) — `Problem`
- `apps/web/modules/home/solution.tsx` (new) — `Solution`
- `apps/web/modules/home/stagger-grid.tsx` (new) — `StaggerGrid`, `StaggerItem` (reusable D-28 stagger-reveal primitives)
- `apps/web/modules/home/features.tsx` (new) — `Features`
- `apps/web/modules/home/cta-banner.tsx` (new) — `CtaBanner`
- `apps/web/modules/home/testimonials.tsx` (new) — `Testimonials`
- `apps/web/next.config.js` (edited) — `images.remotePatterns` for `images.unsplash.com`

<output>
Create `.planning/phases/02-home-contacts-demo/02-01-SUMMARY.md` when done
</output>
