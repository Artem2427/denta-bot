---
phase: 01.1-premium-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/app/premium-theme.css
  - apps/web/app/globals.css
  - apps/web/app/fonts.ts
autonomous: false
requirements: [DESIGN-01, DESIGN-03]

must_haves:
  truths:
    - "apps/web/app/globals.css imports a new apps/web-scoped premium-theme.css after @repo/ui's theme.css import, and none of the new dt-prefixed custom property names collide with any name already defined in packages/ui/styles/theme.css"
    - "motion and @phosphor-icons/react are declared as direct apps/web dependencies only after the user has explicitly approved their npm registry legitimacy"
    - "apps/web/app/fonts.ts exports two next/font/google Inter font instances (a heading instance at weights 600/700, a body instance at weights 400/500), both covering latin and cyrillic subsets, each bound to a distinct CSS variable"
    - "A global prefers-reduced-motion: reduce media rule exists that collapses all CSS transition/animation durations to near-zero, independent of any JS animation library"
  artifacts:
    - path: "apps/web/app/premium-theme.css"
      provides: "apps/web-scoped dt-prefixed Tailwind v4 @theme tokens (colors D-05, radius D-12, shadows D-13, type scale D-09) plus grid/motion custom properties and the global reduced-motion override"
      contains: "--color-dt-navy"
    - path: "apps/web/app/fonts.ts"
      provides: "interHeading / interBody next/font/google exports feeding --font-dt-heading-raw / --font-dt-body-raw"
      exports: "interHeading, interBody"
  key_links:
    - from: "apps/web/app/globals.css"
      to: "apps/web/app/premium-theme.css"
      via: "@import './premium-theme.css'; placed after @import '@repo/ui/styles/theme.css';"
      pattern: "@import './premium-theme.css'"
---

<objective>
Establish the foundation layer of the apps/web-scoped premium design system: gate and install the two genuinely new npm dependencies (`motion`, `@phosphor-icons/react`) plus four already-locked-but-undeclared transitive dependencies, create the `dt-`-prefixed Tailwind v4 token set (colors, radius, shadows, typography scale, grid/motion custom properties), and self-host the Inter heading/body fonts — all without touching `packages/ui/styles/theme.css` (D-01/D-02).

Purpose: DESIGN-01 (bespoke palette/typography/spacing/shadow system, not built on `packages/ui`) and DESIGN-03 (accessibility/performance groundwork — self-hosted fonts, reduced-motion baseline). This is the tokens/fonts layer every later plan in this phase builds on.
Output: `apps/web/app/premium-theme.css` (new), `apps/web/app/fonts.ts` (new), `apps/web/app/globals.css` (updated import), `apps/web/package.json` + `pnpm-lock.yaml` (new deps).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-premium-design-system/01.1-CONTEXT.md
@.planning/phases/01.1-premium-design-system/01.1-RESEARCH.md
@.planning/phases/01.1-premium-design-system/01.1-PATTERNS.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package Legitimacy Gate — motion, @phosphor-icons/react</name>
  <files>(none — this checkpoint installs nothing; it only gates Task 2's install)</files>
  <read_first>
    .planning/phases/01.1-premium-design-system/01.1-RESEARCH.md (## Package Legitimacy Audit table — `motion` flagged SUS on a "too-new" heuristic, `@phosphor-icons/react` already cleared OK)
  </read_first>
  <what-built>
    Nothing yet — this checkpoint runs BEFORE any package install in Task 2. RESEARCH.md's Package Legitimacy Audit flagged `motion` as `SUS` (a likely-false-positive "too-new" heuristic reacting to a recent version-bump publish date, despite ~17.4M weekly downloads and a long-lived, active GitHub repo — `github.com/motiondivision/motion`). `@phosphor-icons/react` already cleared the audit with an OK verdict (~2.9M weekly downloads, `github.com/phosphor-icons/react`) but is bundled into this same checkpoint for a single combined approval, per RESEARCH.md's explicit note. Neither package has been installed in this repo before (confirmed: zero `pnpm-lock.yaml` entries for either).
  </what-built>
  <action>
    Present the 2 npmjs.com package listings below to the user and pause for an explicit approval before Task 2 runs any install command. Do not run `pnpm add` or modify any file in this task — this task is verification-only.
  </action>
  <how-to-verify>
    Check each package's npm registry listing before approving Task 2's install:
    1. https://www.npmjs.com/package/motion — confirm the GitHub repo is `motiondivision/motion` (formerly `framer-motion`), weekly downloads in the millions, active/recent publish history.
    2. https://www.npmjs.com/package/@phosphor-icons/react — confirm the GitHub repo is `phosphor-icons/react`, weekly downloads in the millions, not the deprecated unscoped `phosphor-react` package.
  </how-to-verify>
  <acceptance_criteria>
    <criterion>User has responded with an explicit approval (or has identified a concern that halts the plan) for both packages</criterion>
  </acceptance_criteria>
  <verify>
    <automated>MISSING — this is a human-approval checkpoint, not an automatable check; the resume-signal below is the verification gate</automated>
  </verify>
  <done>User has typed "approved" (or equivalent affirmative), confirming both packages' npmjs.com listings were reviewed and are legitimate.</done>
  <resume-signal>Type "approved" once both packages are confirmed legitimate on npmjs.com, or describe concerns to halt the install.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install dependencies, create the premium token file, wire it into globals.css</name>
  <files>apps/web/package.json, pnpm-lock.yaml, apps/web/app/premium-theme.css, apps/web/app/globals.css</files>
  <read_first>
    apps/web/package.json (current `dependencies` block — being extended)
    apps/web/app/globals.css (current 6-line full file — the new import is inserted here)
    packages/ui/styles/theme.css (full file, 189 lines — every existing custom-property name defined here is the collision-avoidance blocklist for the new `dt-` tokens; also the structural precedent for `@theme` / `@theme inline` / `:root` layering used below, e.g. `--color-brand: var(--brand);` inside `@theme inline` mapping to `--brand: #1d6be4;` in `:root`)
    .planning/phases/01.1-premium-design-system/01.1-CONTEXT.md (D-05 palette hex values, D-09 type scale, D-10 line-height, D-12 radius, D-13 shadows, D-14 grid/spacing, D-32 easing, D-37 reduced-motion)
  </read_first>
  <action>
    Approval from Task 1 is required before this task runs. Run `pnpm --filter web add motion@13.0.0 @phosphor-icons/react@2.1.10 class-variance-authority@^0.7.1 clsx@^2.1.1 tailwind-merge@^3.5.0 radix-ui@^1.4.3` — the first two are the newly-approved packages pinned to their audited versions; the last four are already-locked transitive dependencies (resolved in `pnpm-lock.yaml` via `@repo/ui` at `class-variance-authority@0.7.1`, `clsx@2.1.1`, `radix-ui@1.6.2`, `tailwind-merge@3.6.0`) being declared directly on `apps/web`, matching the exact "Rule 3" precedent Phase 1 used for `next-themes`/`lucide-react` (pnpm's per-package `node_modules` isolation doesn't expose `@repo/ui`'s transitive deps to `apps/web` until declared directly) — use the same semver ranges already present in `packages/ui/package.json` so pnpm resolves to the already-locked versions without a fresh install.

    Create `apps/web/app/premium-theme.css` with two top-level blocks. First, a plain `@theme { ... }` block (no `inline` — these are literal values, matching RESEARCH.md's Pattern 1 example) defining, in order: 7 color tokens under the `--color-dt-*` namespace with these exact hex values (D-05) — `--color-dt-navy: #1A2B3D;`, `--color-dt-teal: #2C7A7B;`, `--color-dt-warm-white: #FAFAF8;`, `--color-dt-coral: #E86B5A;`, `--color-dt-graphite: #2D3436;`, `--color-dt-sage: #7A9E7E;` (reserved for later admin-demo use per D-07, unused this phase), `--color-dt-amber: #D4A24C;` (reserved, unused this phase); 2 radius tokens under `--radius-dt-*` (D-12) — `--radius-dt-card: 0.625rem;` (10px, cards/buttons), `--radius-dt-input: 0.375rem;` (6px, inputs); 3 shadow tokens under `--shadow-dt-*` (D-13, each a 2-3 layer stacked `box-shadow` value using `rgba(26, 43, 61, ...)` — navy-tinted, not pure black) — `--shadow-dt-sm`, `--shadow-dt-card`, `--shadow-dt-hover`, each increasing in blur/spread/opacity (e.g. `--shadow-dt-card: 0 1px 2px rgba(26, 43, 61, 0.04), 0 4px 8px rgba(26, 43, 61, 0.06), 0 12px 24px rgba(26, 43, 61, 0.08);`, `--shadow-dt-hover` one step stronger for the D-30 hover-lift state); 5 type-scale tokens under `--text-dt-*` (D-09, this Tailwind v4 namespace is already proven in this exact codebase — `packages/ui/styles/theme.css` line 136 consumes the built-in `--text-2xl` the same way) — `--text-dt-h1: 4rem;` (hero, within the 3.5-4.5rem range), `--text-dt-h2: 2.5rem;` (sections, within 2.25-2.75rem), `--text-dt-h3: 1.5rem;` (card subheads), `--text-dt-body: 1.125rem;`, `--text-dt-caption: 0.8125rem;`. Each heading token (`--text-dt-h1`, `--text-dt-h2`, `--text-dt-h3`) MUST be paired with a Tailwind v4 line-height modifier token using the `--text-dt-hN--line-height` suffix convention (the same mechanism Tailwind's own built-in `--text-lg`/`--text-lg--line-height` pair uses) set to `1.15` (within D-10's 1.1-1.2 heading range); `--text-dt-body` gets `--text-dt-body--line-height: 1.5;` (D-10's exact body line-height) — this makes `text-dt-h1`/`text-dt-body`/etc. utility classes apply the correct line-height automatically, with no separate `leading-*` utility needed at the call site.

    Add one more group under the same plain `@theme { ... }` block: a `--dt-*` letter-spacing/case pairing is NOT a Tailwind theme namespace, so instead add a small `@layer utilities { .dt-caption { letter-spacing: 0.02em; text-transform: uppercase; } }` block (placed after both `@theme` blocks, alongside the `:root` block below) implementing D-09's caption/status-label spec (`+0.02em` letter-spacing, uppercase) as an opt-in utility class consumers apply alongside `text-dt-caption`.

    Second, a separate `@theme inline { --font-dt-heading: var(--font-dt-heading-raw); --font-dt-body: var(--font-dt-body-raw); }` block — `inline` is required here (unlike the plain-value block above) because these two entries are themselves `var()` references to custom properties that Next.js's font loader injects at runtime via a `className` on `<body>` (Task 3 / Plan 04), not static values Tailwind can resolve at build time; this exactly mirrors `packages/ui/styles/theme.css`'s `@theme inline { --color-brand: var(--brand); }` pattern.

    Below both `@theme` blocks, add a `:root { }` block defining 3 raw custom properties consumed via Tailwind arbitrary-value syntax (not through `@theme`, since they don't need to become named utility classes): `--dt-container-max: 80rem;` (1280px, D-14), `--dt-section-y: 7rem;` (112px, within D-14's 96-140px desktop inter-section range), `--dt-ease-expo-out: cubic-bezier(0.16, 1, 0.3, 1);` (D-32, the one easing curve used everywhere in this system).

    Finally, add a global `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; scroll-behavior: auto !important; } }` block (D-37) — this is the CSS-only half of reduced-motion support; the JS-driven half (Motion's `useReducedMotion()` hook) is Plan 03's concern.

    Edit `apps/web/app/globals.css`: insert `@import './premium-theme.css';` as a new line immediately after the existing `@import '@repo/ui/styles/theme.css';` line, before the blank line and the `@import 'tw-animate-css';` line. Do not reorder or remove any existing line.
  </action>
  <acceptance_criteria>
    <criterion>`apps/web/package.json` `dependencies` includes `motion`, `@phosphor-icons/react`, `class-variance-authority`, `clsx`, `tailwind-merge`, and `radix-ui`</criterion>
    <criterion>`apps/web/app/premium-theme.css` contains all 7 `--color-dt-*` tokens, both `--radius-dt-*` tokens, all 3 `--shadow-dt-*` tokens, all 5 `--text-dt-*` tokens, all 4 `--text-dt-*--line-height` companions (D-10), the `.dt-caption` utility (D-09), both `--font-dt-heading`/`--font-dt-body` `@theme inline` mappings, `--dt-container-max`, `--dt-section-y`, `--dt-ease-expo-out`, and a `prefers-reduced-motion: reduce` media block</criterion>
    <criterion>None of the new custom property names in `apps/web/app/premium-theme.css` (grep for `--color-dt-`, `--radius-dt-`, `--shadow-dt-`, `--text-dt-`, `--font-dt-`) appear anywhere in `packages/ui/styles/theme.css`</criterion>
    <criterion>`apps/web/app/globals.css` still contains exactly the same 4 pre-existing lines (`@import 'tailwindcss';`, `@import '@repo/ui/styles/theme.css';`, `@import 'tw-animate-css';`, `@source '../../../packages/ui/src';`) plus the one new `@import './premium-theme.css';` line, in that relative order</criterion>
    <criterion>`packages/ui/styles/theme.css` has zero diff versus its pre-task state (git diff is empty for this file)</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c '"motion"' apps/web/package.json)" = "1" && \
test "$(grep -c '"@phosphor-icons/react"' apps/web/package.json)" = "1" && \
test "$(grep -c '"class-variance-authority"' apps/web/package.json)" = "1" && \
test "$(grep -c '"radix-ui"' apps/web/package.json)" = "1" && \
test "$(grep -c '\-\-color-dt-navy\|\-\-color-dt-teal\|\-\-color-dt-warm-white\|\-\-color-dt-coral\|\-\-color-dt-graphite\|\-\-color-dt-sage\|\-\-color-dt-amber' apps/web/app/premium-theme.css)" -ge "7" && \
test "$(grep -c '\-\-radius-dt-card\|\-\-radius-dt-input' apps/web/app/premium-theme.css)" -ge "2" && \
test "$(grep -c '\-\-shadow-dt-sm\|\-\-shadow-dt-card\|\-\-shadow-dt-hover' apps/web/app/premium-theme.css)" -ge "3" && \
test "$(grep -c '\-\-text-dt-h1--line-height\|\-\-text-dt-h2--line-height\|\-\-text-dt-h3--line-height\|\-\-text-dt-body--line-height' apps/web/app/premium-theme.css)" -ge "4" && \
test "$(grep -c '.dt-caption' apps/web/app/premium-theme.css)" -ge "1" && \
test "$(grep -c 'prefers-reduced-motion' apps/web/app/premium-theme.css)" -ge "1" && \
test "$(grep -Ec '\-\-color-dt-|\-\-radius-dt-|\-\-shadow-dt-|\-\-text-dt-|\-\-font-dt-' packages/ui/styles/theme.css)" = "0" && \
test "$(grep -c "premium-theme.css" apps/web/app/globals.css)" = "1" && \
git diff --quiet packages/ui/styles/theme.css && \
echo PASS
    </automated>
  </verify>
  <done>`motion`, `@phosphor-icons/react`, and the 4 Rule-3 transitive deps are direct `apps/web` dependencies; `apps/web/app/premium-theme.css` exists with the full `dt-`-prefixed token set and a reduced-motion global rule, imported from `apps/web/app/globals.css` after `@repo/ui`'s theme; `packages/ui/styles/theme.css` is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Self-host Inter heading/body fonts</name>
  <files>apps/web/app/fonts.ts</files>
  <read_first>
    apps/web/app/layout.tsx (current lines 10-17 — the `localFont` call shape being replaced, structurally, in Plan 04; this task only creates the new `fonts.ts` file, it does not edit `layout.tsx`)
    .planning/phases/01.1-premium-design-system/01.1-CONTEXT.md (D-08 typography — headings 600-700 weight, body 400, geometric sans; the "Claude's Discretion" note on Inter Display licensing)
    .planning/phases/01.1-premium-design-system/01.1-RESEARCH.md (Open Question 2 — recommends the plain-Inter fallback over manually self-hosting "Inter Display"; Assumption A1 — Cyrillic subset coverage)
  </read_first>
  <action>
    Create `apps/web/app/fonts.ts`. Per the phase brief's explicit steering (and RESEARCH.md Open Question 2's recommendation), use plain "Inter" via `next/font/google` for BOTH heading and body registers — self-hosted at build time with zero third-party CDN calls from the browser, satisfying D-45 — differentiated by weight (and, for headings, tighter `letter-spacing`) rather than attempting the manual "Inter Display" self-host (which requires manually downloading woff2 files from `github.com/rsms/inter/releases`, is unverified for complete Cyrillic/weight coverage this session, and D-08 explicitly lists plain "Inter" as an acceptable choice). Note this tradeoff in a one-line comment at the top of the file.

    Import `Inter` from `'next/font/google'`. Export `const interHeading = Inter({ subsets: ['latin', 'cyrillic'], weight: ['600', '700'], variable: '--font-dt-heading-raw', display: 'swap' });` and `const interBody = Inter({ subsets: ['latin', 'cyrillic'], weight: ['400', '500'], variable: '--font-dt-body-raw', display: 'swap' });`. The `cyrillic` subset is required because the site's copy is Ukrainian (per PROJECT.md); if `pnpm --filter web build` fails with a Google Fonts subset-validation error naming `cyrillic` as invalid, fall back to `subsets: ['latin']` only and flag this in the plan's SUMMARY as a follow-up risk (Cyrillic glyphs would then fall back to the browser's system sans-serif for headings/body). The `--font-dt-heading-raw` / `--font-dt-body-raw` variable names must match exactly what Task 2's `premium-theme.css` `@theme inline` block references.

    Export both `interHeading` and `interBody` as named exports for Plan 04's `layout.tsx` to consume.
  </action>
  <acceptance_criteria>
    <criterion>`apps/web/app/fonts.ts` imports `Inter` from `next/font/google` (not `next/font/local`)</criterion>
    <criterion>File exports exactly two named consts, `interHeading` and `interBody`</criterion>
    <criterion>`interHeading`'s `variable` option is exactly `--font-dt-heading-raw` and `interBody`'s is exactly `--font-dt-body-raw`</criterion>
    <criterion>Both font calls include `subsets` containing `'cyrillic'` (or the file's top comment explains the latin-only fallback if cyrillic was rejected at build time)</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
test "$(grep -c "from 'next/font/google'" apps/web/app/fonts.ts)" = "1" && \
test "$(grep -c 'export const interHeading' apps/web/app/fonts.ts)" = "1" && \
test "$(grep -c 'export const interBody' apps/web/app/fonts.ts)" = "1" && \
test "$(grep -c "'--font-dt-heading-raw'" apps/web/app/fonts.ts)" = "1" && \
test "$(grep -c "'--font-dt-body-raw'" apps/web/app/fonts.ts)" = "1" && \
pnpm --filter web check-types && \
echo PASS
    </automated>
  </verify>
  <done>`apps/web/app/fonts.ts` exists exporting `interHeading` (600/700) and `interBody` (400/500), both self-hosted via `next/font/google`, both bound to the CSS variable names `premium-theme.css` already expects; `pnpm --filter web check-types` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → `apps/web/package.json` | New, previously-uninstalled dependencies (`motion`, `@phosphor-icons/react`) entering the dependency graph |
| Google Fonts build-time fetch → `apps/web` build output | `next/font/google` downloads and self-hosts font files at build time; no runtime call to Google from the browser (satisfies D-45) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-01-SC | Tampering (supply chain) | `motion`, `@phosphor-icons/react` npm installs | high | mitigate | Task 1 blocking-human checkpoint verifies each package's npmjs.com listing (maintainer, GitHub repo, download counts) before Task 2 installs anything; `workflow.auto_advance` is ignored for this checkpoint per the Package Legitimacy Gate protocol |
| T-01.1-01-02 | Tampering (supply chain, low-risk) | `class-variance-authority`, `clsx`, `tailwind-merge`, `radix-ui` npm installs | low | accept | Already-locked, already-vetted transitive dependencies resolved identically via `@repo/ui`'s `pnpm-lock.yaml` entries — no new code enters the dependency graph, only a direct `package.json` declaration (Rule 3 precedent from Phase 1's `next-themes`/`lucide-react`) |
| T-01.1-01-03 | Tampering (unverified assumption) | `cyrillic` Google Fonts subset for Inter | low | accept | RESEARCH.md flagged this as an unverified assumption this session; the risk is self-detecting — `pnpm --filter web build` fails loudly with an explicit subset-validation error if `cyrillic` is not a valid Inter subset, at which point Task 3's documented fallback (latin-only) applies |

</threat_model>

<verification>
- `pnpm --filter web check-types` passes after Task 3 (Task 3 automated verify)
- Task 2's grep-based acceptance criteria confirm all token names exist and zero-collide against `packages/ui/styles/theme.css`
- `git diff --quiet packages/ui/styles/theme.css` confirms D-01/D-02's "packages/ui untouched" boundary
</verification>

<success_criteria>
`motion` and `@phosphor-icons/react` are installed only after explicit human approval; `apps/web/app/premium-theme.css` defines the full apps/web-scoped `dt-` token set (colors, radius, shadows, type scale, grid/motion constants, reduced-motion baseline) with zero namespace collision against `packages/ui/styles/theme.css`; `apps/web/app/fonts.ts` self-hosts Inter heading/body fonts covering Ukrainian (Cyrillic) copy — DESIGN-01's token foundation and DESIGN-03's font-hosting/reduced-motion groundwork are in place for the rest of the phase to build on.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `apps/web/app/premium-theme.css` (new) — the apps/web-scoped `dt-` prefixed Tailwind v4 token set: `--color-dt-{navy,teal,warm-white,coral,graphite,sage,amber}`, `--radius-dt-{card,input}`, `--shadow-dt-{sm,card,hover}`, `--text-dt-{h1,h2,h3,body,caption}`, `--font-dt-{heading,body}` (mapped via `@theme inline`), `--dt-{container-max,section-y,ease-expo-out}`, plus a global `prefers-reduced-motion: reduce` rule
- `apps/web/app/fonts.ts` (new) — `interHeading`, `interBody` named exports (`next/font/google` Inter, self-hosted)
- `apps/web/app/globals.css` (edited) — imports `premium-theme.css` after `@repo/ui`'s theme
- `apps/web/package.json` / `pnpm-lock.yaml` (edited) — adds `motion`, `@phosphor-icons/react`, `class-variance-authority`, `clsx`, `tailwind-merge`, `radix-ui` as direct dependencies

<output>
Create `.planning/phases/01.1-premium-design-system/01.1-01-SUMMARY.md` when done
</output>
