---
phase: 01.1-premium-design-system
plan: 02
subsystem: ui
tags: [cva, radix-ui, tailwind-v4, clsx, tailwind-merge, design-system]

# Dependency graph
requires:
  - phase: 01.1-premium-design-system (Plan 01)
    provides: apps/web-scoped dt-prefixed Tailwind v4 token set (colors, radius, shadows, type scale) this plan's primitives consume via `dt-` utility classes and CSS custom properties
provides:
  - apps/web/shared/lib/cn.ts — local clsx+tailwind-merge className helper, decoupled from @repo/ui
  - apps/web/shared/components/premium-button.tsx — CVA-based PremiumButton (coral/outline/ghost x default/sm/lg/icon), WCAG-AA-safe coral/navy text pairing, visible focus-visible ring
  - apps/web/shared/components/premium-card.tsx — layered-shadow, dt-radius PremiumCard with translateY-only hover-lift
  - apps/web/shared/components/container.tsx — max-width (1280px) + gutter Container layout wrapper
affects: [01.1-03-motion-system, 01.1-04-shell-retrofit]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "apps/web/shared/ primitives are built directly on dt- tokens with zero @repo/ui dependency — cn() is re-implemented locally (apps/web/shared/lib/cn.ts) rather than imported from packages/ui/src/lib/utils.ts, per D-01/D-03 scope boundary"
    - "Multi-layer shadow tokens (--shadow-dt-card, --shadow-dt-hover) are consumed via Tailwind arbitrary-value syntax (shadow-[var(--shadow-dt-*)]), not the bare shadow-dt-* utility, since they hold full box-shadow value strings rather than single color/size pairs"
    - "Hover micro-interactions use translateY + shadow-token swap only, never scale() (D-30)"

key-files:
  created:
    - apps/web/shared/lib/cn.ts
    - apps/web/shared/components/premium-button.tsx
    - apps/web/shared/components/premium-card.tsx
    - apps/web/shared/components/container.tsx
  modified:
    - .planning/phases/01.1-premium-design-system/deferred-items.md

key-decisions:
  - "Both PremiumCard and Container are Server Components (no 'use client') — the hover-lift is pure CSS :hover, no JS/observer needed, keeping the client boundary at the smallest future leaf per RESEARCH.md Pitfall 2"
  - "premium-card.tsx and container.tsx use inline `export function X(...)` (not a separate `export { X }` statement) to match the plan's literal grep-based acceptance criteria"

patterns-established:
  - "New apps/web primitives live under apps/web/shared/{components,lib}/, never packages/ui/src/components/, per D-03"
  - "cn() imported from '@/shared/lib/cn' (tsconfig @/* -> apps/web root alias) everywhere in apps/web/shared/, never re-implementing clsx/tailwind-merge composition outside cn.ts itself"

requirements-completed: [DESIGN-01, DESIGN-03]

coverage:
  - id: D1
    description: "Local cn() className helper (clsx+tailwind-merge), structurally identical to packages/ui/src/lib/utils.ts but not imported from @repo/ui"
    requirement: "DESIGN-01"
    verification:
      - kind: other
        ref: "Task 1 automated verify block (grep-based acceptance criteria) — see 01.1-02-PLAN.md Task 1"
        status: pass
    human_judgment: false
  - id: D2
    description: "CVA-based PremiumButton with coral/outline/ghost variants x default/sm/lg/icon sizes, dt-token styled, Slot-based asChild support, WCAG-AA-safe coral/navy text pairing (~4.59:1), visible navy focus-visible ring (D-36)"
    requirement: "DESIGN-03"
    verification:
      - kind: other
        ref: "Task 1 automated verify block (grep-based acceptance criteria, including the bg-dt-coral.*text-(dt-warm-white|white) negative-match gate) — see 01.1-02-PLAN.md Task 1"
        status: pass
    human_judgment: false
  - id: D3
    description: "PremiumCard with layered-shadow, dt-radius container and D-30-compliant translateY-only hover-lift (no scale())"
    requirement: "DESIGN-01"
    verification:
      - kind: other
        ref: "Task 2 automated verify block (grep-based acceptance criteria, including the scale(/scale- negative-match gate) — see 01.1-02-PLAN.md Task 2"
        status: pass
    human_judgment: false
  - id: D4
    description: "Container max-width (1280px) + horizontal-padding layout wrapper reading the --dt-container-max custom property"
    requirement: "DESIGN-01"
    verification:
      - kind: other
        ref: "Task 2 automated verify block (grep-based acceptance criteria) — see 01.1-02-PLAN.md Task 2"
        status: pass
    human_judgment: false
  - id: D5
    description: "pnpm --filter web check-types confirms all 4 new apps/web/shared/ files are type-error-free"
    requirement: "DESIGN-01"
    verification:
      - kind: other
        ref: "pnpm --filter web check-types run during Task 2 — only pre-existing, unrelated packages/ui/src/components/shadcn-ui/{button-group,calendar,sidebar}.tsx csstype errors remain (documented in deferred-items.md, tracked since Plan 01.1-01)"
        status: pass
    human_judgment: false

# Metrics
duration: ~12min
completed: 2026-08-08
status: complete
---

# Phase 01.1 Plan 02: Premium UI Primitives Summary

**CVA-variant `PremiumButton` (coral/outline/ghost), `PremiumCard`, `Container` layout wrapper, and a local `cn()` helper — all built directly on Plan 01's `dt-` tokens with zero `@repo/ui` dependency, under `apps/web/shared/`.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-08-08T22:41:05+03:00 (approx, first commit basis)
- **Completed:** 2026-08-08T22:44:14+03:00
- **Tasks:** 2
- **Files modified:** 5 (4 created, 1 doc file appended)

## Accomplishments

- Created `apps/web/shared/lib/cn.ts` — local `clsx`+`tailwind-merge` helper, the only file in `apps/web/shared/` allowed to import those packages directly
- Created `apps/web/shared/components/premium-button.tsx` — CVA button with `coral`/`outline`/`ghost` variants x `default`/`sm`/`lg`/`icon` sizes, `rounded-dt-card` radius, and the coral-variant text pairing locked to `text-dt-navy` (never white/warm-white) to clear WCAG AA's 4.5:1 contrast floor
- Created `apps/web/shared/components/premium-card.tsx` — layered-shadow card using arbitrary-value `shadow-[var(--shadow-dt-card)]`/`shadow-[var(--shadow-dt-hover)]` syntax and a `translateY`-only hover-lift (no `scale()`, per D-30)
- Created `apps/web/shared/components/container.tsx` — max-width (`--dt-container-max`, 1280px) + responsive horizontal-padding layout wrapper
- Verified all 4 new files are type-error-free via `pnpm --filter web check-types`; the only remaining errors are the pre-existing, out-of-scope `csstype` duplicate-resolution conflict in `packages/ui/src/components/shadcn-ui/*`

## Task Commits

Each task was committed atomically:

1. **Task 1: cn() helper and PremiumButton primitive** - `35dec9a` (feat)
2. **Task 2: PremiumCard and Container primitives** - `109d5d4` (feat)

**Plan metadata:** (pending — final docs commit follows this SUMMARY)

## Files Created/Modified

- `apps/web/shared/lib/cn.ts` - New `cn()` className helper (clsx + tailwind-merge)
- `apps/web/shared/components/premium-button.tsx` - New `PremiumButton`/`premiumButtonVariants` (coral/outline/ghost x default/sm/lg/icon)
- `apps/web/shared/components/premium-card.tsx` - New `PremiumCard` layered-shadow card with hover-lift
- `apps/web/shared/components/container.tsx` - New `Container` max-width layout wrapper
- `.planning/phases/01.1-premium-design-system/deferred-items.md` - Appended a Plan 01.1-02 entry confirming the same pre-existing `csstype` conflict, with no new files affected

## Decisions Made

- Both `PremiumCard` and `Container` stay Server Components (no `'use client'`) — their hover/layout behavior is pure CSS, no JS/observer needed, keeping the client boundary at the smallest future leaf
- `premium-card.tsx`/`container.tsx` use inline `export function X(...)` declarations (matching the plan's literal grep-based acceptance criteria) rather than a trailing `export { X }` statement

## Deviations from Plan

None — plan executed exactly as written. Task 2's `pnpm --filter web check-types` step surfaced the same pre-existing `csstype@3.1.3`/`3.2.3` duplicate-resolution conflict already documented and deferred in Plan 01.1-01's SUMMARY/`deferred-items.md`; this is a known, project-wide, out-of-scope blocker (none of the errors touch files this plan created or modified), so per the executor's scope-boundary rule it was logged (not auto-fixed) and this plan's own 4 new files are confirmed type-error-free.

## Issues Encountered

- This worktree had no `node_modules` installed (fresh worktree checkout). Ran `pnpm install --frozen-lockfile` at the repo root before `check-types` could run — this installed existing dependencies from the unchanged lockfile only, no new packages added, no lockfile diff. Not a plan deviation; a prerequisite for running the plan's own verification step.
- `pnpm --filter web check-types` still fails overall due to the pre-existing `csstype` conflict (see Deviations above) — tracked project-wide, not fixed here.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `apps/web/shared/components/{premium-button,premium-card,container}.tsx` and `apps/web/shared/lib/cn.ts` are ready for Plan 03 (motion system) and Plan 04 (shell retrofit) to import.
- Plan 04's header CTA buttons can import `PremiumButton` via `import { PremiumButton } from './premium-button'` per this plan's `key_links` contract.
- Blocker (unchanged, pre-existing): the `csstype` duplicate-resolution conflict still blocks a fully clean `pnpm --filter web check-types`/`build` — unrelated to this plan, tracked project-wide, recommended before/during Phase 2.

---
*Phase: 01.1-premium-design-system*
*Completed: 2026-08-08*

## Self-Check: PASSED

- FOUND: apps/web/shared/lib/cn.ts
- FOUND: apps/web/shared/components/premium-button.tsx
- FOUND: apps/web/shared/components/premium-card.tsx
- FOUND: apps/web/shared/components/container.tsx
- FOUND commit: 35dec9a (Task 1)
- FOUND commit: 109d5d4 (Task 2)
- FOUND commit: 3676c7c (SUMMARY)
