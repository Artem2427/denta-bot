---
phase: 05-clinic-lead-content-management
plan: 07
type: execute
wave: 6
depends_on: ["05-06", "05-04"]
files_modified:
  - apps/platform-admin/src/router.tsx
  - apps/platform-admin/src/modules/content/use-blog-posts.ts
  - apps/platform-admin/src/modules/content/blog-posts-page.tsx
  - apps/platform-admin/src/modules/content/blog-post-form-page.tsx
  - apps/platform-admin/src/modules/content/use-pricing-plans.ts
  - apps/platform-admin/src/modules/content/pricing-plans-page.tsx
  - apps/platform-admin/src/modules/content/pricing-plan-form-dialog.tsx
autonomous: true
requirements: [CMS-01, CMS-03, INFRA-04, INFRA-05]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Content section navigation (Blog Posts / Pricing Plans) is a Tabs-based sub-nav under the sidebar's 'Content' item, matching UI-SPEC's Component Inventory row"
    - "Blog Posts list: zero posts ever renders Empty/'No blog posts yet' + 'New Post' CTA; loading renders Skeleton rows; fetch failure renders 'Couldn't load posts.' + Retry (CMS-01)"
    - "Blog Post create/edit is a full-page Form (not a Dialog — body/excerpt/image fields are too large for a modal, per UI-SPEC's explicit copywriting contract), with a Published Switch requiring no confirmation to toggle (reversible)"
    - "Creating a Blog Post whose slug collides with an existing post surfaces the backend's 409 as an inline error under the slug field, not a silent failure or generic toast (CMS-01 idempotency)"
    - "Deleting a Blog Post shows the AlertDialog copy 'Delete this post? This can't be undone.' before calling DELETE /blog-posts/:id; the row's icon-only Delete button carries aria-label='Delete post' (UI-SPEC Dimension 2 hard requirement)"
    - "Pricing Plans list: zero plans ever renders Empty/'No pricing plans yet' + 'New Plan' CTA (CMS-03)"
    - "Pricing Plan create/edit is a Dialog Form with name/monthlyPrice/yearlyPrice/description/features(dynamic add/remove list, zero allowed, no minimum)/isPopular/published fields; the features list renders as a bulleted list on read views (UI Considerations: zero-one-many)"
    - "Deleting a Pricing Plan shows the AlertDialog copy 'Delete this plan? This can't be undone. It will no longer appear on the Prices page.'; the row's icon-only Delete button carries aria-label='Delete plan'"
    - "Every Blog Post / Pricing Plan detail/edit view renders the trace-field line ('Last updated by {email} on {date}' or 'Not yet updated') below the title, matching Plans 05-05/05-06's exact pattern (INFRA-05)"
    - "Neither Blog Post nor Pricing Plan forms render BlogPost.body as executed/dangerouslySetInnerHTML HTML anywhere in this admin UI — the body field is edited as raw JSON text (Textarea, client-validated as parseable JSON before submit), never interpreted as markup client-side"
    - "Save/Delete mutation buttons on both Blog Post and Pricing Plan screens show an inline Spinner and disable themselves for the duration of their mutation's isPending state, matching Plans 05-05/05-06's established loading-state pattern (UI Considerations: loading)"
  artifacts:
    - path: "apps/platform-admin/src/modules/content/use-blog-posts.ts"
      exports: "useBlogPosts, useBlogPost, useCreateBlogPost, useUpdateBlogPost, useDeleteBlogPost"
    - path: "apps/platform-admin/src/modules/content/blog-posts-page.tsx"
      exports: "BlogPostsPage"
    - path: "apps/platform-admin/src/modules/content/blog-post-form-page.tsx"
      exports: "BlogPostFormPage"
    - path: "apps/platform-admin/src/modules/content/use-pricing-plans.ts"
      exports: "usePricingPlans, usePricingPlan, useCreatePricingPlan, useUpdatePricingPlan, useDeletePricingPlan"
    - path: "apps/platform-admin/src/modules/content/pricing-plans-page.tsx"
      exports: "PricingPlansPage"
    - path: "apps/platform-admin/src/modules/content/pricing-plan-form-dialog.tsx"
      exports: "PricingPlanFormDialog"
  key_links:
    - from: "apps/platform-admin/src/modules/content/blog-post-form-page.tsx"
      to: "apps/platform-admin/src/modules/content/use-blog-posts.ts"
      via: "useCreateBlogPost() / useUpdateBlogPost() mutations"
      pattern: "useCreateBlogPost"
    - from: "apps/platform-admin/src/modules/content/pricing-plan-form-dialog.tsx"
      to: "apps/platform-admin/src/modules/content/use-pricing-plans.ts"
      via: "useCreatePricingPlan() / useUpdatePricingPlan() mutations"
      pattern: "useCreatePricingPlan"
    - from: "apps/platform-admin/src/router.tsx"
      to: "apps/platform-admin/src/modules/content/*"
      via: "content/blog, content/blog/new, content/blog/:id/edit, content/pricing child routes"
      pattern: "content/blog"
  prohibitions:
    - statement: "MUST NOT dangerouslySetInnerHTML or otherwise render BlogPost.body as executed HTML anywhere in this admin UI — it is edited/previewed as raw JSON text only; real end-user rendering (with sanitization) is Phase 6's concern on apps/web"
      status: resolved
      verification: test
    - statement: "MUST NOT ship an icon-only row-action Button (Delete on either Blog Posts or Pricing Plans rows) without an aria-label — UI-SPEC's Dimension 2 hard requirement"
      status: resolved
      verification: test
    - statement: "MUST NOT introduce a new third-party rich-text-editor dependency for the body field — no such library was researched/vetted this phase; a plain Textarea + JSON validation is the in-scope approach"
      status: resolved
      verification: test
---

<objective>
Build the Content management screens — Blog Posts (full-page create/edit, publish toggle, delete) and Pricing Plans (dialog create/edit with a dynamic features list, delete) — completing CMS-01 and CMS-03's frontend against `BlogPostsModule`/`PricingPlansModule` (Plan 05-04), replicating the established pattern from Plans 05-05/05-06.

Purpose: Give PlatformAdmin full control over the two content types the marketing site will read in Phase 6.
Output: `/content/blog`, `/content/blog/new`, `/content/blog/:id/edit`, `/content/pricing` routes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-clinic-lead-content-management/05-RESEARCH.md
@.planning/phases/05-clinic-lead-content-management/05-UI-SPEC.md
@.planning/phases/05-clinic-lead-content-management/05-05-PLAN.md
@.planning/phases/05-clinic-lead-content-management/05-06-PLAN.md
@.planning/phases/05-clinic-lead-content-management/05-04-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Blog Posts — list, full-page create/edit form, publish toggle, delete</name>
  <precondition>Plan 05-06's router.tsx (leads routes) is live; Plan 05-04's BlogPostsModule is registered</precondition>
  <files>apps/platform-admin/src/router.tsx, apps/platform-admin/src/modules/content/use-blog-posts.ts, apps/platform-admin/src/modules/content/blog-posts-page.tsx, apps/platform-admin/src/modules/content/blog-post-form-page.tsx</files>
  <read_first>
    apps/platform-admin/src/modules/clinics/use-clinics.ts, clinics-list-page.tsx, clinic-detail-page.tsx (Plans 05-05 output — hook/list/trace-field pattern this task mirrors)
    apps/platform-admin/src/lib/api/schema.d.ts (regenerate via `pnpm --filter platform-admin run generate:api` with apps/server running if `/blog-posts` paths are missing)
    .planning/phases/05-clinic-lead-content-management/05-UI-SPEC.md (Copywriting Contract's Blog Posts rows, Component Inventory's Tabs row, Color section's published->Badge mapping)
  </read_first>
  <action>
    If schema.d.ts lacks `/blog-posts` paths, regenerate it (apps/server running with BlogPostsModule registered).

    Create apps/platform-admin/src/modules/content/use-blog-posts.ts: `useBlogPosts()` (`useQuery` over `api.GET('/blog-posts')`), `useBlogPost(id)`, `useCreateBlogPost()`, `useUpdateBlogPost()` (invalidates `['blog-posts']` + `['blog-posts', id]`), `useDeleteBlogPost()` (`useMutation` wrapping `api.DELETE('/blog-posts/{id}', { params: { path: { id } } })`, invalidating `['blog-posts']`) — mirror `use-clinics.ts`'s structure exactly.

    Create apps/platform-admin/src/modules/content/blog-posts-page.tsx: page header ("Blog Posts") + a `Tabs` sub-nav (`TabsList`/`TabsTrigger` for "Blog Posts" and "Pricing Plans", navigating via React Router `Link`/`useNavigate` between `/content/blog` and `/content/pricing` — this is the shared Content section nav UI-SPEC's Component Inventory calls for) + "New Post" `Button` navigating to `/content/blog/new`. `DataTable` columns: title, category, a published `Badge` (`true`="Published"/default, `false`="Draft"/outline), a row-action Delete icon `Button` (`variant="ghost"`, `size="icon"`, `aria-label="Delete post"`) opening an `AlertDialog` ("Delete this post? This can't be undone." / Cancel / destructive-variant "Delete") calling `useDeleteBlogPost()`'s mutate on confirm. Loading -> `Skeleton` rows. Error -> "Couldn't load posts." + Retry. Empty -> `Empty`/"No blog posts yet"/"Create your first post to publish it on the site."/"New Post" CTA. Row click (not on the Delete button) navigates to `/content/blog/${row.id}/edit`.

    Create apps/platform-admin/src/modules/content/blog-post-form-page.tsx: `BlogPostFormPage` reads an optional `:id` from `useParams()` — if present, calls `useBlogPost(id)` and pre-fills the form (edit mode, page title "Edit Post"); if absent, starts with empty defaults (create mode, page title "New Post"). A full-page (not Dialog) `Form` with fields: slug, title, excerpt, category, date, readTime, image (all `Input`/`Textarea` as appropriate), a `body` `Textarea` holding the JSON-stringified block content (zod-validated as parseable JSON via a `.refine()` catching `JSON.parse` failures — parsed back to an object immediately before submit, never rendered as HTML), and a `published` `Switch` labeled "Published" (toggles with no confirmation — reversible). On submit, calls `useCreateBlogPost()` or `useUpdateBlogPost()` depending on mode; a 409 (duplicate slug) surfaces as an inline `FormMessage` under the slug field ("A blog post with this slug already exists."). Below the title in edit mode, renders the trace-field line exactly matching Plan 05-05/05-06's pattern ("Last updated by {updatedBy?.email ?? 'Not yet updated'} on {date}"), extending `BlogPostsService.findOne` with `include: { updatedBy: { select: { email: true } } }` if not already present. On successful submit, navigates back to `/content/blog`. The submit `Button` and the row-action Delete `Button` both show an inline `Spinner` and disable themselves for the duration of their mutation's `isPending` state.

    Add `content/blog`, `content/blog/new`, and `content/blog/:id/edit` child routes to router.tsx, pointing at `BlogPostsPage`/`BlogPostFormPage` (the `new` and `:id/edit` routes both render `BlogPostFormPage`, which branches on whether `:id` is present).
  </action>
  <acceptance_criteria>
    <criterion>blog-post-form-page.tsx's body field is validated as parseable JSON before submit and never passed through dangerouslySetInnerHTML anywhere</criterion>
    <criterion>the Delete row-action button in blog-posts-page.tsx carries `aria-label="Delete post"`</criterion>
    <criterion>router.tsx contains `content/blog`, `content/blog/new`, and `content/blog/:id/edit`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
test "$(grep -c \"'content/blog'\" apps/platform-admin/src/router.tsx)" -ge "1"
test "$(grep -c \"content/blog/new\" apps/platform-admin/src/router.tsx)" -ge "1"
test "$(grep -c \"content/blog/:id/edit\" apps/platform-admin/src/router.tsx)" -ge "1"
test "$(grep -c 'aria-label=\"Delete post\"' apps/platform-admin/src/modules/content/blog-posts-page.tsx)" -ge "1"
test "$(grep -c 'dangerouslySetInnerHTML' apps/platform-admin/src/modules/content/blog-post-form-page.tsx)" = "0"
test "$(grep -c 'JSON.parse' apps/platform-admin/src/modules/content/blog-post-form-page.tsx)" -ge "1"
pnpm --filter platform-admin exec tsc -b
pnpm --filter platform-admin run build
echo PASS
    </automated>
  </verify>
  <done>Blog Posts list, full-page create/edit form, publish toggle, and delete-with-confirmation all work live — CMS-01's frontend satisfied.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pricing Plans — list, dialog create/edit form with dynamic features, delete</name>
  <precondition>Task 1's Blog Posts screens and Content Tabs nav are live</precondition>
  <files>apps/platform-admin/src/router.tsx, apps/platform-admin/src/modules/content/use-pricing-plans.ts, apps/platform-admin/src/modules/content/pricing-plans-page.tsx, apps/platform-admin/src/modules/content/pricing-plan-form-dialog.tsx</files>
  <read_first>
    apps/platform-admin/src/modules/content/blog-posts-page.tsx, use-blog-posts.ts (Task 1 output — the list/Tabs/delete-confirmation pattern this task mirrors)
    apps/platform-admin/src/modules/clinics/clinic-form-dialog.tsx (Plan 05-05 output — the Dialog+Form create-flow pattern this task's PricingPlanFormDialog mirrors)
    .planning/phases/05-clinic-lead-content-management/05-UI-SPEC.md (Copywriting Contract's "Pricing Plans list -> create" row: Dialog form with name/monthlyPrice/yearlyPrice/description/features/isPopular/published)
  </read_first>
  <action>
    If schema.d.ts lacks `/pricing-plans` paths, regenerate it.

    Create apps/platform-admin/src/modules/content/use-pricing-plans.ts mirroring use-blog-posts.ts's exact shape (`usePricingPlans`, `usePricingPlan`, `useCreatePricingPlan`, `useUpdatePricingPlan`, `useDeletePricingPlan`).

    Create apps/platform-admin/src/modules/content/pricing-plans-page.tsx: page header ("Pricing Plans") + the same `Tabs` sub-nav as Task 1's blog-posts-page.tsx (active tab "Pricing Plans") + "New Plan" `Button` opening `PricingPlanFormDialog` in create mode. `DataTable` columns: name, monthlyPrice, a published `Badge`, an `isPopular` indicator (small `Badge` "Popular" when true, nothing otherwise), a row-action Delete icon `Button` (`aria-label="Delete plan"`) opening an `AlertDialog` ("Delete this plan? This can't be undone. It will no longer appear on the Prices page." / Cancel / destructive "Delete") calling `useDeletePricingPlan()`'s mutate. Loading/error/empty states mirror Task 1's blog-posts-page.tsx exactly ("No pricing plans yet" / "Add a plan to make it available on the Prices page." / "New Plan" CTA). Row click opens `PricingPlanFormDialog` in edit mode, pre-filled from `usePricingPlan(row.id)`.

    Create apps/platform-admin/src/modules/content/pricing-plan-form-dialog.tsx: `PricingPlanFormDialog({ planId, open, onOpenChange }: { planId?: string; open: boolean; onOpenChange: (open: boolean) => void })` — a `Dialog`/`DialogContent` wrapping a `Form` with fields: name, monthlyPrice, yearlyPrice, description (`Textarea`), a dynamic `features` list (zod `z.array(z.string())`, rendered as one `Input` per existing feature plus a "Remove" icon `Button` per row and an "Add feature" `Button` appending an empty string — zero features allowed, no minimum enforced, matching UI Considerations' zero-one-many resolution), `isPopular` `Switch`, `published` `Switch`. Below the fields in edit mode, the trace-field line matching Task 1's pattern (extending `PricingPlansService.findOne` with `include: { updatedBy: { select: { email: true } } }` if not already present). On submit, calls `useCreatePricingPlan()` or `useUpdatePricingPlan()` depending on whether `planId` is set; on success, closes the dialog (`onOpenChange(false)`) and shows a success toast. The submit `Button` and the list's row-action Delete `Button` both show an inline `Spinner` and disable themselves for the duration of their mutation's `isPending` state.

    Add a `content/pricing` child route to router.tsx pointing at `PricingPlansPage`.
  </action>
  <acceptance_criteria>
    <criterion>the features field allows adding and removing rows down to zero with no validation error</criterion>
    <criterion>the Delete row-action button in pricing-plans-page.tsx carries `aria-label="Delete plan"`</criterion>
    <criterion>router.tsx contains `content/pricing`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
test "$(grep -c \"'content/pricing'\" apps/platform-admin/src/router.tsx)" -ge "1"
test "$(grep -c 'aria-label=\"Delete plan\"' apps/platform-admin/src/modules/content/pricing-plans-page.tsx)" -ge "1"
test "$(grep -c 'Add feature' apps/platform-admin/src/modules/content/pricing-plan-form-dialog.tsx)" -ge "1"
test "$(grep -c \"'No pricing plans yet'\|No pricing plans yet\" apps/platform-admin/src/modules/content/pricing-plans-page.tsx)" -ge "1"
pnpm --filter platform-admin exec tsc -b
pnpm --filter platform-admin run build
echo PASS
    </automated>
    <human-check>
      <test>In the running app, navigate to Content, create a new Pricing Plan with 2 features, remove one, save, then open it again and toggle Published.</test>
      <expected>The plan appears in the list with correct data; the features list shows exactly 1 feature after removal; toggling Published requires no confirmation and updates the Badge immediately.</expected>
      <why_human>Visual confirmation of the dynamic features list and toggle behavior.</why_human>
    </human-check>
  </verify>
  <done>Pricing Plans list, dialog create/edit with a dynamic features list, and delete-with-confirmation all work live — CMS-03's frontend satisfied. Phase 5's entire requirement set (all 14 IDs) is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| PlatformAdmin (authenticated SPA) -> Content write endpoints | Same authenticated-admin-only boundary as Plans 05-05/05-06; both content types are always admin-authored, no public write path exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-05-07-01 | Tampering (XSS) | `BlogPost.body` editing in `blog-post-form-page.tsx` | medium | mitigate | Never rendered via `dangerouslySetInnerHTML` in this admin UI — edited as raw JSON text only, validated for JSON-parseability, not interpreted/executed as markup client-side; real sanitized rendering is Phase 6's concern |
| T-05-07-02 | Repudiation / Data Loss | Hard-delete on Blog Posts/Pricing Plans (no soft-delete/undo) | low | accept | Matches Plan 05-04's backend disposition — an explicit two-step `AlertDialog` confirmation is the only safeguard, consistent with REQUIREMENTS.md's v2-deferred full-audit-log scope |

</threat_model>

<verification>
- Task 1's automated script proves the JSON-only body handling, the aria-label on the Delete action, and all 3 new blog routes exist
- Task 2's automated script proves the dynamic features UI, the aria-label on Pricing Plans' Delete action, and the new route exist; its human-check confirms the live create/edit/toggle round trip
</verification>

<success_criteria>
Blog Posts and Pricing Plans are both fully working CRUD screens (create/edit/delete/publish-toggle) against real data — CMS-01 and CMS-03 satisfied, completing all 14 of Phase 5's requirement IDs across the 7 plans in this phase.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `apps/platform-admin/src/modules/content/use-blog-posts.ts` (new) — `useBlogPosts`, `useBlogPost`, `useCreateBlogPost`, `useUpdateBlogPost`, `useDeleteBlogPost`
- `apps/platform-admin/src/modules/content/blog-posts-page.tsx` (new) — `BlogPostsPage`
- `apps/platform-admin/src/modules/content/blog-post-form-page.tsx` (new) — `BlogPostFormPage`
- `apps/platform-admin/src/modules/content/use-pricing-plans.ts` (new) — `usePricingPlans`, `usePricingPlan`, `useCreatePricingPlan`, `useUpdatePricingPlan`, `useDeletePricingPlan`
- `apps/platform-admin/src/modules/content/pricing-plans-page.tsx` (new) — `PricingPlansPage`
- `apps/platform-admin/src/modules/content/pricing-plan-form-dialog.tsx` (new) — `PricingPlanFormDialog`
- `apps/platform-admin/src/router.tsx` (edited) — adds `content/blog`, `content/blog/new`, `content/blog/:id/edit`, `content/pricing` routes

<output>
Create `.planning/phases/05-clinic-lead-content-management/05-07-SUMMARY.md` when done
</output>
</output>
