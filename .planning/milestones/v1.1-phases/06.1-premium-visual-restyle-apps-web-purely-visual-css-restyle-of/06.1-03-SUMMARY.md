---
phase: 06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of
plan: 03
subsystem: ui
tags: [nextjs, tailwind-v4, design-system, cva, dt-tokens, home-page]

# Dependency graph
requires:
  - phase: 06.1-01
    provides: "Section, SectionHeading, Eyebrow, Stat primitives (apps/web/shared/components/) and the CVA tone-variant pattern, proven end-to-end on Home's Problem/Hero sections"
provides:
  - "Home's Solution, UnifiedSource, Features, Testimonials, CtaBanner sections migrated to Section + SectionHeading primitives"
  - "First real page-level consumer of Section's tone=\"navy\" variant (CtaBanner)"
affects: [06.1-02, 06.1-04, 06.1-05, apps-web-visual-restyle]

# Actuals (#2632)
actuals:
  tokens: 2309
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Section + SectionHeading wrapper/heading migration applied uniformly across all remaining Home sections, matching Plan 01's Problem-section precedent"

key-files:
  created: []
  modified:
    - apps/web/modules/home/solution.tsx
    - apps/web/modules/home/unified-source.tsx
    - apps/web/modules/home/features.tsx
    - apps/web/modules/home/testimonials.tsx
    - apps/web/modules/home/cta-banner.tsx

key-decisions:
  - "unified-source.tsx: used SectionHeading's description prop to preserve its existing subtext paragraph verbatim — the plan's premise that none of the three Task-1 files had a subtext paragraph was incorrect for this file; dropping the paragraph would have violated the plan's own must_haves truth (byte-identical Ukrainian copy), so the description prop (already built for exactly this purpose) was used instead. Documented as Rule 1 deviation below."
  - "cta-banner.tsx: followed the plan's literal instruction to drop the outer mx-auto max-w-2xl wrapper in favor of SectionHeading + a sibling flex button row, both inside the same Reveal — Section/Container already constrain page width and the button row's justify-center still centers it."
  - "testimonials.tsx: left all 5 text-yellow-400 star-rating occurrences and bot-tab.tsx's real-world colors untouched per D-21's explicit exception."

patterns-established: []

requirements-completed: []

coverage:
  - id: D1
    description: "Solution, UnifiedSource, Features sections render via Section + SectionHeading with byte-identical Ukrainian titles (and UnifiedSource's subtext preserved via SectionHeading's description prop); features.tsx's #features anchor target preserved"
    verification:
      - kind: unit
        ref: "grep -c title-strings in solution.tsx/features.tsx == 1, grep id=\"features\" in features.tsx — all pass"
        status: pass
      - kind: unit
        ref: "pnpm exec eslint --max-warnings 0 modules/home/{solution,unified-source,features}.tsx"
        status: pass
      - kind: unit
        ref: "pnpm exec tsc --noEmit (apps/web scope) — zero errors"
        status: pass
    human_judgment: false
  - id: D2
    description: "Testimonials and CtaBanner render via Section + SectionHeading (CtaBanner via tone=\"navy\"); Testimonials' 5 star-rating colors and CtaBanner's two CTA buttons preserved verbatim"
    verification:
      - kind: unit
        ref: "grep -c title/description strings, grep -c text-yellow-400 == 5, grep tone=\"navy\" in cta-banner.tsx — all pass"
        status: pass
      - kind: unit
        ref: "pnpm exec eslint --max-warnings 0 modules/home/{testimonials,cta-banner}.tsx"
        status: pass
      - kind: unit
        ref: "pnpm exec tsc --noEmit (apps/web scope) — zero errors"
        status: pass
    human_judgment: false
  - id: D3
    description: "No more than two coral elements visible per viewport across Home's migrated sections (D-18 visual-judgment constraint)"
    verification: []
    human_judgment: true
    rationale: "Coral-usage density is an explicit per-page visual-judgment check in CONTEXT.md (D-18), not something a grep/lint/tsc check can enforce — requires human/UAT screenshot review at all 5 breakpoints."

duration: ~15min
completed: 2026-08-18
status: complete
---

# Phase 06.1 Plan 03: Home Section Primitive Sweep (Solution/UnifiedSource/Features/Testimonials/CtaBanner) Summary

**Migrated the remaining 5 Home page sections to the Section + SectionHeading primitives built in Plan 01, completing Home's full 7-section D-17 sweep with zero Ukrainian copy changes and Section's first real navy-tone usage (CtaBanner).**

## Performance

- **Duration:** ~15 min
- **Tasks:** 2 completed
- **Files modified:** 5

## Accomplishments
- Solution, UnifiedSource, and Features sections migrated from hand-rolled `<section className="py-8 lg:py-12">` + heading-div markup to `Section` + `SectionHeading`, matching Plan 01's Problem-section precedent
- Features' `id="features"` anchor (used by Hero's "Дізнатись більше" link) preserved on the new `<Section id="features">` element
- Testimonials migrated to Section + SectionHeading; its 5 `text-yellow-400` star-rating colors left untouched per D-21's real-world-color exception
- CtaBanner migrated to `Section tone="navy"` + `SectionHeading tone="navy"` — the first real page-level consumer of Section's navy tone variant; both CTA buttons (coral "Відкрити демо бот" / outline "Переглянути адмін панель") kept as a sibling block below SectionHeading, same variants/hrefs/hover-override className, both still wrapped in the pre-existing `Reveal`
- Combined with Plan 01's Problem/Hero migration, all 7 Home sections now share the same primitive-based Section/SectionHeading structure

## Task Commits

Each task was committed atomically:

1. **Task 1: Solution, UnifiedSource, Features — Section + SectionHeading migration** - `fce6d58` (feat)
2. **Task 2: Testimonials, CtaBanner — Section + SectionHeading migration** - `1215e7d` (feat)

_No TDD tasks this plan — both were `tdd="false"`._

## Files Modified
- `apps/web/modules/home/solution.tsx` - Section + SectionHeading, title "DentaBot бере це на себе"
- `apps/web/modules/home/unified-source.tsx` - Section + SectionHeading with description prop (title + preserved subtext)
- `apps/web/modules/home/features.tsx` - Section (with `id="features"`) + SectionHeading, title "Все що потрібно для роботи клініки"
- `apps/web/modules/home/testimonials.tsx` - Section + SectionHeading, title "Що кажуть клініки", star colors untouched
- `apps/web/modules/home/cta-banner.tsx` - Section tone="navy" + SectionHeading tone="navy" (title + description), CTA button row preserved as sibling block

## Decisions Made
- Followed the plan's exact wrapper/heading swap instructions for Solution, Features, Testimonials, and CtaBanner verbatim.
- For UnifiedSource, deviated from the plan's literal "no description prop" instruction (see Deviations below) to preserve existing copy.
- For CtaBanner, followed the plan's explicit instruction to drop the outer `mx-auto max-w-2xl` centering wrapper in favor of SectionHeading + a sibling button row inside one Reveal, trusting the plan author's deliberate simplification (Section/Container already constrain overall page width).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Plan/reality mismatch] `unified-source.tsx` has a subtext paragraph the plan claimed didn't exist**
- **Found during:** Task 1
- **Issue:** The plan's Task 1 action text states "no `description` prop for any of these three (none currently have a subtext paragraph)". Reading the actual file, `unified-source.tsx` has a `<p className="mx-auto max-w-2xl text-dt-body text-dt-graphite">` subtext directly below its `<h2>` ("Бот і адміністратор працюють в одній системі запису — жодних розбіжностей і подвійних бронювань"). Following the plan literally would have silently dropped this rendered Ukrainian text, directly violating the plan's own must_haves truth ("render byte-identical Ukrainian copy to before").
- **Fix:** Used `SectionHeading`'s existing `description` prop (built for exactly this purpose, already exercised by Plan 01's `problem.tsx`) to carry the subtext verbatim.
- **Files modified:** `apps/web/modules/home/unified-source.tsx`
- **Commit:** `fce6d58`

---

**Total deviations:** 1 auto-fixed (plan/reality mismatch, resolved in favor of the plan's own zero-copy-change guarantee).
**Impact on plan:** Zero scope creep — the fix uses a primitive capability the plan itself relies on elsewhere (Plan 01's `problem.tsx` already uses `description`), just applied to a file the plan's own text incorrectly assumed didn't need it.

## Issues Encountered
- This worktree had no `node_modules` installed (fresh worktree, dependencies not hydrated). Ran `pnpm install --frozen-lockfile` at the repo root before running eslint/tsc verify steps — this resolved packages strictly from the existing, unmodified lockfile (no new dependency added, no lockfile change), consistent with the deviation-rules' package-manager-install exclusion (that exclusion targets installing *new* packages, not hydrating `node_modules` from a frozen lockfile already committed to the repo).
- `pnpm exec tsc --noEmit` from `apps/web` produced zero errors overall in this run (the previously-documented pre-existing `packages/ui/button-group.tsx` csstype error, tracked in STATE.md Deferred Items, was not surfaced by this narrower invocation — not a regression, just outside this command's scope).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All 7 Home sections (Hero, Problem, Solution, UnifiedSource, Features, CtaBanner, Testimonials) now consistently use `Section`/`SectionHeading` — Home's slice of the D-17 sweep is complete.
- `Section`'s `tone="navy"` variant now has a proven real-world page-level consumer (CtaBanner) for subsequent Wave 2 plans (Prices/Demo/Blog/Contacts) to reference.
- D-18's "no more than two coral elements per viewport" constraint on these 5 sections still needs a human/UAT screenshot pass at all 5 breakpoints — not automatable, flagged in coverage as `human_judgment: true`.

## Self-Check: PASSED

All 5 modified files confirmed present on disk with expected content; both task commit hashes (`fce6d58`, `1215e7d`) confirmed present in `git log`.

---
*Phase: 06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of*
*Completed: 2026-08-18*
