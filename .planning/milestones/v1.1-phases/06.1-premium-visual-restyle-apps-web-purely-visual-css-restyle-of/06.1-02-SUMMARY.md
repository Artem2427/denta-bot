---
phase: 06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of
plan: 02
subsystem: ui
tags: [tailwind-v4, css-custom-properties, framer-motion, cva, radix]

requires:
  - phase: 06.1-01
    provides: "dt- design tokens (--dt-border, --dt-ring, --dt-duration-fast/base) and font primitives in premium-theme.css"
provides:
  - "PremiumButton retrofitted to brief spec: coral variant font-extrabold + exact drop shadow + ~6% darker hover, outline variant hairline border, 48px/44px size floor, teal focus ring"
  - "PremiumCard/PremiumDialog/PremiumAccordion/Footer/Header hairline borders migrated from border-dt-navy/10 to --dt-border token"
  - "motion.ts revealVariants/revealContainerVariants retuned to y:14/duration:0.2/stagger:0.06 sitewide"
  - "PremiumInput/PremiumTextarea/PremiumSwitch focus-visible rings switched from navy to teal (--dt-ring)"
  - "Header scrolled state: 86% opacity, 14px backdrop blur, hairline bottom border, --dt-duration-base transition"
affects: ["06.1-03", "06.1-04", "06.1-05"]

actuals:
  tokens: 2653
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Raw CSS custom properties consumed via Tailwind arbitrary-value utilities (e.g. focus-visible:ring-[var(--dt-ring)], border-[var(--dt-border)]) rather than Tailwind color-scale references, since these tokens are plain :root custom properties, not @theme-registered color scales."

key-files:
  created: []
  modified:
    - apps/web/shared/components/premium-button.tsx
    - apps/web/shared/components/premium-card.tsx
    - apps/web/shared/lib/motion.ts
    - apps/web/shared/components/premium-input.tsx
    - apps/web/shared/components/premium-textarea.tsx
    - apps/web/shared/components/premium-switch.tsx
    - apps/web/shared/components/header.tsx
    - apps/web/shared/components/footer.tsx
    - apps/web/shared/components/premium-dialog.tsx
    - apps/web/shared/components/premium-accordion.tsx

key-decisions:
  - "coral hover darken implemented as opacity-based hover:bg-dt-coral/[0.94] (D-13's explicit accepted interpretation, no darken() function available in this codebase)"
  - "default and lg button sizes converge to the same 48px height, differentiated only by padding/text-size — expected per RESEARCH.md's D-13 finding, not a bug"
  - "Input/Textarea border-dt-navy/20 left unchanged (D-24) — form-field borders are a functionally distinct, more-visible use case than the D-04 decorative hairline"
  - "PremiumCard's highlighted prop border-2 border-dt-teal left untouched — distinct semantic highlight, not the D-04 decorative hairline"
  - "hoverLift/idleBounceAnimate/idleBounceTransition in motion.ts left untouched per D-26 (Home Hero idle-bounce stays as-is)"

patterns-established:
  - "Token consumption via arbitrary-value Tailwind utilities: var(--dt-ring), var(--dt-border), var(--dt-duration-fast), var(--dt-duration-base)"

requirements-completed: []

coverage:
  - id: D1
    description: "PremiumButton coral variant is font-extrabold with exact rgba coral shadow and ~6% darker hover; outline variant border references --dt-border; default size 48px, icon size 44px; focus ring teal"
    verification:
      - kind: unit
        ref: "eslint --max-warnings 0 shared/components/premium-button.tsx (pass); tsc --noEmit (only pre-existing spinner.tsx error present)"
        status: pass
    human_judgment: false
  - id: D2
    description: "PremiumCard default border migrated to --dt-border; motion.ts revealVariants/revealContainerVariants retuned to y:14/duration:0.2/stagger:0.06; PremiumInput/PremiumTextarea/PremiumSwitch focus rings teal"
    verification:
      - kind: unit
        ref: "eslint --max-warnings 0 shared/components/premium-card.tsx shared/lib/motion.ts shared/components/premium-input.tsx shared/components/premium-textarea.tsx shared/components/premium-switch.tsx (pass); tsc --noEmit (only pre-existing spinner.tsx error present)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Header scrolled state is 86% opaque with 14px blur and a hairline bottom border; Footer/PremiumDialog/PremiumAccordion borders migrated to --dt-border; zero border-dt-navy/10 occurrences remain in apps/web/shared/components"
    verification:
      - kind: unit
        ref: "eslint --max-warnings 0 shared/components/header.tsx shared/components/footer.tsx shared/components/premium-dialog.tsx shared/components/premium-accordion.tsx (pass); grep -rn border-dt-navy/10 shared/components/ (zero matches)"
        status: pass
      - kind: manual_procedural
        ref: "Load the site, scroll Header past 20px, confirm 86%-opacity/14px-blur/hairline-border scrolled state; tab through Contacts form, confirm teal focus ring"
        status: unknown
    human_judgment: true
    rationale: "Visual/interaction confirmation (blur radius, opacity, ring color as rendered) requires human eyes on a running browser — not fully provable from static CSS class assertions alone."

duration: 22min
completed: 2026-08-18
status: complete
---

# Phase 06.1 Plan 02: Chrome Component Retrofit Summary

**Retrofitted PremiumButton/PremiumCard/motion.ts/form-field rings/Header/Footer/PremiumDialog/PremiumAccordion to the brief's exact coral/hairline/teal-ring/motion spec via existing `--dt-` custom properties, with zero prop/API changes.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-08-18T19:24:00Z
- **Completed:** 2026-08-18T19:46:23Z
- **Tasks:** 3
- **Files modified:** 10

## Accomplishments
- PremiumButton: coral variant is now font-extrabold with the exact `rgba(232,107,90,0.28)` drop shadow and an opacity-based ~6% darker hover; outline variant's border now references `--dt-border`; default size raised to 48px tall (was 40px), icon size to 44px square (was 40px); every focus ring on the component is teal via `--dt-ring`
- PremiumCard's default border now references `--dt-border` (the `highlighted` prop's teal border left untouched)
- `motion.ts`'s `revealVariants`/`revealContainerVariants` retuned sitewide to `y:14`/`duration:0.2`/`staggerChildren:0.06` — every Reveal/StaggerGrid consumer picks this up automatically; `hoverLift`/idle-bounce constants left untouched per D-26
- PremiumInput/PremiumTextarea/PremiumSwitch focus-visible rings switched from navy to teal (`--dt-ring`), ring opacity preserved exactly; border-dt-navy/20 on form fields intentionally left unchanged (D-24)
- Header's scrolled state now matches the brief exactly: 86% opacity, 14px backdrop blur, hairline bottom border, `--dt-duration-base` transition
- All remaining `border-dt-navy/10` hairline call sites in `apps/web/shared/components` (Header mobile-menu-collapse, Footer x2, PremiumDialog, PremiumAccordion) migrated to `--dt-border`

## Task Commits

Each task was committed atomically:

1. **Task 1: PremiumButton — coral/outline spec, 48px/44px size floor, teal ring** - `75f2bdd` (feat)
2. **Task 2: PremiumCard hairline + motion.ts retune + form-field focus rings** - `448e3e9` (feat)
3. **Task 3: Header retrofit + remaining shared-component hairline call sites** - `3e0e81e` (feat)

_No TDD tasks in this plan — all `tdd="false"`, single commit per task._

## Files Created/Modified
- `apps/web/shared/components/premium-button.tsx` - coral/outline variant classes, 48px/44px size floor, teal ring, fast-duration transition
- `apps/web/shared/components/premium-card.tsx` - default border migrated to `--dt-border`
- `apps/web/shared/lib/motion.ts` - `revealVariants`/`revealContainerVariants` retuned to brief spec
- `apps/web/shared/components/premium-input.tsx` - focus ring color migrated to `--dt-ring`
- `apps/web/shared/components/premium-textarea.tsx` - focus ring color migrated to `--dt-ring`
- `apps/web/shared/components/premium-switch.tsx` - focus ring color migrated to `--dt-ring`
- `apps/web/shared/components/header.tsx` - scrolled-state opacity/blur/border/duration retrofit, mobile-menu hairline migration
- `apps/web/shared/components/footer.tsx` - two hairline borders migrated to `--dt-border`
- `apps/web/shared/components/premium-dialog.tsx` - dialog content hairline migrated to `--dt-border`
- `apps/web/shared/components/premium-accordion.tsx` - item hairline migrated to `--dt-border`

## Decisions Made
- Coral hover "6% darker" implemented as `hover:bg-dt-coral/[0.94]` (opacity-based), per D-13's explicit note that this is an acceptable interpretation absent a token-based darken function
- `default` and `lg` button sizes now both resolve to 48px height (differentiated by padding/text-size only) — this convergence is expected per RESEARCH.md's D-13 finding, not treated as a bug
- Left `hoverLift`/`idleBounceAnimate`/`idleBounceTransition` in `motion.ts` untouched, consistent with D-26 (documented in Plan 01) keeping Home Hero's idle-bounce as-is

## Deviations from Plan

None — plan executed exactly as written. All className/constant value changes only; no prop/API/behavior changes to any of the 10 components.

## Issues Encountered

**Pre-existing, out-of-scope tsc error discovered (logged, not fixed):** `packages/ui/src/components/shadcn-ui/spinner.tsx(7,6): error TS2322` appears in every `pnpm exec tsc --noEmit` run in this repo state, confirmed pre-existing via `git stash`/re-run against a clean diff. This file is not in this plan's `files_modified` list and `packages/ui` is explicitly out of scope for `apps/web`'s marketing-site restyle. Per the scope-boundary rule, this was not fixed; logged to `.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/deferred-items.md`. The plan's verify commands (which anticipated only the known `button-group.tsx` pre-existing error) still pass in substance: zero errors attributable to any file this plan touched.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All 10 shared "chrome" components used by every apps/web page now expose the brief-spec coral/hairline/teal-ring/motion treatment
- Wave 2's other four page-sweep plans (06.1-03/04/05, running in parallel in sibling worktrees) will pick up this retrofitted chrome automatically once merged — no second pass required
- Zero `border-dt-navy/10` occurrences remain in `apps/web/shared/components`; any remaining occurrences live in `apps/web/modules`, which the page-sweep plans handle separately

---
*Phase: 06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of*
*Completed: 2026-08-18*

## Self-Check: PASSED

All 10 modified component/lib files and this SUMMARY.md confirmed present on disk. All 3 task commits (75f2bdd, 448e3e9, 3e0e81e) confirmed present in `git log`.
