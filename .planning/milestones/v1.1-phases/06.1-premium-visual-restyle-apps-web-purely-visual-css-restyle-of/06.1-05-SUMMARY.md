---
phase: 06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of
plan: 05
subsystem: ui
tags: [nextjs, tailwind-v4, css-tokens, design-system, demo, blog]

# Dependency graph
requires:
  - phase: 06.1-01
    provides: "Section/SectionHeading/Eyebrow primitives, --dt-border/--dt-ring/--radius-dt-frame(-inner)/--shadow-dt-hover tokens this plan consumes"
provides:
  - "Demo route (app/demo/page.tsx) fully migrated to Section primitive"
  - "bot-tab.tsx phone-mockup frame chrome fully dt- token-based (radius, shadow, hairline borders)"
  - "Blog list/detail routes migrated to Section/SectionHeading primitives"
  - "related-posts.tsx anti-pattern (bare <section>, zero padding) closed — last inconsistent-rhythm section sitewide fixed"
affects: [06.1-06, apps-web-visual-restyle]

# Actuals (#2632)
actuals:
  tokens: 2174
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Section/SectionHeading primitives applied identically across Demo and Blog routes, matching the pattern proven by Plan 01 and used by sibling Wave 2 plans (02/03/04)"

key-files:
  created: []
  modified:
    - apps/web/app/demo/page.tsx
    - apps/web/modules/demo/bot-tab.tsx
    - apps/web/app/blog/page.tsx
    - "apps/web/app/blog/[slug]/page.tsx"
    - apps/web/modules/blog/related-posts.tsx

key-decisions:
  - "Followed CONTEXT.md's D-17/D-22/D-23/D-04 verbatim: bot-tab.tsx's rounded-[3rem]/rounded-[2.5rem] became rounded-dt-frame/rounded-dt-frame-inner, shadow-2xl became shadow-[var(--shadow-dt-hover)], and its 3 border-dt-navy/10 hairline occurrences became border-[var(--dt-border)]; bg-gray-900 bezel and both text-dt-caption chat-timestamp occurrences (lines 169-170) left untouched per D-21 and Plan 01's explicit note"
  - "[slug] page's bottom CTA callout kept its <div> shape (decorative in-article callout, not a page landmark) while its heading+subtext pair was extracted into SectionHeading tone=\"navy\", per the plan's explicit instruction"
  - "related-posts.tsx's previously-bare <section> (RESEARCH.md's flagged anti-pattern, the one section sitewide with zero padding className) now wraps in Section with the sitewide default vertical rhythm, and its heading now goes through SectionHeading (normalizing mb-8 to SectionHeading's mb-dt-48, matching every other migrated heading)"

patterns-established: []

requirements-completed: []

coverage:
  - id: T1
    description: "Demo page's bot phone-mockup frame renders with dt- radius tokens instead of arbitrary rounded-[3rem]/rounded-[2.5rem] values, and its shadow references --shadow-dt-hover instead of plain Tailwind shadow-2xl"
    verification:
      - kind: unit
        ref: "grep confirms zero rounded-[ occurrences remain in bot-tab.tsx; rounded-dt-frame/rounded-dt-frame-inner and shadow-[var(--shadow-dt-hover)] present; bg-gray-900 and 2x text-dt-caption preserved"
        status: pass
    human_judgment: false
  - id: T2
    description: "Blog's list page and post-detail page render byte-identical Ukrainian copy and unchanged post data (fetch calls untouched)"
    verification:
      - kind: unit
        ref: "git diff restricted to +/- lines confirms only JSX structure (Section/SectionHeading wrapping, prop extraction) changed — zero fetch()/getServerApiUrl/notFound/extractPostBodyBlocks calls touched, zero Ukrainian string literal content altered"
        status: pass
    human_judgment: false
  - id: T3
    description: "Related Posts' heading now has the same vertical rhythm every other section on the site has, closing the one section sitewide that previously had no padding className at all"
    verification:
      - kind: unit
        ref: "modules/blog/related-posts.tsx's bare <section> replaced with Section (default clamp(64px,8vw,112px) rhythm); h2 replaced with SectionHeading"
        status: pass
    human_judgment: true
    rationale: "Visual spacing/rhythm confirmation across the rendered page is a human-judgment UAT concern; automated check confirms the primitive swap, not the rendered pixel result"

duration: 20min
completed: 2026-08-18
status: complete
---

# Phase 06.1 Plan 05: Demo + Blog D-17 Sweep Summary

**Migrated Demo and Blog routes to the Section/SectionHeading primitives and finished the bot-tab.tsx phone-frame chrome's D-22/D-23/D-04 token migration — completing the D-17 sweep on the last two of the phase's 5 routes.**

## Performance

- **Duration:** ~20 min
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- `app/demo/page.tsx`: both `<section>` blocks wrapped in `Section` — the hero block keeps its `pb-12` override, the `DemoTabs` wrapper adopts the new default vertical rhythm (previously had no padding className at all)
- `modules/demo/bot-tab.tsx`: phone-mockup frame chrome fully migrated to `dt-` tokens — `rounded-[3rem]`→`rounded-dt-frame`, `rounded-[2.5rem]`→`rounded-dt-frame-inner`, `shadow-2xl`→`shadow-[var(--shadow-dt-hover)]`, and all 3 `border-dt-navy/10` hairline occurrences (message bubble, typing-indicator bubble, scenario-list divider) → `border-[var(--dt-border)]`; `bg-gray-900` bezel and both `text-dt-caption` chat-timestamp occurrences left untouched per D-21
- `app/blog/page.tsx`: hero-title, featured-post, and filters-wrapper `<section>` blocks (main return path) all wrapped in `Section className="pb-12"`, preserving existing padding; empty-state early-return left untouched
- `app/blog/[slug]/page.tsx`: bottom CTA callout's heading+subtext extracted into `SectionHeading tone="navy"`, keeping the `<div>` shape (decorative in-article callout) and the two `PremiumButton` CTAs in their own sibling block exactly as before
- `modules/blog/related-posts.tsx`: closed RESEARCH.md's flagged anti-pattern — the previously-bare `<section>` (zero padding className, the one inconsistent section sitewide) now wraps in `Section` with the default rhythm, and its `h2` now renders via `SectionHeading`

## Task Commits

Each task was committed atomically:

1. **Task 1: Demo sweep — page Section wrap + bot-tab.tsx phone-frame tokens** - `805b953` (feat)
2. **Task 2: Blog sweep — list/detail pages + related-posts.tsx anti-pattern fix** - `ad36c3c` (feat)

_No TDD tasks this plan — both were `tdd="false"`._

## Files Created/Modified
- `apps/web/app/demo/page.tsx` - both sections wrapped in `Section`
- `apps/web/modules/demo/bot-tab.tsx` - phone-frame radius/shadow/hairline-border tokens migrated
- `apps/web/app/blog/page.tsx` - hero/featured/filters sections wrapped in `Section`
- `apps/web/app/blog/[slug]/page.tsx` - bottom CTA heading+subtext extracted into `SectionHeading tone="navy"`
- `apps/web/modules/blog/related-posts.tsx` - bare `<section>` wrapped in `Section`, heading migrated to `SectionHeading`

## Decisions Made
- Followed CONTEXT.md's D-17/D-04/D-21/D-22/D-23 verbatim — no re-litigation of the phone-bezel `bg-gray-900` exception or the `text-dt-caption` timestamp exclusion (both explicitly called out in the plan as untouched).
- `[slug]` page's CTA stayed a `<div>` (not a `Section`) per the plan's explicit instruction — it's a decorative in-article callout, not a page-level landmark.
- `related-posts.tsx`'s `mb-8`→`mb-dt-48` spacing change (via `SectionHeading`) is the intended consistency fix per the plan, not scope overreach.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Worktree had no `node_modules` installed**
- **Found during:** Task 1 verify (eslint not found)
- **Issue:** This worktree was freshly created and had no `node_modules` — `pnpm exec eslint`/`tsc` failed with `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL`
- **Fix:** Ran `pnpm install --frozen-lockfile` at the repo root (fast, reused the pnpm content-addressable store — 9.7s, zero lockfile changes)
- **Files modified:** None (node_modules is gitignored)
- **Committed in:** N/A

**2. [Rule 1 - False positive] `pnpm exec tsc --noEmit` reports a pre-existing, unrelated csstype error in `packages/ui/src/components/shadcn-ui/spinner.tsx`**
- **Found during:** Task 1 and Task 2 verify
- **Issue:** The plan's verify command excludes only the known `button-group.tsx` csstype conflict (documented in `.planning/STATE.md` Deferred Items), but the same pre-existing, monorepo-wide csstype duplicate-resolution conflict also surfaces on `spinner.tsx` — a file untouched by this plan or any sibling Wave 2 plan (all of which touch only `apps/web`)
- **Fix:** Confirmed via `git log`/`git status` that `spinner.tsx` was not modified by this plan (last touched by an unrelated prior-phase commit `a23f7e2`); documented here rather than "fixed" since it's out of this plan's scope (STATE.md already tracks the monorepo-wide `pnpm.overrides` fix as a deferred item)
- **Files modified:** None
- **Committed in:** N/A

---

**Total deviations:** 2 (1 environment setup, 1 documented pre-existing false-positive) — neither required any change to shipped code beyond what the plan already specified.
**Impact on plan:** Zero scope creep.

## Issues Encountered
None beyond the two verify-environment deviations documented above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Combined with sibling Wave 2 Plans 02/03/04, all 5 of the phase's routes (Home, Prices, Contacts, Demo, Blog) have completed the D-17 primitive sweep.
- No blockers introduced by this plan. The pre-existing `packages/ui` csstype conflict (now confirmed to also affect `spinner.tsx`, not just `button-group.tsx`) remains open per STATE.md Deferred Items and is unrelated to this plan's file set.

## Self-Check: PASSED

All 5 modified files confirmed present on disk with expected content; both task commit hashes (`805b953`, `ad36c3c`) confirmed present in `git log`.

---
*Phase: 06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of*
*Completed: 2026-08-18*
