---
phase: 06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of
plan: 04
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - apps/web/modules/prices/pricing-cards.tsx
  - apps/web/modules/prices/comparison-table.tsx
  - apps/web/app/prices/page.tsx
  - apps/web/app/contacts/page.tsx
  - apps/web/modules/contacts/contact-info.tsx
autonomous: true
requirements: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Prices' comparison table and FAQ section, and Contacts' FAQ section, all render via Section tone=\"muted\" instead of each file's own duplicated bg-dt-navy/5 py-8 lg:py-12 className"
    - "Contacts' three benefit chips (green/blue/purple) now render via dt- semantic color tokens instead of Tailwind's default green/blue/purple palette, with the same 3 icons and the same Ukrainian labels"
    - "Prices and Contacts pages render byte-identical Ukrainian copy and unchanged plan/pricing data (still fetched from the real API per Phase 6 — this plan does not touch any fetch call)"
  artifacts: []
  key_links:
    - from: "apps/web/modules/prices/comparison-table.tsx"
      to: "apps/web/shared/components/section.tsx"
      via: "Section tone=\"muted\" replacing bg-dt-navy/5 py-8 lg:py-12"
      pattern: "tone=\"muted\""
    - from: "apps/web/app/contacts/page.tsx"
      to: "apps/web/shared/components/section.tsx"
      via: "Section tone=\"muted\" replacing bg-dt-navy/5 py-8 lg:py-12 on the FAQ block"
      pattern: "tone=\"muted\""
---

<objective>
Apply D-17's primitive sweep to the Prices and Contacts routes: Section/SectionHeading migration across `pricing-cards.tsx`, `comparison-table.tsx`, `app/prices/page.tsx`'s FAQ section, `app/contacts/page.tsx`'s three sections, plus D-21's hardcoded-color migration in `contact-info.tsx`. `Section tone="muted"` (new in Plan 01) gets its first two real page-level consumers here — the exact-duplicate `bg-dt-navy/5 py-8 lg:py-12` string RESEARCH.md found identically in these two FAQ-shaped sections is the reason that tone exists.

Purpose: completes the D-17 sweep on two of the five routes. Runs in parallel with Plans 02/03/05 — all depend only on Plan 01, and this plan's file list has zero overlap with any other Wave 2 plan.
Output: Prices and Contacts routes fully migrated to Section/SectionHeading; contact-info.tsx's benefit chips migrated to dt- tokens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-CONTEXT.md
@.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-RESEARCH.md
@apps/web/shared/components/section.tsx
@apps/web/shared/components/section-heading.tsx
@apps/web/app/prices/page.tsx
@apps/web/modules/prices/pricing-cards.tsx
@apps/web/modules/prices/comparison-table.tsx
@apps/web/app/contacts/page.tsx
@apps/web/modules/contacts/contact-info.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Prices sweep — pricing-cards.tsx, comparison-table.tsx, prices/page.tsx FAQ</name>
  <files>apps/web/modules/prices/pricing-cards.tsx, apps/web/modules/prices/comparison-table.tsx, apps/web/app/prices/page.tsx</files>
  <action>
    In apps/web/modules/prices/pricing-cards.tsx: this file's two `<section>` blocks (the `pb-12` hero-title block and the `pb-8 lg:pb-12` cards-grid block) are page-hero-shaped, not the standard mid-page rhythm — wrap each in `<Section>` with a className override preserving its current exact padding (`className="pb-12"` on the first, `className="pb-8 lg:pb-12"` on the second — cn's tailwind-merge lets these override Section's default py- clamp), rather than adopting Section's default vertical rhythm, consistent with Plan 01's guidance to leave page-hero-shaped sections' existing offsets alone. Also add `tabular-nums` to the price `<span className="text-dt-h2 font-dt-heading font-bold text-dt-navy">{isYearly ? plan.yearlyPrice : plan.monthlyPrice} ₴</span>` element, so the digits don't visually reflow when the yearly/monthly switch toggles (D-11's "KPI/number display" intent, applied as a minimal className addition here rather than forcing this inline value+unit-suffix layout into the Stat primitive's stacked value+label shape, which doesn't match this layout).

    In apps/web/modules/prices/comparison-table.tsx (D-19, D-04): replace `<section className="bg-dt-navy/5 py-8 lg:py-12">` with `<Section tone="muted">`. Replace the `<Reveal><h2 className="mb-12 text-center text-dt-h2 font-dt-heading font-bold text-dt-navy">Детальне порівняння планів</h2></Reveal>` with `<Reveal><SectionHeading title="Детальне порівняння планів" /></Reveal>`. Migrate this file's two `border-dt-navy/10` occurrences (the table header row's bottom border and each body row's bottom border) to `border-[var(--dt-border)]`.

    In apps/web/app/prices/page.tsx (D-19): replace the FAQ `<section className="bg-dt-navy/5 py-8 lg:py-12">` with `<Section tone="muted">`, and its `<Reveal><h2 className="mb-12 text-center text-dt-h2 font-dt-heading font-bold text-dt-navy">Часті питання</h2></Reveal>` with `<Reveal><SectionHeading title="Часті питання" /></Reveal>`. This file's other two states (the plans.length===0 empty-state block and the main return's overall `<div className="min-h-screen pb-16 pt-24 lg:pt-32">` wrapper) are page-chrome, not the duplicated Section pattern — leave them untouched. Do not touch this file's `fetch()` call or any plan-data logic — this is a visual-only change to an async Server Component.
  </action>
  <verify>
    <automated>cd apps/web && grep -c "Прості та прозорі ціни" modules/prices/pricing-cards.tsx | grep -qx 1 && grep -q 'tone="muted"' modules/prices/comparison-table.tsx && grep -c "Детальне порівняння планів" modules/prices/comparison-table.tsx | grep -qx 1 && grep -q 'tone="muted"' app/prices/page.tsx && grep -c "Часті питання" app/prices/page.tsx | grep -qx 1 && pnpm exec eslint --max-warnings 0 modules/prices/pricing-cards.tsx modules/prices/comparison-table.tsx app/prices/page.tsx && ! pnpm exec tsc --noEmit 2>&1 | grep "error TS" | grep -v "button-group.tsx"</automated>
  </verify>
  <done>pricing-cards.tsx's two sections use Section with their original padding preserved and tabular-nums on the price display; comparison-table.tsx renders via Section tone="muted" + SectionHeading with hairline borders migrated; prices/page.tsx's FAQ section renders via Section tone="muted" + SectionHeading; the fetch/data logic in prices/page.tsx is byte-identical to before.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Contacts sweep — contacts/page.tsx sections + contact-info.tsx D-21 colors</name>
  <files>apps/web/app/contacts/page.tsx, apps/web/modules/contacts/contact-info.tsx</files>
  <action>
    In apps/web/app/contacts/page.tsx: this page has 3 `<section>` blocks. The first (hero title, `pb-12 pt-24 lg:pb-16 lg:pt-32`) is page-hero-shaped — wrap in `<Section className="pb-12 pt-24 lg:pb-16 lg:pt-32">`, preserving its exact current offset rather than Section's default rhythm (same reasoning as pricing-cards.tsx's hero block in Task 1). The second (the ContactForm/ContactInfo two-column grid, `pb-8 lg:pb-12`) is a mid-page content block — wrap in `<Section className="pb-8 lg:pb-12">` (still overriding padding since it has no top padding of its own, relying on the previous section's bottom spacing; do not let Section's symmetric py- clamp add unwanted top padding here). The third (FAQ, D-19) — replace `<section className="bg-dt-navy/5 py-8 lg:py-12">` with `<Section tone="muted">`, and its `<Reveal><h2 className="text-dt-h2 font-dt-heading font-bold text-dt-navy text-center mb-12">Часті питання</h2></Reveal>` with `<Reveal><SectionHeading title="Часті питання" /></Reveal>`.

    In apps/web/modules/contacts/contact-info.tsx (D-21): migrate the three `benefits` entries' hardcoded Tailwind palette colors to the fixed dt- palette: the `bg: 'bg-green-50', iconColor: 'text-green-600'` entry (Clock icon, "Відповідаємо за 2 години") becomes `bg: 'bg-dt-teal/10', iconColor: 'text-dt-teal'`; the `bg: 'bg-blue-50', iconColor: 'text-blue-600'` entry (Sparkle icon, "Безкоштовна демонстрація") becomes `bg: 'bg-dt-navy/10', iconColor: 'text-dt-navy'` (blue has no direct warm-palette analog — navy is the nearest cool/neutral accent in the fixed 7-color palette); the `bg: 'bg-purple-50', iconColor: 'text-purple-600'` entry (Lightning icon, "Налаштування за 1 день") becomes `bg: 'bg-dt-coral/10', iconColor: 'text-dt-coral'`. Keep every icon component, label string, and the `contactMethods` array (already token-clean, uses `bg-dt-navy`/`text-dt-warm-white`) exactly as-is — this task only changes the 6 hardcoded `bg`/`iconColor` string values in the `benefits` array.
  </action>
  <verify>
    <automated>cd apps/web && grep -q 'tone="muted"' app/contacts/page.tsx && grep -c "Часті питання" app/contacts/page.tsx | grep -qx 1 && ! grep -qE "bg-(green|blue|purple)-[0-9]+|text-(green|blue|purple)-[0-9]+" modules/contacts/contact-info.tsx && grep -c "Відповідаємо за 2 години" modules/contacts/contact-info.tsx | grep -qx 1 && pnpm exec eslint --max-warnings 0 app/contacts/page.tsx modules/contacts/contact-info.tsx && ! pnpm exec tsc --noEmit 2>&1 | grep "error TS" | grep -v "button-group.tsx"</automated>
  </verify>
  <done>Contacts' 3 sections all render via Section (hero/form sections with preserved padding, FAQ via tone="muted" + SectionHeading); contact-info.tsx's 3 benefit chips use dt- tokens with zero remaining green/blue/purple Tailwind palette classes, same icons and Ukrainian labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Prices page async Server Component | Fetches real pricing-plan data from apps/server's public API at request/build time — this plan touches only the JSX wrapper around that data, never the fetch() call, its URL, or its error handling |
| Contacts page ContactForm | react-hook-form + zod validated lead-submission form — this plan touches only the page-level Section wrapper around ContactForm, not contact-form.tsx itself (already retrofitted via Plan 02's PremiumInput/Button changes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-06.1-07 | Tampering | app/prices/page.tsx JSX wrapper restructuring around its existing fetch() call | low | accept | The fetch URL, revalidate option, and error-throw path are outside this task's edits (only the surrounding <section>/<h2> markup changes) — confirmed by reading the file before editing |
| T-06.1-08 | Information Disclosure | contact-info.tsx className/color-token changes | low | accept | Purely presentational string values (bg/iconColor) in a static array — no data flow change |

</threat_model>

<verification>
Run `cd apps/web && pnpm exec eslint --max-warnings 0 modules/prices/pricing-cards.tsx modules/prices/comparison-table.tsx app/prices/page.tsx app/contacts/page.tsx modules/contacts/contact-info.tsx` and confirm zero output. Run `cd apps/web && pnpm exec tsc --noEmit 2>&1 | grep "error TS"` and confirm only the known pre-existing packages/ui/button-group.tsx line. Load /prices and /contacts and diff against pre-phase screenshots at all 5 breakpoints to confirm no text lost, no horizontal scroll, and the FAQ sections' muted background band renders identically on both pages (same color, same top/bottom edges). <human-check>Confirm no more than two coral elements are visible in any single viewport on /prices and /contacts (D-18).</human-check>
</verification>

<success_criteria>
Prices and Contacts routes fully adopt Section/SectionHeading with the new muted tone shared identically between both FAQ sections and comparison-table.tsx; contact-info.tsx's D-21 color migration is complete with zero non-dt hardcoded palette colors remaining in this file.
</success_criteria>

<output>
Create `.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-04-SUMMARY.md` when done
</output>
