---
phase: 06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of
plan: 04
subsystem: ui
tags: [nextjs, tailwind-v4, design-system, react]

# Dependency graph
requires:
  - phase: 06.1-01
    provides: Section/Eyebrow/SectionHeading primitives, dt- tokens (--dt-border, --dt-ring, spacing scale), Manrope+JetBrains Mono fonts
provides:
  - Prices route (pricing-cards.tsx, comparison-table.tsx, app/prices/page.tsx) fully migrated to Section/SectionHeading primitives
  - Contacts route (app/contacts/page.tsx) fully migrated to Section primitives
  - contact-info.tsx benefit chips migrated from hardcoded Tailwind green/blue/purple to dt-teal/dt-navy/dt-coral tokens (D-21)
  - First two real consumers of Section tone="muted" (comparison-table.tsx FAQ-shaped section, prices/page.tsx FAQ, contacts/page.tsx FAQ)
affects: [06.1 remaining waves, any future prices/contacts visual work]

actuals:
  tokens: 2161
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Section with className override (tailwind-merge) for page-hero-shaped sections that must preserve exact original padding instead of Section's default py- clamp"
    - "Section tone=\"muted\" for FAQ-shaped bg-dt-navy/5 py-8 lg:py-12 sections"
    - "border-[var(--dt-border)] as the canonical hairline border replacing border-dt-navy/10"
    - "tabular-nums on price digit displays to prevent reflow on toggle"

key-files:
  created: []
  modified:
    - apps/web/modules/prices/pricing-cards.tsx
    - apps/web/modules/prices/comparison-table.tsx
    - apps/web/app/prices/page.tsx
    - apps/web/app/contacts/page.tsx
    - apps/web/modules/contacts/contact-info.tsx

key-decisions:
  - "Pricing-cards.tsx and contacts/page.tsx's hero/form sections wrapped in Section with explicit className overrides preserving their exact original padding, rather than adopting Section's default vertical rhythm — consistent with Plan 01's guidance for page-hero-shaped sections"
  - "Blue benefit chip (Sparkle icon) mapped to dt-navy since blue has no direct warm-palette analog in the fixed 7-color dt- palette"

patterns-established:
  - "Section tone=\"muted\" is now the shared pattern for all FAQ-shaped bg-dt-navy/5 py-8 lg:py-12 blocks across routes"

requirements-completed: []

coverage:
  - id: D1
    description: "Prices route (pricing-cards.tsx, comparison-table.tsx, app/prices/page.tsx FAQ) migrated to Section/SectionHeading primitives with tabular-nums on price display"
    verification:
      - kind: other
        ref: "eslint --max-warnings 0 on modules/prices/pricing-cards.tsx modules/prices/comparison-table.tsx app/prices/page.tsx; grep checks for tone=\"muted\" and unchanged Ukrainian headings"
        status: pass
    human_judgment: true
    rationale: "Visual rendering (FAQ muted-band color/edges, no horizontal scroll, coral-element cap per D-18) requires screenshot comparison at 5 breakpoints per the plan's <human-check> gate — not automatable from this executor."
  - id: D2
    description: "Contacts route (3 sections) migrated to Section primitives; contact-info.tsx's 3 benefit chips migrated to dt-teal/dt-navy/dt-coral tokens"
    verification:
      - kind: other
        ref: "eslint --max-warnings 0 on app/contacts/page.tsx modules/contacts/contact-info.tsx; grep checks confirming zero remaining bg-(green|blue|purple)-* / text-(green|blue|purple)-* classes and unchanged Ukrainian labels"
        status: pass
    human_judgment: true
    rationale: "Visual rendering (FAQ muted-band matching Prices identically, no horizontal scroll, coral-element cap per D-18) requires screenshot comparison at 5 breakpoints per the plan's <human-check> gate."

duration: 15min
completed: 2026-08-18
status: complete
---

# Phase 06.1 Plan 04: Prices + Contacts Section/SectionHeading Sweep Summary

**Migrated Prices and Contacts routes to the Section/SectionHeading primitives from Plan 01, introducing the first two real consumers of `Section tone="muted"` and completing the D-21 hardcoded-color migration in contact-info.tsx's benefit chips.**

## Performance

- **Duration:** ~15 min
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- `pricing-cards.tsx`'s two hero-shaped sections wrapped in `Section` with exact original padding preserved (`pb-12` / `pb-8 lg:pb-12`); price display gets `tabular-nums` so digits don't reflow on the monthly/yearly toggle
- `comparison-table.tsx` renders via `Section tone="muted"` + `SectionHeading`, with its two `border-dt-navy/10` hairlines migrated to `border-[var(--dt-border)]`
- `app/prices/page.tsx`'s FAQ section renders via `Section tone="muted"` + `SectionHeading`; the `fetch()` call and plan-data logic are byte-identical to before
- `app/contacts/page.tsx`'s all 3 sections migrated to `Section` — hero and form-grid sections with preserved padding via className overrides, FAQ via `tone="muted"` + `SectionHeading`
- `contact-info.tsx`'s 3 benefit chips (Clock/Sparkle/Lightning) migrated from hardcoded `bg-green-50`/`bg-blue-50`/`bg-purple-50` + matching text colors to `bg-dt-teal/10`/`bg-dt-navy/10`/`bg-dt-coral/10` with matching `text-dt-*` icon colors — same 3 icons, same Ukrainian labels

## Task Commits

Each task was committed atomically:

1. **Task 1: Prices sweep — pricing-cards.tsx, comparison-table.tsx, prices/page.tsx FAQ** - `5e61530` (feat)
2. **Task 2: Contacts sweep — contacts/page.tsx sections + contact-info.tsx D-21 colors** - `b833ebe` (feat)

_No TDD tasks in this plan; both were purely visual `type="auto"` changes._

## Files Created/Modified
- `apps/web/modules/prices/pricing-cards.tsx` - Two sections wrapped in `Section` with preserved padding; `tabular-nums` on price display
- `apps/web/modules/prices/comparison-table.tsx` - `Section tone="muted"` + `SectionHeading`; hairline borders migrated to `border-[var(--dt-border)]`
- `apps/web/app/prices/page.tsx` - FAQ section migrated to `Section tone="muted"` + `SectionHeading`; fetch/data logic untouched
- `apps/web/app/contacts/page.tsx` - All 3 sections migrated to `Section` (hero/form padding preserved, FAQ via `tone="muted"` + `SectionHeading`)
- `apps/web/modules/contacts/contact-info.tsx` - 3 benefit chips migrated from Tailwind green/blue/purple to dt-teal/dt-navy/dt-coral tokens

## Decisions Made
- Preserved exact original padding on page-hero-shaped sections (pricing-cards.tsx's hero/cards-grid, contacts/page.tsx's hero/form-grid) via `Section` className overrides rather than adopting Section's default `py-` clamp rhythm — matches Plan 01's guidance for page-hero-shaped blocks.
- Mapped the Sparkle/blue benefit chip to `dt-navy` (not a warm-palette color) since blue has no direct analog in the fixed 7-color dt- palette — this was the plan's explicit instruction, not an improvisation.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered
- Pre-existing TypeScript error in `packages/ui/src/components/shadcn-ui/spinner.tsx` (unrelated to this plan's files) surfaces in `tsc --noEmit` alongside the known `button-group.tsx` line. Confirmed via `git stash`/`tsc`/`git stash pop` that this error exists identically before this plan's changes — it is out of scope (packages/ui, not touched by this plan) and was not fixed, per the deviation-rules scope boundary.
- `node_modules` was not present in this worktree at start; ran `pnpm install --frozen-lockfile` at the worktree root before running eslint/tsc (no lockfile or package.json changes resulted).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Prices and Contacts routes fully adopt Section/SectionHeading; `tone="muted"` now has 3 real consumers total (comparison-table.tsx, prices FAQ, contacts FAQ) rendering identically.
- Human verification still required for the plan's `<human-check>` gate (D-18 coral-element cap, muted-band visual match across both pages at 5 breakpoints) — not automatable from this executor; flagged in `coverage` above.
- No blockers for other Wave 2 plans (02/03/05) — this plan's file list (prices/contacts modules + pages) has zero overlap with theirs.
