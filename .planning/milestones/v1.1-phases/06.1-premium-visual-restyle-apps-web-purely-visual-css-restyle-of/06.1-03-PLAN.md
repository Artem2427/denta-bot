---
phase: 06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of
plan: 03
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - apps/web/modules/home/solution.tsx
  - apps/web/modules/home/unified-source.tsx
  - apps/web/modules/home/features.tsx
  - apps/web/modules/home/testimonials.tsx
  - apps/web/modules/home/cta-banner.tsx
autonomous: true
requirements: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Home's Solution, UnifiedSource, Features, Testimonials, and CtaBanner sections render byte-identical Ukrainian copy to before, now composed from Section + SectionHeading instead of each file's own duplicated <section>/heading-div markup"
    - "The Home page's full section order (Hero, Problem, Solution, UnifiedSource, Features, CtaBanner, Testimonials) and each section's PremiumCard/StaggerGrid content is unchanged — only the outer wrapper and heading markup shapes changed"
  artifacts: []
  key_links:
    - from: "apps/web/modules/home/cta-banner.tsx"
      to: "apps/web/shared/components/section.tsx"
      via: "Section tone=\"navy\" replacing the hand-rolled bg-dt-navy section className"
      pattern: "tone=\"navy\""
---

<objective>
Complete Home's D-17 primitive sweep: apply the Section + SectionHeading primitives (built in Plan 01, Wave 1) to the five Home sections not touched by Plan 01's tracer task — Solution, UnifiedSource, Features, Testimonials, CtaBanner. Every change is a wrapper/heading markup swap; no rendered Ukrainian copy changes anywhere in this plan.

Purpose: finishes the largest single-page slice of D-17's sweep (Home has 7 sections total; Problem and Hero were already migrated in Plan 01's tracer). Runs in parallel with Plans 02/04/05 — all four Wave 2 plans depend only on Plan 01 and touch disjoint files.
Output: 5 Home module files migrated to Section/SectionHeading, zero copy changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-CONTEXT.md
@.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-RESEARCH.md
@apps/web/shared/components/section.tsx
@apps/web/shared/components/section-heading.tsx
@apps/web/modules/home/problem.tsx
@apps/web/modules/home/solution.tsx
@apps/web/modules/home/unified-source.tsx
@apps/web/modules/home/features.tsx
@apps/web/modules/home/testimonials.tsx
@apps/web/modules/home/cta-banner.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Solution, UnifiedSource, Features — Section + SectionHeading migration</name>
  <files>apps/web/modules/home/solution.tsx, apps/web/modules/home/unified-source.tsx, apps/web/modules/home/features.tsx</files>
  <action>
    All three files share the exact `<section className="py-8 lg:py-12"><Container><Reveal><div className="mb-12 text-center"><h2 className="text-dt-h2 font-dt-heading font-bold text-dt-navy">{title}</h2></div></Reveal>...` shape (D-17, matching the Pattern 1/Pattern 2 duplication RESEARCH.md catalogued). For each of the three files, import Section from '@/shared/components/section' and SectionHeading from '@/shared/components/section-heading' (mirroring the imports already applied to problem.tsx in Plan 01), replace the outer `<section className="py-8 lg:py-12">` with `<Section>` (default tone="warm-white", no className override needed since none of these three sections currently carry a background color), and replace the inner `<Reveal><div className="mb-12 text-center"><h2 ...>{title}</h2></div></Reveal>` with `<Reveal><SectionHeading title="{exact existing Ukrainian h2 text}" /></Reveal>` — no `description` prop for any of these three (none currently have a subtext paragraph) and no `eyebrow` prop (per Plan 01's scope note — do not invent new label copy). Leave every StaggerGrid/PremiumCard/icon/content block below the heading completely untouched in all three files — this task only touches the outer `<section>` tag and the heading div.

    Exact titles to preserve verbatim: solution.tsx — "DentaBot бере це на себе"; unified-source.tsx — "Єдине джерело правди — незалежно від каналу запису"; features.tsx — "Все що потрібно для роботи клініки". features.tsx's `<section id="features" ...>` id attribute (used by Home's Hero "Дізнатись більше" anchor link) must be preserved on the new `<Section id="features">` element — do not drop it.
  </action>
  <verify>
    <automated>cd apps/web && grep -c "DentaBot бере це на себе" modules/home/solution.tsx | grep -qx 1 && grep -c "Все що потрібно для роботи клініки" modules/home/features.tsx | grep -qx 1 && grep -q 'id="features"' modules/home/features.tsx && pnpm exec eslint --max-warnings 0 modules/home/solution.tsx modules/home/unified-source.tsx modules/home/features.tsx && ! pnpm exec tsc --noEmit 2>&1 | grep "error TS" | grep -v "button-group.tsx"</automated>
  </verify>
  <done>All three sections render via Section + SectionHeading with byte-identical Ukrainian titles; features.tsx's #features anchor target is preserved; StaggerGrid/PremiumCard content below each heading is untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Testimonials, CtaBanner — Section + SectionHeading migration</name>
  <files>apps/web/modules/home/testimonials.tsx, apps/web/modules/home/cta-banner.tsx</files>
  <action>
    In apps/web/modules/home/testimonials.tsx: same migration as Task 1's pattern — `<section className="py-8 lg:py-12">` becomes `<Section>`, the `<Reveal><div className="mb-12 text-center"><h2>Що кажуть клініки</h2></div></Reveal>` becomes `<Reveal><SectionHeading title="Що кажуть клініки" /></Reveal>`. Explicit no-change note (D-21): leave the Stars component's five `text-yellow-400` occurrences exactly as-is — star-rating yellow is a "real-world" semantic color with no defensible dt- palette equivalent, per CONTEXT.md's explicit exception (only contact-info.tsx and problem.tsx's icon-chip colors are in scope for the D-21 migration, both handled elsewhere in this phase).

    In apps/web/modules/home/cta-banner.tsx: this section's shape differs slightly from the other 8 duplicated blocks — its heading/subtext/button-row all live inside one `<div className="mx-auto max-w-2xl space-y-6 text-center">`, on a `bg-dt-navy` background. Replace `<section className="bg-dt-navy py-8 lg:py-12">` with `<Section tone="navy">` (D-11's navy tone, first real page-level consumer). Extract just the heading+subtext pair into SectionHeading with `tone="navy"`: `<SectionHeading tone="navy" title="Спробуйте як це працює прямо зараз" description="Живий демо бот. Без реєстрації. Займає 2 хвилини." />`, keeping the two PremiumButton CTAs ("Відкрити демо бот" / "Переглянути адмін панель") in their own sibling block below SectionHeading exactly as they render today (same `flex flex-col justify-center gap-4 sm:flex-row` wrapper, same button variants/hrefs/hover-override className on the second button). Wrap both SectionHeading and the button row in the same outer `<Reveal>` that already exists.
  </action>
  <verify>
    <automated>cd apps/web && grep -c "Що кажуть клініки" modules/home/testimonials.tsx | grep -qx 1 && grep -c "text-yellow-400" modules/home/testimonials.tsx | grep -qx 5 && grep -c "Живий демо бот" modules/home/cta-banner.tsx | grep -qx 1 && grep -q 'tone="navy"' modules/home/cta-banner.tsx && pnpm exec eslint --max-warnings 0 modules/home/testimonials.tsx modules/home/cta-banner.tsx && ! pnpm exec tsc --noEmit 2>&1 | grep "error TS" | grep -v "button-group.tsx"</automated>
  </verify>
  <done>Testimonials renders via Section + SectionHeading with its 5 star-rating colors untouched; CtaBanner renders via Section tone="navy" + SectionHeading tone="navy" with both CTA buttons preserved verbatim below it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Static marketing content | All 5 files render hardcoded Ukrainian strings and mock data (testimonials, feature lists) — no user input, no new data flow introduced by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-06.1-06 | Tampering | Home module section markup restructuring | low | accept | Pure JSX wrapper/heading restructuring around static content — no new props, no new data source, no change to any Link href or PremiumButton onClick |

</threat_model>

<verification>
Run `cd apps/web && pnpm exec eslint --max-warnings 0 modules/home/solution.tsx modules/home/unified-source.tsx modules/home/features.tsx modules/home/testimonials.tsx modules/home/cta-banner.tsx` and confirm zero output. Run `cd apps/web && pnpm exec tsc --noEmit 2>&1 | grep "error TS"` and confirm only the known pre-existing packages/ui/button-group.tsx line. Load the Home page and diff each of these 5 sections against pre-phase screenshots at all 5 breakpoints (375-767 / 768-1023 / 1024-1279 / 1280-1535 / 1536+) to confirm no text is lost, no horizontal scroll, and the #features anchor still scrolls correctly from the Hero CTA. <human-check>Confirm no more than two coral elements are visible in any single viewport across Home's sections (D-18's per-page visual-judgment constraint).</human-check>
</verification>

<success_criteria>
Home's remaining 5 sections (Solution, UnifiedSource, Features, Testimonials, CtaBanner) all render through Section + SectionHeading with zero Ukrainian copy changes; combined with Plan 01's Problem/Hero migration, all 7 Home sections now share the same primitive-based structure.
</success_criteria>

<output>
Create `.planning/phases/06.1-premium-visual-restyle-apps-web-purely-visual-css-restyle-of/06.1-03-SUMMARY.md` when done
</output>
