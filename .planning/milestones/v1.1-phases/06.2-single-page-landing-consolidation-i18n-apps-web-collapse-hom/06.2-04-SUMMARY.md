---
phase: 06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom
plan: 04
subsystem: ui
tags: [nextjs, react, next-intl, phosphor-icons, lucide-react, tailwind-v4, framer-motion]

# Dependency graph
requires:
  - phase: 06.2-03
    provides: uk/ru/en message namespaces (marquee, problemSolution, howItWorks, adminShowcase) consumed by this plan's components
provides:
  - apps/web/modules/landing/channel-marquee.tsx — infinite-scroll channel-logo strip
  - apps/web/modules/landing/problem-solution.tsx — dark-navy "Без DentaBot" vs "З DentaBot" comparison, id="product"
  - apps/web/modules/landing/how-it-works.tsx — static 3-step explainer, no id
  - apps/web/modules/landing/admin-showcase.tsx — browser-chrome frame embedding existing interactive DemoTabs, id="demo"
  - emoji-free apps/web/modules/demo/admin-tab.tsx + bot-tab.tsx (client's no-emoji constraint honored)
affects: [06.2-07 (page composition plan that assembles these sections into apps/web/app/page.tsx)]

actuals:
  tokens: 2815
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Server Component + getTranslations() for static/non-interactive landing sections (channel-marquee, how-it-works, admin-showcase) vs 'use client' + useTranslations() only where a section itself needs client-side hooks (problem-solution has none currently but plan mandated 'use client' for consistency with future interactive additions)"
    - "Inline <style> tag with a scoped @keyframes for a single-component CSS animation, instead of adding a new global CSS file or extending premium-theme.css, per plan's explicit 'do not add a new global CSS file for one component' instruction"

key-files:
  created:
    - apps/web/modules/landing/channel-marquee.tsx
    - apps/web/modules/landing/problem-solution.tsx
    - apps/web/modules/landing/how-it-works.tsx
    - apps/web/modules/landing/admin-showcase.tsx
  modified:
    - apps/web/modules/demo/admin-tab.tsx
    - apps/web/modules/demo/bot-tab.tsx

key-decisions:
  - "Used CheckIcon (not deprecated Check) from @phosphor-icons/react/ssr for the problem-solution 'after' column checkmark, matching the newer Icon-suffixed convention already introduced in testimonials.tsx"
  - "admin-showcase.tsx's DemoTabs content area gets a bg-dt-warm-white p-6 lg:p-8 wrapper inside the browser-chrome frame (not flush-to-edge), since DemoTabs' own internal layout (tab buttons + tab panel) needs breathing room the design's static screenshot slot didn't have to account for"

patterns-established:
  - "Browser-chrome frame wrapper pattern (3 colored dots + url-bar pill top bar) for embedding live interactive content inside a landing section, reusable if future sections need a similar 'live app' framing device"

requirements-completed: []

coverage:
  - id: D1
    description: "channel-marquee.tsx renders all 8 channel names duplicated for a seamless CSS marquee loop"
    verification:
      - kind: unit
        ref: "manual code inspection — track = [...channels, ...channels], verified via Read"
        status: pass
    human_judgment: false
  - id: D2
    description: "problem-solution.tsx renders the dark-navy two-column 'Без DentaBot' vs 'З DentaBot' comparison, translated via useTranslations('problemSolution')"
    verification:
      - kind: unit
        ref: "grep -c useTranslations apps/web/modules/landing/problem-solution.tsx -> 2"
        status: pass
    human_judgment: false
  - id: D3
    description: "how-it-works.tsx renders 3 step cards from howItWorks.steps, no id attribute on the section"
    verification:
      - kind: unit
        ref: "grep -c howItWorks apps/web/modules/landing/how-it-works.tsx -> 1; manual inspection confirms no id prop on <Section>"
        status: pass
    human_judgment: false
  - id: D4
    description: "admin-showcase.tsx embeds the existing interactive DemoTabs inside a browser-chrome frame, id='demo'"
    verification:
      - kind: unit
        ref: "manual code inspection — <Section id=\"demo\" ...><DemoTabs /></Section>"
        status: pass
    human_judgment: false
  - id: D5
    description: "Zero emoji remain in admin-tab.tsx / bot-tab.tsx (client's no-emoji constraint, Phase 06.1 precedent)"
    verification:
      - kind: unit
        ref: "grep -rE '📊|📅|👨‍⚕️|🧑‍🤝‍🧑|⚙️|🦷' apps/web/modules/demo/admin-tab.tsx apps/web/modules/demo/bot-tab.tsx | wc -l -> 0"
        status: pass
    human_judgment: false
  - id: D6
    description: "admin-showcase.tsx's browser-chrome frame reads as an intentional 'live demo, different surface' moment rather than a jarring inconsistency at landing-page scroll scale"
    verification: []
    human_judgment: true
    rationale: "Visual judgment call flagged by the plan's own <human-check> (RESEARCH Open Question 3, the @repo/ui-vs-dt-* visual seam). No dev server / screenshot tooling was run in this executor session; requires a human (or a UI-review pass) looking at the rendered page once Plan 07 composes it in."

duration: ~5min
completed: 2026-08-19
status: complete
---

# Phase 06.2 Plan 04: Upper-Middle Landing Sections Summary

**Channel marquee, dark-navy problem/solution comparison, static how-it-works explainer, and an admin-showcase section embedding the existing interactive bot/admin demo (emoji-free) — all wired to translation namespaces from Plan 03.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-08-19T09:19:38Z (prior plan completion)
- **Completed:** 2026-08-19T09:24:15Z
- **Tasks:** 3
- **Files modified:** 6 (4 created, 2 modified)

## Accomplishments
- Built `channel-marquee.tsx`: an 8-brand infinite-scroll logo strip using a scoped inline `@keyframes` CSS animation (no new global CSS file)
- Built `problem-solution.tsx`: dark-navy `id="product"` two-column "Без DentaBot" vs "З DentaBot" comparison, replacing the earlier light 4-card `problem.tsx` visual pattern per the design source-of-truth
- Built `how-it-works.tsx`: static 3-step explainer using `PremiumCard`s in a `StaggerGrid`, deliberately carrying no `id` (the `#demo` anchor moved to `admin-showcase.tsx`)
- Built `admin-showcase.tsx`: browser-chrome-framed section (`id="demo"`) that embeds the already-built `DemoTabs` (bot chat simulation + admin-panel tab-switcher) instead of a static screenshot placeholder — resolving CONTEXT.md's "Claude's Discretion" question in favor of keeping the interactive demo
- Cleaned up both RESEARCH-flagged emoji issues: 5 sidebar-button emoji in `admin-tab.tsx` replaced with `lucide-react` icons (the file's own established icon convention, not Phosphor), and the trailing 🦷 emoji dropped from `bot-tab.tsx`'s `seedGreetings()`

## Task Commits

Each task was committed atomically:

1. **Task 1: channel-marquee.tsx + problem-solution.tsx** - `0512aaf` (feat)
2. **Task 2: how-it-works.tsx — static 3-step explainer** - `2a810a9` (feat)
3. **Task 3: admin-showcase.tsx + emoji cleanup in demo components** - `7ecb40b` (feat)

**Plan metadata:** pending (this commit)

## Files Created/Modified
- `apps/web/modules/landing/channel-marquee.tsx` - infinite-scroll channel-logo strip, `getTranslations('marquee')`
- `apps/web/modules/landing/problem-solution.tsx` - dark-navy problem/solution comparison, `'use client'` + `useTranslations('problemSolution')`
- `apps/web/modules/landing/how-it-works.tsx` - static 3-step explainer, `getTranslations('howItWorks')`
- `apps/web/modules/landing/admin-showcase.tsx` - browser-chrome frame embedding `<DemoTabs />`, `getTranslations('adminShowcase')`
- `apps/web/modules/demo/admin-tab.tsx` - 5 emoji sidebar labels replaced with `lucide-react` icons
- `apps/web/modules/demo/bot-tab.tsx` - dropped trailing 🦷 emoji from bot greeting text

## Decisions Made
- Used `CheckIcon` (not the deprecated `Check`) from `@phosphor-icons/react/ssr` for the "after" column checkmark, matching the newer Icon-suffixed import convention already present in `testimonials.tsx` (uncommitted, pre-existing local change, not part of this plan)
- Gave `admin-showcase.tsx`'s embedded `DemoTabs` content area a `p-6 lg:p-8` padding wrapper inside the browser-chrome frame rather than rendering it flush-to-edge, since `DemoTabs`' own internal tab-button + tab-panel layout needs breathing room the design's static screenshot slot didn't need to account for
- `channel-marquee.tsx` and `how-it-works.tsx` and `admin-showcase.tsx` implemented as Server Components (`getTranslations`) rather than client components, since none of them need client-side hooks of their own — only `problem-solution.tsx` is `'use client'`, per the plan's explicit instruction

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- `pnpm --filter web check-types` fails on a pre-existing `csstype@3.1.3`/`3.2.3` duplicate-resolution conflict confined to `packages/ui/src/components/shadcn-ui/{button-group,calendar,sidebar}.tsx` — confirmed via `grep` on the tsc output that zero errors reference any file this plan touched (`modules/landing/*`, `modules/demo/*`). This is the same open, unrelated blocker tracked in `STATE.md`'s "Blockers/Concerns" since Phase 1; not addressed here per scope boundary.
- `pnpm --filter web lint` reports one pre-existing unrelated warning (`shared/lib/api-url.ts`, `turbo/no-undeclared-env-vars`) — confirmed unrelated to this plan's files, not fixed here (out of scope).
- A pre-existing uncommitted local modification to `apps/web/modules/home/testimonials.tsx` (not in this plan's `files_modified`) was present in the working tree at session start and left untouched — not staged or committed by this plan.

## Known Stubs

None - all 4 new components render real, translated content wired to Plan 03's message namespaces; `admin-showcase.tsx` embeds the real, already-functional `DemoTabs` simulation rather than a placeholder.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

All 4 sections (`channel-marquee`, `problem-solution`, `how-it-works`, `admin-showcase`) are ready for Plan 07 to import and compose into `apps/web/app/page.tsx`, in that order per the design file's section sequence. The `@repo/ui`-vs-`dt-*` visual seam inside `admin-showcase.tsx` (RESEARCH Pitfall 2 / Open Question 3) is a deliberate, accepted tradeoff — flagged above as coverage item D6 for a human/UI-review look once the full page is composed and visible in a browser.

## Self-Check: PASSED

All 6 created/modified files confirmed present on disk; all 3 task commits (`0512aaf`, `2a810a9`, `7ecb40b`) confirmed in `git log`.

---
*Phase: 06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom*
*Completed: 2026-08-19*
