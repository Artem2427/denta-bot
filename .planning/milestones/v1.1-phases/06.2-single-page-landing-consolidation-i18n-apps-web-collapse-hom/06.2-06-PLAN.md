---
phase: 06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom
plan: 06
type: execute
wave: 3
depends_on: ["06.2-03"]
files_modified:
  - apps/web/modules/landing/types.ts
  - apps/web/modules/landing/pricing-section.tsx
  - apps/web/modules/landing/lead-section.tsx
autonomous: true
requirements: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The pricing section renders real PricingPlan[] data (D-04) passed as a prop, with translated chrome (billing toggle, footnote, CTA) but Ukrainian-only plan name/description/features (D-11, unchanged)"
    - "The lead-capture section POSTs to /leads with source: 'contacts' (D-06) using the same field set/zod validation shape as the existing contact-form.tsx, now with fully translated labels/placeholders/errors/toasts"
    - "There is exactly ONE conversion form on the landing page (D-07 resolved: single-funnel to #lead) — no separate demo-lead-form modal is wired into any new section"
  artifacts:
    - "apps/web/modules/landing/types.ts (PricingPlan type, moved from modules/prices/types.ts)"
    - "apps/web/modules/landing/pricing-section.tsx"
    - "apps/web/modules/landing/lead-section.tsx"
  key_links:
    - "pricing-section.tsx is a presentational client component (plans passed as a prop) — the actual GET /public/pricing-plans fetch happens in Plan 07's app/[locale]/page.tsx Server Component, following the existing Phase 6 fetch-then-pass-as-prop pattern"
    - "lead-section.tsx defines its zod schema via a factory function taking `t` as a parameter, so validation error messages are translated too — not just labels/placeholders"
---

<objective>
Build the two conversion-critical sections: Pricing (D-04, real API-backed data with translated chrome only) and the single Lead-capture form (D-06, reusing the existing Contacts form's fields/schema/POST shape, now translated). Resolves D-07 (Claude's discretion) by funneling the ENTIRE page to this one `#lead` section — no separate demo-lead-form modal — since the interactive demo simulation (Plan 04's admin-showcase) already gives visitors a live "try it" experience, and the client's explicit "site must not be overloaded" instruction argues against a second, duplicate conversion form.

Advisory note: per D-11, `PricingPlan.name`/`description`/`features` stay Ukrainian-only regardless of active locale (the DB has no locale field) — only the surrounding chrome (billing toggle, CTA, footnote) is translated. This is a known, deliberate content-accuracy limitation on `/ru`/`/en`, not a bug.

Purpose: land the two sections that actually drive the business outcome (a submitted Lead), fully parallel to Plan 04/05 (disjoint files).
Output: `modules/landing/types.ts`, `pricing-section.tsx`, `lead-section.tsx`.
</objective>

<artifacts>
## Artifacts this phase produces (this plan)
- `apps/web/modules/landing/types.ts` — new (`PricingPlan` type, canonical location going forward)
- `apps/web/modules/landing/pricing-section.tsx` — new, `id="pricing"`
- `apps/web/modules/landing/lead-section.tsx` — new, `id="lead"`
</artifacts>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom/06.2-CONTEXT.md
@apps/web/messages/uk.json
@apps/web/modules/prices/pricing-cards.tsx
@apps/web/modules/prices/types.ts
@apps/web/modules/contacts/contact-form.tsx
@apps/web/shared/lib/api-url.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: modules/landing/types.ts + pricing-section.tsx</name>
  <files>apps/web/modules/landing/types.ts, apps/web/modules/landing/pricing-section.tsx</files>
  <read_first>
    - apps/web/modules/prices/types.ts (`PricingPlan` type — `{id, name, monthlyPrice, yearlyPrice, description, features, isPopular}`)
    - apps/web/modules/prices/pricing-cards.tsx (existing fetch-then-render pattern, `PremiumSwitch` monthly/yearly toggle, `getGridClassName` helper, `PremiumBadge` "Популярний" treatment)
    - .planning/phases/06.2-.../06.2-RESEARCH.md — "Common Pitfalls / Pitfall 4" (design's per-plan `cta` label doesn't map to a DB field — resolved below by keeping one generic translated CTA, not a per-plan-name lookup, for determinism)
  </read_first>
  <action>
    Create `apps/web/modules/landing/types.ts` containing the `PricingPlan` type, copied verbatim from `apps/web/modules/prices/types.ts` (this becomes the new canonical location; `modules/prices/types.ts` is deleted in Plan 07 once nothing imports it).

    Create `apps/web/modules/landing/pricing-section.tsx`, `'use client'`, accepting `{ plans: PricingPlan[] }` as its only prop (the actual `fetch('/public/pricing-plans')` call happens in Plan 07's Server Component page, not here — matches the existing Phase 6 pattern). Structure: `<Section id="pricing" className="scroll-mt-16 lg:scroll-mt-20">` wrapping `Container`, a heading row (`t('pricing.eyebrow')`/`t('pricing.title')` via `SectionHeading` or an inline flex row matching design's side-by-side heading+billing-toggle layout) plus the existing `PremiumSwitch` monthly/yearly toggle (`t('pricing.billingMonthly')`/`t('pricing.billingYearly')`, `PremiumBadge` showing `t('pricing.billingDiscount')`). Below it, the existing `getGridClassName(plans.length)`-driven card grid from `pricing-cards.tsx`, reusing `PremiumCard`/`PremiumBadge`("Популярний"→still hardcoded Ukrainian is acceptable here since it labels a Ukrainian-only plan name, OR translate via `t('pricing.ctaLabel')`-adjacent key if you added one — keep simple, hardcode "Популярний" as today). Plan `name`/`description`/`features` render EXACTLY as returned by the API (Ukrainian, unchanged by locale — D-11). The per-plan CTA button uses ONE translated label for all plans (`t('pricing.ctaLabel')`, "Обрати план") — do NOT attempt to map the design's distinct per-plan `cta` strings ("Почати"/"Замовити демо"/"Обговорити") by matching against `plan.name`, that's a brittle string-match with no DB backing (Pitfall 4, resolved by using the simpler, safer generic label). CTA `href="#lead"` (not `routes.contacts`, which is retired). Footnote: `t('pricing.footnote')` below the grid.
  </action>
  <verify>
    <automated>grep -c "'#lead'" apps/web/modules/landing/pricing-section.tsx</automated>
  </verify>
  <done>Renders the plan grid from a `plans` prop (no internal fetch), CTA hrefs point to `#lead`, billing toggle and footnote are translated via `useTranslations('pricing')`, plan content itself is untouched pass-through from the API. `pnpm --filter web check-types` passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: lead-section.tsx — single translated conversion form</name>
  <files>apps/web/modules/landing/lead-section.tsx</files>
  <read_first>
    - apps/web/modules/contacts/contact-form.tsx (the exact field set/zod schema/POST payload this section reuses per D-06 — `name`/`clinic`/`contact`/`message`, `source: 'contacts'`, `getClientApiUrl()` + `/leads` POST, 429/error/success toast handling — read this file fully, it is the behavioral contract being ported)
    - Design file lines 236-288 (the lead section's 2-column layout: left = eyebrow/title/description/guarantees list, right = the form card)
  </read_first>
  <action>
    Create `apps/web/modules/landing/lead-section.tsx`, `'use client'`. Inside the component, define a schema FACTORY function `createLeadSchema(t: ReturnType<typeof useTranslations>)` returning the zod object with translated messages: `name: z.string().trim().min(2, t('lead.nameError'))`, `clinic: z.string().trim().optional()`, `contact: z.string().trim().min(1, t('lead.contactRequiredError')).refine(...same phone/email regex as contact-form.tsx..., { message: t('lead.contactInvalidError') })`, `message: z.string().trim().optional()` — call this factory once per render (`const schema = React.useMemo(() => createLeadSchema(t), [t])`) and pass to `useForm({ resolver: zodResolver(schema), ... })`.

    Structure: `<Section id="lead" className="scroll-mt-16 lg:scroll-mt-20" tone="navy">` wrapping `Container`, a 2-column grid (`grid gap-12 lg:grid-cols-2 items-start`): left column renders `t('lead.eyebrow')`/`t('lead.title')`/`t('lead.description')` (navy-tone `SectionHeading`-style typography, not centered — left-aligned per design) plus `t.raw('lead.guarantees')` (3 strings) each with a small checkmark bullet; right column is a `PremiumCard` (light bg, contrasts against the navy section per design) containing the form itself, structurally identical to `contact-form.tsx`'s JSX (same `PremiumInput`/`PremiumTextarea`/`PremiumButton` usage, same `isSubmitted` toggle between form and thanks-state) but every label/placeholder/error/toast/button string sourced from the `lead` namespace (`t('lead.formTitle')`, `t('lead.nameLabel')`, `t('lead.namePlaceholder')`, `t('lead.clinicLabel')`, `t('lead.contactLabel')`, `t('lead.contactPlaceholder')`, `t('lead.messageLabel')`, `t('lead.messagePlaceholder')`, `t('lead.submitLabel')`/`t('lead.submittingLabel')`, `t('lead.consentNote')`, `t('lead.thanksTitle')`/`t('lead.thanksBody')`, toast strings `t('lead.toastRateLimited')`/`t('lead.toastGenericError')`/`t('lead.toastSuccess')`). The POST body/endpoint/source tag stay IDENTICAL to `contact-form.tsx`: `fetch(\`${getClientApiUrl()}/leads\`, { method: 'POST', ... body: JSON.stringify({ name, clinicName: clinic || undefined, [isEmail ? 'email' : 'phone']: contact, message: message || undefined, source: 'contacts' }) })` — the `source: 'contacts'` literal is D-06's explicit requirement, do not change it to `'demo'` even though this section visually sits near the demo showcase.

    Do NOT wire `modules/demo/demo-cta.tsx` or `modules/demo/demo-lead-form.tsx` into this section or anywhere else in the new landing page (D-07 resolution: single funnel to this one form) — those two files become dead code, removed in Plan 07's cleanup task.
  </action>
  <verify>
    <automated>grep -c "source: 'contacts'" apps/web/modules/landing/lead-section.tsx</automated>
  </verify>
  <done>Form POSTs to `/leads` with `source: 'contacts'` (verified above), `id="lead"` present with scroll-mt, zod error messages are translated (not hardcoded Ukrainian strings), no import of `demo-cta.tsx`/`demo-lead-form.tsx` anywhere in this file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → `POST /leads` | Unauthenticated public endpoint accepting name/contact/message from an anonymous visitor — already shipped and rate-limited in Phase 6, unchanged by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-09 | Denial of Service | `lead-section.tsx` → `POST /leads` | low | accept | Same endpoint, same `@nestjs/throttler` rate limiting already shipped in Phase 6 (T-06 threat register) — this plan adds a second call site to an already-mitigated endpoint, no new surface. |
| T-06.2-10 | Tampering (input validation) | `lead-section.tsx` zod schema | low | mitigate | Client-side `react-hook-form`+`zod` validation (translated messages, same regex/rules as the already-shipped `contact-form.tsx`) is backed by server-side DTO validation on `POST /leads` (Phase 6) — translating the error MESSAGE text does not change the underlying validation RULE, so no validation bypass is introduced. |
</threat_model>

<verification>
`pnpm --filter web check-types` passes; pricing section renders a `plans` prop with translated chrome only; lead section POSTs `source: 'contacts'` with fully translated UI and validation messages; no reference to `demo-cta.tsx`/`demo-lead-form.tsx` in either new file.
</verification>

<success_criteria>
The two conversion-driving sections are built and translated, with D-04/D-06/D-07/D-11's decisions all implemented exactly as specified, ready for Plan 07's final page composition.
</success_criteria>

<output>
Create `.planning/phases/06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom/06.2-06-SUMMARY.md` when done
</output>
