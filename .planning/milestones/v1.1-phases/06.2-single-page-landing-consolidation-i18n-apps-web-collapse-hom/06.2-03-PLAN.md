---
phase: 06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom
plan: 03
type: execute
wave: 2
depends_on: ["06.2-01"]
files_modified:
  - apps/web/messages/uk.json
  - apps/web/messages/ru.json
  - apps/web/messages/en.json
autonomous: true
requirements: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every remaining landing-section namespace (marquee/problemSolution/howItWorks/adminShowcase/features/pricing/reviews/lead/faq) exists in all 3 message files with identical key structure"
    - "uk.json carries the design file's persuasive Ukrainian sales copy (source of truth, D-10); ru.json/en.json are adapted-not-literal translations preserving the same persuasive intent"
  artifacts:
    - "apps/web/messages/uk.json fully populated (all namespaces)"
    - "apps/web/messages/ru.json fully populated (all namespaces)"
    - "apps/web/messages/en.json fully populated (all namespaces)"
  key_links:
    - "Plans 04/05/06 (Wave 3) read these exact namespace/key names via useTranslations() — this plan is the single source of truth for the message contract; do not rename/restructure keys documented here without updating this SUMMARY"
---

<objective>
Author the full translated copy for every landing-page section this phase adds (D-10: Claude translates/adapts ru/en from the design file's Ukrainian text), extending the `nav`/`footer`/`hero` namespaces Plan 01 already created in `apps/web/messages/{uk,ru,en}.json`. This is a content-only plan — no component code changes — so it can run as a single-owner Wave 2 step ahead of Wave 3's fully-parallel component-building plans (04/05/06), which will consume these exact keys without ever touching these 3 files themselves.

Advisory note: FAQ/reviews/features copy here is landing-page chrome, translated per D-09's stated scope. Blog post content (a separate, un-translated domain) is explicitly NOT touched by this plan (D-09 boundary, not reopened).

Purpose: give Wave 3's component-building plans a complete, stable, pre-authored content contract so they can build UI purely from `useTranslations()` calls with zero ambiguity about copy.
Output: fully populated `apps/web/messages/{uk,ru,en}.json`.
</objective>

<artifacts>
## Artifacts this phase produces (this plan)
- `apps/web/messages/uk.json`, `apps/web/messages/ru.json`, `apps/web/messages/en.json` — modified (extended, not new files), adding 9 message-key namespaces: `marquee`, `problemSolution`, `howItWorks`, `adminShowcase`, `features`, `pricing`, `reviews`, `lead`, `faq`
</artifacts>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom/06.2-CONTEXT.md
@apps/web/messages/uk.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: author uk.json — canonical namespaces for all remaining sections</name>
  <files>apps/web/messages/uk.json</files>
  <read_first>
    - apps/web/messages/uk.json (Plan 01's existing `nav`/`footer`/`hero` namespaces — extend this file, do not remove or restructure those keys)
    - Design file `/Users/artemdanko/Downloads/Design system tokens Phase 01.1/DentaBot Landing.dc.html` — this is a Claude Design canvas export (`.dc.html`), read for content/structure/copy only, not literal markup. Relevant line ranges: 94-98 (channel marquee), 101-133 (problem/product section), 135-152 (how-it-works/"demo" section), 154-174 (admin showcase), 176-187 (features), 189-219 (pricing chrome — NOT plan data, that's DB content per Plan 02), 221-234 (reviews), 236-288 (lead section), 290-305 (FAQ), 517-616 (the JS prototype's literal string arrays — `painBefore`/`painAfter`/`steps`/`features`/`reviews`/`guarantees`/`faq`, the actual canonical copy to port).
  </read_first>
  <action>
    Add the following namespaces to `apps/web/messages/uk.json`, keeping `nav`/`footer`/`hero` untouched. Exact key names below are the contract Wave 3 plans will consume verbatim — do not rename:

    ```
    marquee: { channels: string[8] }  — "Telegram","Viber","WhatsApp","Google Calendar","SMS-шлюз","Instagram","Веб-віджет","API" (design line 96, proper nouns — identical across all 3 locale files, not meaningfully translatable)

    problemSolution: {
      eyebrow: "Проблема", title: "Клініка втрачає пацієнтів на дрібницях",
      description: "Не там записали, не туди передзвонили, забули нагадати. Кожен такий випадок — це не помилка людини, а відсутність системи.",
      beforeLabel: "Без DentaBot", afterLabel: "З DentaBot",
      before: string[4] (design's painBefore, line 518-521),
      after: string[4] (design's painAfter, line 523-526)
    }

    howItWorks: {
      eyebrow: "Як це працює", title: "Три хвилини пацієнта — нуль хвилин адміністратора",
      steps: [{n,title,text} x3] (design's `steps` array, line 529-532)
    }

    adminShowcase: {
      eyebrow: "Адмін панель", title: "Уся клініка в одному вікні",
      description: "Записи з бота і записи, які адміністратор вніс вручну, живуть в одному розкладі.",
      urlLabel: "app.dentabot.ua/clinic"
    }

    features: { title: "Усе, що потрібно для роботи клініки", items: [{title,text} x8] } (design's `features` array, line 563-571 — drop the `mark` field, the 8-item grid in Plan 05 reuses existing Phosphor icons per icon-per-index, not the design's "01"-"08" numeric marks)

    pricing: {
      eyebrow: "Тарифи", title: "Прозорі тарифи без прихованих платежів",
      billingMonthly: "Щомісяця", billingYearly: "Щороку", billingDiscount: "-20%",
      footnote: "Підключення, налаштування бота і навчання команди — безкоштовно на всіх тарифах.",
      ctaLabel: "Обрати план"
    } (chrome only — plan name/description/features stay DB-sourced Ukrainian-only per D-11/D-04, this namespace never touches plan content)

    reviews: { title: "Що кажуть клініки", items: [{text,initials,name,clinic} x3] } (design's `reviews` array, line 590-593)

    lead: {
      eyebrow: "Підключення", title: "Демо адмінки та бота для вашої клініки",
      description: "Покажемо панель на даних вашого профілю послуг і налаштуємо бота під ваш розклад. Демо — 20 хвилин, підключення — один робочий день.",
      guarantees: string[3] (design's `guarantees` array, line 595),
      formTitle: "Залишити заявку", thanksTitle: "Дякуємо!",
      thanksBody: "Ваша заявка успішно надіслана. Ми зв'яжемося з вами найближчим часом.",
      nameLabel: "Ім'я *", namePlaceholder: "Ваше ім'я", nameError: "Ім'я має містити щонайменше 2 символи",
      clinicLabel: "Назва клініки", clinicPlaceholder: "Назва вашої клініки",
      contactLabel: "Телефон або email *", contactPlaceholder: "+380 XX XXX XX XX або email@example.com",
      contactRequiredError: "Вкажіть телефон або email", contactInvalidError: "Введіть коректний номер телефону або email",
      messageLabel: "Повідомлення", messagePlaceholder: "Розкажіть про ваші потреби...",
      submitLabel: "Надіслати заявку", submittingLabel: "Надсилаємо…",
      consentNote: "Натискаючи кнопку, ви погоджуєтесь на обробку контактних даних. Без спаму.",
      toastRateLimited: "Забагато спроб. Зачекайте хвилину і спробуйте ще раз.",
      toastGenericError: "Не вдалося надіслати заявку. Спробуйте ще раз.",
      toastSuccess: "Заявку успішно надіслано!"
    } (reuses apps/web/modules/contacts/contact-form.tsx's existing field labels/errors verbatim where they already match — this is the D-06 reused-form content, now made translatable)

    faq: { title: "Питання, які ставлять найчастіше", items: [{question,answer} x5] } (design's `faq` array, line 605-610 — the canonical 5-question set; do NOT merge in the old modules/prices or modules/contacts FAQ content, Pitfall 5)
    ```
  </action>
  <verify>
    <automated>node -e "const m=require('./apps/web/messages/uk.json'); const ns=['nav','footer','hero','marquee','problemSolution','howItWorks','adminShowcase','features','pricing','reviews','lead','faq']; const missing=ns.filter(k=>!(k in m)); if(missing.length){console.error('MISSING:',missing);process.exit(1)} console.log('all namespaces present')"</automated>
  </verify>
  <done>`apps/web/messages/uk.json` is valid JSON, retains its existing `nav`/`footer`/`hero` namespaces untouched, and adds all 12 namespaces listed above with the exact key structure specified (verified by the automated check above).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: author ru.json and en.json — adapted (not literal) translations, key-parity verified</name>
  <files>apps/web/messages/ru.json, apps/web/messages/en.json</files>
  <read_first>
    - apps/web/messages/uk.json (Task 1's output — the canonical structure and content to adapt from)
    - .planning/phases/06.2-.../06.2-CONTEXT.md — D-10 ("Claude translates/adapts the ru/en marketing copy from the design file's Ukrainian text, same persuasive intent, not a literal word-for-word translation")
  </read_first>
  <action>
    Extend `apps/web/messages/ru.json` and `apps/web/messages/en.json` with the same 12 namespaces and exact key structure as `uk.json` (Task 1), preserving whatever `nav`/`footer`/`hero` content Plan 01 already put there. For every string/array value, write a persuasive-intent adaptation in Russian (ru.json) and English (en.json) respectively — not a mechanical word-for-word translation. `marquee.channels` (brand/proper nouns: Telegram, Viber, WhatsApp, etc.) stays IDENTICAL across all 3 files — do not translate proper nouns. Every array (`before`/`after`/`steps`/`items`/`guarantees`) must have the SAME LENGTH as its uk.json counterpart (4/4/3/8/3/5/3 respectively) — key-parity, not just top-level namespace parity, is what the verify step below checks.
  </action>
  <verify>
    <automated>node -e "
const uk=require('./apps/web/messages/uk.json');
const ru=require('./apps/web/messages/ru.json');
const en=require('./apps/web/messages/en.json');
function keys(o,prefix){let out=[];for(const k of Object.keys(o)){const p=prefix?prefix+'.'+k:k;if(Array.isArray(o[k])){out.push(p+'[]='+o[k].length);if(o[k][0]&&typeof o[k][0]==='object')out=out.concat(keys(o[k][0],p+'[0]'));}else if(o[k]&&typeof o[k]==='object'){out=out.concat(keys(o[k],p));}else{out.push(p);}}return out.sort();}
const ukKeys=keys(uk), ruKeys=keys(ru), enKeys=keys(en);
const missingRu=ukKeys.filter(k=>!ruKeys.includes(k));
const missingEn=ukKeys.filter(k=>!enKeys.includes(k));
if(missingRu.length||missingEn.length){console.error('missingRu',missingRu,'missingEn',missingEn);process.exit(1)}
console.log('key parity confirmed across uk/ru/en');
"</automated>
  </verify>
  <done>`apps/web/messages/ru.json` and `apps/web/messages/en.json` are valid JSON with 100% key-and-array-length parity against `uk.json` (verified by the automated check above), containing genuinely adapted (not machine-literal) copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this plan only edits static JSON content files bundled at build time; no new runtime input surface, no user-facing trust boundary change.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-06 | Information Disclosure | `apps/web/messages/*.json` | low | accept | Purely static marketing copy, no secrets/PII/config values are ever placed in these files — standard next-intl message-file usage. |
</threat_model>

<verification>
All 3 message files are valid JSON, contain the same 12 namespaces with 100% key-and-array-length parity, and `nav`/`footer`/`hero` from Plan 01 remain intact.
</verification>

<success_criteria>
Wave 3's 3 parallel component-building plans (04/05/06) can consume every remaining section's copy via `useTranslations()` with zero missing-key runtime errors on any of the 3 locales.
</success_criteria>

<output>
Create `.planning/phases/06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom/06.2-03-SUMMARY.md` when done
</output>
