---
phase: 06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom
plan: 06
subsystem: ui
tags: [next-intl, react-hook-form, zod, tailwind, pricing, lead-capture, i18n]

# Dependency graph
requires:
  - phase: 06.2-03
    provides: uk/ru/en message files with pricing.* and lead.* translation keys already authored
provides:
  - "apps/web/modules/landing/types.ts — canonical PricingPlan type"
  - "apps/web/modules/landing/pricing-section.tsx — id=\"pricing\" presentational client component, plans passed as a prop, translated chrome only"
  - "apps/web/modules/landing/lead-section.tsx — id=\"lead\" single translated conversion form, POSTs source: 'contacts'"
affects: ["06.2-07 (final page composition — imports both sections, does the actual GET /public/pricing-plans fetch)"]

actuals:
  tokens: 3509
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Pricing section stays a presentational client component (plans as prop) — fetch happens in the page-level Server Component, not here"
    - "Lead form's zod schema is built via a factory function taking `t` as a parameter, so validation error messages are translated, matching the label/placeholder translation pattern"

key-files:
  created:
    - apps/web/modules/landing/types.ts
    - apps/web/modules/landing/pricing-section.tsx
    - apps/web/modules/landing/lead-section.tsx
  modified: []

key-decisions:
  - "Per-plan CTA href stored in a single LEAD_ANCHOR = '#lead' constant rather than repeated inline strings, keeping the single-funnel target (D-07) in exactly one place"
  - "PricingCard's 'Популярний' badge label kept hardcoded Ukrainian (matches the plan's explicit guidance — it labels a Ukrainian-only plan name per D-11, translating just the badge word would be inconsistent)"
  - "Lead form's post-submission 'send another' button reuses t('lead.submitLabel') — the lead namespace (authored in Plan 03) has no dedicated 'submit another' key, and the plan's instruction was to source every string from the lead namespace, not add new keys"

patterns-established: []

requirements-completed: []

coverage:
  - id: D1
    description: "Pricing section renders real PricingPlan[] data passed as a prop (no internal fetch), with translated chrome (billing toggle, footnote, CTA) and Ukrainian-only plan name/description/features"
    verification:
      - kind: manual_procedural
        ref: "grep -c \"'#lead'\" apps/web/modules/landing/pricing-section.tsx (returns 1); pnpm --filter web check-types (no errors attributable to this file)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Lead-capture section POSTs to /leads with source: 'contacts', same field set/zod validation shape as contact-form.tsx, fully translated labels/placeholders/errors/toasts"
    verification:
      - kind: manual_procedural
        ref: "grep -c \"source: 'contacts'\" apps/web/modules/landing/lead-section.tsx (returns 2 — object literal + comment); pnpm --filter web check-types (no errors attributable to this file)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Exactly one conversion form on the landing page (D-07) — no demo-lead-form.tsx modal wired into either new section"
    verification:
      - kind: manual_procedural
        ref: "grep -n 'demo-cta\\|demo-lead-form' apps/web/modules/landing/{pricing-section,lead-section}.tsx — no import statements found, only an explanatory code comment"
        status: pass
    human_judgment: false

duration: 6min
completed: 2026-08-19
status: complete
---

# Phase 06.2 Plan 06: Pricing + Lead-Capture Sections Summary

**Built `modules/landing/pricing-section.tsx` (real PricingPlan[] data via prop, translated chrome only) and `modules/landing/lead-section.tsx` (single translated conversion form reusing contact-form.tsx's exact POST shape and `source: 'contacts'`), resolving D-07's single-funnel design by having every pricing CTA point to `#lead`.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-08-19T09:30:XX Z (STATE.md session start)
- **Completed:** 2026-08-19T09:36Z
- **Tasks:** 2
- **Files modified:** 3 (all new)

## Accomplishments
- `modules/landing/types.ts` — `PricingPlan` type moved to its new canonical location, copied verbatim from `modules/prices/types.ts`
- `modules/landing/pricing-section.tsx` — `id="pricing"`, plans-as-prop presentational component reusing `pricing-cards.tsx`'s grid/toggle logic, every plan CTA linking to `#lead` via a single `LEAD_ANCHOR` constant
- `modules/landing/lead-section.tsx` — `id="lead"`, 2-column navy-tone layout (eyebrow/title/description/guarantees left, form card right), zod schema factory (`createLeadSchema(t)`) so validation messages translate alongside labels/placeholders/toasts

## Task Commits

Each task was committed atomically:

1. **Task 1: modules/landing/types.ts + pricing-section.tsx** - `7553526` (feat)
2. **Task 2: lead-section.tsx — single translated conversion form** - `9174ba7` (feat)

_Note: no TDD tasks in this plan; single feat commit per task._

## Files Created/Modified
- `apps/web/modules/landing/types.ts` - `PricingPlan` type, canonical location
- `apps/web/modules/landing/pricing-section.tsx` - Presentational pricing grid + billing toggle, translated chrome, CTA → `#lead`
- `apps/web/modules/landing/lead-section.tsx` - Single lead-capture form, translated end-to-end, POSTs `source: 'contacts'`

## Decisions Made
- Used a `LEAD_ANCHOR = '#lead'` named constant in `pricing-section.tsx` instead of repeating the literal string per plan card — single source of truth for the funnel target, per D-07.
- Kept `PremiumCard`'s "Популярний" badge text hardcoded Ukrainian rather than adding a new translation key, per the plan's explicit "keep simple, hardcode as today" guidance — it labels a plan name that is itself Ukrainian-only (D-11).
- Reused `t('lead.submitLabel')` for the post-submission "send another" button since the plan-03-authored `lead` namespace has no separate key for that button and the task instructed sourcing all copy from the existing namespace, not inventing new keys.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Both sections are ready to be imported and composed by Plan 07's `app/[locale]/page.tsx`, which will perform the actual `GET /public/pricing-plans` Server Component fetch and pass the result into `<PricingSection plans={...} />`.
- `modules/prices/types.ts` still exists (unused by these new files) — Plan 07's cleanup task deletes it once nothing imports it, per the plan's own note.
- `pnpm --filter web check-types` shows zero errors attributable to any file this plan touched; the only failures present are the pre-existing `csstype@3.1.3`/`3.2.3` duplicate-resolution conflict in `packages/ui/src/components/shadcn-ui/{button-group,calendar,sidebar}.tsx`, tracked as an open blocker in STATE.md since Phase 1, unrelated to this plan.

---
*Phase: 06.2-single-page-landing-consolidation-i18n-apps-web-collapse-hom*
*Completed: 2026-08-19*

## Self-Check: PASSED

All created files verified present on disk (`types.ts`, `pricing-section.tsx`, `lead-section.tsx`, this SUMMARY.md). Both task commits (`7553526`, `9174ba7`) verified present in `git log`.
