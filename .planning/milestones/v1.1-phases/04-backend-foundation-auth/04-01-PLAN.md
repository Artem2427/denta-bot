---
phase: 04-backend-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - .gitignore
  - turbo.json
  - package.json
  - pnpm-lock.yaml
  - README.md
  - packages/db/package.json
  - packages/db/prisma.config.ts
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/seed.ts
  - packages/db/prisma/migrations/**
  - packages/db/src/index.ts
  - apps/server/package.json
  - apps/server/src/main.ts
  - apps/server/src/app.module.ts
  - apps/server/src/config/env.validation.ts
  - apps/server/src/prisma/prisma.module.ts
  - apps/server/src/prisma/prisma.service.ts
  - apps/server/src/auth/auth.module.ts
  - apps/server/src/auth/auth.controller.ts
  - apps/server/src/auth/auth.service.ts
  - apps/server/src/auth/dto/login.dto.ts
  - apps/server/src/auth/dto/auth-response.dto.ts
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, AUTH-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Running `npx prisma migrate dev --name init` (from `packages/db`, against the local Docker Postgres) produces all 6 tables — PlatformAdmin, RefreshToken, Clinic, Lead, BlogPost, PricingPlan — from one committed migration under `packages/db/prisma/migrations/`; no manual `psql`/GUI schema edit is used anywhere in this plan (INFRA-01)"
    - "`packages/db` (`@repo/db`) is a valid `workspace:*`-installable pnpm package whose generated Prisma types are actually imported by `apps/server` (`PrismaService extends PrismaClient`); `apps/web`/`apps/platform-admin` are NOT wired to import it this phase — frontends stay behind the REST boundary per RESEARCH.md's Open Question 1 recommendation (INFRA-02)"
    - "`GET /api/docs` serves a browsable Swagger UI and `GET /api/docs-json` serves the underlying OpenAPI document listing `POST /auth/login` (and, once Plan 04-02 lands, `/auth/refresh`, `/auth/logout`, `/auth/me`); the document is generated once at boot before `app.listen()`, so it is served identically to concurrent readers (INFRA-03)"
    - "`POST /auth/login` with the seeded PlatformAdmin's correct email+password returns 200/201 with `{ accessToken, platformAdmin: { id, email, createdAt } }` in the JSON body and a `refresh_token` cookie (`HttpOnly`) in `Set-Cookie`; a `RefreshToken` row is created in the DB with a `familyId` and a hash of the token, never the raw token (AUTH-01)"
    - "`POST /auth/login` with a wrong password and `POST /auth/login` with an unknown email return byte-identical response bodies and the same 401 status — no user enumeration"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "Prisma schema — PlatformAdmin, RefreshToken, Clinic, Lead, BlogPost, PricingPlan models + ClinicStatus/LeadSource/LeadStatus enums"
      contains: "model PlatformAdmin"
    - path: "packages/db/prisma/seed.ts"
      provides: "D-15 seed script — upserts the first PlatformAdmin from PLATFORM_ADMIN_EMAIL/PLATFORM_ADMIN_PASSWORD env vars using argon2"
    - path: "packages/db/src/index.ts"
      provides: "@repo/db barrel re-exporting the generated Prisma client + model types"
    - path: "docker-compose.yml"
      provides: "Local Postgres 17 service, root-level, no pgAdmin (D-01/D-02/D-04)"
    - path: ".env.example"
      provides: "Documented env var contract (DATABASE_URL, JWT secrets, CORS, cookie config, seed admin credentials)"
    - path: "apps/server/src/prisma/prisma.service.ts"
      provides: "PrismaService — driver-adapter-based PrismaClient wrapper (Prisma 7 Pattern 1)"
      exports: "PrismaService"
    - path: "apps/server/src/prisma/prisma.module.ts"
      provides: "Global module exporting PrismaService"
      exports: "PrismaModule"
    - path: "apps/server/src/auth/auth.controller.ts"
      provides: "POST /auth/login route"
      exports: "AuthController"
    - path: "apps/server/src/auth/auth.service.ts"
      provides: "AuthService.login() + issueTokenPair() helper reused by Plan 04-02's refresh()"
      exports: "AuthService"
    - path: "apps/server/src/auth/dto/auth-response.dto.ts"
      provides: "AuthResponseDto / PlatformAdminSummaryDto — never re-exports the raw PlatformAdmin model"
      exports: "AuthResponseDto, PlatformAdminSummaryDto"
    - path: "apps/server/src/config/env.validation.ts"
      provides: "zod env schema — fails boot fast on missing/invalid env, enforces JWT_ACCESS_SECRET != JWT_REFRESH_SECRET"
      exports: "envSchema, validateEnv"
  key_links:
    - from: "apps/server/src/auth/auth.controller.ts"
      to: "apps/server/src/auth/auth.service.ts"
      via: "constructor DI"
      pattern: "constructor(private readonly authService: AuthService)"
    - from: "apps/server/src/auth/auth.service.ts"
      to: "apps/server/src/prisma/prisma.service.ts"
      via: "constructor DI, this.prisma.platformAdmin.findUnique / this.prisma.refreshToken.create"
      pattern: "this.prisma.platformAdmin"
    - from: "apps/server/src/prisma/prisma.service.ts"
      to: "@repo/db"
      via: "extends PrismaClient, PrismaPg adapter in super()"
      pattern: "extends PrismaClient"
    - from: "apps/server/src/app.module.ts"
      to: "ConfigModule / PrismaModule / AuthModule"
      via: "imports array"
      pattern: "imports: [ConfigModule.forRoot"
    - from: "turbo.json db:generate task"
      to: "apps/server build/dev/check-types"
      via: "dependsOn [\"^db:generate\"]"
      pattern: "db:generate"
  prohibitions:
    - statement: "MUST NOT hardcode a fallback DATABASE_URL/credentials string in `prisma.config.ts` or `PrismaService` — connection info comes exclusively from the `DATABASE_URL` env var, so a misconfigured environment fails loudly rather than silently connecting to a wrong/default database"
      status: resolved
      verification: test
    - statement: "MUST NOT perform destructive/wide-scope writes in `packages/db/prisma/seed.ts` — it only upserts the single PlatformAdmin record from env vars, never `deleteMany`/`truncate` any table"
      status: resolved
      verification: test
    - statement: "MUST NOT import `@repo/db`'s Prisma Client/types directly into `apps/web` or `apps/platform-admin` this phase — frontends stay behind the REST/HTTP boundary; only `apps/server` imports `@repo/db` in Phase 4"
      status: resolved
      verification: test
    - statement: "MUST NOT return distinguishable error responses (message/status) for 'email not found' vs 'wrong password' — `POST /auth/login` returns an identical generic 401 body in both cases"
      status: resolved
      verification: test
    - statement: "MUST NOT log the plaintext password, the raw access token, or the raw refresh token anywhere (console output, thrown-error messages) — only non-sensitive identifiers (email, admin id) may appear in logs/errors"
      status: resolved
      verification: test
    - statement: "MUST NOT return a raw `PlatformAdmin` Prisma model (carries `passwordHash`) from any controller response — every auth response is mapped through `AuthResponseDto`"
      status: resolved
      verification: test
    - statement: "Restricting/authenticating the Swagger UI in production"
      status: dismissed
      verification: null
      reason: "No production deployment target exists this phase (no hosting/domain decision yet, per D-05) — Swagger doc auth-gating is deferred until a real deployment topology exists; out of Phase 4's own stated verification scope (API/Swagger-level only)."
    - statement: "Generic input validation / SQL injection / secrets-in-URL canon items"
      status: dismissed
      verification: null
      reason: "Canon security concerns — covered by Prisma's parameterized client, class-validator DTOs, and /gsd-secure-phase; not minted as bespoke prohibitions here."
---

<objective>
Stand up a real Postgres + Prisma 7 + NestJS backend: a new `packages/db` (`@repo/db`) workspace package owning the full 6-model schema and migration history, wired into Turborepo's task graph, plus `apps/server`'s Prisma integration and a working `POST /auth/login` — the phase's tracer bullet, proving DB → ORM → service → controller → Swagger end-to-end on the single highest-value path before Plan 04-02 expands session lifecycle. Satisfies INFRA-01, INFRA-02, INFRA-03, AUTH-01 and implements CONTEXT.md decisions D-01 through D-16.

Purpose: Replace the untouched NestJS scaffold with a real, migration-driven backend and prove the riskiest architectural seam (Prisma 7 monorepo wiring — RESEARCH.md's "Primary recommendation") before building the rest of AuthModule on top of it.
Output: `packages/db` (schema, migrations, seed, generated client), `apps/server`'s `PrismaModule`/`PrismaService`, a working `AuthModule` with `POST /auth/login`, Swagger docs, and a documented local dev bootstrap.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-backend-foundation-auth/04-CONTEXT.md
@.planning/phases/04-backend-foundation-auth/04-RESEARCH.md
@.planning/phases/04-backend-foundation-auth/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Docker/env/turbo scaffolding + packages/db schema, migration, seed</name>
  <precondition>Docker daemon is running (`docker info` succeeds)</precondition>
  <reversibility rating="costly">D-10 (Clinic.plan kept independent of PricingPlan, not an FK) is implemented in this task's schema.prisma. Rated `costly` (not `one-way`) in CONTEXT.md: undoing it later means a data migration reconciling Clinic.plan values against PricingPlan rows. Decision is already made by the user — no checkpoint, rating carried through for the record.</reversibility>
  <files>docker-compose.yml, .env.example, .gitignore, turbo.json, package.json, packages/db/package.json, packages/db/prisma.config.ts, packages/db/prisma/schema.prisma, packages/db/prisma/seed.ts, packages/db/src/index.ts</files>
  <read_first>
    package.json (root — current `engines.node: ">=18"`, scripts, packageManager pnpm@9.0.0)
    turbo.json (root — current `build`/`lint`/`check-types`/`dev` tasks, no db task yet)
    .gitignore (root — confirms `.env`/`.env.local` already ignored; `packages/db/generated/` is NOT yet ignored)
    packages/ui/package.json (workspace-package convention: `name`, `private: true`, `main`/`types`/`exports` pointing at a single TS entry file — mirror for `@repo/db`)
    apps/web/modules/blog/_data.ts (`Post`/`PostBodyBlock` type — D-12 mandates a field-for-field mirror for the `BlogPost` model)
    apps/web/modules/prices/pricing-cards.tsx (`plans` array shape — D-13 mandates a field-for-field mirror for the `PricingPlan` model)
    apps/platform-admin/vite.config.ts (no explicit `server.port` configured — Vite's default dev port 5173 applies; needed for the CORS allowlist, D-08, documented in `.env.example` this task creates)
    .planning/phases/04-backend-foundation-auth/04-CONTEXT.md (full `<decisions>` block D-01 through D-16 — this task implements D-01–D-04, D-08 (env var only), D-09–D-16)
    .planning/phases/04-backend-foundation-auth/04-RESEARCH.md (Prisma Config reference, Package Legitimacy Audit table, "PlatformAdmin Seed Script" code example, Common Pitfalls 1/2/5/6, Open Questions (RESOLVED))
    .planning/phases/04-backend-foundation-auth/04-PATTERNS.md (target-state guidance for `packages/db/package.json`, `turbo.json`, root `package.json`)
  </read_first>
  <action>
    This is the first slice of the phase's tracer: the local Postgres/env/Turborepo scaffolding plus `packages/db`'s full schema, migration, and seed — the ground Tasks 2 and 3 build on. Work in this order:

    **A. Monorepo/infra scaffolding.** Create `docker-compose.yml` at repo root (D-02) with a single `postgres:17` service (D-01, D-04 — no pgAdmin), `POSTGRES_USER=postgres`/`POSTGRES_PASSWORD=postgres`/`POSTGRES_DB=denta_bot_dev` (D-03), host port `5432:5432`, a named volume for persistence, and a `pg_isready`-based healthcheck. Before starting it, check for a port conflict on 5432 (a local Postgres.app instance may already be listening, per RESEARCH.md's Environment Availability note) — if occupied, stop the conflicting local service for the duration of this task's work, since the committed `.env.example` assumes port 5432 is free. Create root `.env.example` documenting (placeholder values only, never real secrets): `DATABASE_URL="postgresql://postgres:postgres@localhost:5432/denta_bot_dev?schema=public"`, `PORT=4000`, `JWT_ACCESS_SECRET=`, `JWT_REFRESH_SECRET=`, `PLATFORM_ADMIN_EMAIL=`, `PLATFORM_ADMIN_PASSWORD=`, `CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173` (D-08 — `apps/web` dev origin `:3000` + `apps/platform-admin` dev origin `:5173`), `REFRESH_COOKIE_DOMAIN=` (empty for localhost, D-05), `REFRESH_COOKIE_SAMESITE=strict` (RESEARCH.md Open Question 2's resolution). Edit root `.gitignore` to add `packages/db/generated/` (RESEARCH.md Pitfall 5 — the generated client is a build artifact, not source). Edit root `package.json`: change `engines.node` from `">=18"` to `">=20.19.0"` (Prisma 7's floor) — no other field changes.

    **B. `packages/db` (`@repo/db`) package.** Create `packages/db/package.json`: `name: "@repo/db"`, `private: true`, `main`/`types` pointing at `./src/index.ts` (mirrors `@repo/ui`'s convention of exposing raw TS source). Scripts: `db:generate` = `"prisma generate"`, `db:migrate` = `"dotenv -e ../../.env -- prisma migrate dev"`, `db:migrate:deploy` = `"dotenv -e ../../.env -- prisma migrate deploy"`, `db:seed` = `"dotenv -e ../../.env -- tsx prisma/seed.ts"`. Dependencies: `@prisma/client@7.9.1`, `@prisma/adapter-pg@7.9.1`, `pg@8.23.0`, `argon2@0.45.1` (consumed directly by `seed.ts`'s password hashing, per D-15 — `packages/db` needs its own declaration since `seed.ts` runs standalone via `tsx`, outside `apps/server`'s dependency graph). DevDependencies: `prisma@7.9.1`, `dotenv-cli`, `@types/pg`, `tsx`. Run `pnpm install` once at repo root so pnpm registers the new workspace member (matches `pnpm-workspace.yaml`'s `packages/*` glob), then `pnpm --filter @repo/db add @prisma/client@7.9.1 @prisma/adapter-pg@7.9.1 pg@8.23.0 argon2@0.45.1` and `pnpm --filter @repo/db add -D prisma@7.9.1 dotenv-cli @types/pg tsx`.

    Create `packages/db/prisma.config.ts` per Prisma 7's config format (RESEARCH.md's Prisma Config reference source) — schema path `prisma/schema.prisma`, migrations path `prisma/migrations`, and load env from the ROOT `.env` explicitly (`import { config } from 'dotenv'; config({ path: '../../.env' })` at the top) to avoid RESEARCH.md Pitfall 2 (cwd-relative `.env` resolution failing when Prisma CLI runs from `packages/db`).

    Create `packages/db/prisma/schema.prisma`: `generator client` block with `provider = "prisma-client"`, `output = "../generated/prisma"`, `moduleFormat = "cjs"` (RESEARCH.md Pitfall 6 — keeps `apps/server`'s current CommonJS toolchain working, no ESM migration). `datasource db` with `provider = "postgresql"`, `url = env("DATABASE_URL")`. Six models, all with `id String @id @default(cuid())`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt` unless noted:
    - `PlatformAdmin`: `email String @unique`, `passwordHash String`, back-relation `refreshTokens RefreshToken[]`.
    - `RefreshToken`: `tokenHash String` (SHA-256 hex of the raw token, never the raw token itself), `familyId String` (indexed), `platformAdminId String` + `platformAdmin PlatformAdmin @relation(...)`, `revokedAt DateTime?`, `expiresAt DateTime`, no `updatedAt`. Index on `familyId` and on `platformAdminId`. The row's `id` is always explicitly supplied at creation time (set to the JWT's `jti` claim, a `crypto.randomUUID()` value) — never left to the `@default(cuid())`, since Task 3's `AuthService` looks rows up by `id: payload.jti`.
    - `Clinic` (D-09, D-10, D-11): `name String`, `email String @unique`, `phone String?`, `status ClinicStatus @default(trial)`, `plan String` (independent of `PricingPlan` per D-10 — no FK), `messageCount Int @default(0)`, `bookingsCount Int @default(0)`, `lastActiveAt DateTime?`, back-relation `leads Lead[]`.
    - `Lead` (D-14): `name String`, `clinicName String?`, `email String?`, `phone String?`, `message String?` (free-text fields not enumerated in CONTEXT.md — Claude's Discretion, informed by the existing Phase 2 Contacts form's `name`/`clinic`/`contact`/`message` field shape), `source LeadSource`, `status LeadStatus @default(new)`, `clinicId String?` + `clinic Clinic? @relation(...)` (nullable — populated only once a Phase 5 lead-conversion happens).
    - `BlogPost` (D-12): `slug String @unique`, `title String`, `excerpt String`, `category String`, `date String`, `readTime String`, `image String`, `body Json`, `published Boolean @default(false)`.
    - `PricingPlan` (D-13): `name String`, `monthlyPrice String`, `yearlyPrice String`, `description String`, `features String[]`, `isPopular Boolean @default(false)`, `sortOrder Int @default(0)`, `published Boolean @default(false)`.
    Enums: `ClinicStatus { trial active suspended cancelled }` (D-09), `LeadSource { contacts demo }`, `LeadStatus { new contacted converted }` (D-14).

    Create `packages/db/prisma/seed.ts` per RESEARCH.md's "PlatformAdmin Seed Script" code example (D-15): reads `PLATFORM_ADMIN_EMAIL`/`PLATFORM_ADMIN_PASSWORD` from env, throws if either is missing, hashes the password with `argon2.hash()` (argon2id default), and `prisma.platformAdmin.upsert()`s by `email` — never `deleteMany`/`truncate` on any table (this is a plan-level prohibition). No create-admin endpoint is built anywhere this phase (D-16) — the seed script is the only bootstrap mechanism.

    Create `packages/db/src/index.ts`: a single barrel re-exporting everything from the generated client output (`../generated/prisma`) — this is the only import surface `@repo/db` consumers use.

    **[BLOCKING] Run the bootstrap migration** (Schema Push Detection Gate — must run after schema.prisma is written, before this task's own verify): start Postgres (`docker compose up -d postgres`, wait for healthy via `pg_isready`), then from `packages/db` run `npx dotenv -e ../../.env -- npx prisma migrate dev --name init` — this is `prisma migrate dev`, NOT `db push` (INFRA-01 requires committed, version-controlled migration history). Confirm `packages/db/prisma/migrations/` now contains a timestamped `init` migration directory. Before this step, create the actual local root `.env` (gitignored, never committed) as a filled-in copy of `.env.example`: `DATABASE_URL` as documented, `PORT=4000`, `PLATFORM_ADMIN_EMAIL=platformadmin@dentabot.dev`, `PLATFORM_ADMIN_PASSWORD=DevAdmin!2026` (dev-only, throwaway), `JWT_ACCESS_SECRET`/`JWT_REFRESH_SECRET` as two DISTINCT random dev strings (e.g. two separate `openssl rand -hex 32` outputs — never the same value for both, this is load-bearing for AUTH-04's later token-confusion prohibition), `CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173`, `REFRESH_COOKIE_DOMAIN=`, `REFRESH_COOKIE_SAMESITE=strict`. Then run `pnpm --filter @repo/db run db:seed` to create the seeded PlatformAdmin. This root `.env` persists on disk for Tasks 2 and 3 to reuse — it is not recreated per task.

    **C. Turbo/root wiring.** Edit `turbo.json`: add `"db:generate": { "cache": false }` task; wire `"dependsOn": ["^db:generate"]` into `dev` and `check-types`, and `"dependsOn": ["^build", "^db:generate"]` into `build` (RESEARCH.md Pitfall 1 — Prisma client generation is otherwise invisible to Turborepo's task graph). Preserve all existing `build`/`lint`/`dev` config unchanged otherwise.
  </action>
  <acceptance_criteria>
    <criterion>`packages/db/prisma/schema.prisma` contains `model PlatformAdmin`, `model RefreshToken`, `model Clinic`, `model Lead`, `model BlogPost`, `model PricingPlan`, and `moduleFormat = "cjs"`</criterion>
    <criterion>`packages/db/prisma/migrations/` contains a committed `init` migration directory</criterion>
    <criterion>`turbo.json`'s `build`/`dev`/`check-types` tasks each list `^db:generate` in their `dependsOn` array</criterion>
    <criterion>root `package.json` `engines.node` reads `">=20.19.0"`</criterion>
    <criterion>root `.gitignore` contains `packages/db/generated/`</criterion>
    <criterion>the `PlatformAdmin` table has exactly one row after `db:seed` runs, matching `PLATFORM_ADMIN_EMAIL`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
test "$(grep -c 'model PlatformAdmin' packages/db/prisma/schema.prisma)" -ge "1"
test "$(grep -c 'model RefreshToken' packages/db/prisma/schema.prisma)" -ge "1"
test "$(grep -c 'model Clinic' packages/db/prisma/schema.prisma)" -ge "1"
test "$(grep -c 'model Lead' packages/db/prisma/schema.prisma)" -ge "1"
test "$(grep -c 'model BlogPost' packages/db/prisma/schema.prisma)" -ge "1"
test "$(grep -c 'model PricingPlan' packages/db/prisma/schema.prisma)" -ge "1"
test "$(grep -c 'moduleFormat = "cjs"' packages/db/prisma/schema.prisma)" -ge "1"
test "$(grep -c 'db:generate' turbo.json)" -ge "3"
test "$(grep -c '">=20.19.0"' package.json)" = "1"
test "$(grep -c 'packages/db/generated' .gitignore)" -ge "1"
docker compose up -d postgres
for i in $(seq 1 30); do docker compose exec -T postgres pg_isready -U postgres -d denta_bot_dev >/dev/null 2>&1 && break; sleep 1; done
ls packages/db/prisma/migrations | grep -q init
SEED_COUNT=$(docker compose exec -T postgres psql -U postgres -d denta_bot_dev -tAc 'SELECT count(*) FROM "PlatformAdmin" WHERE email = '"'"'platformadmin@dentabot.dev'"'"';' | tr -d '[:space:]')
test "$SEED_COUNT" = "1"
echo PASS
    </automated>
    <human-check>
      <test>Package Legitimacy Gate (RESEARCH.md Package Legitimacy Audit table) — before or shortly after this task's `pnpm --filter @repo/db add` installs, confirm on npmjs.com: https://www.npmjs.com/package/prisma, /@prisma/client, /@prisma/adapter-pg, /pg, /argon2.</test>
      <expected>Each of the 5 packages resolves to its expected GitHub org (prisma/prisma, brianc/node-postgres, ranisalt/node-argon2) with multi-million weekly downloads, matching RESEARCH.md's "Keep — false positive" verdicts (all were flagged `SUS` only by the legitimacy checker's too-new-publish-date heuristic, not by any real trust signal).</expected>
      <why_human>Supply-chain trust judgment is not mechanically provable by grep alone, though RESEARCH.md already performed the diligence this human-check re-confirms.</why_human>
    </human-check>
  </verify>
  <done>Docker Postgres runs locally via Compose; `packages/db`'s full 6-model schema is migrated via a committed `prisma migrate dev` history and seeded with the first PlatformAdmin; Turborepo's task graph makes Prisma client generation visible via `db:generate`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: apps/server Prisma wiring (PrismaService/Module) + main.ts bootstrap (Swagger/CORS/ValidationPipe)</name>
  <precondition>Task 1's `packages/db/prisma/migrations/init` exists, the local Postgres container is healthy, and `@repo/db`'s generated Prisma client exists (`packages/db/generated/prisma`)</precondition>
  <files>apps/server/package.json, apps/server/src/main.ts, apps/server/src/app.module.ts, apps/server/src/config/env.validation.ts, apps/server/src/prisma/prisma.module.ts, apps/server/src/prisma/prisma.service.ts</files>
  <read_first>
    apps/server/src/main.ts (current 8-line scaffold — `NestFactory.create` then `app.listen(process.env.PORT ?? 3000)`, everything after `create` is new)
    apps/server/src/app.module.ts (current — empty `imports: []`, keep `AppController`/`AppService` untouched)
    apps/server/package.json (current dependencies — @nestjs/common/core/platform-express ^11.0.1 only)
    packages/db/src/index.ts (Task 1 output — `@repo/db` barrel this task's `PrismaService` imports `PrismaClient` from)
    .planning/phases/04-backend-foundation-auth/04-CONTEXT.md (D-08 CORS allowlist, D-05 cookie-domain env var design intent for later reference)
    .planning/phases/04-backend-foundation-auth/04-RESEARCH.md (Pattern 1 "PrismaService with Driver Adapter", Pattern 4 "Swagger Setup", Anti-Patterns, Common Pitfalls 2/5/6, Package Legitimacy Audit table)
    .planning/phases/04-backend-foundation-auth/04-PATTERNS.md (exact target-state guidance for `main.ts`, `app.module.ts`, `prisma.service.ts`/`prisma.module.ts`)
  </read_first>
  <action>
    With Task 1 providing the DB, migration, and generated Prisma client, this task wires `apps/server` to actually consume it and boots a real NestJS server with Swagger/CORS — no auth routes yet (Task 3 adds those). Work in this order:

    **A. `apps/server` package.json — Prisma/bootstrap dependencies.** Add `"@repo/db": "workspace:*"` to dependencies (mirrors `apps/admin-panel`'s `"@repo/ui": "workspace:*"` pattern). Add dependencies: `@prisma/adapter-pg@7.9.1` (imported directly by this task's `prisma.service.ts` — each workspace package must declare its own dependencies explicitly under pnpm's non-hoisted linking), `@nestjs/swagger@11.4.6`, `class-validator@0.15.1`, `class-transformer@0.5.1`, `cookie-parser@1.4.7`, `@nestjs/config@4.0.4`, `zod@^3` (matches `apps/web`'s already-locked `zod@^3`, not a new major). DevDependencies: `@types/cookie-parser`. Run `pnpm --filter server add "@repo/db@workspace:*" @prisma/adapter-pg@7.9.1 @nestjs/swagger@11.4.6 class-validator@0.15.1 class-transformer@0.5.1 cookie-parser@1.4.7 @nestjs/config@4.0.4 zod@^3` and `pnpm --filter server add -D @types/cookie-parser`.

    **B. `apps/server/src/config/env.validation.ts`.** A zod object schema (`envSchema`) validating: `DATABASE_URL` (non-empty string), `PORT` (coerced number, default 4000), `JWT_ACCESS_SECRET`/`JWT_REFRESH_SECRET` (string, `min(16)`), `PLATFORM_ADMIN_EMAIL`/`PLATFORM_ADMIN_PASSWORD` (string), `CORS_ALLOWED_ORIGINS` (non-empty string), `REFRESH_COOKIE_DOMAIN` (string, optional), `REFRESH_COOKIE_SAMESITE` (enum `['strict','lax','none']`, default `'strict'`) — plus a top-level `.refine()` asserting `JWT_ACCESS_SECRET !== JWT_REFRESH_SECRET` (fails boot fast if the two secrets are ever equal — enforces the future AUTH-04 "distinct secrets" prohibition at the config layer, even though the login flow that consumes these secrets is only wired in Task 3). Export a `validateEnv(config: Record<string, unknown>)` function that parses `config` through `envSchema` and throws on failure (NestJS's `ConfigModule.forRoot({ validate })` contract).

    **C. `apps/server/src/prisma/prisma.service.ts` + `prisma.module.ts`.** Copy RESEARCH.md Pattern 1 verbatim: `PrismaService extends PrismaClient` (imported from `@repo/db`) implementing `OnModuleInit`/`OnModuleDestroy`, constructing `super({ adapter: new PrismaPg({ connectionString: process.env.DATABASE_URL }) })` (import `PrismaPg` from `@prisma/adapter-pg`), `$connect()`/`$disconnect()` in the lifecycle hooks. `prisma.module.ts`: `@Global() @Module({ providers: [PrismaService], exports: [PrismaService] })`.

    **D. `apps/server/src/app.module.ts`.** Set `imports: [ConfigModule.forRoot({ isGlobal: true, envFilePath: path.resolve(process.cwd(), '../../.env'), validate: validateEnv }), PrismaModule]` — `envFilePath` is `process.cwd()`-relative (not `__dirname`-relative), since `pnpm --filter server run <script>`/turbo always sets cwd to `apps/server`, making this robust across both `ts-node`/`nest start --watch` (dev) and compiled `dist` (prod), unlike a `__dirname`-relative path which differs between the two (RESEARCH.md Pitfall 2's app-side analog). Keep `AppController`/`AppService` in `controllers`/`providers` unchanged. `AuthModule` is NOT added to this array yet — Task 3 extends this same import array once `AuthModule` exists.

    **E. `apps/server/src/main.ts`.** Copy RESEARCH.md's "Global ValidationPipe + Cookie/CORS Bootstrap" example, adapted: `app.use(cookieParser())`; `app.enableCors({ origin: (process.env.CORS_ALLOWED_ORIGINS ?? '').split(',').filter(Boolean), credentials: true })` (D-08 — an explicit array, never `origin: '*'` with `credentials: true`, which browsers reject outright per RESEARCH.md's Anti-Patterns); `app.useGlobalPipes(new ValidationPipe({ whitelist: true, forbidNonWhitelisted: true, transform: true }))`; Swagger via RESEARCH.md Pattern 4 (`DocumentBuilder().setTitle('denta-bot Platform API').setDescription(...).setVersion('1.0').addBearerAuth(undefined, 'access-token').build()`, `SwaggerModule.setup('api/docs', app, document, { swaggerOptions: { persistAuthorization: true } })`); `await app.listen(process.env.PORT ?? 4000)` — changed from the scaffold's `3000` default (RESEARCH.md/PATTERNS.md: `3000` collides with `apps/web`'s dev port).
  </action>
  <acceptance_criteria>
    <criterion>`apps/server/src/prisma/prisma.service.ts` extends `PrismaClient` and constructs a `PrismaPg` adapter imported from `@prisma/adapter-pg`</criterion>
    <criterion>`apps/server/src/config/env.validation.ts` contains a `.refine()` call enforcing `JWT_ACCESS_SECRET !== JWT_REFRESH_SECRET`</criterion>
    <criterion>`apps/server/src/main.ts` calls `cookieParser()`, `app.enableCors`, and `SwaggerModule.setup('api/docs', ...)`</criterion>
    <criterion>`apps/server/src/app.module.ts`'s `imports` array includes `ConfigModule.forRoot({ ... validate: validateEnv ... })` and `PrismaModule`, and does NOT yet reference `AuthModule`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
test "$(grep -c 'extends PrismaClient' apps/server/src/prisma/prisma.service.ts)" -ge "1"
test "$(grep -c 'PrismaPg' apps/server/src/prisma/prisma.service.ts)" -ge "1"
test "$(grep -c '.refine(' apps/server/src/config/env.validation.ts)" -ge "1"
test "$(grep -c 'cookieParser' apps/server/src/main.ts)" -ge "1"
test "$(grep -c 'SwaggerModule' apps/server/src/main.ts)" -ge "1"
test "$(grep -c 'PrismaModule' apps/server/src/app.module.ts)" -ge "1"
test "$(grep -c 'validateEnv' apps/server/src/app.module.ts)" -ge "1"
test "$(grep -c 'AuthModule' apps/server/src/app.module.ts)" = "0"
docker compose up -d postgres
for i in $(seq 1 30); do docker compose exec -T postgres pg_isready -U postgres -d denta_bot_dev >/dev/null 2>&1 && break; sleep 1; done
cd packages/db && npx dotenv -e ../../.env -- npx prisma migrate deploy && cd ../..
pnpm --filter server run build
( pnpm --filter server run start > /tmp/denta-server-2.log 2>&1 & echo $! > /tmp/denta-server-2.pid )
for i in $(seq 1 30); do curl -s http://localhost:4000/api/docs-json >/dev/null 2>&1 && break; sleep 1; done
curl -s http://localhost:4000/api/docs-json | grep -q '"openapi"'
curl -s -i -X OPTIONS http://localhost:4000/api/docs-json -H "Origin: http://localhost:3000" -H "Access-Control-Request-Method: GET" | grep -qi "access-control-allow-origin: http://localhost:3000"
curl -s -i -X OPTIONS http://localhost:4000/api/docs-json -H "Origin: http://localhost:5173" -H "Access-Control-Request-Method: GET" | grep -qi "access-control-allow-origin: http://localhost:5173"
kill "$(cat /tmp/denta-server-2.pid)" 2>/dev/null || true
echo PASS
    </automated>
    <human-check>
      <test>Package Legitimacy Gate — confirm on npmjs.com: https://www.npmjs.com/package/@nestjs/swagger (the one new SUS-flagged package this task installs; `@prisma/adapter-pg` was already confirmed in Task 1). Open http://localhost:4000/api/docs in a browser.</test>
      <expected>`@nestjs/swagger` resolves to the official `nestjs/swagger` GitHub org with multi-million weekly downloads, matching RESEARCH.md's "Keep — false positive" verdict. Swagger UI at /api/docs renders (titled "denta-bot Platform API") — no `/auth/login` operation listed yet, that's expected until Task 3.</expected>
      <why_human>Supply-chain trust judgment and a visual confirmation the docs page renders — not mechanically provable by grep alone.</why_human>
    </human-check>
  </verify>
  <done>apps/server boots successfully with Prisma driver-adapter wiring (`PrismaService`/`PrismaModule`), env validation at boot, a global CORS allowlist covering both dev origins, cookie-parser, and a browsable Swagger doc at `/api/docs` — no auth routes exist yet.</done>
</task>

<task type="tracer" tdd="false">
  <name>Task 3: AuthModule — POST /auth/login (DTOs, service, controller) wired into app.module.ts</name>
  <precondition>Task 2's apps/server boots, serves Swagger at `/api/docs-json`, and Task 1's seeded PlatformAdmin (`platformadmin@dentabot.dev` / `DevAdmin!2026`) exists in the local Postgres DB</precondition>
  <reversibility rating="costly">D-06 (access-token-in-memory-on-frontend / refresh-token-in-httpOnly-cookie architecture) is implemented in this task via `AuthResponseDto` (which omits `refreshToken`) and `auth.controller.ts`'s `res.cookie(...)` call. Rated `costly` (not `one-way`) in CONTEXT.md: undoing it later means re-touching every frontend auth call site, not just a config flag. Decision is already made by the user — no checkpoint, rating carried through for the record.</reversibility>
  <files>apps/server/package.json, apps/server/src/app.module.ts, apps/server/src/auth/auth.module.ts, apps/server/src/auth/auth.controller.ts, apps/server/src/auth/auth.service.ts, apps/server/src/auth/dto/login.dto.ts, apps/server/src/auth/dto/auth-response.dto.ts</files>
  <read_first>
    apps/server/src/app.module.ts (Task 2 output — current `imports` array this task extends with `AuthModule`)
    apps/server/src/prisma/prisma.service.ts (Task 2 output — `PrismaService` this task's `AuthService` injects)
    apps/server/src/main.ts (Task 2 output — cookie-parser/CORS/ValidationPipe already active; this task's cookie-setting code relies on `cookie-parser` being registered)
    .planning/phases/04-backend-foundation-auth/04-CONTEXT.md (D-06, D-07, D-15, D-16 — this task's DTO shape, token TTLs, and login-only scope)
    .planning/phases/04-backend-foundation-auth/04-RESEARCH.md (Pattern 3 "Refresh Token Rotation" for the `issueTokenPair` helper Plan 04-02 reuses, Assumption A5 SHA-256-not-argon2 for token-at-rest hashing, Security Domain, Anti-Patterns — never re-export the raw PlatformAdmin model)
    .planning/phases/04-backend-foundation-auth/04-PATTERNS.md (target-state guidance for `auth/*`)
  </read_first>
  <action>
    This is the phase's tracer: the task that completes the single end-to-end path (Docker Postgres → Prisma schema/migration → generated client → NestJS service/controller → Swagger doc) proven by Tasks 1-2, wired to a real `POST /auth/login`. Work in this order:

    **A. `apps/server` package.json — auth dependencies.** Add dependencies: `@nestjs/jwt@11.0.2`, `@nestjs/passport@11.0.5`, `passport-jwt@4.0.1`, `passport@0.7.0`, `argon2@0.45.1`. DevDependencies: `@types/passport-jwt`. `passport-jwt`/`passport`/`@nestjs/passport` are installed now (bundled with this single install command) even though they are only consumed by Plan 04-02's strategies — avoids a second `pnpm add` touching `package.json`/`pnpm-lock.yaml` across plans. Run `pnpm --filter server add @nestjs/jwt@11.0.2 @nestjs/passport@11.0.5 passport-jwt@4.0.1 passport@0.7.0 argon2@0.45.1` and `pnpm --filter server add -D @types/passport-jwt`. `argon2` was already installed and package-legitimacy-confirmed for `packages/db` in Task 1 — this is a second, separate declaration for `apps/server`'s own `node_modules` (pnpm workspaces require each consuming package to declare its own dependencies explicitly), not a fresh unvetted package.

    **B. `apps/server/src/auth/*` — login only.** `dto/login.dto.ts`: `LoginDto` class with `@ApiProperty() @IsEmail() email: string` and `@ApiProperty() @IsString() @IsNotEmpty() password: string` (class-validator + class-transformer decorated). `dto/auth-response.dto.ts`: `PlatformAdminSummaryDto { id: string; email: string; createdAt: Date }` and `AuthResponseDto { accessToken: string; platformAdmin: PlatformAdminSummaryDto }` — deliberately has NO `refreshToken`/`passwordHash` field (D-06 — refresh token travels only via the httpOnly cookie, never the JSON body).

    `auth.service.ts` — `AuthService` constructor-injects `PrismaService`, `JwtService` (from `@nestjs/jwt`), `ConfigService`. Implements a private `issueTokenPair(platformAdminId: string, familyId = crypto.randomUUID())` helper (Node's built-in `crypto` module, no new dependency) that: signs an access token (`sub: platformAdminId`, `secret: JWT_ACCESS_SECRET`, `expiresIn: '15m'` — D-07), generates a fresh `jti = crypto.randomUUID()`, signs a refresh token (`sub`, `familyId`, `jti`, `secret: JWT_REFRESH_SECRET`, `expiresIn: '7d'` — D-07), computes `tokenHash = sha256(refreshToken)` (Node's built-in `crypto.createHash('sha256')` — RESEARCH.md Assumption A5, not argon2, refresh tokens are high-entropy already), and creates a `RefreshToken` row with `id: jti`, `tokenHash`, `familyId`, `platformAdminId`, `expiresAt` (now + 7 days). Returns `{ accessToken, refreshToken, familyId }`. This helper is the shared building block Plan 04-02's `refresh()` reuses with the SAME `familyId`.

    Implements `login(email: string, password: string)`: looks up `PlatformAdmin` by email; if not found OR `argon2.verify(admin.passwordHash, password)` fails, throw the SAME `UnauthorizedException('Invalid credentials')` in both branches (no distinguishing message/status/timing shortcut — no user enumeration). On success, calls `issueTokenPair(admin.id)` (no `familyId` passed — a fresh random family per login) and returns `{ accessToken, refreshToken, platformAdmin: { id: admin.id, email: admin.email, createdAt: admin.createdAt } }` — never returns the raw `admin` object.

    `auth.controller.ts` — `@Controller('auth')`, `@ApiTags('auth')`. `POST /login`: `@Public()` is NOT yet available this task (Plan 04-02 adds the guard/decorator) — no guard is active on this route this task, so no decorator is needed yet. Body validated as `LoginDto`. Calls `authService.login()`, sets the refresh cookie on the Express `Response` (`@Res({ passthrough: true })`) — `res.cookie('refresh_token', refreshToken, { httpOnly: true, secure: process.env.NODE_ENV === 'production', sameSite: process.env.REFRESH_COOKIE_SAMESITE ?? 'strict', domain: process.env.REFRESH_COOKIE_DOMAIN || undefined, path: '/auth', maxAge: 7 * 24 * 60 * 60 * 1000 })` (standard practice: `secure` gates on `NODE_ENV==='production'`, not always-true, so local `http://localhost` curl/browser testing works — matches D-06's production security intent without blocking dev iteration). Returns `AuthResponseDto` (accessToken + platformAdmin only, per the DTO above).

    `auth.module.ts` — imports `PassportModule`, `JwtModule.register({})` (no default secret/expiry — every `sign()` call passes explicit `secret`/`expiresIn`, avoiding a single shared-secret pitfall), providers `[AuthService]`, controllers `[AuthController]`.

    **C. `apps/server/src/app.module.ts` — wire in AuthModule.** Extend Task 2's `imports: [ConfigModule.forRoot(...), PrismaModule]` array to append `AuthModule` (imported from `./auth/auth.module`), giving the final `imports: [ConfigModule.forRoot({...}), PrismaModule, AuthModule]`. Keep `AppController`/`AppService` and the `ConfigModule.forRoot(...)` options exactly as Task 2 wrote them.
  </action>
  <acceptance_criteria>
    <criterion>`apps/server/src/auth/dto/auth-response.dto.ts` does not contain the field name `refreshToken` or `passwordHash` anywhere</criterion>
    <criterion>`apps/server/src/auth/auth.service.ts` throws the identical `UnauthorizedException` message in both the "admin not found" and "password mismatch" branches of `login()`</criterion>
    <criterion>`apps/server/src/auth/auth.controller.ts` sets the `refresh_token` cookie with `httpOnly: true` and never includes the refresh token in the JSON response body</criterion>
    <criterion>`apps/server/src/app.module.ts`'s `imports` array now includes `AuthModule`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
set -e
cd /Users/artemdanko/Developer/denta-bot
test "$(grep -Ec 'refreshToken|passwordHash' apps/server/src/auth/dto/auth-response.dto.ts)" = "0"
test "$(grep -c 'AuthModule' apps/server/src/app.module.ts)" -ge "1"
docker compose up -d postgres
for i in $(seq 1 30); do docker compose exec -T postgres pg_isready -U postgres -d denta_bot_dev >/dev/null 2>&1 && break; sleep 1; done
cd packages/db && npx dotenv -e ../../.env -- npx prisma migrate deploy && cd ../..
pnpm --filter server run build
( pnpm --filter server run start > /tmp/denta-server-3.log 2>&1 & echo $! > /tmp/denta-server-3.pid )
for i in $(seq 1 30); do curl -s http://localhost:4000/api/docs-json >/dev/null 2>&1 && break; sleep 1; done
RESP=$(curl -s -i -X POST http://localhost:4000/auth/login -H "Content-Type: application/json" -d '{"email":"platformadmin@dentabot.dev","password":"DevAdmin!2026"}')
echo "$RESP" | grep -qE "HTTP/1\.1 (200|201)"
echo "$RESP" | grep -qi "^set-cookie:.*refresh_token=.*HttpOnly"
echo "$RESP" | grep -q '"accessToken"'
echo "$RESP" | grep -qv "passwordHash"
curl -s http://localhost:4000/api/docs-json | grep -q '/auth/login'
curl -s -i -X OPTIONS http://localhost:4000/auth/login -H "Origin: http://localhost:3000" -H "Access-Control-Request-Method: POST" | grep -qi "access-control-allow-origin: http://localhost:3000"
curl -s -i -X OPTIONS http://localhost:4000/auth/login -H "Origin: http://localhost:5173" -H "Access-Control-Request-Method: POST" | grep -qi "access-control-allow-origin: http://localhost:5173"
WRONG_PW=$(curl -s -X POST http://localhost:4000/auth/login -H "Content-Type: application/json" -d '{"email":"platformadmin@dentabot.dev","password":"wrong"}')
UNKNOWN_EMAIL=$(curl -s -X POST http://localhost:4000/auth/login -H "Content-Type: application/json" -d '{"email":"nobody@example.com","password":"wrong"}')
test "$WRONG_PW" = "$UNKNOWN_EMAIL"
kill "$(cat /tmp/denta-server-3.pid)" 2>/dev/null || true
echo PASS
    </automated>
    <human-check>
      <test>Package Legitimacy Gate — `argon2` is re-installed here into `apps/server` (already vetted for `packages/db` in Task 1's human-check); no new unvetted package this task. Open http://localhost:4000/api/docs in a browser.</test>
      <expected>Swagger UI at /api/docs now lists the `/auth/login` operation under the "auth" tag.</expected>
      <why_human>Final visual confirmation the docs page renders the new route — not mechanically provable by grep alone.</why_human>
    </human-check>
  </verify>
  <done>`AuthModule` is wired into `app.module.ts`; `POST /auth/login` with the seeded PlatformAdmin's credentials returns an access token + httpOnly refresh cookie, with no user-enumeration and no `passwordHash` leakage — INFRA-01, INFRA-02, INFRA-03, and AUTH-01 are fully satisfied by the combined output of Tasks 1-3.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Clean-regenerate verification (Pitfall 1) + local dev bootstrap docs</name>
  <precondition>Task 1's `packages/db/prisma/migrations/init` exists and Task 3's `apps/server` build succeeded (AuthModule wired)</precondition>
  <files>README.md, packages/db/generated (deleted and regenerated, not hand-authored)</files>
  <read_first>
    packages/db/package.json (Task 1 output — `db:generate` script this task re-runs from a clean state)
    turbo.json (Task 1 output — `db:generate` wiring this task proves actually works, not just exists on paper)
    README.md (current — default `create-turbo` content, "### Develop" section is the natural insertion point)
    .planning/phases/04-backend-foundation-auth/04-RESEARCH.md (Common Pitfall 1 — "Prisma Client Generation Invisible to Turborepo's Task Graph" — this task's whole purpose is proving this pitfall is actually closed, not just documented)
  </read_first>
  <action>
    Delete `packages/db/generated/` entirely (simulating a fresh clone that has never run `prisma generate`), then run `pnpm --filter @repo/db run db:generate` and confirm the directory is regenerated with no errors, then run `pnpm --filter server run build` and confirm it still succeeds — this proves RESEARCH.md Pitfall 1 (Prisma client generation invisible to Turborepo) is actually closed by the `db:generate`/`dependsOn` wiring from Task 1, not merely present in `turbo.json` untested.

    Edit `README.md`: insert a new `## Backend Development (apps/server)` section immediately after the existing `### Develop` section (before `### Remote Caching`), documenting the local bootstrap sequence a new developer or a future Claude session needs to run this backend from a clean clone: (1) `docker compose up -d` to start local Postgres; (2) copy `.env.example` to `.env` at repo root and fill in `PLATFORM_ADMIN_EMAIL`/`PLATFORM_ADMIN_PASSWORD`/two distinct `JWT_*_SECRET` values; (3) `pnpm --filter @repo/db run db:migrate` (or `db:migrate:deploy` in CI/prod, per INFRA-01 — never `db push`) to apply migrations; (4) `pnpm --filter @repo/db run db:seed` to create the first PlatformAdmin; (5) `pnpm --filter server run dev` (or the root `dev:server` script) to start the API at `http://localhost:4000`, Swagger at `/api/docs`. State plainly that Prisma Studio (`pnpm --filter @repo/db exec prisma studio`) is the documented way to browse the DB (D-04 — no pgAdmin).
  </action>
  <acceptance_criteria>
    <criterion>`packages/db/generated/prisma` exists and is non-empty after the delete+regenerate cycle</criterion>
    <criterion>`pnpm --filter server run build` exits 0 after the clean regenerate, with no manual intervention beyond the documented commands</criterion>
    <criterion>`README.md` contains a `## Backend Development` heading with `docker compose up` and `prisma migrate` mentioned</criterion>
  </acceptance_criteria>
  <verify>
    <automated>
cd /Users/artemdanko/Developer/denta-bot
rm -rf packages/db/generated
pnpm --filter @repo/db run db:generate
test -d packages/db/generated/prisma
pnpm --filter server run build
test "$(grep -c '## Backend Development' README.md)" -ge "1"
test "$(grep -c 'docker compose up' README.md)" -ge "1"
test "$(grep -c 'prisma migrate' README.md)" -ge "1"
echo PASS
    </automated>
  </verify>
  <done>A from-scratch `db:generate` + `apps/server` build succeeds with zero manual intervention, and `README.md` documents the exact bootstrap sequence for the next developer/session.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry → `apps/server`/`packages/db` installs | 6 packages flagged `SUS` by the automated legitimacy checker (all false positives per RESEARCH.md's audit, human-reconfirmed via this plan's `<human-check>`) |
| curl/Swagger client → `POST /auth/login` | Untrusted credentials input; this phase's only externally-reachable write path |
| `apps/web`(:3000) / `apps/platform-admin`(:5173) dev origins → `apps/server`(:4000) | Cross-origin browser requests, gated by an explicit CORS allowlist (D-08) |
| Docker Postgres (local) → `packages/db` migrations | Local-only, dev-default credentials (D-03) — no production topology decided yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-01-SC | Tampering (supply chain) | `prisma`, `@prisma/client`, `@prisma/adapter-pg`, `pg`, `argon2`, `@nestjs/swagger` npm installs | high | mitigate | `<human-check>` re-confirms RESEARCH.md's per-package "Keep — false positive" verdicts (org repo, download counts) before/shortly after each install — split across Task 1 (`prisma`, `@prisma/client`, `@prisma/adapter-pg`, `pg`, `argon2` in `packages/db`), Task 2 (`@nestjs/swagger`, `@prisma/adapter-pg` re-declared in `apps/server`), and Task 3 (`argon2` re-declared in `apps/server`) |
| T-04-01-01 | Spoofing / Information Disclosure | `AuthService.login()` error path | high | mitigate | Identical `UnauthorizedException('Invalid credentials')` for unknown-email and wrong-password branches — no user enumeration |
| T-04-01-02 | Information Disclosure | `AuthController` login response | high | mitigate | `AuthResponseDto` never includes `passwordHash` or the raw `PlatformAdmin` model; refresh token travels only via the httpOnly cookie, never the JSON body |
| T-04-01-03 | Tampering | `PrismaService`/`prisma.config.ts` DB connection | medium | mitigate | `DATABASE_URL` sourced exclusively from env, no hardcoded fallback connection string |
| T-04-01-04 | Information Disclosure | Root `.env` (real DB creds, JWT secrets, admin password) | high | mitigate | Already `.gitignore`d; `.env.example` carries placeholder values only, never real secrets |
| T-04-01-05 | Tampering | `packages/db/prisma/seed.ts` | medium | mitigate | Seed script only `upsert`s the single PlatformAdmin by email; never `deleteMany`/`truncate` |
| T-04-01-06 | Information Disclosure | `GET /api/docs` Swagger UI, unauthenticated this phase | low | accept | No production deployment target exists yet (D-05 undecided); phase is explicitly verified "at the API/Swagger level" per ROADMAP.md |

</threat_model>

<verification>
- Task 1's automated script proves the DB layer: Docker Postgres up, migration applied, seed run, the seeded PlatformAdmin row exists
- Task 2's automated script proves the bootstrap layer: `apps/server` builds and boots with Prisma driver-adapter wiring, Swagger reachable, CORS allows both configured dev origins — no auth routes yet
- Task 3's automated script proves the full tracer: server boots with `AuthModule` wired, `POST /auth/login` returns an access token + httpOnly refresh cookie, Swagger lists `/auth/login`, no user enumeration, no `passwordHash` leakage
- Task 4's automated script proves RESEARCH.md Pitfall 1 (Turborepo/Prisma wiring) is actually closed via a from-scratch regenerate, and that `README.md` documents the bootstrap sequence
- `<human-check>` (split across Tasks 1-3) re-confirms the 6 `SUS`-flagged package installs against their npm registry listings, and a visual Swagger UI render
</verification>

<success_criteria>
`packages/db` owns a version-controlled, migration-driven 6-model schema; `apps/server` imports the generated client via `@repo/db`, serves a browsable Swagger doc, and `POST /auth/login` works end-to-end against the real seeded PlatformAdmin — INFRA-01, INFRA-02, INFRA-03, AUTH-01, and ROADMAP Phase 4 success criteria 1-3 (plus the login half of criterion 4) are satisfied.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `docker-compose.yml` (new, root) — local Postgres 17 service
- `.env.example` (new, root) — documented env var contract
- `packages/db/package.json` (new) — `@repo/db` workspace package
- `packages/db/prisma.config.ts` (new) — Prisma 7 config
- `packages/db/prisma/schema.prisma` (new) — `PlatformAdmin`, `RefreshToken`, `Clinic`, `Lead`, `BlogPost`, `PricingPlan` models; `ClinicStatus`, `LeadSource`, `LeadStatus` enums
- `packages/db/prisma/seed.ts` (new) — D-15 seed script
- `packages/db/src/index.ts` (new) — `@repo/db` barrel export
- `apps/server/src/config/env.validation.ts` (new) — `envSchema`, `validateEnv`
- `apps/server/src/prisma/prisma.service.ts` (new) — `PrismaService`
- `apps/server/src/prisma/prisma.module.ts` (new) — `PrismaModule`
- `apps/server/src/auth/auth.module.ts` (new) — `AuthModule`
- `apps/server/src/auth/auth.controller.ts` (new) — `AuthController` (`POST /auth/login`)
- `apps/server/src/auth/auth.service.ts` (new) — `AuthService` (`login`, `issueTokenPair`)
- `apps/server/src/auth/dto/login.dto.ts` (new) — `LoginDto`
- `apps/server/src/auth/dto/auth-response.dto.ts` (new) — `AuthResponseDto`, `PlatformAdminSummaryDto`
- `apps/server/src/main.ts` (edited) — cookie-parser, CORS, ValidationPipe, Swagger bootstrap
- `apps/server/src/app.module.ts` (edited) — `ConfigModule`/`PrismaModule`/`AuthModule` wiring
- `turbo.json` (edited) — `db:generate` task
- `package.json` (root, edited) — `engines.node` bump
- `README.md` (edited) — `## Backend Development` section

<output>
Create `.planning/phases/04-backend-foundation-auth/04-01-SUMMARY.md` when done
</output>
