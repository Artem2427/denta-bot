---
type: quick
slug: 260820-1bb-make-all-section-heading-h2-font-sizes-r
autonomous: true
files_modified:
  - apps/web/app/premium-theme.css
  - apps/web/shared/components/section-heading.tsx
must_haves:
  truths:
    - 'apps/web/app/premium-theme.css --text-dt-h2 matches the reference design prototype''s devtools-inspected computed style exactly: clamp(1.9rem, 3.4vw, 2.75rem), line-height 1.1, letter-spacing -0.03em — not a hand-derived approximation'
    - 'FLAGGED SHAPE CHANGE (surface to user, do not silently accept): under this exact reference clamp, phone (≤430px) and tablet-portrait (up to ~894px, e.g. 768px) render IDENTICALLY at the 1.9rem/30.4px floor — there is no "phone smaller than tablet" distinction in this range. Only tablet-landscape and up (~894-1294px) actively scales toward the 2.75rem/44px desktop cap. This is a materially different shape than the original quick-task ask (continuous 3-tier phone<tablet<desktop stepping) — it is accepted here ONLY because it is sourced directly from the original design prototype''s own devtools-inspected computed style, which is the ground-truth source of visual truth for this design-archive-port project'
    - 'Desktop-width (≥1294px) section headings render at 2.75rem (44px) — smaller than the previous 3.25rem (52px) cap. This is an intentional decrease sourced from the reference design, not a regression; flag to user since it moves further from the earlier "дуже мілке" (too small) client fix''s cap value (git log 9e630a6), even though it is sourced from ground truth this time rather than derived'
    - 'Every element that reads the text-dt-h2 utility class (SectionHeading, blog list/error headings, Stat, pricing plan price, lead-section heading) inherits the new font-size/line-height/letter-spacing automatically via the shared token, since none of them override it locally'
    - 'section-heading.tsx''s own <h2> renders font-extrabold (800) instead of font-bold (700), matching the reference''s font-weight — scoped to this one component only, not swept to Stat/pricing-section/lead-section/blog headings which keep font-bold'
  artifacts:
    - apps/web/app/premium-theme.css
    - apps/web/shared/components/section-heading.tsx
  key_links:
    - '--text-dt-h2 / --text-dt-h2--line-height / --text-dt-h2--letter-spacing in premium-theme.css (@theme block) are the single source every consumer reads via the text-dt-h2 Tailwind utility class — Tailwind v4 auto-applies all three paired sub-properties together when a component uses text-dt-h2, so retuning the token propagates everywhere without touching most consumer files'
---

<objective>
Update the `--text-dt-h2` fluid type-scale token in `apps/web/app/premium-theme.css` to match the reference design prototype's exact devtools-inspected computed style — `clamp(1.9rem, 3.4vw, 2.75rem)`, `line-height: 1.1`, `letter-spacing: -0.03em` — superseding this plan's earlier hand-derived clamp `clamp(1.75rem, 2.6vw + 1.15rem, 3.25rem)`. The user supplied these exact values from the original prototype's devtools inspector as ground truth (relayed via a peer session), which takes priority over any independently-derived coefficients per this project's "design-archive port, match the reference exactly" convention.

Purpose: The original task asked for h2 to scale down visibly at tablet and phone widths instead of the old token's flat plateaus. Once the user supplied the actual reference computed style, that became the authoritative target instead of hand-tuning new coefficients. **Important caveat surfaced to the user (see must_haves):** this reference clamp does NOT produce a continuous 3-tier phone<tablet<desktop stepping — it produces a 2-tier curve where phone and tablet-portrait (≤~894px) render identically at the 30.4px floor, and only tablet-landscape+ (~894-1294px) scales up to the 44px desktop cap. This is accepted as-is because it is sourced directly from the design prototype's own computed style (ground truth), not because it fully satisfies the original literal "smaller for tablet, even smaller for phone" wording — flagging this discrepancy rather than silently reconciling it.
Output: `--text-dt-h2` token in `apps/web/app/premium-theme.css` matches the reference exactly (font-size clamp, line-height, new letter-spacing companion token). `section-heading.tsx`'s own `<h2>` also gets a scoped, low-risk `font-bold`→`font-extrabold` swap to match the reference's font-weight 800, isolated to that one component.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/artemdanko/Developer/denta-bot/.claude/CLAUDE.md
@/Users/artemdanko/Developer/denta-bot/apps/web/app/premium-theme.css
@/Users/artemdanko/Developer/denta-bot/apps/web/shared/components/section-heading.tsx

**Current token (line 37 of premium-theme.css):**
```
--text-dt-h2: clamp(2rem, 4.5vw + 0.75rem, 3.25rem); /* ... */
--text-dt-h2--line-height: 1.15;
```

**Reference design prototype's computed style (devtools-inspected by the user, ground truth for this task):**
- `font-size: clamp(1.9rem, 3.4vw, 2.75rem)`
- `line-height: 1.1`
- `letter-spacing: -0.03em`
- `font-weight: 800`
- `font-family: Manrope` — already matches `--font-dt-heading`, no change needed

**Confirmed computed values at representative widths (verified by running the exact clamp math, not assumed):**
| Viewport | Computed h2 size |
|---|---|
| 320px | 30.40px (1.9rem floor) |
| 390px (phone) | 30.40px (floor) |
| 768px (tablet portrait) | 30.40px (floor — same as phone) |
| 894px | 30.40px (floor engages up to here) |
| 1024px (tablet landscape) | 34.82px (actively scaling) |
| 1280px | 43.52px (actively scaling, just under cap) |
| 1294px+ | 44.00px (2.75rem cap engages here) |

Floor (1.9rem/30.4px) is flat for every width ≤894px — meaning phone (375-430px) and tablet-portrait (e.g. 768px) render at the exact same size. Only tablet-landscape and wider (~894-1294px) shows growth, reaching the 2.75rem/44px cap at ~1294px and staying flat above that. **This is a 2-tier shape (flat-small, then scale-up-to-flat-large), not the 3-tier "phone < tablet < desktop" shape the original task wording asked for** — implemented as-is per the user's explicit devtools-sourced correction, flagged rather than silently reconciled.

**Tailwind v4 confirmed (via docs) to support paired theme sub-properties for the `--text-*` namespace:** `--text-<name>--line-height`, `--text-<name>--letter-spacing`, and `--text-<name>--font-weight` are all valid companions to a `--text-<name>` font-size variable — Tailwind auto-applies all present sub-properties when a component uses the `text-<name>` utility class. The file already uses this exact pattern for `--text-dt-eyebrow--letter-spacing` (line 45) — follow that convention for the new `--text-dt-h2--letter-spacing`.

**Font-weight is deliberately NOT added as `--text-dt-h2--font-weight` to the shared token.** Every `text-dt-h2` consumer (`section-heading.tsx`, `stat.tsx`, `pricing-section.tsx`, `lead-section.tsx`, blog headings) already carries an explicit `font-bold` utility class in its own JSX. Adding a competing `--text-dt-h2--font-weight: 800` to the token would create an ambiguous same-specificity conflict between the token-driven weight and each consumer's explicit `font-bold` class. Instead, the weight bump to 800 is applied ONLY in `section-heading.tsx` — the literal "section heading" component this task is about — by swapping its own `font-bold` class to `font-extrabold`. `stat.tsx`/`pricing-section.tsx`/`lead-section.tsx`/blog headings keep `font-bold` (700) unchanged; this is Claude's discretion, scoped narrowly per the correction's explicit "don't let it block or complicate the plan" guidance.

**Confirmed via grep — every `text-dt-h2` consumer in apps/web (all inherit font-size/line-height/letter-spacing automatically via the token; only section-heading.tsx also gets the font-weight edit):**
- `apps/web/shared/components/section-heading.tsx:38-42` — the shared component every page-level section heading routes through; gets the font-weight edit
- `apps/web/shared/components/stat.tsx:18` — hero stat numbers (500+/15 000+/98%); kept on the same token by design (Claude's discretion, carried forward from the original plan) — no complaint suggests decoupling it, and stat numbers tracking the same fluid scale/tracking is desirable, not harmful
- `apps/web/modules/landing/pricing-section.tsx:119` — plan price display; same discretion as Stat
- `apps/web/modules/landing/lead-section.tsx:100` — lead-capture section heading (its own `<h2>`, not routed through the shared component)
- `apps/web/app/[locale]/blog/page.tsx:88` and `apps/web/app/[locale]/blog/error.tsx:23` — blog list/error headings

**Confirmed out of scope (do not touch):** `apps/web/modules/blog/post-body.tsx:29-34` has an inline `<h2>` with a hardcoded `text-3xl` class for in-article blog-post content headings — editorial article typography inside a `.prose` block, not a page-level "section heading." Leave as-is.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Match --text-dt-h2 token to the reference design's exact computed style</name>
  <files>apps/web/app/premium-theme.css</files>
  <action>
In `apps/web/app/premium-theme.css`, update the `--text-dt-h2` block (currently line 37-38):

Old:
```
--text-dt-h2: clamp(2rem, 4.5vw + 0.75rem, 3.25rem); /* ... */
--text-dt-h2--line-height: 1.15;
```

New:
- `--text-dt-h2: clamp(1.9rem, 3.4vw, 2.75rem);` — matches the reference design prototype's devtools-inspected computed font-size exactly.
- `--text-dt-h2--line-height: 1.1;` — matches the reference's computed line-height (was 1.15).
- Add a new line directly after: `--text-dt-h2--letter-spacing: -0.03em;` — follows the exact same companion-token pattern already used for `--text-dt-eyebrow--letter-spacing` a few lines below in this file; Tailwind v4's `--text-*` namespace applies this automatically wherever `text-dt-h2` is used, with no consumer file changes required.

Rewrite the trailing inline comment to state the token now matches the reference design prototype's own devtools-inspected computed style (font-size/line-height/letter-spacing), sourced 2026-08-20 (quick task 260820-1bb), superseding the earlier "bumped up... too small" narrative. Note in the comment that this reference clamp is flat (30.4px) for all viewport widths ≤~894px (phone and tablet-portrait render identically) and only scales up between ~894-1294px toward the 44px desktop cap — a deliberate, ground-truth-sourced departure from a continuous 3-tier phone/tablet/desktop stepping.

Do not touch `--text-dt-h1`, `--text-dt-h3`, or any other token in the file. Do not edit `stat.tsx`, `pricing-section.tsx`, `lead-section.tsx`, or the blog list/error pages — they inherit the font-size/line-height/letter-spacing change purely through the shared custom property.
  </action>
  <verify>
    <automated>cd /Users/artemdanko/Developer/denta-bot && grep -F "clamp(1.9rem, 3.4vw, 2.75rem)" apps/web/app/premium-theme.css && grep -F "1.1;" apps/web/app/premium-theme.css | grep -F "h2--line-height" apps/web/app/premium-theme.css > /dev/null; grep -F -- "--text-dt-h2--line-height: 1.1;" apps/web/app/premium-theme.css && grep -F -- "--text-dt-h2--letter-spacing: -0.03em;" apps/web/app/premium-theme.css && node -e "const clamp=(minRem,vwCoef,maxRem,w)=>{const px=vwCoef*w/100;return Math.max(minRem*16,Math.min(maxRem*16,px));};const widths=[320,390,768,894,1024,1280,1294,1440];const vals=widths.map(w=>clamp(1.9,3.4,2.75,w));widths.forEach((w,i)=>console.log(w+'px -> '+vals[i].toFixed(2)+'px'));const [p320,p390,p768,p894,p1024,p1280,p1294,p1440]=vals;const floorFlat=(0.05>=Math.abs(p320-30.4))&&(0.05>=Math.abs(p390-30.4))&&(0.05>=Math.abs(p768-30.4))&&(0.05>=Math.abs(p894-30.4));const scaleOk=(p1024>p894)&&(p1280>p1024);const capOk=(0.05>=Math.abs(p1294-44))&&(0.05>=Math.abs(p1440-44));if(!(floorFlat&&scaleOk&&capOk)){console.error('FAIL: expected flat 30.4px floor through 894px, scaling 894 to 1294, flat 44px cap from 1294');process.exit(1);}console.log('PASS floor='+p390.toFixed(2)+'px desktop='+p1440.toFixed(2)+'px');" && echo TASK1_OK</automated>
  </verify>
  <done>apps/web/app/premium-theme.css's --text-dt-h2 reads clamp(1.9rem, 3.4vw, 2.75rem), --text-dt-h2--line-height reads 1.1, --text-dt-h2--letter-spacing reads -0.03em (new line, following the --text-dt-eyebrow--letter-spacing pattern); computed values match the reference table exactly (flat 30.4px ≤894px, scaling to flat 44px ≥1294px); no other token or consumer file was modified.</done>
</task>

<task type="auto">
  <name>Task 2: Bump section-heading.tsx's h2 to font-extrabold (800), matching the reference weight</name>
  <files>apps/web/shared/components/section-heading.tsx</files>
  <action>
In `apps/web/shared/components/section-heading.tsx`, in the `<h2>` element's `cn(...)` className call (currently `'text-dt-h2 font-dt-heading font-bold text-balance'`), change `font-bold` to `font-extrabold` — matches the reference design's computed `font-weight: 800` (Tailwind's `font-extrabold` maps to weight 800; `font-bold` is 700).

Scope this change to `section-heading.tsx` ONLY — do not touch `stat.tsx`, `pricing-section.tsx`, `lead-section.tsx`, or the blog list/error `<h2>`/`<h1>` elements. Those were not part of the reference's "section heading" inspection and keeping them at `font-bold` avoids scope creep beyond what the user's correction explicitly asked for (secondary/low-risk nice-to-have, scoped to the one named component).
  </action>
  <verify>
    <automated>cd /Users/artemdanko/Developer/denta-bot && grep -F "font-extrabold" apps/web/shared/components/section-heading.tsx > /dev/null && ! grep -F "font-bold" apps/web/shared/components/section-heading.tsx > /dev/null && echo TASK2_OK</automated>
  </verify>
  <done>section-heading.tsx's h2 className includes font-extrabold (Tailwind weight 800) instead of font-bold (700); no other file (stat.tsx, pricing-section.tsx, lead-section.tsx, blog headings) was modified.</done>
</task>

</tasks>

<verification>
1. `grep -F "clamp(1.9rem, 3.4vw, 2.75rem)" apps/web/app/premium-theme.css` finds the new font-size value; `--text-dt-h2--line-height: 1.1;` and `--text-dt-h2--letter-spacing: -0.03em;` are both present.
2. The Task 1 automated `node -e` check confirms the exact reference shape: flat 30.4px through 894px (phone and tablet-portrait render identically — this is the flagged, intentional 2-tier shape, not a bug), scaling 894→1294px, flat 44px cap from 1294px onward.
3. `grep -F "font-extrabold" apps/web/shared/components/section-heading.tsx` finds the new weight class; no `font-bold` remains in that file.
4. `git diff --stat` shows exactly 2 files changed: `apps/web/app/premium-theme.css` and `apps/web/shared/components/section-heading.tsx`. No consumer file (`stat.tsx`, `pricing-section.tsx`, `lead-section.tsx`, blog list/error pages) is touched.
5. Optional (not blocking): `pnpm --filter web dev` and resize the browser through ~390px / ~768px / ~1024px / ~1440px on a page with a `SectionHeading` to visually confirm phone/tablet-portrait render the same size, tablet-landscape/desktop grow toward the new smaller 44px cap, and the heading now reads bolder/tighter-tracked.
</verification>

<success_criteria>
- `--text-dt-h2` in `apps/web/app/premium-theme.css` is `clamp(1.9rem, 3.4vw, 2.75rem)`, matching the reference design prototype's devtools-inspected computed style exactly.
- `--text-dt-h2--line-height` is `1.1` and a new `--text-dt-h2--letter-spacing: -0.03em` companion token exists, following the file's existing `--text-dt-eyebrow--letter-spacing` pattern.
- Computed size is flat at 30.4px for every viewport ≤~894px (phone and tablet-portrait identical — flagged shape difference from the original 3-tier ask, accepted because it is ground-truth-sourced), then scales up to a flat 44px cap at ≥~1294px.
- `section-heading.tsx`'s `<h2>` renders `font-extrabold` (800); all other `text-dt-h2` consumers (Stat, pricing price, lead-section, blog list/error headings) keep `font-bold` (700) unchanged, by explicit scoped discretion.
- Only `apps/web/app/premium-theme.css` and `apps/web/shared/components/section-heading.tsx` are touched; `apps/web/modules/blog/post-body.tsx`'s unrelated hardcoded-`text-3xl` in-article `<h2>` is left as-is (different semantic role, out of scope).
- **User-facing flag carried into the SUMMARY:** the reference clamp does not give phone a visibly smaller heading than tablet-portrait (both 30.4px) — only tablet-landscape and desktop differ from phone/tablet-portrait. If the user's original intent was strictly "phone smaller than tablet, tablet smaller than desktop" at every breakpoint, this reference value does not fully deliver that; it delivers exact parity with the design prototype instead. Surfaced for user confirmation, not silently resolved.
</success_criteria>

<output>
Create `.planning/quick/260820-1bb-make-all-section-heading-h2-font-sizes-r/260820-1bb-SUMMARY.md` when done.
</output>