---
task_id: 260809-v35
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/shared/lib/motion.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The idle bounce animation on the Home hero's 'Новий запис від Олени Коваль' notification card completes one loop in 1.6s (down from 3s), peaks at -8px vertical offset (up from -6px), and eases using the project's EASE_DT_EXPO_OUT cubic-bezier token instead of the generic 'easeInOut' string — so it reads as snappier and more consistent with revealVariants/hoverLift's motion language"
    - "The animation remains an infinite smooth loop (repeat: Infinity), and hero.tsx's existing prefers-reduced-motion gating (which sets animate/transition to undefined when reduced motion is preferred) continues to work unmodified since hero.tsx is not touched"
  artifacts:
    - apps/web/shared/lib/motion.ts
  key_links:
    - "hero.tsx already imports idleBounceAnimate/idleBounceTransition by reference and applies them to the notification card's motion.div via animate/transition props, gated by useReducedMotion() — tuning the constants in motion.ts propagates automatically with zero changes needed in hero.tsx"
---

<objective>
Tune the idle bounce animation on the Home hero's "Новий запис від Олени Коваль" notification card (`apps/web/modules/home/hero.tsx`) so it feels snappier and more alive, per direct user feedback that the current motion (added in quick task 260809-kcz) is too slow and too subtle to read as intentional. All changes are confined to the `idleBounceAnimate`/`idleBounceTransition` constants in `apps/web/shared/lib/motion.ts`.

Purpose: The current 3s-duration, 6px-amplitude, generic-`easeInOut` bounce reads as laggy/sluggish rather than a deliberate "new booking arrived" motion cue. Faster duration, larger amplitude, and the project's signature `EASE_DT_EXPO_OUT` easing (already used by `revealVariants` and `hoverLift`) make the card feel alive and visually consistent with the rest of the premium motion system.
Output: Updated `idleBounceAnimate`/`idleBounceTransition` exports in `apps/web/shared/lib/motion.ts`. `hero.tsx` and its `useReducedMotion()` gating are untouched — it already consumes both exports by reference.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/artemdanko/Developer/denta-bot/apps/web/shared/lib/motion.ts
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/home/hero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tune idleBounceAnimate/idleBounceTransition for snappier motion</name>
  <files>apps/web/shared/lib/motion.ts</files>
  <action>
    In `apps/web/shared/lib/motion.ts`, update only the `idleBounceAnimate` and `idleBounceTransition` exports (currently the last two exports in the file). Do NOT touch `EASE_DT_EXPO_OUT`, `revealVariants`, `revealContainerVariants`, or `hoverLift` — leave those four exports byte-identical.

    Change `idleBounceAnimate` from `{ y: [0, -6, 0] }` to `{ y: [0, -8, 0] }` — increases the bounce's peak vertical offset from 6px to 8px for a more noticeable motion.

    Change `idleBounceTransition` from `{ duration: 3, repeat: Infinity, ease: 'easeInOut' }` to `{ duration: 1.6, repeat: Infinity, ease: EASE_DT_EXPO_OUT }` — cuts the loop duration from 3s to 1.6s for a snappier feel, keeps `repeat: Infinity` unchanged so it stays a continuous loop, and swaps the generic `'easeInOut'` string easing for the file's own `EASE_DT_EXPO_OUT` token (the same `[0.16, 1, 0.3, 1]` cubic-bezier already used by `revealVariants` and `hoverLift`, imported/used by reference — not re-declared) for visual consistency with the rest of the motion system.

    Do not modify `apps/web/modules/home/hero.tsx` — it already imports and applies `idleBounceAnimate`/`idleBounceTransition` by reference in the notification card's `motion.div`, and its `useReducedMotion()` gating (setting `animate`/`transition` to `undefined` when reduced motion is preferred) requires zero changes to keep working.
  </action>
  <verify>
    <automated>
test "$(grep -c 'y: \[0, -8, 0\]' apps/web/shared/lib/motion.ts)" = "1" && \
test "$(grep -c 'y: \[0, -6, 0\]' apps/web/shared/lib/motion.ts)" = "0" && \
test "$(grep -c 'duration: 1.6' apps/web/shared/lib/motion.ts)" = "1" && \
test "$(grep -c 'duration: 3,' apps/web/shared/lib/motion.ts)" = "0" && \
test "$(grep -c "ease: 'easeInOut'" apps/web/shared/lib/motion.ts)" = "0" && \
test "$(grep -c 'ease: EASE_DT_EXPO_OUT' apps/web/shared/lib/motion.ts)" = "3" && \
test "$(grep -c 'repeat: Infinity' apps/web/shared/lib/motion.ts)" = "1" && \
test "$(grep -c 'export const EASE_DT_EXPO_OUT' apps/web/shared/lib/motion.ts)" = "1" && \
test "$(grep -c 'duration: 0.5, ease: EASE_DT_EXPO_OUT' apps/web/shared/lib/motion.ts)" = "1" && \
test "$(grep -c 'duration: 0.2, ease: EASE_DT_EXPO_OUT' apps/web/shared/lib/motion.ts)" = "1" && \
test "$(grep -c 'staggerChildren: 0.08' apps/web/shared/lib/motion.ts)" = "1" && \
git diff --name-only -- apps/web | grep -qx 'apps/web/shared/lib/motion.ts' && \
test "$(git diff --name-only -- apps/web | wc -l | tr -d ' ')" = "1" && \
pnpm --filter web lint && \
CT_OUT="$(pnpm --filter web check-types 2>&1)"; \
test -z "$(printf '%s' "$CT_OUT" | grep 'error TS' | grep -v 'button-group.tsx\|calendar.tsx\|sidebar.tsx')" && \
echo PASS
    </automated>
  </verify>
  <done>`apps/web/shared/lib/motion.ts`'s `idleBounceAnimate` is `{ y: [0, -8, 0] }` and `idleBounceTransition` is `{ duration: 1.6, repeat: Infinity, ease: EASE_DT_EXPO_OUT }`. `EASE_DT_EXPO_OUT`, `revealVariants`, `revealContainerVariants`, and `hoverLift` are unchanged. `apps/web/modules/home/hero.tsx` has zero diff. `pnpm --filter web lint` and `pnpm --filter web check-types` (filtered for pre-existing csstype errors) both pass.</done>
</task>

</tasks>

<verification>
- `grep`-based structural checks confirm the exact new `y`/`duration`/`ease` values are present, the old `-6`/`3`/`'easeInOut'` values are gone, `repeat: Infinity` and all other exports (`EASE_DT_EXPO_OUT`, `revealVariants`, `revealContainerVariants`, `hoverLift`) are unchanged.
- `git diff --name-only` confirms `apps/web/shared/lib/motion.ts` is the only file touched — `hero.tsx` has zero diff.
- `pnpm --filter web lint` and `pnpm --filter web check-types` (pre-existing `button-group.tsx`/`calendar.tsx`/`sidebar.tsx` csstype errors excluded) pass after the change.
</verification>

<success_criteria>
- Idle bounce loop duration is 1.6s, amplitude peaks at -8px, and uses `EASE_DT_EXPO_OUT` easing — matching `revealVariants`/`hoverLift`'s easing token.
- `repeat: Infinity` (continuous loop) is preserved.
- Only `apps/web/shared/lib/motion.ts` is modified; `hero.tsx`'s `useReducedMotion()` gating keeps working unmodified.
- `pnpm --filter web lint` and `pnpm --filter web check-types` pass (filtered for pre-existing csstype errors).
</success_criteria>

<output>
Create `.planning/quick/260809-v35-tune-idle-bounce-animation-on-hero-notif/260809-v35-SUMMARY.md` when done
</output>
