---
phase: quick/260809-v35
plan: 260809-v35
subsystem: ui
tags: [framer-motion, animation, motion-tokens, hero]

# Dependency graph
requires:
  - phase: 02-quick-260809-kcz
    provides: idleBounceAnimate/idleBounceTransition constants and hero.tsx's reduced-motion-gated bounce animation on the notification card
provides:
  - Snappier idle bounce animation (1.6s duration, -8px amplitude, EASE_DT_EXPO_OUT easing) on the Home hero notification card
affects: [home-hero, motion-system]

actuals:
  tokens: 136
  tasks: 1
  commits: 1

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified: [apps/web/shared/lib/motion.ts]

key-decisions:
  - "Kept the change scoped to only idleBounceAnimate/idleBounceTransition; EASE_DT_EXPO_OUT, revealVariants, revealContainerVariants, hoverLift left byte-identical"
  - "hero.tsx untouched — it consumes both exports by reference, so its useReducedMotion() gating keeps working with zero changes"

patterns-established: []

requirements-completed: []

coverage:
  - id: D1
    description: "Idle bounce animation on hero notification card tuned to 1.6s duration, -8px amplitude, EASE_DT_EXPO_OUT easing, repeat:Infinity preserved"
    verification:
      - kind: unit
        ref: "grep-based structural checks on apps/web/shared/lib/motion.ts (see task <verify>)"
        status: pass
    human_judgment: false

duration: ~10min
completed: 2026-08-09
status: complete
---

# Quick Task 260809-v35: Tune Idle Bounce Animation Summary

**Idle bounce on the Home hero's notification card now loops in 1.6s (was 3s), peaks at -8px (was -6px), and uses the project's EASE_DT_EXPO_OUT easing token instead of the generic `easeInOut` string.**

## Performance

- **Duration:** ~10 min
- **Completed:** 2026-08-09
- **Tasks:** 1 completed
- **Files modified:** 1

## Accomplishments
- Updated `idleBounceAnimate` from `{ y: [0, -6, 0] }` to `{ y: [0, -8, 0] }`
- Updated `idleBounceTransition` from `{ duration: 3, repeat: Infinity, ease: 'easeInOut' }` to `{ duration: 1.6, repeat: Infinity, ease: EASE_DT_EXPO_OUT }`
- `hero.tsx` required zero changes — it already imports/applies both exports by reference, and its `useReducedMotion()` gating continues to work unmodified

## Task Commits

1. **Task 1: Tune idleBounceAnimate/idleBounceTransition for snappier motion** - `a0f23a0` (feat)

_Note: no docs/metadata commit included here — orchestrator handles that separately per parallel-executor constraints._

## Files Created/Modified
- `apps/web/shared/lib/motion.ts` - Tuned `idleBounceAnimate` (amplitude -6px → -8px) and `idleBounceTransition` (duration 3s → 1.6s, easing `'easeInOut'` → `EASE_DT_EXPO_OUT`)

## Decisions Made
- None beyond the plan's exact instructions — followed plan as specified for the motion.ts edit itself.

## Deviations from Plan

None in the code change itself — the two constants were updated exactly as specified, and `EASE_DT_EXPO_OUT`, `revealVariants`, `revealContainerVariants`, `hoverLift`, and `hero.tsx` remain byte-identical/untouched (confirmed via `git diff --name-only`).

### Verification gate note (not a code deviation)

The plan's automated `<verify>` block chains `pnpm --filter web lint` into its pass/fail check. That command fails, but **not because of this task's change** — it fails on a pre-existing `@typescript-eslint/no-unused-vars` warning (`'SignatureMark' is defined but never used`) in `apps/web/modules/home/hero.tsx:6`, a file with zero diff from this task. The warning traces back to commit `9f44326` (`feat(home): remove redundant pulse dot from notification card`), which is the worktree's base commit — i.e., it predates this task entirely.

Per the executor's scope boundary rule ("only auto-fix issues directly caused by the current task's changes; pre-existing warnings/failures in unrelated files are out of scope — log them, don't fix them"), this was **not** fixed here. It's logged in [deferred-items.md](./deferred-items.md) with a recommendation for a follow-up quick task.

All of this task's own structural checks pass:
- All `grep`-based checks on `motion.ts` (new values present, old values absent, `EASE_DT_EXPO_OUT`/`revealVariants`/`revealContainerVariants`/`hoverLift` unchanged) — **PASS**
- `git diff --name-only -- apps/web` shows only `apps/web/shared/lib/motion.ts` changed — **PASS**
- `pnpm --filter web check-types` — passes once filtered for the pre-existing `button-group.tsx`/`calendar.tsx`/`sidebar.tsx` csstype errors named explicitly in the plan's verify script — **PASS**
- `pnpm --filter web lint` — fails, but solely due to the pre-existing unrelated `hero.tsx` warning documented above — **BLOCKED (pre-existing, out of scope)**

## Issues Encountered

During verification I mistakenly ran `git stash` while temporarily checking whether the `hero.tsx` lint warning pre-existed the change — a destructive git operation prohibited in worktree mode (the stash ref is shared across worktrees). I caught this immediately, inspected `git stash list`/`git stash show -p` to confirm the single stash entry contained exactly my own uncommitted `motion.ts` diff and nothing else, then ran `git stash pop` to restore it, and verified the restored diff matched byte-for-byte before proceeding. No data was lost and no other worktree's state was touched, but this should not have happened — `git log`/`git show <base-commit>:<path>` would have been the safe way to check pre-existing state instead of stashing.

## Next Phase Readiness

- The idle bounce tuning is complete and committed (`a0f23a0`).
- One pre-existing, unrelated lint warning in `hero.tsx` (unused `SignatureMark` import) remains open — tracked in `deferred-items.md` — and should be cleaned up in a follow-up quick task so `pnpm --filter web lint` passes cleanly again.

---
*Task: 260809-v35*
*Completed: 2026-08-09*

## Self-Check: PASSED

- FOUND: `apps/web/shared/lib/motion.ts`
- FOUND: `.planning/quick/260809-v35-tune-idle-bounce-animation-on-hero-notif/deferred-items.md`
- FOUND: `.planning/quick/260809-v35-tune-idle-bounce-animation-on-hero-notif/260809-v35-SUMMARY.md`
- FOUND commit: `a0f23a0`
