---
task_id: 260809-k1r
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/modules/home/features.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Each of the 8 feature cards in the Home page's Features section shows a one-line description below its title, using the exact verbatim Ukrainian copy from the design reference"
    - "The description text style matches problem.tsx's card-description pattern (`<p className=\"text-dt-graphite\">`)"
    - "The existing icon + h3 title markup, StaggerGrid/StaggerItem/PremiumCard wrapper structure, and dt- token usage are unchanged apart from the added description paragraphs"
  artifacts:
    - apps/web/modules/home/features.tsx
  key_links:
    - "Each StaggerItem > PremiumCard block gains a `<p className=\"text-dt-graphite\">` sibling directly after its existing `<h3>`, in the same order as the 8 cards already appear (Telegram бот, Розклад лікарів, Автонагадування, Аналітика, Відгуки, Адмін панель, Управління персоналом, Скасування запису) — mismatched order would pair the wrong description with the wrong card"
---

<objective>
Add a one-line description `<p>` below each of the 8 feature card titles in the Home page's Features section (`apps/web/modules/home/features.tsx`), using the exact verbatim Ukrainian copy transcribed from the client's reference screenshot.

Purpose: Each card currently only shows an icon + h3 title with no body text, which reads as superficial compared to the client's reference design — this quick task closes that gap using the same card-description text style already established in `problem.tsx`, with no structural changes to the existing card/grid components.
Output: `apps/web/modules/home/features.tsx` updated — each of the 8 `PremiumCard` blocks gains a `<p className="text-dt-graphite">` description directly under its `<h3>` title. No other file changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/home/features.tsx
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/home/problem.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add description paragraph to each of the 8 feature cards</name>
  <files>apps/web/modules/home/features.tsx</files>
  <action>
    Edit `apps/web/modules/home/features.tsx`. For each of the 8 `PremiumCard` blocks inside the `StaggerGrid`, add a `<p className="text-dt-graphite">` immediately after the existing `<h3 className="text-dt-h3 font-dt-heading font-semibold text-dt-navy">...</h3>` closing tag, inside the same `PremiumCard`, matching the description-paragraph style and placement `problem.tsx` uses (`<h3>...</h3>` followed by `<p className="text-dt-graphite">...</p>`, both direct children of `PremiumCard`). Do not add a wrapping `<div>` around icon+title+description — keep the existing flat structure inside `PremiumCard` (icon, then `h3`, then the new `p`).

    Use this exact verbatim copy, in card order (do not paraphrase or reorder):
    1. "Telegram бот" card -> "Пацієнти записуються через звичний месенджер"
    2. "Розклад лікарів" card -> "Налаштування робочих годин та вихідних"
    3. "Автонагадування" card -> "Автоматичні повідомлення перед прийомом"
    4. "Аналітика" card -> "Статистика записів та завантаженості"
    5. "Відгуки" card -> "Збір відгуків після відвідування"
    6. "Адмін панель" card -> "Зручне управління всіма записами"
    7. "Управління персоналом" card -> "Додавання лікарів та налаштування прав"
    8. "Скасування запису" card -> "Пацієнти можуть перенести або скасувати"

    Do not touch imports, icons, the `h3` title text, `StaggerGrid`/`StaggerItem` wrapper structure, the section heading, or any other file. Only add the 8 new `<p>` elements.
  </action>
  <verify>
    <automated>
test "$(grep -c 'Пацієнти записуються через звичний месенджер' apps/web/modules/home/features.tsx)" = "1" && \
test "$(grep -c 'Налаштування робочих годин та вихідних' apps/web/modules/home/features.tsx)" = "1" && \
test "$(grep -c 'Автоматичні повідомлення перед прийомом' apps/web/modules/home/features.tsx)" = "1" && \
test "$(grep -c 'Статистика записів та завантаженості' apps/web/modules/home/features.tsx)" = "1" && \
test "$(grep -c 'Збір відгуків після відвідування' apps/web/modules/home/features.tsx)" = "1" && \
test "$(grep -c 'Зручне управління всіма записами' apps/web/modules/home/features.tsx)" = "1" && \
test "$(grep -c 'Додавання лікарів та налаштування прав' apps/web/modules/home/features.tsx)" = "1" && \
test "$(grep -c 'Пацієнти можуть перенести або скасувати' apps/web/modules/home/features.tsx)" = "1" && \
test "$(grep -c 'text-dt-graphite' apps/web/modules/home/features.tsx)" -ge "8" && \
test "$(grep -c '<h3' apps/web/modules/home/features.tsx)" = "8" && \
pnpm --filter web lint && \
CT_OUT="$(pnpm --filter web check-types 2>&1)"; \
test -z "$(printf '%s' "$CT_OUT" | grep 'error TS' | grep -v 'button-group.tsx\|calendar.tsx\|sidebar.tsx')" && \
echo PASS
    </automated>
  </verify>
  <done>Each of the 8 `PremiumCard` blocks in `apps/web/modules/home/features.tsx` renders a `<p className="text-dt-graphite">` description directly below its `<h3>` title, using the exact verbatim copy specified, in the correct card order. `pnpm --filter web lint` passes with zero warnings; `pnpm --filter web check-types` produces no new TS errors (pre-existing button-group.tsx/calendar.tsx/sidebar.tsx csstype errors excluded).</done>
</task>

</tasks>

<verification>
- `grep` confirms all 8 exact verbatim descriptions are present exactly once each, at least 8 `text-dt-graphite` occurrences, and the 8 `h3` titles are unchanged in count.
- `pnpm --filter web lint` and `pnpm --filter web check-types` (filtered for pre-existing csstype errors) both pass.
- Non-blocking note: a human visual spot-check of http://localhost:3000/ (Features section) is worthwhile to confirm each description renders under the correct card and row heights still align, but per this task's constraints it does not block completion — the acceptance criteria above are concrete, objectively-verifiable text/structure assertions.
</verification>

<success_criteria>
- All 8 feature cards show a one-line description below the title, using the exact verbatim copy specified, in the correct order.
- Description text style matches `problem.tsx`'s pattern (`<p className="text-dt-graphite">`).
- Existing icon + h3 markup, `StaggerGrid`/`StaggerItem`/`PremiumCard` structure, and `dt-` token usage are otherwise unchanged.
- No files other than `apps/web/modules/home/features.tsx` are modified.
</success_criteria>

<output>
Create `.planning/quick/260809-k1r-add-a-one-line-description-under-each-of/260809-k1r-SUMMARY.md` when done
</output>
