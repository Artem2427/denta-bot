---
type: quick
slug: 260820-1r2-apps-web-modules-landing-lead-section-ts
autonomous: true
files_modified:
  - apps/web/modules/landing/lead-section.tsx
must_haves:
  truths:
    - "lead-section.tsx's eyebrow/title/description render via the shared SectionHeading component (tone=\"navy\") instead of duplicated inline <span>/<h2>/<p> markup, using the exact same t('eyebrow')/t('title')/t('description') translation calls as before — no i18n message file changes"
    - "The heading now renders font-extrabold (SectionHeading's weight) instead of the old font-bold, picking up the recent h2 weight fix (quick task 260820-1bb) automatically instead of needing a separate edit"
    - "Visual spacing to the guarantees <ul> below is unchanged (32px gap via the ul's own mt-8) because SectionHeading's default mb-dt-48 wrapper margin is overridden with className=\"mb-0\", following the exact precedent already used in reviews.tsx"
  artifacts:
    - apps/web/modules/landing/lead-section.tsx
  key_links:
    - "SectionHeading import wired into lead-section.tsx's import block, alphabetically between the existing Section and getClientApiUrl imports"
    - "SectionHeading's eyebrow/title/description props are sourced from the same t('eyebrow')/t('title')/t('description') calls the removed inline markup used — no i18n namespace or key changes required"
---

<objective>
Replace the inline eyebrow+h2+description block in `apps/web/modules/landing/lead-section.tsx` (lines 96-105) with the shared `SectionHeading` component from `apps/web/shared/components/section-heading.tsx`, which already supports `eyebrow`/`title`/`description`/`tone="navy"` matching this block's styling exactly.

Purpose: Deduplicate markup that's already centralized in a shared component, and pick up the recent `font-extrabold` h2 weight fix (quick task 260820-1bb) automatically instead of this file staying on the old `font-bold`.
Output: `lead-section.tsx` uses `<SectionHeading eyebrow={t('eyebrow')} title={t('title')} description={t('description')} tone="navy" className="mb-0" />` in place of the old inline block. No other file is touched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/artemdanko/Developer/denta-bot/.claude/CLAUDE.md
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/landing/lead-section.tsx
@/Users/artemdanko/Developer/denta-bot/apps/web/shared/components/section-heading.tsx

**Current block being replaced (lead-section.tsx lines 96-105, first column of a `grid lg:grid-cols-2`, immediately followed by a sibling `<ul className="mt-8 space-y-4">` of guarantee bullets at line 106 — that `<ul>` is NOT part of this replacement and must be left exactly as-is):**
```tsx
<div>
  <span className="font-dt-mono text-dt-eyebrow text-dt-warm-white/80 uppercase">
    {t('eyebrow')}
  </span>
  <h2 className="mt-2 text-dt-h2 font-dt-heading font-bold text-balance text-dt-warm-white">
    {t('title')}
  </h2>
  <p className="mt-4 text-dt-body text-pretty text-dt-warm-white/80">
    {t('description')}
  </p>
  <ul className="mt-8 space-y-4">...</ul>
</div>
```

**Current import block (lines 11-17), grouped/sorted alphabetically by path — the new `SectionHeading` import belongs between `Section` and `getClientApiUrl`:**
```tsx
import { Container } from '@/shared/components/container';
import { PremiumButton } from '@/shared/components/premium-button';
import { PremiumCard } from '@/shared/components/premium-card';
import { PremiumInput } from '@/shared/components/premium-input';
import { PremiumTextarea } from '@/shared/components/premium-textarea';
import { Section } from '@/shared/components/section';
import { getClientApiUrl } from '@/shared/lib/api-url';
```

**Target `SectionHeading` component (confirmed current source, `apps/web/shared/components/section-heading.tsx`):** accepts `eyebrow?: string`, `title: React.ReactNode`, `description?: React.ReactNode`, `tone?: 'warm-white' | 'navy'` (default `'warm-white'`), `className?: string`. Wraps everything in `<div data-slot="section-heading" className={cn('mb-dt-48', className)}>`. With `tone="navy"`: eyebrow renders via `<Eyebrow tone="on-navy" className="mb-2 block">` (confirmed identical classes to the removed inline `<span>`: `text-dt-warm-white/80 uppercase font-dt-mono text-dt-eyebrow`); the `<h2>` renders `text-dt-h2 font-dt-heading font-extrabold text-balance text-dt-warm-white` (note: `font-extrabold`, not the old `font-bold` — an intentional, wanted upgrade); the `<p>` renders `mt-4 max-w-2xl text-dt-body text-pretty text-dt-warm-white/80`.

**Only material difference requiring action:** `SectionHeading`'s wrapper defaults to `mb-dt-48` (48px). The current inline block has no such margin — spacing to the guarantees `<ul>` below comes entirely from the `<ul>`'s own `mt-8` (32px). Passing `className="mb-0"` to `SectionHeading` removes the wrapper's default margin so the `<ul>`'s existing `mt-8` remains the only spacing, preserving the current 32px gap exactly. This exact `className="mb-0"` override pattern is already used at `apps/web/modules/landing/reviews.tsx`'s `<SectionHeading title={t('title')} className="mb-0" />` call — do not invent a different override.

**`max-w-2xl` on the description `<p>` (672px) is a no-op here:** this block sits inside a `grid lg:grid-cols-2` column, which is already narrower than 672px at every container width this site uses, so the added `max-w-2xl` will not visibly change wrapping.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline eyebrow+h2+description block with SectionHeading in lead-section.tsx</name>
  <files>apps/web/modules/landing/lead-section.tsx</files>
  <action>
In `apps/web/modules/landing/lead-section.tsx`:

1. Add `import { SectionHeading } from '@/shared/components/section-heading';` to the import block, placed immediately after the existing `import { Section } from '@/shared/components/section';` line and before `import { getClientApiUrl } from '@/shared/lib/api-url';` — preserves the file's existing alphabetical-by-path import ordering.

2. Replace the inline block (the `<span>` eyebrow + `<h2>` title + `<p>` description, currently lines 97-105, all inside the `<div>` that opens at line 96) with a single `SectionHeading` call:
   - `eyebrow={t('eyebrow')}`
   - `title={t('title')}`
   - `description={t('description')}`
   - `tone="navy"`
   - `className="mb-0"` — required to preserve the existing 32px gap to the sibling guarantees `<ul>` below (see context: without this override, `SectionHeading`'s default `mb-dt-48` would stack with the `<ul>`'s `mt-8`, producing an unwanted 80px gap). This exact override matches the precedent in `reviews.tsx`.

   The outer `<div>` wrapping this block and the sibling `<ul className="mt-8 space-y-4">...guarantees...</ul>` stay exactly as they are — only the three inline elements they currently wrap are replaced by the single `SectionHeading` element.

3. Do not modify the guarantees `<ul>`, the `PremiumCard` form column, the `onSubmit` handler, the zod schema, or any other part of the file. Do not modify `reviews.tsx`, `section-heading.tsx`, `eyebrow.tsx`, or any i18n message file — the same `lead` namespace keys (`eyebrow`, `title`, `description`) are reused unchanged, just passed as props instead of interpolated inline.
  </action>
  <verify>
    <automated>cd /Users/artemdanko/Developer/denta-bot && grep -F "import { SectionHeading } from '@/shared/components/section-heading';" apps/web/modules/landing/lead-section.tsx > /dev/null && grep -F "<SectionHeading" apps/web/modules/landing/lead-section.tsx > /dev/null && grep -F 'tone="navy"' apps/web/modules/landing/lead-section.tsx > /dev/null && grep -F 'className="mb-0"' apps/web/modules/landing/lead-section.tsx > /dev/null && ! grep -F '<span className="font-dt-mono text-dt-eyebrow text-dt-warm-white/80 uppercase">' apps/web/modules/landing/lead-section.tsx > /dev/null && grep -F '<ul className="mt-8 space-y-4">' apps/web/modules/landing/lead-section.tsx > /dev/null && pnpm --filter web check-types && echo TASK1_OK</automated>
  </verify>
  <done>lead-section.tsx imports SectionHeading from '@/shared/components/section-heading' (placed between the Section and getClientApiUrl imports); the old inline `<span>`/`<h2>`/`<p>` block (lines 97-105) is replaced by a single `<SectionHeading eyebrow={t('eyebrow')} title={t('title')} description={t('description')} tone="navy" className="mb-0" />`; the sibling guarantees `<ul className="mt-8 space-y-4">` and everything else in the file (form column, submit handler, zod schema) is unchanged; `pnpm --filter web check-types` passes with no errors.</done>
</task>

</tasks>

<verification>
1. `grep -F "import { SectionHeading }" apps/web/modules/landing/lead-section.tsx` finds the new import, correctly ordered between `Section` and `getClientApiUrl`.
2. `grep -F "<SectionHeading" apps/web/modules/landing/lead-section.tsx` finds the new component usage with `tone="navy"` and `className="mb-0"`.
3. The old inline `<span className="font-dt-mono text-dt-eyebrow text-dt-warm-white/80 uppercase">` markup no longer appears in the file — confirms the duplicated block was removed, not just supplemented.
4. `git diff --stat` shows exactly one file changed: `apps/web/modules/landing/lead-section.tsx`. No other file (`section-heading.tsx`, `reviews.tsx`, `eyebrow.tsx`, i18n message files) is touched.
5. `pnpm --filter web check-types` passes — confirms the `SectionHeading` props (`eyebrow`, `title`, `description`, `tone`, `className`) type-check against the component's signature.
6. Optional (not blocking): `pnpm --filter web dev` and visually confirm the Lead section (`#lead`, navy background) still shows eyebrow/heading/description/guarantees with the same 32px gap to the guarantees list, and the heading now reads at the bolder `font-extrabold` weight.
</verification>

<success_criteria>
- `apps/web/modules/landing/lead-section.tsx` renders its eyebrow/title/description via the shared `SectionHeading` component instead of duplicated inline markup, using the same `t('eyebrow')`/`t('title')`/`t('description')` translation keys as before.
- `tone="navy"` and `className="mb-0"` are both passed, preserving the exact navy-tone styling and the existing 32px gap to the guarantees list (no `mb-dt-48` + `mt-8` stacking).
- The heading now inherits `font-extrabold` from `SectionHeading` (an intentional upgrade from the prior `font-bold`), consistent with the recent site-wide h2 weight fix.
- Only `apps/web/modules/landing/lead-section.tsx` is modified. No changes to `section-heading.tsx`, `reviews.tsx`, `eyebrow.tsx`, or any i18n message file.
- `pnpm --filter web check-types` passes.
</success_criteria>

<output>
Create `.planning/quick/260820-1r2-apps-web-modules-landing-lead-section-ts/260820-1r2-SUMMARY.md` when done.
</output>