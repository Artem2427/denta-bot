---
type: quick
slug: 260820-if0-standardize-phosphor-icons-react-imports
autonomous: true
files_modified:
  - apps/web/app/not-found.tsx
  - apps/web/app/[locale]/blog/page.tsx
  - apps/web/app/[locale]/blog/[slug]/page.tsx
  - apps/web/shared/components/locale-switcher.tsx
  - apps/web/shared/components/header.tsx
  - apps/web/shared/components/premium-accordion.tsx
  - apps/web/shared/components/theme-toggle.tsx
  - apps/web/shared/components/premium-dialog.tsx
  - apps/web/modules/demo/demo-tabs.tsx
  - apps/web/modules/demo/bot-tab.tsx
  - apps/web/modules/landing/pricing-section.tsx
  - apps/web/modules/landing/features.tsx
  - apps/web/modules/landing/lead-section.tsx
  - apps/web/modules/blog/blog-filters.tsx
must_haves:
  truths:
    - "Every @phosphor-icons/react and @phosphor-icons/react/ssr import across the 14 target files uses the Icon-suffixed component name (e.g. Check imported as CheckIcon, CheckCircle imported as CheckCircleIcon) instead of the bare name — matching the convention already correctly used in hero.tsx (BellIcon), reviews.tsx (CaretLeftIcon/CaretRightIcon/StarIcon), hero-notification-badge.tsx (CheckIcon), and problem-solution.tsx (CheckIcon)."
    - "Every JSX usage site of a renamed icon, and every non-JSX identifier reference (features.tsx's FEATURE_ICONS array), is updated to the new Icon-suffixed identifier in the same file as its import — no file contains a stale bare-name reference, so every file still compiles."
    - "Renames are true renames to the library's real Icon-suffixed named export, not local import aliases (no `import { Check as CheckIcon }` pattern anywhere)."
    - "Each file's original @phosphor-icons/react import subpath (either the bare package or its /ssr subpath) is preserved exactly — only the imported identifier name changes, never the subpath."
    - "apps/web/shared/components/header.tsx's pre-existing uncommitted nav-link-reorder edit (unstaged in the working tree before this plan started) remains present and unmodified — only the icon import line and the isMobileMenuOpen ternary's two icon JSX sites are touched in that file."
    - "apps/web/modules/landing/features.tsx's FEATURE_ICONS array element order is unchanged (only the 8 identifiers are renamed in place, not reordered or resorted) since array order encodes the per-feature icon-to-content mapping consumed via items.map/FEATURE_ICONS[i]."
    - "apps/web/modules/landing/hero.tsx, apps/web/modules/landing/reviews.tsx, apps/web/modules/landing/hero-notification-badge.tsx, and apps/web/modules/landing/problem-solution.tsx are not edited by this plan — no task in this plan lists any of them in its `<files>`, and each still contains its already-correct Icon-suffixed identifiers (BellIcon; CaretLeftIcon/CaretRightIcon/StarIcon; CheckIcon; CheckIcon respectively) with no stray double-suffixed `IconIcon` artifact, confirming they were not touched by a rename mistakenly applied to them."
    - "pnpm --filter web check-types and pnpm --filter web lint show no new errors or warnings referencing any of the 14 touched files (any pre-existing, unrelated repo-wide warnings are untouched and acceptable)."
    - "All 14 touched files pass prettier --check against this project's .prettierrc."
  artifacts:
    - apps/web/app/not-found.tsx
    - apps/web/app/[locale]/blog/page.tsx
    - apps/web/app/[locale]/blog/[slug]/page.tsx
    - apps/web/shared/components/locale-switcher.tsx
    - apps/web/shared/components/header.tsx
    - apps/web/shared/components/premium-accordion.tsx
    - apps/web/shared/components/theme-toggle.tsx
    - apps/web/shared/components/premium-dialog.tsx
    - apps/web/modules/demo/demo-tabs.tsx
    - apps/web/modules/demo/bot-tab.tsx
    - apps/web/modules/landing/pricing-section.tsx
    - apps/web/modules/landing/features.tsx
    - apps/web/modules/landing/lead-section.tsx
    - apps/web/modules/blog/blog-filters.tsx
  key_links:
    - "Each renamed import specifier and its corresponding JSX/array usage site(s) within the same file stay wired together — a partial rename (import renamed but a JSX tag or array entry left stale, or vice versa) breaks that file's compilation, which is exactly what the per-task and final sitewide grep-plus-check-types gates are designed to catch."
    - "features.tsx's import block and its FEATURE_ICONS array both reference the same 8 renamed identifiers, in the same unchanged order — a mismatch between the two (wrong identifier, or reordered array) breaks the per-feature icon-to-content mapping silently (wrong icon rendered next to a feature's text) rather than failing to compile, so the plan's verify checks for presence of all 8 new identifiers by name rather than relying on check-types alone."
---

<objective>
Rename every bare (non-Icon-suffixed) `@phosphor-icons/react` / `@phosphor-icons/react/ssr` icon import across the remaining 14 `apps/web` files to its `Icon`-suffixed equivalent (e.g. `Check` → `CheckIcon`, `CheckCircle` → `CheckCircleIcon`), updating both the import specifier and every JSX/array usage site in each file, per explicit user instruction to standardize on the naming convention already correctly used in `hero.tsx`, `reviews.tsx`, `hero-notification-badge.tsx`, and `problem-solution.tsx`.

This is a mechanical, drop-in identifier rename — `@phosphor-icons/react`/`@phosphor-icons/react/ssr` export both the bare name and the `Icon`-suffixed name as equivalent, valid named exports for every icon used in this codebase (confirmed via a dynamic ESM import test in this repo's `apps/web` environment). No behavior, styling, or props change.

Purpose: Sitewide naming consistency for icon imports across apps/web, eliminating the current split between two naming conventions for the same library.
Output: 14 files with every `@phosphor-icons/react`/`@phosphor-icons/react/ssr` icon import and usage using the `Icon`-suffixed name; zero remaining bare-name usages anywhere in apps/web; the 4 already-correct files untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/artemdanko/Developer/denta-bot/.claude/CLAUDE.md

**Exact per-file rename map — old bare identifier → new Icon-suffixed identifier. The import subpath (`@phosphor-icons/react` vs `@phosphor-icons/react/ssr`) is exactly as it already is in each file today — copy it unchanged, only the imported identifier(s) change:**

- `apps/web/app/not-found.tsx` (`@phosphor-icons/react/ssr`): `House` → `HouseIcon` (1 import site, 1 JSX site)
- `apps/web/app/[locale]/blog/page.tsx` (`@phosphor-icons/react/ssr`): `Clock` → `ClockIcon` (1 import site, 1 JSX site)
- `apps/web/app/[locale]/blog/[slug]/page.tsx` (`@phosphor-icons/react/ssr`): `ArrowLeft` → `ArrowLeftIcon`, `Clock` → `ClockIcon`, `ShareNetwork` → `ShareNetworkIcon` (1 import site with all 3, 1 JSX site each)
- `apps/web/shared/components/locale-switcher.tsx` (`@phosphor-icons/react/ssr`): `CaretDown` → `CaretDownIcon`, `Check` → `CheckIcon` (1 import site with both, 1 JSX site each)
- `apps/web/shared/components/header.tsx` (`@phosphor-icons/react/ssr`): `List` → `ListIcon`, `X` → `XIcon` (1 import site with both, 1 JSX site each, inside the mobile-menu-button ternary) — **this file has a pre-existing, uncommitted, unstaged local edit (a nav-link reorder) already present in its current on-disk content — read the file fresh before editing and touch ONLY the import line and the two icon JSX sites; leave the navLinks array and everything else exactly as currently on disk.**
- `apps/web/shared/components/premium-accordion.tsx` (`@phosphor-icons/react/ssr`): `CaretDown` → `CaretDownIcon` (1 import site, 1 JSX site)
- `apps/web/shared/components/theme-toggle.tsx` (`@phosphor-icons/react/ssr`): `Moon` → `MoonIcon`, `Sun` → `SunIcon` (1 import site with both; `Sun` appears at 2 JSX sites — the `!mounted` fallback and the `theme === 'dark'` branch of the ternary; `Moon` appears at 1 JSX site — the `else` branch of that same ternary)
- `apps/web/shared/components/premium-dialog.tsx` (`@phosphor-icons/react` — no `/ssr` subpath; keep it exactly as-is): `X` → `XIcon` (1 import site, 1 JSX site, the dialog close button)
- `apps/web/modules/demo/demo-tabs.tsx` (`@phosphor-icons/react/ssr`): `GearSix` → `GearSixIcon`, `Robot` → `RobotIcon` (1 import site with both, 1 JSX site each, inside the two tab-trigger buttons)
- `apps/web/modules/demo/bot-tab.tsx` (`@phosphor-icons/react` — no `/ssr` subpath; keep it exactly as-is): `Checks` → `ChecksIcon`, `Robot` → `RobotIcon` (1 import site with both, 1 JSX site each — `Robot` in the chat-header avatar, `Checks` in the read-receipt indicator inside a `message.type === 'user'` conditional)
- `apps/web/modules/landing/pricing-section.tsx` (`@phosphor-icons/react/ssr`): `Check` → `CheckIcon` (1 import site, 1 JSX site inside the `plan.features.map` list)
- `apps/web/modules/landing/features.tsx` (`@phosphor-icons/react/ssr`): `Bell` → `BellIcon`, `Calendar` → `CalendarIcon`, `ChartBar` → `ChartBarIcon`, `ChatCircleText` → `ChatCircleTextIcon`, `Check` → `CheckIcon`, `Gear` → `GearIcon`, `Star` → `StarIcon`, `Users` → `UsersIcon` (1 multi-line import block with all 8 — see FEATURE_ICONS note below for the non-JSX usage site)
- `apps/web/modules/landing/lead-section.tsx` (`@phosphor-icons/react/ssr`): `CheckCircle` → `CheckCircleIcon` (1 import site, 2 JSX sites — the guarantees-list checkmark inside `guarantees.map`, and the post-submit thank-you icon in the `isSubmitted` branch)
- `apps/web/modules/blog/blog-filters.tsx` (`@phosphor-icons/react/ssr`): `Clock` → `ClockIcon`, `MagnifyingGlass` → `MagnifyingGlassIcon` (1 import site with both, 1 JSX site each)

**FEATURE_ICONS array (features.tsx only, non-JSX usage site):** this file defines `const FEATURE_ICONS = [ChatCircleText, Calendar, Bell, ChartBar, Star, Gear, Users, Check]` — a bare array of component references consumed via `FEATURE_ICONS[i]` and then rendered as `<Icon .../>`. Rename each of the 8 array elements in place to its Icon-suffixed identifier, preserving the EXACT SAME element order (`ChatCircleTextIcon, CalendarIcon, BellIcon, ChartBarIcon, StarIcon, GearIcon, UsersIcon, CheckIcon`) — this order is not alphabetical and encodes the 1:1 mapping to each feature's position in the translated `items` array; do not resort it to match the (separately, alphabetically sorted) import block.

**General rules for every file above:**
- Update BOTH the import statement's named-import specifier(s) AND every JSX usage (`<IconName ... />`) or other identifier reference (the FEATURE_ICONS array) of that identifier within the same file — a rename that only touches the import line and leaves a stale usage site will fail to compile.
- Do NOT add an import alias (`import { Check as CheckIcon }`). Rename to the library's real, distinct `CheckIcon` named export — every icon above genuinely exports both the bare and Icon-suffixed name.
- Do NOT change the import subpath (`@phosphor-icons/react` vs `@phosphor-icons/react/ssr`) — copy each file's existing subpath unchanged.
- Do NOT touch any `weight`, `className`, or other prop on any icon JSX element — only the tag name changes.
- After editing each file, run `pnpm exec prettier --write <file>` from `apps/web/` so this project's `.prettierrc` (including its `@trivago/prettier-plugin-sort-imports` plugin) formats the resulting import statement correctly.

**Files intentionally OUT of scope — do not touch:** `apps/web/modules/landing/hero.tsx`, `apps/web/modules/landing/reviews.tsx`, `apps/web/modules/landing/hero-notification-badge.tsx`, `apps/web/modules/landing/problem-solution.tsx` (already use the Icon-suffixed convention correctly). `apps/web/modules/landing/pricing-section.tsx` currently has an unrelated, pre-existing uncommitted `console.log(plans, 'plans')` debug line — leave it exactly as-is; this plan only touches its one `Check` → `CheckIcon` site.
</context>

<!-- planner-discipline-allow: House -->
<!-- planner-discipline-allow: Clock -->
<!-- planner-discipline-allow: ArrowLeft -->
<!-- planner-discipline-allow: ShareNetwork -->
<!-- planner-discipline-allow: CaretDown -->
<!-- planner-discipline-allow: Check -->
<!-- planner-discipline-allow: List -->
<!-- planner-discipline-allow: X -->
<!-- planner-discipline-allow: Moon -->
<!-- planner-discipline-allow: Sun -->
<!-- planner-discipline-allow: GearSix -->
<!-- planner-discipline-allow: Robot -->
<!-- planner-discipline-allow: Checks -->
<!-- planner-discipline-allow: Bell -->
<!-- planner-discipline-allow: Calendar -->
<!-- planner-discipline-allow: ChartBar -->
<!-- planner-discipline-allow: ChatCircleText -->
<!-- planner-discipline-allow: Gear -->
<!-- planner-discipline-allow: Star -->
<!-- planner-discipline-allow: Users -->
<!-- planner-discipline-allow: CheckCircle -->
<!-- planner-discipline-allow: MagnifyingGlass -->

<tasks>

<task type="auto">
  <name>Task 1: Rename icon imports in shared chrome components</name>
  <files>apps/web/shared/components/locale-switcher.tsx, apps/web/shared/components/header.tsx, apps/web/shared/components/premium-accordion.tsx, apps/web/shared/components/theme-toggle.tsx, apps/web/shared/components/premium-dialog.tsx</files>
  <precondition>apps/web/shared/components/header.tsx has a pre-existing, uncommitted, unstaged local edit (a nav-link reorder) already on disk — read the file fresh at execution time and preserve that edit exactly, touching only the icon import line and the two icon JSX sites.</precondition>
  <action>
Apply the per-file rename map from `<context>` to these five files (7 renamed identifiers total, each with one import site and one-or-more JSX sites):

`locale-switcher.tsx`: rename `CaretDown`/`Check` to `CaretDownIcon`/`CheckIcon` in the import line and at their one JSX site each (the trigger button's caret, and the active-locale checkmark inside the dropdown item).

`header.tsx`: read the file fresh first (see precondition). Rename `List`/`X` to `ListIcon`/`XIcon` in the import line and at their one JSX site each, inside the mobile-menu-button's `isMobileMenuOpen` ternary. Do not touch `navLinks`, the desktop/mobile nav rendering, or any other part of the file.

`premium-accordion.tsx`: rename `CaretDown` to `CaretDownIcon` in the import line and at its one JSX site (the accordion trigger's chevron).

`theme-toggle.tsx`: rename `Moon`/`Sun` to `MoonIcon`/`SunIcon` in the import line. `Sun` has two JSX sites (the `!mounted` fallback, and the `theme === 'dark'` branch); `Moon` has one JSX site (the ternary's `else` branch). Rename all three JSX sites.

`premium-dialog.tsx`: rename `X` to `XIcon` in the import line (subpath stays `@phosphor-icons/react`, no `/ssr` — do not add it) and at its one JSX site (the dialog's close button).

Run `pnpm exec prettier --write` on all five files from `apps/web/` after editing.
  </action>
  <verify>
    <automated>cd /Users/artemdanko/Developer/denta-bot/apps/web && ! grep -nE '\b(CaretDown|Check|List|X|Moon|Sun)\b' shared/components/locale-switcher.tsx shared/components/header.tsx shared/components/premium-accordion.tsx shared/components/theme-toggle.tsx shared/components/premium-dialog.tsx && grep -q 'CaretDownIcon' shared/components/locale-switcher.tsx && grep -q 'CheckIcon' shared/components/locale-switcher.tsx && grep -q 'ListIcon' shared/components/header.tsx && grep -q 'XIcon' shared/components/header.tsx && grep -q 'CaretDownIcon' shared/components/premium-accordion.tsx && grep -q 'MoonIcon' shared/components/theme-toggle.tsx && grep -q 'SunIcon' shared/components/theme-toggle.tsx && grep -q 'XIcon' shared/components/premium-dialog.tsx && pnpm exec prettier --check shared/components/locale-switcher.tsx shared/components/header.tsx shared/components/premium-accordion.tsx shared/components/theme-toggle.tsx shared/components/premium-dialog.tsx && echo TASK1_OK</automated>
  </verify>
  <done>All five shared component files contain zero bare (non-Icon-suffixed) occurrences of CaretDown/Check/List/X/Moon/Sun as standalone identifiers; each renamed identifier's Icon-suffixed form is present in its file; header.tsx's pre-existing nav-link-reorder edit is preserved; all five files pass prettier --check.</done>
</task>

<task type="auto">
  <name>Task 2: Rename icon imports in app routes and demo modules</name>
  <files>apps/web/app/not-found.tsx, apps/web/app/[locale]/blog/page.tsx, apps/web/app/[locale]/blog/[slug]/page.tsx, apps/web/modules/demo/demo-tabs.tsx, apps/web/modules/demo/bot-tab.tsx</files>
  <action>
Apply the per-file rename map from `<context>` to these five files (7 renamed identifiers total):

`not-found.tsx`: rename `House` to `HouseIcon` in the import line and at its one JSX site (the "На головну" link icon).

`app/[locale]/blog/page.tsx`: rename `Clock` to `ClockIcon` in the import line and at its one JSX site (the featured-post read-time icon).

`app/[locale]/blog/[slug]/page.tsx`: rename `ArrowLeft`/`Clock`/`ShareNetwork` to `ArrowLeftIcon`/`ClockIcon`/`ShareNetworkIcon` in the single multi-name import line and at their one JSX site each (back-link, read-time, share button).

`demo-tabs.tsx`: rename `GearSix`/`Robot` to `GearSixIcon`/`RobotIcon` in the import line and at their one JSX site each (the two tab-trigger buttons).

`bot-tab.tsx`: rename `Checks`/`Robot` to `ChecksIcon`/`RobotIcon` in the import line (subpath stays `@phosphor-icons/react`, no `/ssr` — do not add it) and at their one JSX site each — `Robot` in the chat-header avatar, `Checks` inside the `message.type === 'user'` conditional in the read-receipt indicator.

Run `pnpm exec prettier --write` on all five files from `apps/web/` after editing.
  </action>
  <verify>
    <automated>cd /Users/artemdanko/Developer/denta-bot/apps/web && ! grep -nE '\b(House|Clock|ArrowLeft|ShareNetwork|GearSix|Robot|Checks)\b' app/not-found.tsx "app/[locale]/blog/page.tsx" "app/[locale]/blog/[slug]/page.tsx" modules/demo/demo-tabs.tsx modules/demo/bot-tab.tsx && grep -q 'HouseIcon' app/not-found.tsx && grep -q 'ClockIcon' "app/[locale]/blog/page.tsx" && grep -q 'ArrowLeftIcon' "app/[locale]/blog/[slug]/page.tsx" && grep -q 'ClockIcon' "app/[locale]/blog/[slug]/page.tsx" && grep -q 'ShareNetworkIcon' "app/[locale]/blog/[slug]/page.tsx" && grep -q 'GearSixIcon' modules/demo/demo-tabs.tsx && grep -q 'RobotIcon' modules/demo/demo-tabs.tsx && grep -q 'ChecksIcon' modules/demo/bot-tab.tsx && grep -q 'RobotIcon' modules/demo/bot-tab.tsx && pnpm exec prettier --check app/not-found.tsx "app/[locale]/blog/page.tsx" "app/[locale]/blog/[slug]/page.tsx" modules/demo/demo-tabs.tsx modules/demo/bot-tab.tsx && echo TASK2_OK</automated>
  </verify>
  <done>All five files contain zero bare occurrences of House/Clock/ArrowLeft/ShareNetwork/GearSix/Robot/Checks as standalone identifiers; each renamed identifier's Icon-suffixed form is present; all five files pass prettier --check.</done>
</task>

<task type="auto">
  <name>Task 3: Rename icon imports in landing/blog modules, then run full sitewide verification</name>
  <files>apps/web/modules/landing/pricing-section.tsx, apps/web/modules/landing/features.tsx, apps/web/modules/landing/lead-section.tsx, apps/web/modules/blog/blog-filters.tsx</files>
  <action>
Apply the per-file rename map from `<context>` to these four files (11 renamed identifiers total across imports, JSX sites, and one array):

`pricing-section.tsx`: rename `Check` to `CheckIcon` in the import line and at its one JSX site (the plan-features checkmark inside `plan.features.map`). Leave the file's unrelated pre-existing `console.log(plans, 'plans')` line untouched.

`features.tsx`: rename all 8 identifiers (`Bell`, `Calendar`, `ChartBar`, `ChatCircleText`, `Check`, `Gear`, `Star`, `Users`) to their Icon-suffixed forms in the multi-line import block, AND in the `FEATURE_ICONS` array — per `<context>`'s FEATURE_ICONS note, rename each array element in place (`ChatCircleTextIcon, CalendarIcon, BellIcon, ChartBarIcon, StarIcon, GearIcon, UsersIcon, CheckIcon`) while preserving that exact element order; do not resort the array to match the import block's alphabetical order.

`lead-section.tsx`: rename `CheckCircle` to `CheckCircleIcon` in the import line and at BOTH of its JSX sites — the guarantees-list checkmark inside `guarantees.map`, and the post-submit thank-you icon in the `isSubmitted` branch.

`blog-filters.tsx`: rename `Clock`/`MagnifyingGlass` to `ClockIcon`/`MagnifyingGlassIcon` in the import line and at their one JSX site each (post read-time, search input icon).

Run `pnpm exec prettier --write` on all four files from `apps/web/` after editing.

Then run this task's full verification, which also re-confirms the sitewide state across ALL 14 files touched by this plan (Tasks 1-3 combined), not just this task's own 4 files — this is the plan's final aggregate check.
  </action>
  <verify>
    <automated>cd /Users/artemdanko/Developer/denta-bot && ! grep -nE '\b(Check|Bell|Calendar|ChartBar|ChatCircleText|Gear|Star|Users|CheckCircle|Clock|MagnifyingGlass)\b' apps/web/modules/landing/pricing-section.tsx apps/web/modules/landing/features.tsx apps/web/modules/landing/lead-section.tsx apps/web/modules/blog/blog-filters.tsx && grep -q 'CheckIcon' apps/web/modules/landing/pricing-section.tsx && grep -q 'BellIcon' apps/web/modules/landing/features.tsx && grep -q 'CalendarIcon' apps/web/modules/landing/features.tsx && grep -q 'ChartBarIcon' apps/web/modules/landing/features.tsx && grep -q 'ChatCircleTextIcon' apps/web/modules/landing/features.tsx && grep -q 'CheckIcon' apps/web/modules/landing/features.tsx && grep -q 'GearIcon' apps/web/modules/landing/features.tsx && grep -q 'StarIcon' apps/web/modules/landing/features.tsx && grep -q 'UsersIcon' apps/web/modules/landing/features.tsx && [ "$(grep -o 'CheckCircleIcon' apps/web/modules/landing/lead-section.tsx | wc -l | tr -d ' ')" -ge 3 ] && grep -q 'ClockIcon' apps/web/modules/blog/blog-filters.tsx && grep -q 'MagnifyingGlassIcon' apps/web/modules/blog/blog-filters.tsx && (cd apps/web && pnpm exec prettier --check modules/landing/pricing-section.tsx modules/landing/features.tsx modules/landing/lead-section.tsx modules/blog/blog-filters.tsx) && ! grep -nE '\b(House|Clock|ArrowLeft|ShareNetwork|CaretDown|Check|List|X|Moon|Sun|GearSix|Robot|Checks|Bell|Calendar|ChartBar|ChatCircleText|Gear|Star|Users|CheckCircle|MagnifyingGlass)\b' apps/web/app/not-found.tsx "apps/web/app/[locale]/blog/page.tsx" "apps/web/app/[locale]/blog/[slug]/page.tsx" apps/web/shared/components/locale-switcher.tsx apps/web/shared/components/header.tsx apps/web/shared/components/premium-accordion.tsx apps/web/shared/components/theme-toggle.tsx apps/web/shared/components/premium-dialog.tsx apps/web/modules/demo/demo-tabs.tsx apps/web/modules/demo/bot-tab.tsx apps/web/modules/landing/pricing-section.tsx apps/web/modules/landing/features.tsx apps/web/modules/landing/lead-section.tsx apps/web/modules/blog/blog-filters.tsx && echo SITEWIDE_GREP_ZERO_OK && grep -q 'BellIcon' apps/web/modules/landing/hero.tsx && ! grep -q 'IconIcon' apps/web/modules/landing/hero.tsx && grep -q 'CaretLeftIcon' apps/web/modules/landing/reviews.tsx && grep -q 'CaretRightIcon' apps/web/modules/landing/reviews.tsx && grep -q 'StarIcon' apps/web/modules/landing/reviews.tsx && ! grep -q 'IconIcon' apps/web/modules/landing/reviews.tsx && grep -q 'CheckIcon' apps/web/modules/landing/hero-notification-badge.tsx && ! grep -q 'IconIcon' apps/web/modules/landing/hero-notification-badge.tsx && grep -q 'CheckIcon' apps/web/modules/landing/problem-solution.tsx && ! grep -q 'IconIcon' apps/web/modules/landing/problem-solution.tsx && echo EXCLUDED_FILES_UNTOUCHED_OK; TOUCHED='not-found\.tsx|blog/page\.tsx|blog/\[slug\]/page\.tsx|locale-switcher\.tsx|header\.tsx|premium-accordion\.tsx|theme-toggle\.tsx|premium-dialog\.tsx|demo-tabs\.tsx|bot-tab\.tsx|pricing-section\.tsx|features\.tsx|lead-section\.tsx|blog-filters\.tsx'; CT_OUT=$(pnpm --filter web check-types 2>&1); if echo "$CT_OUT" | grep -qE "$TOUCHED"; then echo "$CT_OUT"; exit 1; fi; echo CHECK_TYPES_OK; LINT_OUT=$(pnpm --filter web lint 2>&1); if echo "$LINT_OUT" | grep -qE "$TOUCHED"; then echo "$LINT_OUT"; exit 1; fi; echo LINT_OK; (cd apps/web && pnpm exec prettier --check app/not-found.tsx "app/[locale]/blog/page.tsx" "app/[locale]/blog/[slug]/page.tsx" shared/components/locale-switcher.tsx shared/components/header.tsx shared/components/premium-accordion.tsx shared/components/theme-toggle.tsx shared/components/premium-dialog.tsx modules/demo/demo-tabs.tsx modules/demo/bot-tab.tsx modules/landing/pricing-section.tsx modules/landing/features.tsx modules/landing/lead-section.tsx modules/blog/blog-filters.tsx) && echo TASK3_OK</automated>
  </verify>
  <done>All four files in this task contain zero bare occurrences of their target identifiers; features.tsx's FEATURE_ICONS array is renamed in place with unchanged order; lead-section.tsx has 3 CheckCircleIcon occurrences (1 import + 2 JSX); a sitewide grep across all 14 plan-touched files finds zero remaining bare-name icon usages; the 4 excluded files (hero.tsx, reviews.tsx, hero-notification-badge.tsx, problem-solution.tsx) each still contain their existing, unmodified Icon-suffixed identifiers with no stray double-suffixed artifact; pnpm --filter web check-types and lint show no errors referencing any of the 14 touched files; all 14 touched files pass prettier --check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This is a pure static identifier rename (import specifier + JSX tag name + one array's element identifiers) with no new dependency, no user input, no data crossing any trust boundary, and no change to any icon's props, styling, or rendered behavior.

## STRIDE Threat Register

No applicable threats. The change touches only import statements and component tag names within already-trusted, already-reviewed source files — no I/O, no network calls, no new attack surface, no auth/data-handling code path.
</threat_model>

<verification>
1. A grep across all 14 plan-touched files finds zero bare (non-Icon-suffixed) occurrences of any of the 22 renamed icon identifiers as standalone words.
2. Every renamed identifier's Icon-suffixed form is present in its expected file(s), including features.tsx's FEATURE_ICONS array (order preserved) and lead-section.tsx's 2 JSX sites for CheckCircleIcon.
3. `apps/web/shared/components/header.tsx`'s pre-existing uncommitted nav-link-reorder edit is preserved, verified by inspecting the file's navLinks content is unchanged from its pre-plan on-disk state.
4. The 4 already-correct files (hero.tsx, reviews.tsx, hero-notification-badge.tsx, problem-solution.tsx) are not edited by this plan (none appear in any task's `<files>`) and still contain their pre-existing Icon-suffixed identifiers intact with no stray double-suffixed artifact — checked by content, not by `git diff`, since these files may carry unrelated pre-existing uncommitted changes from other in-flight work.
5. `pnpm --filter web check-types` and `pnpm --filter web lint` produce no output referencing any of the 14 touched files.
6. All 14 touched files pass `prettier --check`.
</verification>

<success_criteria>
- All `@phosphor-icons/react`/`@phosphor-icons/react/ssr` icon imports and usages across the 14 target files use the Icon-suffixed convention.
- Every import subpath (`@phosphor-icons/react` vs `/ssr`) is unchanged from before this plan.
- No import alias was introduced anywhere — every rename is to the library's real Icon-suffixed export.
- `header.tsx`'s pre-existing uncommitted nav-link-reorder edit is intact.
- `features.tsx`'s FEATURE_ICONS array order is unchanged.
- The 4 already-correct files are untouched.
- `check-types`, `lint`, and `prettier --check` all pass (or show only pre-existing, unrelated failures) across every touched file.
</success_criteria>

<output>
Create `.planning/quick/260820-if0-standardize-phosphor-icons-react-imports/260820-if0-SUMMARY.md` when done.
</output>
