---
task_id: 260809-v68
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/modules/home/hero.tsx
  - apps/web/modules/home/problem.tsx
  - apps/web/modules/home/solution.tsx
  - apps/web/modules/home/features.tsx
  - apps/web/modules/home/cta-banner.tsx
  - apps/web/modules/home/testimonials.tsx
  - apps/web/app/contacts/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "On Home, at every adjacent-section boundary (Hero→Problem, Problem→Solution, Solution→Features, Features→CtaBanner, CtaBanner→Testimonials), the visible empty gap no longer sums two full section paddings — each section's stacking-relevant vertical padding is halved from py-16/lg:py-24 (or the equivalent pb-16/lg:pb-24 half of a split pt-*/pb-*) to py-8/lg:py-12, so the combined boundary gap matches what a single section's original padding visually intended."
    - "Hero's top padding (pt-24 lg:pt-32, header clearance above the first section) is unchanged — only its bottom padding (pb-16 lg:pb-24 → pb-8 lg:pb-12) is halved, since that's the only edge stacking against the next section."
    - "On Contacts, the gap between the contact-form section and the FAQ section no longer double-counts padding — both sections' padding is halved to pb-8/lg:pb-12 and py-8/lg:py-12 respectively — while the first (hero) section's padding is untouched, since it has no section above it to stack against."
  artifacts:
    - apps/web/modules/home/hero.tsx
    - apps/web/modules/home/problem.tsx
    - apps/web/modules/home/solution.tsx
    - apps/web/modules/home/features.tsx
    - apps/web/modules/home/cta-banner.tsx
    - apps/web/modules/home/testimonials.tsx
    - apps/web/app/contacts/page.tsx
  key_links:
    - "Each file's outermost <section> className directly controls the visible box-model gap at DevTools inspection — Container, headings, and all other markup inside stay unchanged, so the fix is purely a padding-value edit with zero layout/structure risk."
    - "Contacts' three sections are independent siblings in one file (app/contacts/page.tsx) — only sections 2 and 3 change; section 1 (pb-12 pt-24 lg:pb-16 lg:pt-32) is intentionally left alone since it has no section above it."
---

<objective>
Fix the "double padding" visual bug the user flagged via DevTools box-model inspection at mobile width: CSS padding never collapses between sibling elements (unlike margin), and every Home section (plus 2 of 3 Contacts sections) uses a symmetric `py-16 lg:py-24` (or split pt-*/pb-*) class, so the visible gap between any two adjacent sections is the SUM of both sections' padding — e.g. 64px+64px=128px on mobile, 96px+96px=192px on `lg` — reading as an oversized, unintentional gap instead of deliberate breathing room.

Purpose: Halve the vertical padding on every affected section so the combined gap at each section boundary matches what a single section's current padding already visually intended, restoring intentional-looking spacing without touching any other markup, imports, or logic.
Output: 6 Home section files (`hero.tsx`, `problem.tsx`, `solution.tsx`, `features.tsx`, `cta-banner.tsx`, `testimonials.tsx`) and `apps/web/app/contacts/page.tsx` (2 of its 3 `<section>` elements), each with exactly one `<section>` className edit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/home/hero.tsx
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/home/problem.tsx
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/home/solution.tsx
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/home/features.tsx
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/home/cta-banner.tsx
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/home/testimonials.tsx
@/Users/artemdanko/Developer/denta-bot/apps/web/app/contacts/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Halve stacking padding on all 6 Home sections</name>
  <files>apps/web/modules/home/hero.tsx, apps/web/modules/home/problem.tsx, apps/web/modules/home/solution.tsx, apps/web/modules/home/features.tsx, apps/web/modules/home/cta-banner.tsx, apps/web/modules/home/testimonials.tsx</files>
  <action>
    In each file, edit ONLY the outermost `<section>` element's `className` attribute value — do not touch `Container`, headings, icons, or any other markup, imports, or logic:

    1. `hero.tsx` (~line 24): change `className="pt-24 pb-16 lg:pt-32 lg:pb-24"` to `className="pt-24 pb-8 lg:pt-32 lg:pb-12"`. Keep `pt-24`/`lg:pt-32` exactly as-is — that's header clearance above the first section, not inter-section stacking; only the `pb-16 lg:pb-24` half (which stacks against Problem below it) is halved to `pb-8 lg:pb-12`.
    2. `problem.tsx` (~line 11): change `className="py-16 lg:py-24"` to `className="py-8 lg:py-12"`.
    3. `solution.tsx` (~line 71): change `className="py-16 lg:py-24"` to `className="py-8 lg:py-12"`.
    4. `features.tsx` (~line 22): change `<section id="features" className="py-16 lg:py-24">` to `<section id="features" className="py-8 lg:py-12">` — keep the `id="features"` attribute unchanged.
    5. `cta-banner.tsx` (~line 10): change `className="bg-dt-navy py-16 lg:py-24"` to `className="bg-dt-navy py-8 lg:py-12"` — keep `bg-dt-navy` unchanged.
    6. `testimonials.tsx` (~line 26): change `className="py-16 lg:py-24"` to `className="py-8 lg:py-12"`.
  </action>
  <verify>
    <automated>
test "$(grep -c 'className="pt-24 pb-8 lg:pt-32 lg:pb-12"' apps/web/modules/home/hero.tsx)" = "1" && \
test "$(grep -c 'className="pt-24 pb-16 lg:pt-32 lg:pb-24"' apps/web/modules/home/hero.tsx)" = "0" && \
test "$(grep -c 'className="py-8 lg:py-12"' apps/web/modules/home/problem.tsx)" = "1" && \
test "$(grep -c 'className="py-16 lg:py-24"' apps/web/modules/home/problem.tsx)" = "0" && \
test "$(grep -c 'className="py-8 lg:py-12"' apps/web/modules/home/solution.tsx)" = "1" && \
test "$(grep -c 'className="py-16 lg:py-24"' apps/web/modules/home/solution.tsx)" = "0" && \
test "$(grep -c '<section id="features" className="py-8 lg:py-12">' apps/web/modules/home/features.tsx)" = "1" && \
test "$(grep -c 'className="py-16 lg:py-24"' apps/web/modules/home/features.tsx)" = "0" && \
test "$(grep -c 'className="bg-dt-navy py-8 lg:py-12"' apps/web/modules/home/cta-banner.tsx)" = "1" && \
test "$(grep -c 'className="bg-dt-navy py-16 lg:py-24"' apps/web/modules/home/cta-banner.tsx)" = "0" && \
test "$(grep -c 'className="py-8 lg:py-12"' apps/web/modules/home/testimonials.tsx)" = "1" && \
test "$(grep -c 'className="py-16 lg:py-24"' apps/web/modules/home/testimonials.tsx)" = "0" && \
pnpm --filter web lint && \
CT_OUT="$(pnpm --filter web check-types 2>&1)"; \
test -z "$(printf '%s' "$CT_OUT" | grep 'error TS' | grep -v 'button-group.tsx\|calendar.tsx\|sidebar.tsx')" && \
echo PASS
    </automated>
  </verify>
  <done>All 6 Home section files have their outermost `<section>` className updated to the halved padding values exactly as specified (Hero keeps `pt-24 lg:pt-32` and halves only `pb-*`; the other 5 change `py-16 lg:py-24` to `py-8 lg:py-12`, with `id="features"` and `bg-dt-navy` preserved where applicable). No other className, markup, import, or logic changed in any of the 6 files. `pnpm --filter web lint` and `pnpm --filter web check-types` (filtered for pre-existing csstype errors) both pass.</done>
</task>

<task type="auto">
  <name>Task 2: Halve stacking padding on Contacts' form and FAQ sections</name>
  <files>apps/web/app/contacts/page.tsx</files>
  <action>
    In `apps/web/app/contacts/page.tsx`, edit ONLY the `className` of the 2nd and 3rd `<section>` elements — do not touch the 1st section, `Container`, headings, or any other markup, imports, or logic:

    - 2nd section (~line 23, contact form + info): change `className="pb-16 lg:pb-24"` to `className="pb-8 lg:pb-12"`.
    - 3rd section (~line 31, FAQ): change `className="bg-dt-navy/5 py-16 lg:py-24"` to `className="bg-dt-navy/5 py-8 lg:py-12"` — keep `bg-dt-navy/5` unchanged.

    Do NOT touch the 1st section (`className="pb-12 pt-24 lg:pb-16 lg:pt-32"`) — it has no adjacent section above it stacking padding, so it's not part of this bug.
  </action>
  <verify>
    <automated>
test "$(grep -c 'className="pb-12 pt-24 lg:pb-16 lg:pt-32"' apps/web/app/contacts/page.tsx)" = "1" && \
test "$(grep -c 'className="pb-8 lg:pb-12"' apps/web/app/contacts/page.tsx)" = "1" && \
test "$(grep -c 'className="pb-16 lg:pb-24"' apps/web/app/contacts/page.tsx)" = "0" && \
test "$(grep -c 'className="bg-dt-navy/5 py-8 lg:py-12"' apps/web/app/contacts/page.tsx)" = "1" && \
test "$(grep -c 'className="bg-dt-navy/5 py-16 lg:py-24"' apps/web/app/contacts/page.tsx)" = "0" && \
pnpm --filter web lint && \
CT_OUT="$(pnpm --filter web check-types 2>&1)"; \
test -z "$(printf '%s' "$CT_OUT" | grep 'error TS' | grep -v 'button-group.tsx\|calendar.tsx\|sidebar.tsx')" && \
echo PASS
    </automated>
  </verify>
  <done>Contacts' 2nd section className is `pb-8 lg:pb-12` and 3rd section className is `bg-dt-navy/5 py-8 lg:py-12`; the 1st section's className (`pb-12 pt-24 lg:pb-16 lg:pt-32`) is byte-identical to before. No other markup, import, or logic changed. `pnpm --filter web lint` and `pnpm --filter web check-types` (filtered for pre-existing csstype errors) both pass.</done>
</task>

</tasks>

<verification>
- `grep`-based structural checks confirm the exact new className string is present (count 1) and the exact old className string is absent (count 0) in each of the 7 files, for each of the 8 edited `<section>` elements (6 on Home, 2 on Contacts).
- Contacts' untouched 1st section className is grepped for byte-identical presence, confirming the "don't touch" boundary was respected.
- `pnpm --filter web lint` and `pnpm --filter web check-types` (pre-existing `button-group.tsx`/`calendar.tsx`/`sidebar.tsx` csstype errors excluded per prior phase precedent) pass after each task.
- Non-blocking note: a human DevTools box-model spot-check at 375px mobile width on Home and Contacts is worthwhile to visually confirm the gaps now read as intentional — but per this plan's constraints it doesn't block completion, since every edit has a concrete, objectively-verifiable grep-based acceptance criterion above.
</verification>

<success_criteria>
- Every one of the 6 Home sections (Hero, Problem, Solution, Features, CtaBanner, Testimonials) has its stacking-relevant vertical padding halved (py-16/lg:py-24 → py-8/lg:py-12, or Hero's pb-16/lg:pb-24 → pb-8/lg:pb-12 with pt-* untouched).
- Contacts' 2nd (contact form) and 3rd (FAQ) sections have their padding halved identically; the 1st (hero) section is untouched.
- Only the 7 named files change, each via a single-line `<section>` className edit — no other markup, imports, or logic touched anywhere.
- `apps/web/app/demo/page.tsx` is not modified (confirmed out of scope — no double-padding pattern present).
- `pnpm --filter web lint` and `pnpm --filter web check-types` pass (filtered for pre-existing csstype errors) after both tasks.
</success_criteria>

<output>
Create `.planning/quick/260809-v68-remove-double-padding-gaps-between-adjac/260809-v68-SUMMARY.md` when done
</output>
