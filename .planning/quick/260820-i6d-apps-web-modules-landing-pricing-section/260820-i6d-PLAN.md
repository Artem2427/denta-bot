---
phase: quick
plan: 260820-i6d
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/modules/landing/pricing-section.tsx
autonomous: true
requirements: []

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The pricing section's heading (eyebrow + title, via SectionHeading) and the billing-period toggle sit vertically centered relative to each other in the desktop (lg+) row layout, instead of bottom-aligned"
    - "The mobile/tablet stacked layout for the heading+toggle row (flex-col, items-center, gap-6, text-center) is byte-identical to before this change — only the lg: alignment utility changes"
    - "The 3-plan pricing grid stretches to fill Container's full available width at lg+ breakpoints (no artificial max-width narrower than Container's own max-w-[var(--dt-container-max)] creating visible empty side gutters)"
    - "The 1-plan (max-w-md) and 2-plan (max-w-4xl) grid width caps in getGridClassName are unchanged — only the 3-plan branch loses its max-w-6xl"
  artifacts:
    - apps/web/modules/landing/pricing-section.tsx
  key_links:
    - "getGridClassName(3)'s return string -> the 3-plan grid div's className -> no longer wrapped in max-w-6xl, so it fills Container's max-w-[var(--dt-container-max)] (1280px) minus Container's own px-4 lg:px-8 padding, instead of centering within an artificially narrower 1152px band"
    - "Heading+toggle row's lg:items-center -> CSS align-items on the lg:flex-row layout -> SectionHeading block and billing-toggle div become vertically centered against each other instead of bottom-aligned"
---

<objective>
Fix two layout issues in `apps/web/modules/landing/pricing-section.tsx`, both from a user-supplied annotated screenshot: (1) the heading+toggle row bottom-aligns its two unequal-height children instead of centering them, and (2) the 3-plan grid is capped at a `max-w-6xl` (1152px) width nested inside `Container`'s own 1280px cap, creating a redundant inner constraint that leaves visible empty gutters on both sides.

Purpose: Make the pricing section's desktop layout read as visually balanced (heading level with the toggle) and let the 3-card grid use the full width Container already allocates it, matching the user's annotated feedback exactly.
Output: Two small className edits in `pricing-section.tsx` — `lg:items-end` -> `lg:items-center` on the heading+toggle row, and `max-w-6xl` removed from the 3-plan branch of `getGridClassName`. No other file touched; the pre-existing `console.log(plans, 'plans')` debug line and the 1-plan/2-plan grid branches are left exactly as-is.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/artemdanko/Developer/denta-bot/.claude/CLAUDE.md
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/landing/pricing-section.tsx
@/Users/artemdanko/Developer/denta-bot/apps/web/shared/components/container.tsx
@/Users/artemdanko/Developer/denta-bot/apps/web/shared/components/section-heading.tsx

**Current `getGridClassName` (lines 21-29), only the final `return` (3-plan case) changes:**
```tsx
function getGridClassName(count: number): string {
  if (count === 1) {
    return 'mx-auto grid max-w-md gap-6';
  }
  if (count === 2) {
    return 'mx-auto grid max-w-4xl gap-6 lg:grid-cols-2';
  }
  return 'mx-auto grid max-w-6xl gap-6 lg:grid-cols-3';
}
```
The 1-plan and 2-plan branches intentionally cap width (fewer, stretched-out cards would look wrong) and must not change. Only the 3-plan branch's `max-w-6xl` is removed — `mx-auto` is kept for consistency with the other two branches even though it becomes a functional no-op once the grid is exactly as wide as its parent (harmless, not worth special-casing).

**Current heading+toggle row (line 51):**
```tsx
<div className="mb-dt-48 flex flex-col items-center gap-6 text-center lg:flex-row lg:items-end lg:justify-between lg:text-left">
  <SectionHeading eyebrow={t('eyebrow')} title={t('title')} className="mb-0 lg:mx-0" />
  <div className="inline-flex items-center gap-1 rounded-full border border-[var(--dt-border)] bg-dt-warm-white p-1">
    {/* Monthly/Yearly toggle buttons, unchanged */}
  </div>
</div>
```
Only `lg:items-end` becomes `lg:items-center`. Every other token in that className string (`mb-dt-48 flex flex-col items-center gap-6 text-center lg:flex-row lg:justify-between lg:text-left`) stays exactly as-is — the mobile/tablet stacked case (`flex-col items-center gap-6 text-center`, no `lg:` prefix) is untouched.

**Container's own width/padding (`apps/web/shared/components/container.tsx`):** `mx-auto w-full max-w-[var(--dt-container-max)] px-4 lg:px-8`. This confirms the 1152px `max-w-6xl` inner cap was strictly tighter than Container's own ~1280px cap minus its padding — removing it lets the grid fill whatever width Container actually allocates.

**Out of scope, do not touch:** the pre-existing `console.log(plans, 'plans')` debug line (currently line 46) is unrelated leftover from a prior task and must remain exactly as-is. Do not touch `container.tsx`, `section-heading.tsx`, any i18n message file, or the 1-plan/2-plan grid branches.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Center heading+toggle row and remove redundant 3-plan grid width cap</name>
  <files>apps/web/modules/landing/pricing-section.tsx</files>
  <action>
In `apps/web/modules/landing/pricing-section.tsx`, make exactly two className edits:

1. On the heading+toggle wrapper `div` (currently line 51), change `lg:items-end` to `lg:items-center`. The full className string becomes `"mb-dt-48 flex flex-col items-center gap-6 text-center lg:flex-row lg:items-center lg:justify-between lg:text-left"` — every other token is unchanged, including the base (non-`lg:`) `items-center` used for the mobile/tablet stacked layout.

2. In `getGridClassName` (currently lines 21-29), change the final `return` statement (the 3-plan/`else` case) from `return 'mx-auto grid max-w-6xl gap-6 lg:grid-cols-3';` to `return 'mx-auto grid gap-6 lg:grid-cols-3';` — drop only the `max-w-6xl` token, keep `mx-auto`, `grid`, `gap-6`, and `lg:grid-cols-3` exactly as they are. Do not touch the `count === 1` or `count === 2` branches.

Do not touch the `console.log(plans, 'plans')` line, imports, the pricing-card markup below the grid, `container.tsx`, or `section-heading.tsx`.
  </action>
  <verify>
    <automated>cd /Users/artemdanko/Developer/denta-bot && grep -F 'lg:flex-row lg:items-center lg:justify-between lg:text-left' apps/web/modules/landing/pricing-section.tsx > /dev/null && grep -c 'lg:items-end' apps/web/modules/landing/pricing-section.tsx | grep -qx 0 && grep -F "return 'mx-auto grid gap-6 lg:grid-cols-3';" apps/web/modules/landing/pricing-section.tsx > /dev/null && grep -c 'max-w-6xl' apps/web/modules/landing/pricing-section.tsx | grep -qx 0 && grep -F "return 'mx-auto grid max-w-md gap-6';" apps/web/modules/landing/pricing-section.tsx > /dev/null && grep -F "return 'mx-auto grid max-w-4xl gap-6 lg:grid-cols-2';" apps/web/modules/landing/pricing-section.tsx > /dev/null && grep -F "console.log(plans, 'plans')" apps/web/modules/landing/pricing-section.tsx > /dev/null && pnpm --filter web check-types && echo TASK1_OK</automated>
  </verify>
  <done>The heading+toggle row's className reads `"mb-dt-48 flex flex-col items-center gap-6 text-center lg:flex-row lg:items-center lg:justify-between lg:text-left"` (no `lg:items-end` anywhere in the file); `getGridClassName`'s 3-plan branch returns `'mx-auto grid gap-6 lg:grid-cols-3'` (no `max-w-6xl` anywhere in the file); the 1-plan (`max-w-md`) and 2-plan (`max-w-4xl`) branches, the `console.log(plans, 'plans')` line, and every other part of the file are byte-identical to before; `pnpm --filter web check-types` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Live dev-server check — confirm both fixes render on the running server</name>
  <files>apps/web/modules/landing/pricing-section.tsx</files>
  <action>
Reuse the dev server already running on port 3000 from earlier in this session (confirmed listening before this plan was written) — do not start a new one. If, at execution time, nothing is listening on port 3000, start one with `pnpm --filter web dev` in the background, wait for it to become ready, then proceed with the checks below.

Fetch the live-rendered home page HTML from the running server and grep it directly (not the source file) to confirm the Fast-Refresh-recompiled output actually reflects both Task 1 edits, catching any stale-cache/HMR failure that a source-only check would miss:

1. `curl -s http://localhost:3000/` (follow to the default `uk` locale if the root rewrites) should contain the literal substring `lg:items-center lg:justify-between lg:text-left` and must NOT contain `lg:items-end`.
2. The same fetched HTML should contain the literal substring `mx-auto grid gap-6 lg:grid-cols-3` and must NOT contain `max-w-6xl`.

Then, using whatever visual/screenshot tooling is available in this session (browser automation tool, or the `run` skill's screenshot capability) — or failing that, by describing exact manual steps for the user to follow — view `http://localhost:3000/#pricing` (or `http://localhost:3000/uk#pricing`) at a desktop viewport width of at least 1024px (the `lg` breakpoint) and visually confirm:
- The "ТАРИФИ" eyebrow + "Прозорі тарифи без прихованих платежів" heading block and the "Щомісяця" / "Щороку · -20%" toggle now sit level with each other (vertically centered as a pair), not bottom-aligned.
- The 3-card pricing grid below now stretches edge-to-edge to Container's padded width, with no visible empty gutter band between the grid and Container's own left/right padding.

Also confirm at a narrower (mobile/tablet, <1024px) viewport that the heading+toggle still stacks centered as before (unaffected by this change).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/ | grep -qx 200 && HTML="$(curl -s http://localhost:3000/)"; printf '%s' "$HTML" | grep -qF 'lg:items-center lg:justify-between lg:text-left' && ! printf '%s' "$HTML" | grep -qF 'lg:items-end' && printf '%s' "$HTML" | grep -qF 'mx-auto grid gap-6 lg:grid-cols-3' && ! printf '%s' "$HTML" | grep -qF 'max-w-6xl' && echo TASK2_OK</automated>
  </verify>
  <done>The live dev server (reused, not newly started unless none was running) serves HTML containing the updated `lg:items-center` alignment and the `max-w-6xl`-free 3-plan grid class string; a visual check at >=1024px viewport confirms the heading+toggle pair reads as vertically centered and the 3-card grid fills Container's width with no side gutters; a check at a narrower viewport confirms the stacked mobile/tablet layout is unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this is a purely presentational Tailwind className change to an existing client component. No new data flow, user input, or trust boundary is introduced.

## STRIDE Threat Register

No new threats introduced. This change modifies only two Tailwind utility-class strings on an already-reviewed component; no new inputs, dependencies, or attack surface are added.
</threat_model>

<verification>
1. `grep -F 'lg:flex-row lg:items-center lg:justify-between lg:text-left' apps/web/modules/landing/pricing-section.tsx` matches the heading+toggle row; `grep -c 'lg:items-end' apps/web/modules/landing/pricing-section.tsx` returns 0.
2. `grep -F "return 'mx-auto grid gap-6 lg:grid-cols-3';"` matches the 3-plan branch; `grep -c 'max-w-6xl' apps/web/modules/landing/pricing-section.tsx` returns 0; the 1-plan (`max-w-md`) and 2-plan (`max-w-4xl`) branches are untouched.
3. `git diff --stat` shows exactly one file changed: `apps/web/modules/landing/pricing-section.tsx`. The `console.log(plans, 'plans')` line is still present, unmodified.
4. `pnpm --filter web check-types` passes.
5. Live dev-server check (Task 2): curled HTML from the running server reflects both updated class strings, and a visual pass at >=1024px confirms the heading/toggle read as level and the 3-card grid fills Container's width with no side gutters; a narrower-viewport pass confirms the stacked mobile layout is unaffected.
</verification>

<success_criteria>
- The pricing section's heading+toggle row renders vertically centered (not bottom-aligned) at `lg+` viewport widths; the mobile/tablet stacked layout is unchanged.
- The 3-plan pricing grid fills Container's full available width with no empty side gutters; the 1-plan and 2-plan grid widths are unchanged.
- Only `apps/web/modules/landing/pricing-section.tsx` is modified; the out-of-scope `console.log` debug line is untouched.
- `pnpm --filter web check-types` passes.
- A live dev-server check (reusing the already-running server) confirms both fixes are actually rendered, not just present in source.
</success_criteria>

<output>
Create `.planning/quick/260820-i6d-apps-web-modules-landing-pricing-section/260820-i6d-SUMMARY.md` when done
</output>