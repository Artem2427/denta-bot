---
task_id: 260809-jr2
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/modules/home/solution.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The Home page's Solution section renders a centered h2 heading 'DentaBot бере це на себе' above the two-column step grid"
    - "Each of the two step columns ('Для пацієнта' / 'Для клініки') is visually a bordered PremiumCard, not a bare div"
    - "Each of the 6 numbered steps shows both a title and a one-line description underneath it, using the exact verbatim Ukrainian copy from the design reference"
    - "The 'Спробувати як це працює' CTA button shows a trailing ArrowRight icon, matching the icon-button pattern used elsewhere on the Home page"
  artifacts:
    - apps/web/modules/home/solution.tsx
  key_links:
    - "StepColumn's `steps` prop changes from `string[]` to `{ title: string; description: string }[]` -> both `patientSteps` and `clinicSteps` data arrays must be updated to the new object shape with the exact verbatim copy, or the component's `.map()` over `step.title`/`step.description` breaks at the type level"
    - "PremiumCard becomes the wrapper inside StepColumn's existing `Reveal` (Reveal -> PremiumCard -> heading + steps), following the same `Reveal`-wraps-`PremiumCard` nesting order problem.tsx's `StaggerItem`-wraps-`PremiumCard` pattern uses, so the entrance animation and the card's hover/shadow styling both keep working together"
---

<objective>
Restore missing content and structure in the Home page's Solution section (`apps/web/modules/home/solution.tsx`) so it matches the client-provided reference design: a section heading above the two-column step grid, each column wrapped in a bordered `PremiumCard`, a one-line description under each numbered step title, and a trailing arrow icon on the CTA button.

Purpose: The current implementation silently dropped four pieces of design-specified content/structure during the Phase 01.1 premium redesign migration — this quick task closes that gap using the exact copy transcribed from the user's reference screenshot, reusing existing site primitives (`PremiumCard`, `Reveal`, `ArrowRight` from `@phosphor-icons/react/ssr`) with no new components.
Output: `apps/web/modules/home/solution.tsx` updated — section h2 heading, `PremiumCard`-wrapped step columns, `{title, description}` step data with description rendered under each step, `ArrowRight` icon on the CTA button. No other file changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/home/solution.tsx
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/home/problem.tsx
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/home/hero.tsx
@/Users/artemdanko/Developer/denta-bot/apps/web/shared/components/premium-card.tsx
@/Users/artemdanko/Developer/denta-bot/apps/web/shared/components/premium-button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add section heading, PremiumCard wrapper, step descriptions, and CTA icon to Solution section</name>
  <files>apps/web/modules/home/solution.tsx</files>
  <action>
    Rewrite `apps/web/modules/home/solution.tsx` with four changes, keeping every existing `dt-` token class, the `Reveal`-per-column wrapper, and the filled `bg-dt-navy` numbered-circle-badge pattern exactly as they are today — only add what is missing.

    1. Add the import `ArrowRight` from `@phosphor-icons/react/ssr` and `PremiumCard` from `@/shared/components/premium-card`, alongside the existing `Link`, `Container`, `PremiumButton`, `Reveal`, `routes` imports.

    2. Change `patientSteps` and `clinicSteps` from `string[]` to arrays of `{ title: string; description: string }` objects, using this exact verbatim copy (do not paraphrase):
       - `patientSteps`: `{ title: 'Відкриває Telegram бота', description: 'Один клік — бот вітається і показує вільні часи' }`, `{ title: 'Обирає час і лікаря', description: 'Зручний календар з реальними вільними слотами' }`, `{ title: 'Отримує підтвердження', description: 'Автоматичне нагадування за день і годину до прийому' }`.
       - `clinicSteps`: `{ title: "Запис з'являється в системі", description: 'Миттєво видно в адмін панелі та календарі лікаря' }`, `{ title: 'Бот надсилає нагадування', description: 'Автоматично, без участі адміністратора' }`, `{ title: 'Аналітика та звіти', description: 'Бачите статистику, завантаженість, можете оптимізувати роботу' }`.

    3. In `StepColumn`, update the `steps` prop type to `{ title: string; description: string }[]`. Replace the bare `<div>` wrapper inside `Reveal` with `<PremiumCard>` (`Reveal` still wraps `PremiumCard`, matching the `Reveal`/`StaggerItem`-wraps-`PremiumCard` nesting problem.tsx uses for its cards) — move the existing `h3` heading and the `space-y-4` steps list inside the `PremiumCard`. In the `.map()`, key by `step.title` instead of `step`. Inside each step row, replace the single `<span className="pt-2 font-medium text-dt-navy">{step}</span>` with a stacked text container: a `<div>` (not `<span>`, since it now holds two lines) containing `<p className="font-medium text-dt-navy">{step.title}</p>` and `<p className="text-sm text-dt-graphite">{step.description}</p>`; keep the badge div (the `bg-dt-navy` numbered circle) unchanged, and keep the same `flex items-start gap-4` row wrapper.

    4. Add the section heading above the grid: inside `Solution`, before the `grid gap-12 lg:grid-cols-2` div, add `<Reveal><div className="mb-12 text-center"><h2 className="text-dt-h2 font-dt-heading font-bold text-dt-navy">DentaBot бере це на себе</h2></div></Reveal>` — matching problem.tsx's heading markup/classes exactly. Do not add a subtitle paragraph under the heading; only the h2 was specified in the reference design for this section.

    5. Add the trailing `ArrowRight` icon to the CTA button: inside the existing `<Link href={routes.demo}>Спробувати як це працює</Link>`, add `<ArrowRight weight="regular" className="ml-2 h-5 w-5" />` right after the button label text, matching the icon-button pattern in hero.tsx's coral "Спробувати демо" button (same `weight`/`className` values). Keep the button's existing `variant="outline" size="lg" asChild` props unchanged.
  </action>
  <verify>
    <automated>
test "$(grep -c 'DentaBot бере це на себе' apps/web/modules/home/solution.tsx)" = "1" && \
test "$(grep -c '<PremiumCard>' apps/web/modules/home/solution.tsx)" -ge "2" && \
test "$(grep -c 'ArrowRight' apps/web/modules/home/solution.tsx)" -ge "1" && \
test "$(grep -c 'Один клік — бот вітається і показує вільні часи' apps/web/modules/home/solution.tsx)" = "1" && \
test "$(grep -c 'Зручний календар з реальними вільними слотами' apps/web/modules/home/solution.tsx)" = "1" && \
test "$(grep -c 'Автоматичне нагадування за день і годину до прийому' apps/web/modules/home/solution.tsx)" = "1" && \
test "$(grep -c 'Миттєво видно в адмін панелі та календарі лікаря' apps/web/modules/home/solution.tsx)" = "1" && \
test "$(grep -c 'Автоматично, без участі адміністратора' apps/web/modules/home/solution.tsx)" = "1" && \
test "$(grep -c 'Бачите статистику, завантаженість, можете оптимізувати роботу' apps/web/modules/home/solution.tsx)" = "1" && \
test "$(grep -c 'description: string' apps/web/modules/home/solution.tsx)" -ge "1" && \
pnpm --filter web lint && \
CT_OUT="$(pnpm --filter web check-types 2>&1)"; \
test -z "$(printf '%s' "$CT_OUT" | grep 'error TS' | grep -v 'button-group.tsx\|calendar.tsx\|sidebar.tsx')" && \
echo PASS
    </automated>
  </verify>
  <done>`apps/web/modules/home/solution.tsx` renders the "DentaBot бере це на себе" h2 above the step grid, both step columns are wrapped in `PremiumCard`, all 6 steps show title + description with the exact verbatim copy, and the CTA button renders a trailing `ArrowRight` icon. `pnpm --filter web lint` passes with zero warnings; `pnpm --filter web check-types` produces no new TS errors (pre-existing button-group.tsx/calendar.tsx/sidebar.tsx csstype errors excluded).</done>
</task>

</tasks>

<verification>
- `grep` confirms the exact heading text, `PremiumCard` wrapper (x2), `ArrowRight` icon usage, and all 6 verbatim step descriptions are present in `solution.tsx`.
- `pnpm --filter web lint` and `pnpm --filter web check-types` (filtered for pre-existing csstype errors) both pass.
- Non-blocking note: a human visual spot-check of http://localhost:3000/ (Solution section, between "Знайомо?" and the testimonials) is worthwhile to confirm the card/heading/icon render as expected, but per this task's constraints it does not block completion — the acceptance criteria above are concrete, objectively-verifiable text/structure assertions.
</verification>

<success_criteria>
- Solution section has a centered "DentaBot бере це на себе" h2 heading above the step grid.
- Both step columns are wrapped in `PremiumCard` (bordered card), not bare divs.
- All 6 steps show a title and a one-line description, using the exact verbatim copy specified.
- The CTA button shows a trailing `ArrowRight` icon.
- `Reveal`-per-column animation wrapper, numbered-circle-badge pattern, and existing `dt-` token usage are all preserved unchanged.
- No files other than `apps/web/modules/home/solution.tsx` are modified.
</success_criteria>

<output>
Create `.planning/quick/260809-jr2-restore-missing-content-structure-in-hom/260809-jr2-SUMMARY.md` when done
</output>
