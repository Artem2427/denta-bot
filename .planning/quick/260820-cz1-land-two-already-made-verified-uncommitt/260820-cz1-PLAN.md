---
type: quick
slug: 260820-cz1-land-two-already-made-verified-uncommitt
autonomous: true
files_modified:
  - apps/web/app/premium-theme.css
  - apps/web/modules/landing/lead-section.tsx
must_haves:
  truths:
    - 'apps/web/app/premium-theme.css has --text-dt-body: 1rem (not 1.125rem) — user-confirmed intentional, matches their devtools inspection of the reference design SectionHeading description paragraph'
    - 'apps/web/app/premium-theme.css has no other semantic token value changes beyond --text-dt-body — the shadow/clamp multi-line declarations differ only in Prettier formatting (line wrapping), not values'
    - 'apps/web/modules/landing/lead-section.tsx has zero content/logic diff from its last-committed state — only import statement ordering changed'
    - 'Both files pass pnpm exec prettier --check individually'
    - 'The working tree has no uncommitted changes to these two files after the task runs'
  artifacts:
    - apps/web/app/premium-theme.css
    - apps/web/modules/landing/lead-section.tsx
  key_links:
    - '--text-dt-body is consumed by SectionHeading (apps/web/shared/components/section-heading.tsx) description paragraph via the text-dt-body utility class — the 1.125rem to 1rem change directly affects that rendered text size site-wide'
---

<objective>
Commit two already-made, already-verified uncommitted working-tree edits exactly as they currently sit on disk. No new authoring: `apps/web/app/premium-theme.css` (`--text-dt-body` 1.125rem to 1rem, user-confirmed intentional, plus Prettier reformatting of shadow/clamp declarations) and `apps/web/modules/landing/lead-section.tsx` (pure import-order Prettier reformat, zero content change).

The orchestrator already confirmed both files currently pass `pnpm exec prettier --check` and match this description. This task's job is to independently re-verify that description still holds at execution time (file content can drift between planning and execution in this session's active multi-agent context), then commit — not to blindly trust the planning-time state.

Purpose: Land verified-correct, already-formatted working-tree state without re-deriving or re-authoring anything; guard against silently committing something that changed underneath the plan.
Output: One commit containing exactly these two files' current on-disk content.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/artemdanko/Developer/denta-bot/.claude/CLAUDE.md
@/Users/artemdanko/Developer/denta-bot/apps/web/app/premium-theme.css
@/Users/artemdanko/Developer/denta-bot/apps/web/modules/landing/lead-section.tsx

Already independently confirmed in the planning session (re-verify at execution time, do not skip):
- `pnpm exec prettier --check apps/web/app/premium-theme.css` passes on current on-disk content.
- `pnpm exec prettier --check apps/web/modules/landing/lead-section.tsx` passes on current on-disk content.
- `git diff apps/web/app/premium-theme.css` at planning time showed exactly one semantic change (`--text-dt-body: 1.125rem;` to `--text-dt-body: 1rem;`, around line 48) plus Prettier line-wrap reformatting of the `--shadow-dt-card` / `--shadow-dt-hover` multi-line values (around lines 25-30) — no other custom property value changed.
- `git diff apps/web/modules/landing/lead-section.tsx` at planning time showed only import statement reordering (third-party imports `@hookform/resolvers/zod`, `@phosphor-icons/react/ssr`, `next-intl`, `react`, `react-hook-form`, `sonner`, `zod` moved after the `@/shared/...` imports) — zero body/JSX/logic change.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify both files match the described diff and are Prettier-clean, then commit as-is</name>
  <files>apps/web/app/premium-theme.css, apps/web/modules/landing/lead-section.tsx</files>
  <action>
Do NOT edit either file's content. This task only re-verifies the current on-disk state matches the description below, then stages and commits exactly what is currently on disk.

Verification steps (run all before committing, in order):

1. Run `git diff apps/web/app/premium-theme.css` and confirm the only semantic (non-whitespace/non-formatting) value change is `--text-dt-body: 1.125rem;` becoming `--text-dt-body: 1rem;`. All other diff hunks in this file must be pure Prettier line-wrap reformatting of the `--shadow-dt-card`/`--shadow-dt-hover` declarations (same values, different line breaks) — no other custom property's value may differ from HEAD. If any other value changed, halt and do not commit — report the unexpected diff instead of proceeding.
2. Run `git diff apps/web/modules/landing/lead-section.tsx` and confirm every changed line is an import statement being reordered — no added/removed line touches anything outside the top-of-file `import ... from ...;` block (no JSX, no function body, no logic). If any non-import line changed, halt and do not commit — report the unexpected diff instead of proceeding.
3. Run `pnpm exec prettier --check apps/web/app/premium-theme.css apps/web/modules/landing/lead-section.tsx` and confirm it exits 0 (both files pass). If it fails, halt — the on-disk state no longer matches what was verified at planning time; report the failure instead of committing.
4. Only if all three checks pass: stage both files (`git add apps/web/app/premium-theme.css apps/web/modules/landing/lead-section.tsx`) and commit with a message describing both changes, e.g. `fix(quick-260820-cz1): bump --text-dt-body to 1rem per devtools reference; format lead-section.tsx imports`.

This is a landing task, not an authoring task — if re-verification fails at any step, the correct outcome is to halt and report the discrepancy, not to alter either file to force it to match the description.
  </action>
  <verify>
    <automated>cd /Users/artemdanko/Developer/denta-bot && pnpm exec prettier --check apps/web/app/premium-theme.css apps/web/modules/landing/lead-section.tsx && grep -F -- '--text-dt-body: 1rem;' apps/web/app/premium-theme.css > /dev/null && git diff --quiet apps/web/app/premium-theme.css apps/web/modules/landing/lead-section.tsx && echo COMMITTED_CLEAN</automated>
  </verify>
  <done>Both files' on-disk content is confirmed to match the described diff (single --text-dt-body value change plus formatting-only diffs in premium-theme.css; import-order-only diff in lead-section.tsx), both pass `pnpm exec prettier --check`, and a single commit containing exactly these two files exists — the working tree shows no further uncommitted changes to either file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this task commits pre-existing, already-verified static content (a CSS custom-property value and an import reordering) with no user input, no data crossing a trust boundary, and no new dependency.

## STRIDE Threat Register

No applicable threats. The task performs a verification-then-commit of an already-authored, non-input-handling styling token change and a mechanical import reordering. No new attack surface is introduced.
</threat_model>

<verification>
1. `git log -1 --stat` shows a single new commit touching exactly `apps/web/app/premium-theme.css` and `apps/web/modules/landing/lead-section.tsx`.
2. `git diff --quiet apps/web/app/premium-theme.css apps/web/modules/landing/lead-section.tsx` exits 0 (no remaining uncommitted changes to either file).
3. `pnpm exec prettier --check apps/web/app/premium-theme.css apps/web/modules/landing/lead-section.tsx` passes.
4. `grep -F -- '--text-dt-body: 1rem;' apps/web/app/premium-theme.css` finds the expected value.
</verification>

<success_criteria>
- Both files are committed with exactly their pre-task on-disk content — no re-authoring occurred.
- `--text-dt-body` is `1rem` in the committed `premium-theme.css`.
- `lead-section.tsx`'s committed content has zero logic/JSX diff from its prior committed state — only import order changed.
- Both files pass `pnpm exec prettier --check`.
- No other file was modified.
</success_criteria>

<output>
Create `.planning/quick/260820-cz1-land-two-already-made-verified-uncommitt/260820-cz1-SUMMARY.md` when done.
</output>
